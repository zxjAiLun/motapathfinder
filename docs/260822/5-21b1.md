# PR-5.21b1 — Contract Closure for Canonical DP Stateful Exact-Cache Correctness

```ini
MILESTONE = PR-5.21b1
STATUS = VERIFIED
KIND = CONTRACT_CLOSURE
ITERATION_STANDARD = LEAN
DATE_STARTED = 2026-08-22
BASE_COMMIT = 179bf9eb5745953bd758fd07db6178c849b60357
IMPLEMENTATION_COMMIT = 3c9cb3f
REVIEW_STATUS = AWAITING USER REVIEW
FINAL_COMMIT = 54fb447
EVIDENCE_SCHEMA = motapathfinder.onlyup-real-route-gate-evidence.v2
MT2_GATE_VERDICT = REAL_MT1_GATE_PASSED
FIRST_REGION_GATE_VERDICT = REAL_FIRST_REGION_GATE_FAILED
FIRST_REGION_FAILURE_REASON = RESOURCE_LIMIT
FIRST_REGION_BINDING_CONSTRAINT = wall
FIRST_REGION_VERDICT_CLASSIFICATION = CACHE_CORRECTNESS_FIXED / FIRST_REGION_STILL_TIME_BLOCKED
STATIC_CORE_PROMOTION = EXPERIMENTAL_FROZEN / REGRESSION_ONLY
SCOPE = 100-expansion fixed contract assertions, manifest generator curation sync & zero-diff idempotency, action-expansion-cache-correctness promotion into cleanCheckout/static suite, first-region bestProgress strict assertion, and Repair 1 evidence consistency hygiene.
```

## 背景与收口目标

PR-5.21b 完成了 exact stateful caches 移除、A/B accounting 继承修复、route-store 双方言与严格指纹模式，以及第一区域直接闸门重测。
PR-5.21b1 第一轮收口解决了 100-expansion 固定基线断言、manifest generator 漂移与 58->59 项 static suite 恢复、新 checker 晋升与 bestProgress 严格化。
PR-5.21b1 Repair 1 解决剩余的 4 项 P2 证据一致性问题：
1. `check-battle-evaluation-projection.js`：stdout JSON 中的 `structuralDelta` 从已校验的 sample 对象直接派生，消除硬编码的旧数值。
2. `check-remaining-materialization-attribution.js`：`repairUpperBoundNodes` 从 `attribution.materializedNodesWithoutTravelStateEscape` 动态派生（当前为 191），消除字面量 156。
3. `scripts/generate-solver-manifest.js`：修正 `check-onlyup-first-region-real-route-gate.js` 的 manifest note，删除“rss or wall (they trip near simultaneously...)”，与 5.21b 保持一致。
4. 交付清单记录 `pr-4.8a1-structured-entry-validation.json` 由恢复 static suite 后的确定性重建产生。
5. 运行完整的静态套件与回归验证并在文档中记录证据。

本轮不改搜索算法、不改缓存策略、不改预算，不重新跑 30s MT6 长闸门（保留 5.21b 的 4832 / 30.001s / 215.7MB 证据）。

---

## 任务完成与实现细节

### 1. 补强 100-Expansion Regression Contract
在 `shared-solver/check-action-expansion-cache-correctness.js` 中冻结固定基准：
- `EXPECTED_FRONTIER_SIZE = 53`
- `EXPECTED_REGISTERED = 171`
- `EXPECTED_GENERATED = 342`
- `EXPECTED_STOPPED_REASON = null`
- `EXPECTED_BEST_PROGRESS_KEY` = 包含 MT2 / hero / inventory / flags / mutations 的完整归一化 JSON 字符串。
- 同时保留两全新独立 simulator 的确定性一致性比较。

### 2. 同步 Manifest Generator 并实现零差异幂等
- `shared-solver/scripts/generate-solver-manifest.js` 补齐与 `37e6c84` 对齐的完整 `TEST_OVERRIDES` 与 58 个 `static.requiredChecks`。
- 新增 `shared-solver/check-action-expansion-cache-correctness.js` 至 `TEST_OVERRIDES`（`cleanCheckout: true`, `grade: "local-regression"`）并加入 `static.requiredChecks`（扩展为 59 个 requiredChecks）。
- 修正 `check-onlyup-first-region-real-route-gate.js` 的 manifest note 描述。
- 验证 `npm run manifest:refresh` 连续二次 refresh 得到零差异（zero-diff idempotent）。

### 3. First-Region Checker 合同收紧
- `shared-solver/check-onlyup-first-region-real-route-gate.js` 移除 `result.bestProgress || result.bestSeen` fallback，严格执行 `assert.ok(result.bestProgress)`。

### 4. Repair 1 证据输出动态派生与一致性收口
- `check-battle-evaluation-projection.js`：将 final stdout JSON `structuralDelta` 各字段改为直接读取 `controls[0]` 与 `repairs[0]` 的实际统计值，消除了硬编码字面量，与当前断言基准（control 1672, repair 1318）100% 一致。
- `check-remaining-materialization-attribution.js`：`repairUpperBoundNodes` 改为读取 `attribution.materializedNodesWithoutTravelStateEscape`（输出为 191），消除了旧字面量 156。
- `pr-4.8a1-structured-entry-validation.json`：由恢复 `check-region-entry-contract.js` 入 static suite 触发现场 negative controls 确定性重建，为预期衍生构建产物。

---

## 验证结果

```text
node --check shared-solver/check-action-expansion-cache-correctness.js       PASS
node --check shared-solver/check-battle-evaluation-projection.js          PASS
node --check shared-solver/check-remaining-materialization-attribution.js   PASS
node --check shared-solver/scripts/generate-solver-manifest.js             PASS
npm run check:manifest --prefix shared-solver                              PASS (143 modules, 157 graded tests)
npm run check:no-tower-solver-js --prefix shared-solver                    PASS
node shared-solver/check-action-expansion-cache-correctness.js             PASS (ok)
node shared-solver/check-route-store-exact.js                             PASS (ok)
node shared-solver/check-battle-evaluation-projection.js                  PASS (structuralDelta control 1672 / repair 1318)
node shared-solver/check-remaining-materialization-attribution.js         PASS (repairUpperBoundNodes: 191)
npm run check:onlyup-mt1-real-route-gate --prefix shared-solver            PASS (REAL_MT1_GATE_PASSED, 10/10 strict fingerprint matches)
```

---

## 交付产物清单

- `shared-solver/check-action-expansion-cache-correctness.js`
- `shared-solver/check-battle-evaluation-projection.js`
- `shared-solver/check-remaining-materialization-attribution.js`
- `shared-solver/check-onlyup-first-region-real-route-gate.js`
- `shared-solver/check-reachability-rebase-attribution.js`
- `shared-solver/check-topology-first-materialization.js`
- `shared-solver/scripts/generate-solver-manifest.js`
- `shared-solver/solver-manifest.json`
- `shared-solver/routes/generated/agenda-policy-evaluation/pr-4.8a1-structured-entry-validation.json` (确定性衍生重建产物)
- `docs/260822/5-21b1.md`
- `20260804handoff.md`
