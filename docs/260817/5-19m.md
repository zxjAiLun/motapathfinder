# PR-5.19m — Post-Recovery Continuation Breakpoint Attribution

```ini
MILESTONE = PR-5.19m
STATUS = ACCEPTED / CLOSED
DATE_STARTED = 2026-08-17
BASE_COMMIT = c219e5128e8ffcaf09a65936c1fd1a06a5590cee
FINAL_COMMIT = <本关闭提交>
RESEARCH_VERDICT = CASE-A / FIXED-CALL-CAP IS CURRENT DIRECT BREAKPOINT
REVIEW_STATUS = APPROVED / CLOSED
SCOPE = observation-only attribution of continuation lifecycle and scheduling after each witness-backed survival-opportunity recovery
```

## 授权与问题

PR-5.19l 已 `APPROVED / CLOSED`，verdict 为 `MECHANISM POSITIVE / NOT PROMOTED`。当前真实 frozen run 中第二个 opportunity recovery 已 materialize 并创建 parent continuation，但其后续 breakpoint 尚未正式冻结：尚不清楚 continuation 是否 resume、是否重新看到 lethal child、是否编译下一 prerequisite，以及未执行时的确切 scheduler reason。

本轮只回答第二个 recovery 之后实际发生了什么，不改变 solver 行为。

## 本轮范围

- 观察每个 survival-opportunity recovery 后的 continuation lifecycle。
- 记录 opportunity target、post exact fingerprint、next boundary stage、HP、damage、survivalMargin。
- 记录 parentContinuationId、anchorNodeId、anchor floor、parent target floor、finalCreated。
- 记录 callsUsedAtRecovery、callsRemainingAtRecovery、totalWorkRemaining。
- 跟踪 continuation resumed、anchor expansion requested/selected、next boundary attribution、next prerequisite compile/schedule/execute。
- 将未执行原因明确分类为 `call-cap-exhausted`、`outstanding-barrier`、`attempt-deduplicated`、`anchor-not-expanded`、`waiting-for-parent-floor` 或 `no-next-prerequisite`。

## 非目标与未授权

- 不调整 call allocation、maxCalls、perCall、total work 或 lazy drain。
- 不新增 connector capability、generic predicate、recovery 行为或 scheduler 行为。
- 不连续抽取第三个 opportunity，不复用同一 witness graph 生成新行为。
- 不引入 scalar ranking、survivalMargin planner objective、known-route input、DP key、dominance 或 replay semantics 变化。
- D3 仍 `NOT AUTHORIZED`。

## 不变量

```text
total work = 1000
maxCalls = 8
perCall = 50
lazyDrainEvery unchanged
observation-only
```

## Breakpoint gate

必须为第二个 witness recovery 提供可复核的 lifecycle attribution，并明确归入以下一种：

```text
Case A: resume → next lethal child compiled → call-cap-exhausted
Case B: waiting → anchor not expanded
Case C: resume → no next prerequisite
Case D: resume + compile → dedupe or another explicit barrier
```

若证据属于 Case A，后续才可另行讨论固定 8 calls 下的 deeper recovery 或已付费 witness graph evidence reuse。该后续工作本轮不授权。

## 验收

| Gate | Pass condition |
|---|---|
| Observation integrity | 不改变真实 solver outcome、预算及调用计数 |
| Continuation lifecycle | 每次 recovery 的 resume/create/final 状态可追踪 |
| Breakpoint attribution | 第二个 recovery 后的下一步或阻断 reason 明确且可复核 |
| Scope discipline | 仅 observation/report/checker/docs，未改变 solver 行为 |

## 真实 frozen observation

命令：

```text
npm run qualification:strategic-survival-opportunity-prerequisite --prefix shared-solver
```

结果：PASS。首次相同命令因本地工具 120 秒上限被终止，随后以更长工具超时重跑，输入和 solver 参数未改变。

```text
totalSearchExpansions = 1000
battleAccessPrerequisiteCalls = 8
rootLevelCalls = 5
continuationDerivedCalls = 3
frontierSize = 737
goalFound = false
stoppedReason = total-search-budget
deferredWorkRemaining = true

survival opportunity recoveries = 2
parent continuations created = 4
parent continuation resumes = 2
continuation calls = 6
next prerequisites compiled = 4
child prerequisites scheduled = 3
child prerequisites executed = 3
anchor expansion requests = 2
anchor expansion selections = 2
anchor inactive/already-expanded skips = 0 / 0
```

第二个 witness 的精确链：

```text
Witness 2 target       = battle:devilWarrior@MT5:11,11
post exact fingerprint = f8cece6abc67e69b
parentContinuationId   = a2d5828eac1f8c24
anchorNodeId           = 261
recovery expansion     = 104
calls remaining        = 0
  next boundary stage    = lethal
  next boundary          = battle-unsurvivable MT5:9,10
next prerequisite      = e9c03049d436f6f2
next prerequisite kind = battle-access-prerequisite
continuation status    = next-prerequisite-not-schedulable
blocking reason        = call-cap-exhausted
```

该 continuation 已 resume；anchor 261 在 continuation 创建前已扩展，未发生 anchor starvation。下一 boundary 的 stage 是 `lethal`，所以本次编译的是 generic `battle-access-prerequisite`，而不是只在 `attack-blocked` 时生成的 `battle-stage-prerequisite`。`e9c03049d436f6f2` 已编译但未 schedule/execute，因为 recovery 创建时 `callsRemainingAtCreation = 0`。因此本轮结论为：

```text
Case A: resume → next lethal child compiled → call-cap-exhausted
```

其余观察：前两个 anchor expansion request/selection 均为同一 expansion 立即选择；没有 `anchor-not-expanded`、`waiting-for-parent-floor`、attempt dedupe 或 outstanding barrier 作为第二个 recovery 后的最终阻断。总 work、calls、预算和搜索结果保持冻结合同。

## 结果与边界

- 5.19m 已回答第二次 recovery 后的 breakpoint，确认当前直接阻断是固定 `maxCalls=8` 下的 call cap。
- 这只是 attribution，不是 promotion，也不证明增加 calls 或复用 witness graph 会成功。
- P2 = 1 non-blocking evidence-label clarification：原先无 boundary 归属的 `post battle stage = attack-blocked` 已改为明确的 `next boundary stage = lethal`。
- 本轮无 solver contract 变化，因此不新增 full qualification。
- 已付费 witness graph evidence reuse、第三个 opportunity、call allocation 和 scheduler 修改均仍未授权。
- D2 仍 `OPEN`，D3 仍 `NOT AUTHORIZED`。

## 下一授权 gate

用户已批准关闭 5.19m。下一轮仅授权 PR-5.19n 的 observation-only residual paid-witness graph reuse attribution；未形成 R1/R2/R3/R4 证据前，不实现任何 reuse、recovery、call allocation 或 scheduler 机制。

## 最终 Verdict

```text
observation           VERIFIED
breakpoint            CASE A
continuation resume   VERIFIED
anchor scheduling     NOT BLOCKED
next compile          VERIFIED
next schedule         BLOCKED
blocking reason       CALL-CAP-EXHAUSTED

P0 = 0
P1 = 0
P2 = 1 non-blocking evidence-label clarification

RESEARCH_VERDICT = CASE-A / FIXED-CALL-CAP IS CURRENT DIRECT BREAKPOINT
REVIEW_STATUS = APPROVED / CLOSED
```
