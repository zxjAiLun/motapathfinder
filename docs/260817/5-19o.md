# PR-5.19o — Prefix-Compatible Residual Witness-Suffix Recovery

```ini
MILESTONE = PR-5.19o
STATUS = AUTHORIZED
DATE_STARTED = 2026-08-17
BASE_COMMIT = 46c3710cc06348e35319202b40df4dac3cfc1bcf
SCOPE = consume exactly one first-BFS prefix-compatible strict-replay-valid residual witness suffix from the same capture-complete failed connector graph after O2 materialization
```

## 授权与问题

PR-5.19n 已 `APPROVED / CLOSED`，真实结果为 `R1 / PAID RESIDUAL SUFFIX AVAILABLE`。第二次 generic lethal connector 的 paid graph 已证明存在：

```text
O2 devilWarrior@MT5:11,11
→ exact witness prefix
→ O3 skeletonPresbyter@MT5:3,10
→ one-edge residual suffix
→ strict replay PASS
```

本轮验证 planner 能否在固定 `maxCalls=8` 已耗尽后，消费该同一 paid graph 的一个 residual suffix，而不是启动第 9 次 connector search。

## 初始设计

仅允许以下来源链：

```text
same failed connector attempt
→ complete retained snapshot
→ selected first-positive O2 witness
→ O3 full witness has O2 witnessEdges as exact prefix
→ residual suffix strict replay from O2 materialized post state
→ discrete O3 completion
→ canonical materialization
→ preserve parent continuation
```

这不是新的 connector result，也不是新的 search。residual suffix 的 primitive edges 已由原 connector call 支付；本轮只重新验证并消费该 evidence。

## 本轮范围

- 仅使用同一 `originFailedAttemptId` 的 retained witness snapshot。
- 仅使用 `snapshot.captureComplete === true` 的 graph。
- 仅使用 later named positive battle opportunity。
- 仅使用 selected O2 `witnessEdges` 为 O3 `witnessEdges` exact prefix 的 candidate。
- 按 snapshot `discoveryOrdinal` ascending 选择第一个 candidate。
- 对 O2 materialized exact state 与 residual suffix 再次执行 `verifyConnectorChain()`。
- replay PASS 且 O3 discrete completion true 后，调用现有 canonical materialization。
- 只消费一个 O3，然后回到现有 parent continuation hierarchy。

## 非目标与未授权

- exact-state reroot recovery 本轮明确未授权，即使 observation 能够识别 reroot candidate。
- 不连续消费 O4/O5，不从一个 graph 批量展开 opportunity ladder。
- 不增加 connector call、connector search expansion、total work、maxCalls、perCall 或 lazy drain。
- 不调整 call allocation、hierarchy priority、anchor scheduling 或任何 scheduler 行为。
- 不改变 DP key、dominance、selection、replay semantics 或 generic predicate。
- 不使用 scalar survivalMargin/HP/damage ranking。
- 不使用 known route、历史 best chain、手写 candidate target 或坐标。
- D3 仍 `NOT AUTHORIZED`。

## Frozen controls and accounting

```text
total work = 1000
maxCalls = 8
perCall = 50
lazyDrainEvery unchanged

new connector calls = 0
new connector search expansions = 0
residual suffix search expansions = 0
```

Recovery attribution must record:

```text
sourceType = paid-residual-witness-suffix
originFailedAttemptId
originSelectedOpportunityId
originSnapshotCaptureComplete
selectedResidualOpportunityId
residualRecoverySelected
residualReplayValid
residualPrerequisiteSatisfied
residualSearchExpansions = 0
connectorCallsCharged = 0
```

## Selection and replay contract

Selection policy is frozen as:

```text
first-prefix-compatible-replay-valid-residual-by-bfs-discovery
```

`deltaSurvivalMargin > 0` only discovers candidates. It is not the completion predicate and cannot rank candidates.

For the selected O3:

```text
O2.witnessEdges is an exact prefix of O3.witnessEdges
O3 suffix = O3.witnessEdges.slice(O2.witnessEdges.length)
```

Recovery must then execute:

```text
verifyConnectorChain(
  simulator,
  O2.materializedFinalState,
  O3 residual suffix,
  expectedPostExactStateKey = O3 paid postExactStateKey
)
```

Only when both conditions hold may it materialize:

```text
replay.valid == true
AND
O3 discrete completion predicate == true
```

The prior observation field `suffixReplayValid=true` is evidence only and cannot replace this replay.

## Fail-closed guards

No residual recovery is allowed when any condition fails:

```text
captureComplete == false
selected O2 post exact state mismatch
O2 witness is not an exact prefix of candidate witness
candidate is reroot-only
candidate is branch-incompatible
residual strict replay fails
O3 discrete completion is false
```

No recovery path may materialize an O3 state from a partial snapshot or an observation-only result.

## Synthetic gates

The focused checker must lock:

```text
O2 witness = [a]
O3 witness = [a,b]
O4 witness = [a,c]
O3 discovered before O4
```

Required assertions:

```text
O3 selected
b replayed from O2 post state
O3 discrete predicate before=false / after=true
higher-margin O4 does not win
branch candidate [x,y] is not selectable
reroot-only candidate is not selectable
connector calls unchanged
connector expansions unchanged
```

## Frozen real mechanism gate

Control must preserve 5.19m:

```text
O1 recovery
→ O2 recovery
→ continuation resume
→ next lethal prerequisite compiled
→ call-cap-exhausted
```

Candidate must add exactly one residual recovery:

```text
O1 recovery
→ O2 recovery
→ first paid prefix residual O3
→ residual strict replay PASS
→ O3 discrete completion true
→ O3 materialized
→ parent continuation preserved
```

Required first-layer evidence:

```text
residualRecoverySelected = 1
residualReplayValid = true
residualPrerequisiteSatisfied = true
```

Required second-layer evidence:

```text
O3 materialized = true
parent continuation created/preserved = true
```

The post-O3 boundary and scheduler outcome must be observed, not predetermined.

## Promotion gate

The promotion gate is unchanged:

```text
best > -903
OR terminalActionGenerated > 0
OR goalFound + strict replay
```

Successful O3 consumption alone is mechanism-positive and does not close D2.

## CI and publication

This is a real solver behavior change: focused synthetic and frozen qualification must pass before a remote full qualification marker is created. Generated probes remain ignored; stable checker and milestone documents are tracked.

## Current state

本轮已授权，尚未实现。唯一下一步是完成 prefix-only residual recovery 与 frozen mechanism qualification；未授权 reroot recovery、连续 residual consumption、call allocation、scheduler 修改或 D3。
