# PR-5.19n — Residual Paid-Witness Graph Reuse Attribution

```ini
MILESTONE = PR-5.19n
STATUS = AUTHORIZED
DATE_STARTED = 2026-08-17
BASE_COMMIT = b4fe280edf072650a1fc9d8f9a99fde882260a9a
SCOPE = observation-only attribution of residual positive opportunities in the second failed connector's already-paid witness graph after the selected devilWarrior recovery
```

## 授权与问题

PR-5.19m 已 `APPROVED / CLOSED`，结论为 `CASE-A / FIXED-CALL-CAP IS CURRENT DIRECT BREAKPOINT`。第二个 `devilWarrior` recovery 后，continuation 已 resume，next lethal prerequisite 已 compile，但第 9 个 connector call 被固定 `maxCalls=8` 阻断。

本轮只回答：第二次 failed connector 已经支付并探索的 witness graph 中，除被选中的 first-positive `devilWarrior` opportunity 外，是否存在一个从其 materialized post exact state 合法延续的 residual positive opportunity。不实现 reuse。

## 本轮范围

- 复用第二次 failed connector 的 observer evidence 做只读分析。
- 保留并检查每个 candidate 的完整 `witnessEdges`、`sourceExactStateKey`、`preExactStateKey`、`postExactStateKey`。
- 以选中的 O2 `devilWarrior@MT5:11,11` witness 为基准，检查 later positive opportunities 的 path compatibility。
- 对 prefix-compatible candidate 检查 O2 witness edges 是否为 candidate witness edges 的 exact prefix。
- 对 exact-state reroot-compatible candidate 检查 candidate path 中是否存在 `edge.preExactStateKey == O2.postExactStateKey` 的合法 suffix 起点。
- 从 O2 materialized post state 对每个 residual suffix 执行 strict replay，仅作为诊断，不计 connector search expansion，不驱动 solver。
- 按原 BFS discovery order 展示多个 compatible candidates，不按 margin、HP、damage 或 source count 排序。

## 非目标与未授权

- 不实现 residual witness-suffix recovery 或任何新的 materialization。
- 不新增 connector call、connector capability、recovery predicate 或 scheduler 行为。
- 不改变 `total work`、`maxCalls`、`perCall`、`lazyDrainEvery`、call allocation 或 continuation lifecycle。
- 不改变 DP key、dominance、replay semantics、search selection 或 hierarchy priority。
- 不使用 known route、历史 best chain、手写坐标或手工挑选 opportunity。
- 不把同一 graph 的跨 branch positive edge 当作可复用 suffix。
- D3 仍 `NOT AUTHORIZED`。

## 不变量与 frozen controls

```text
total = 1000
maxCalls = 8
perCall = 50
lazyDrainEvery unchanged

O1 recovered = true
O2 recovered = true
next prerequisite compiled = true
next prerequisite scheduled/executed = false
blocking reason = call-cap-exhausted
best = -903
goalFound = false
```

ON/OFF semantic result 必须保持一致；本轮新增的 observation 只能增加 attribution，不得改变上述结果。

## Compatibility contracts

### Prefix-compatible

candidate O3 的完整 witness chain 必须满足：

```text
O2.witnessEdges is an exact prefix of O3.witnessEdges
```

只有此时，candidate 的 residual suffix 才能从 O2 post state 取得路径来源。

### Exact-state reroot-compatible

较宽的诊断类别允许 candidate path 中存在：

```text
edge.preExactStateKey == O2.postExactStateKey
```

然后从该 exact-state edge 截取 suffix。该类别同样必须 strict replay；exact key 匹配本身不构成 recovery authorization。

### Branch safety

O2 与 O3 若来自同一 source 但分属不同 BFS branch，不得认定为兼容；不能跨 branch transfer witness。

## Required residual replay output

每个 later positive candidate 必须记录：

```text
candidateTarget
candidateDiscoveryExpansion
candidateDiscoveryDepth
compatibilityKind
compatibilityStartEdge
suffixLength
suffixReplayValid
expectedPostExactStateKey
```

若 replay 失败，必须 fail closed，不得 materialize 或改变 planner outcome。

## Result classification

最终 observation 必须归入以下一种：

```text
R1: PAID RESIDUAL SUFFIX AVAILABLE
    O2 → same paid graph later positive opportunity
    path-compatible
    suffix strict replay PASS

R2: POSITIVE OPPORTUNITIES EXIST BUT BRANCH-INCOMPATIBLE
    same graph has later positive opportunity
    no legal O2-post-state continuation

R3: RESIDUAL PATH FOUND BUT STRICT REPLAY FAILS
    candidate/suffix located
    replay invalid

R4: NO LATER POSITIVE OPPORTUNITY IN PAID GRAPH
```

若出现多个 candidate，按 BFS discovery order报告，不能按 scalar survival metrics 选择。

## 验收与拒绝门

| Gate | Pass condition |
|---|---|
| Frozen outcome parity | O1/O2、calls=8、best=-903、goalFound=false 保持不变 |
| Evidence provenance | 每个 candidate 来自当前 failed connector observer evidence |
| Branch safety | 未把不同 branch 的 edge 错认作 O2 suffix |
| Strict replay authority | 每个声称 residual 的 suffix 从 O2 post state replay 并明确 PASS/FAIL |
| Scope discipline | 仅 observation/report/checker/docs，无 reuse 行为 |

## 当前状态

本轮已授权，尚未实现。唯一下一步是完成本地 observation-only attribution，并将结果归入 R1/R2/R3/R4；在此之前不授权 PR-5.19o 或任何 solver 行为变化。
