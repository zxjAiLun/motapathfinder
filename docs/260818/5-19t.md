# PR-5.19t — Pre-Hierarchy Root Call Chronology Attribution

```ini
MILESTONE = PR-5.19t
STATUS = ACCEPTED
DATE_STARTED = 2026-08-18
BASE_COMMIT = 8d97cb3
IMPLEMENTATION_COMMIT = e7269d6
REVIEW_STATUS = APPROVED / CLOSED
RESEARCH_VERDICT = PRE-HIERARCHY ROOT CALL CHRONOLOGY ATTRIBUTED
FINAL_COMMIT = <本关闭提交>
SCOPE = observation-only chronological attribution of the 8 charged connector calls relative to first hierarchy events, to identify a non-oracle reservation boundary before any fixed-8 reallocation experiment
```

## 授权与已有证据

PR-5.19s（含 Repair 1）已 `APPROVED / CLOSED`：

```text
root:   5 calls, 1 productive, 4 failedWithoutRecoveredProgress, productiveCallRate=0.2
level1: 1 call, 1 productive, productiveCallRate=1.0
level2+: 2 calls, 2 productive (failed but produced O1/O2/O3/O4 evidence), productiveCallRate=1.0
```

已知“哪些 calls 最终有价值”，但不知道“solver 当时做决策时是否还有机会合法保留一个 slot”。5.19t 只回答时间顺序问题，不做 allocation 实验。

## 本轮范围（observation-only）

在 5.19q/r/s 同一 frozen A/B 上，按 charging 顺序记录 8 个 calls：

```text
call #1..#8:
  callOrdinal
  hierarchyLevel
  attemptId
  expansionAtCharge
  callsRemainingAfter
  firstHierarchyActivationOccurred（charge 时）
  activeContinuationCount（charge 时）
  productive（search-end 回绑，attemptId 主键）
  directSatisfied
```

锁定三个 checkpoint：

```text
A. first parent continuation created
B. first hierarchy-priority activation
C. first level-1 call charged
```

每个 checkpoint 输出：

```text
expansion
callOrdinal（当时已 charged 的 calls）
rootCallsSpent
callsRemaining
childCallsSpent
```

五个 root calls 分析：

```text
按 callOrdinal 列出 productive / directSatisfied / firstHierarchyActivationOccurredAtCharge
first hierarchy activation 前/后的 root calls 数量
```

## 不在本轮 / 未授权

- 不做 allocation A/B、不删 root call、不做 static reserve / root-ceiling 实验。
- 不修改 call allocation、hierarchy priority、anchor 调度、scheduler、budget、maxCalls、perCall。
- 不改 DP key、dominance、canonical selection、replay、materialization 或 continuation 生命周期。
- 不进行 O5 recovery、reroot、第 9 次 search、call reservation、dynamic allocation。
- D3 = NOT AUTHORIZED。

## Frozen controls

与 5.19s 相同：

```text
total work = 1000
maxCalls = 8
root = 5
child = 3
```

Observation off/on parity 必须保持（totalSearchExpansions / expansions / battleAccessPrerequisiteCalls / rootLevelCalls / continuationDerivedCalls）。

## Output contract

```text
hierarchyCallChronology:
  schema
  calls[8]: callOrdinal / hierarchyLevel / attemptId / expansionAtCharge /
            callsRemainingAfter / firstHierarchyActivationOccurred /
            activeContinuationCount / productive / directSatisfied
  checkpoints:
    firstContinuationCreated: {expansion, callOrdinal, rootCallsSpent, callsRemaining}
    firstHierarchyActivation: {expansion, callOrdinal, rootCallsSpent, callsRemaining}
    firstLevelOneCallCharged: {expansion, callOrdinal, rootCallsSpent, callsRemaining}
  rootCallsAnalysis:
    beforeFirstHierarchyActivation / afterFirstHierarchyActivation
    byOrdinal[5]: callOrdinal / productive / directSatisfied / firstHierarchyActivationOccurredAtCharge
```

## Key gate（回答后才有资格设计 reallocation）

```text
若 first hierarchy activation 时 root calls spent = 5、remaining = 3：
  → 解释为何最终只有 level1/2/3 三个 child slots，depth-4 必然撞 cap。

root call #5：
  若 charge 时无任何 hierarchy signal 且最终 unproductive
    → 支持下一轮测试 non-oracle static-reserve（root ceiling=4）
  若 root call #5 正是生成后续 productive chain 所需的 direct satisfied call
    → 简单 5→4 会剪掉整条 hierarchy
```

两者都不在本轮执行；本轮只出时序事实。

## 实施顺序

1. D2 增加 opt-in `enableHierarchyCallChronology`：charge 时记录 call 条目，checkpoint 处记录事件（continuation created / hierarchy activation / level-1 call）。
2. search 结束后按 attemptId 回绑 productive/directSatisfied，构建 analysis。
3. checker 增加 `--hierarchy-call-chronology` 模式与 package script。
4. 本地运行冻结结果。
5. observation-only 轮无需 dedicated remote job；docs/handoff 更新后提交等待 review。

## 下一授权 gate

只有 5.19t 的时序证据到位后，才可能讨论第一个非 oracle 的 fixed-8 call reallocation experiment（如 static reserve）。任何 allocation 改动必须 review 后另行授权。

## 迭代日志

### Iteration 1 — 2026-08-18 — observation implementation

- Change: D2 增加 opt-in `enableHierarchyCallChronology`。charge 时记录 call 条目（callOrdinal/hierarchyLevel/attemptId/expansionAtCharge/callsRemainingAfter/firstHierarchyActivationOccurred/activeContinuationCount）；checkpoint 记录 firstContinuationCreated / firstHierarchyActivation / firstLevelOneCallCharged；search 结束后按 attemptId 回绑 directSatisfied/productive 并构建 rootCallsAnalysis。
- Checker: `--hierarchy-call-chronology` 模式（与 `--hierarchy-call-attribution` 同跑），断言 calls=8 且 callOrdinal=1..8、checkpoint 结构、root 分析；同时 5.19s evidence-hardening exact 断言（root 5/1/4/0.2、level1 1/1.0、level2+ 2/0/2/0/1.0、cb70ef61@f35ece 与 e9c03049@8bf509 两个 attempt）已在同一 checker 锁定。
- 修正：`cb70ef61@` 前缀会误匹配 level-0 的 root call（同 prerequisite hash 不同 state hash），改用完整 attemptId 精确匹配。
- q/r/s regression、manifest、syntax 全部通过。
- Outcome: implementation commit `e7269d6`。

### Iteration 2 — 2026-08-18 — real observation

- Command:

```text
NODE_OPTIONS=--max-old-space-size=4096 npm run check:strategic-hierarchy-call-chronology --prefix shared-solver
```

- Frozen result:

```text
chronology (8 calls, charging order):
  #1 root  e9c03049@b840475714ca0aea  exp=8   rem=7  hierarchySignal=false  productive=false
  #2 root  e9c03049@93815e03a9a3eb0c  exp=16  rem=6  hierarchySignal=false  productive=false
  #3 root  cb70ef61@ae860948a49af6e5  exp=24  rem=5  hierarchySignal=false  productive=false
  #4 root  e9c03049@2dfe471267188e43  exp=32  rem=4  hierarchySignal=false  productive=false
  #5 root  3632e7cfab66d64b@69c2c78b137e0442 exp=40 rem=3 hierarchySignal=false productive=true (direct satisfied)
  #6 level1 67b5c35c97cb35bd@542bdbd0b11b3fb9 exp=64 rem=2 hierarchySignal=true active=1 productive=true
  #7 level2 cb70ef61ad4b231a@f35ece354048abe0 exp=88 rem=1 hierarchySignal=true productive=true (O1)
  #8 level3 e9c03049d436f6f2@8bf509342ae539d2 exp=104 rem=0 hierarchySignal=true productive=true (O2+O3/O4)

checkpoints:
  A firstContinuationCreated: expansion=40 callOrdinal=5 rootSpent=5 child=0 remaining=3
  B firstHierarchyActivation: expansion=40 callOrdinal=5 rootSpent=5 child=0 remaining=3
  C firstLevelOneCallCharged: expansion=64 callOrdinal=6 rootSpent=5 child=0 remaining=2

rootCallsAnalysis:
  total=5  beforeFirstHierarchyActivation=5  afterFirstHierarchyActivation=0
  productive=1 (call #5)  failedWithoutRecoveredProgress=4 (calls #1-#4)
```

## Verified Result

```text
PRE_HIERARCHY_ROOT_CALL_CHRONOLOGY_ATTRIBUTED

key gate 命中预测：
  first hierarchy activation 时 root calls spent = 5, calls remaining = 3
  → 解释为何最终只有 level1/level2/level3 三个 child slots，depth-4 必然撞 cap

root call #5（唯一 productive 的 root call，direct satisfied）：
  charge 时无任何 hierarchy signal（firstHierarchyActivationOccurred=false）
  → 简单的 static-reserve（root ceiling=4）会在 hierarchy 开始前剪掉 #5，
    而 #5 正是 first continuation 的来源（checkpoint A/B 发生在 call #5 的
    materialization）
  → 以 hierarchy signal 为边界的 non-oracle policy 无法区分 #1-#4（unproductive）
    与 #5（productive），两者 charge 时都无 signal

root calls #1-#4：4 个 failedWithoutRecoveredProgress，全部在 hierarchy signal 前
  → 它们是 hierarchy 开始前的候选 root 探索，是 5.19s 中 root 低 productive 的
    时间来源
```

This is observation-only. It does NOT authorize any allocation change; it provides the chronology facts needed to design (not execute) a non-oracle reallocation experiment.

## Review State

Reviewer 确认：

```text
PR-5.19t
charging chronology:             VERIFIED
continuation checkpoint:         VERIFIED
hierarchy activation checkpoint: VERIFIED
first-child checkpoint:          VERIFIED
attemptId productive binding:    VERIFIED
observation-only isolation:      VERIFIED
non-oracle timing conclusion:    VERIFIED

P0 = 0
P1 = 0
P2 blocking = 0
P2 non-blocking = 1
  - exact A/B/C + root #1-#5 pattern 已在 checker 补 exact assertions（evidence-hardening）

RESEARCH_VERDICT = PRE-HIERARCHY ROOT CALL CHRONOLOGY ATTRIBUTED
REVIEW_STATUS = APPROVED / CLOSED
```

核心结论（frozen）：

```text
hierarchy signal 首次出现（expansion 40）时 root calls spent = 5、remaining = 3
→ 当前 frozen trajectory 下恰好只剩 level1/2/3 三个 child slots，depth-4 撞 cap

root #1-#4: charge 时 hierarchySignal=false，最终 unproductive
root #5:    charge 时 hierarchySignal=false，最终 productive（first continuation 来源）

→ static reserve（root ceiling=4）会在 hierarchy 开始前剪掉 #5
→ 以 hierarchy signal 为边界的 reserve 太晚（signal 出现时已花完 5 个 root calls）
→ fixed-8 下真正问题是 root selection 顺序，不是简单地 5→4
```

D2 remains `OPEN`, D3 remains `NOT AUTHORIZED`.

## 下一授权 gate

下一轮 PR-5.19u（Pre-Charge Root Attempt Separability Attribution，observation-only）：回答 productive root #5 在 connector 执行前是否有 non-oracle、可泛化的 semantic features 把它与 #1-#4 区分开（U1/U2/U3/U4）。只有 U1 才有资格设计 fixed-8 root selection A/B；U2/U3 则应转向 maxCalls 8→9 或带 fallback/release 的 bounded reservation 讨论。
