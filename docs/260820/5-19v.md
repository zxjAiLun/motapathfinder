# PR-5.19v — Pre-Hierarchy Root Retry Novelty Attribution

```ini
MILESTONE = PR-5.19v
STATUS = VERIFIED
DATE_STARTED = 2026-08-20
BASE_COMMIT = 9967abb
IMPLEMENTATION_COMMIT = cb06e66
REPAIR1_COMMIT = 11c44d8
REPAIR2_COMMIT = 8cb46c1
REVIEW_STATUS = AWAITING USER REVIEW
FINAL_COMMIT = <提交实际存在后再填>
SCOPE = observation-only attribution of whether root calls #1-#4 contain a charge-time identifiable retry (same prerequisite + same boundary, no metric improvement over an earlier root attempt), as future shadow-counterfactual input only
```

## 授权与已有证据

PR-5.19u 已 `APPROVED / CLOSED`（FINAL_COMMIT=`9967abb`）。已有：

```text
root #1-#4 unproductive（#1/#2/#4 同 evilHero@9,10；#3 evilHero@2,8）
root #5    productive（skeletonPresbyter@3,10，attackMargin=+377）
前四 pool 无正 attackMargin 候选；#5 的 attempt/identity 从未更早出现
（5.19u verdict: NO-EARLIER-POSITIVE-ATTACK-MARGIN-OR-EXACT-ATTEMPT-EVIDENCE）
```

5.19v 追问：#1-#4 中是否存在 charge 时（完全不使用 productive/directSatisfied/materialization 等事后字段）就能识别为“同 prerequisite、同 boundary、相对更早 root attempt 无指标进展或已被支配”的 retry。目标只取得未来 shadow counterfactual 的输入证据，**不证明真实 suppression 安全**。

## 本轮范围（observation-only）

- root charge record 补充 `prerequisiteId` 与 `parentDependencyId`；identity 继续用 floorId/enemyId/x/y。
- 纯 helper `classifyPreHierarchyRootRetryNovelty(rootCalls)`：只比较 `firstHierarchyActivationOccurred=false` 的 root calls，按 prerequisiteId + 完整 boundary identity 分组；每个 retry 与同组**所有**更早 call 比较（不只最近一次）。
- 指标合同（required metrics）：
  - 越高越好：attackMargin、survivalMargin、sourceTerminalProgressScore
  - 越低越好：damage
  - false→true 改善：reachableAtCompileTime
  - 只记录为 `contextDifferenceKeys`、不能冒充指标改善：sourceDepth、sourceFloor、beforeStage、stageGoal、compiledCandidateRank/Count
- 每个 comparison 输出：improvedFields / regressedFields / equalFields / missingFields / contextDifferenceKeys / exactSemanticEqual。
- 分类：FIRST-SEEN、EXACT-SEMANTIC-RETRY、METRIC-TIE-CONTEXT-ONLY、PRIOR-ATTEMPT-DOMINATES、CURRENT-ATTEMPT-IMPROVES、MIXED-TRADEOFF、EVIDENCE-INCOMPLETE（只有 required metric 缺失时才用 EVIDENCE-INCOMPLETE）。
- search-end 生成 `stats.rootRetryNoveltyAttribution`（schema v2）：`preChargeComparisons` 严禁 productive/directSatisfied/satisfied/materialized/continuation outcome 等事后字段；`postSearchEvaluation` 单独连接既有 label。
- offline verdict 四类：
  - `TRACE-LOCAL-NONPRODUCTIVE-DOMINATED-RETRY-OBSERVED`（仅说明当前 trace 存在未来 shadow 候选，不授权 suppression）
  - `PRODUCTIVE-ROOT-WOULD-BE-FLAGGED`
  - `NO-PRECHARGE-NONIMPROVING-RETRY`
  - `EVIDENCE-INCOMPLETE`

## 不在本轮 / 未授权

- 不实现 skip/suppress/reorder；不实现 fixed-8 A/B、8→9 A/B。
- 不修改 selectFeedbackAwareDependencyAttempts、dependencyAttemptDedupe、lazyWork/agenda 排序、hierarchyPriority、reservation、connectorMaxCalls、maxOutstanding、lazyDrainEvery。
- 不做 O5、reroot、第 9 次 search。
- 不改 DP key、dominance、replay、materialization、continuation 生命周期。
- D3 = NOT AUTHORIZED。

## Output contract

```text
compactRootRetryNoveltyAttribution:
  schema（...root-retry-novelty-attribution.v2）
  rootCallCount
  repeatGroupCount
  comparisons（精简：callOrdinal/attemptId/groupKey/classification/
              improvedFields/regressedFields/contextDifferenceKeys/exactSemanticEqual/
              comparedCallOrdinals）
  postSearchEvaluation（per call：callOrdinal/attemptId/productive/directSatisfied）
  verdict
  JSON.stringify(compact).length < 20000
```

禁止输出 raw rootCompileEvents / candidate pools。

## Synthetic gates（checker 内，纯 helper）

- exact retry（semantic 全同）→ EXACT-SEMANTIC-RETRY
- metric tie + context only → METRIC-TIE-CONTEXT-ONLY
- strict dominance（更早 attempt 全指标 ≥ 且至少一个严格更好）→ PRIOR-ATTEMPT-DOMINATES
- improvement → CURRENT-ATTEMPT-IMPROVES
- mixed tradeoff → MIXED-TRADEOFF
- missing metric → EVIDENCE-INCOMPLETE
- FIRST-SEEN

## 实施顺序

1. d2-search：root charge 补 prerequisiteId/parentDependencyId；纯 helper + export；search-end attribution + verdict + compact。
2. checker：`--root-retry-novelty`（isolation + synthetic + frozen）；package script。
3. 本地运行冻结结果（不预设必须正结果）。
4. observation-only 轮无需 dedicated remote job；docs/handoff 更新后提交等待 review。

## 下一授权 gate

仅把 retry 证据作为未来 shadow counterfactual 输入；任何真实 skip/suppress/reorder 或 allocation 改动必须 review 后另行授权。

## 迭代日志

### Iteration 1 — 2026-08-20 — observation implementation

- Change: d2-search 增加 opt-in `enableRootRetryNoveltyAttribution`。root charge record 补充 `prerequisiteId`/`parentDependencyId`（identity 保持 floorId/enemyId/x/y）。新增并导出纯 helper `classifyPreHierarchyRootRetryNovelty(rootCalls)`：只比较 `firstHierarchyActivationOccurred=false` 的 root calls，按 prerequisiteId+boundary identity 分组，retry 与同组所有更早 call 比较；required metrics（attackMargin/survivalMargin/sourceTerminalProgressScore 越高越好、damage 越低越好、reachableAtCompileTime false→true 改善），context 字段只进 `contextDifferenceKeys`。分类 FIRST-SEEN / EXACT-SEMANTIC-RETRY / METRIC-TIE-CONTEXT-ONLY / PRIOR-ATTEMPT-DOMINATES / CURRENT-ATTEMPT-IMPROVES / MIXED-TRADEOFF / EVIDENCE-INCOMPLETE（仅 required metric 缺失）。
- search-end 生成 `stats.rootRetryNoveltyAttribution`（v1）：`preChargeComparisons` 无任何事后字段；`postSearchEvaluation` 单独连接 label；offline verdict 四类。
- Checker：`--root-retry-novelty`（isolation：control/candidate 行为全同只切 retry flag，复用 candidate 消除重复 run；pure-helper synthetic 覆盖 exact retry / metric tie / strict dominance / improvement / mixed / missing metric / context-only / FIRST-SEEN；post-hoc 泄漏断言）。package script `check:strategic-root-retry-novelty`。
- stdout 只输出 `compactRootRetryNoveltyAttribution`（schema/rootCallCount/repeatGroupCount/精简 comparisons/postSearchEvaluation/verdict），`JSON.stringify(compact).length < 20000`；禁止 raw rootCompileEvents/candidate pools。
- 未修改 selectFeedbackAwareDependencyAttempts / dedupe / lazyWork/agenda / hierarchyPriority / reservation / connectorMaxCalls / maxOutstanding / lazyDrainEvery；未实现 skip/suppress/reorder。
- Outcome: implementation commit `cb06e66`。

### Iteration 2 — 2026-08-20 — real observation

- Command:

```text
NODE_OPTIONS=--max-old-space-size=4096 npm run check:strategic-root-retry-novelty --prefix shared-solver
```

- Frozen result:

```text
rootCallCount = 5
repeatGroupCount = 1
repeat group = e9c03049d436f6f2|MT5|evilHero|9|10  → callOrdinals [1, 2, 4]

comparisons（输出顺序）:
  #1 e9c03049@b840  evilHero@9,10   FIRST-SEEN
  #2 e9c03049@9381  evilHero@9,10   EVIDENCE-INCOMPLETE（vs #1；damage/survivalMargin missing，
                                    contextDifferenceKeys=[sourceDepth]）
  #4 e9c03049@2dfe  evilHero@9,10   EVIDENCE-INCOMPLETE（vs #1,#2；
                                    improvedFields=[attackMargin, sourceTerminalProgressScore]，
                                    contextDifferenceKeys=[sourceDepth, beforeStage]）
  #3 cb70ef61@ae86  evilHero@2,8    FIRST-SEEN
  #5 3632e7cf@69c2  skeletonPresbyter@3,10 FIRST-SEEN

postSearchEvaluation: #1-#4 productive=false, #5 productive=true

verdict = EVIDENCE-INCOMPLETE
```

## Verified Result

```text
PRE_HIERARCHY_ROOT_RETRY_NOVELTY_ATTRIBUTED

唯一 repeat group（evilHero@9,10）中的 retry #2/#4 均为 EVIDENCE-INCOMPLETE：
  更早的 attack-blocked attempts（#1/#2）缺少 damage/survivalMargin，
  无法证明 retry 无指标进展/被支配。
  #4 实际在 attackMargin(+100) 与 sourceTerminalProgressScore 上 improved，
  但因缺失指标被归入 EVIDENCE-INCOMPLETE，不声称被支配。

productive root #5 在其组内 FIRST-SEEN，不会被任何 pre-charge retry
分类标记为 retry。

→ 当前 trace 不产生 TRACE-LOCAL-NONPRODUCTIVE-DOMINATED-RETRY-OBSERVED；
  不能从本证据主张任何 shadow suppression 候选。
  verdict = EVIDENCE-INCOMPLETE（诚实负结果，非 suppression 授权）。
```

This is observation-only. It does not authorize any skip/suppress/reorder; the missing-metric outcome is itself the key finding for the frozen trace.

## Repair 1 — Pairwise Witness Preservation + Safety Verdict Priority + Compact Evidence Completion（P2_ONLY，commit `11c44d8`）

保留原 Iteration 1/2，不重写历史。本轮只改分类合同、身份范围与输出紧凑性，不重新设计搜索实验，不开 5.19w。

### Review 反例与修复合同

* **exact vector 含 null**：`damage/survivalMargin=null` 的 exact semantic retry 必须保留为 `EXACT-SEMANTIC-RETRY` witness，不能被 `missingFields` 覆盖（pairwise 优先级：`exact → missing → MIXED → CURRENT_IMPROVES → PRIOR_DOMINATES → METRIC-TIE`）。
* **决定性 witness**：synthetic retry（vs #1 improved, vs #2 dominated 且 #2 缺失）的重试级分类由决定性 earlier witness 聚合；若任一 pair 为 `PRIOR-ATTEMPT-DOMINATES` 的完整 earlier，则重试级必为该类且 `decisiveEarlierCallOrdinals=[完整 earlier]`，不被另一个缺失 witness 稀释。真实 frozen #4 仍是两个 `EVIDENCE-INCOMPLETE` pair，不是该 dominance synthetic。
* **安全 verdict 优先级**：离线 verdict 提取为纯 helper `classifyRootRetryOfflineVerdict`，优先级固定 `PRODUCTIVE-ROOT-WOULD-BE-FLAGGED > EVIDENCE-INCOMPLETE > TRACE-LOCAL-NONPRODUCTIVE-DOMINATED > NO-PRECHARGE`；合成用例：productive flagged + 无关 incomplete 同时存在 → verdict 必须为 `PRODUCTIVE-ROOT-WOULD-BE-FLAGGED`。

### 实现变更

* `classifyPreHierarchyRootRetryNovelty` 重构：每 retry×each earlier 独立生成 `pairwiseComparisons`（earlierCallOrdinal, classification, improved/regressed/missing/equal, contextDifferenceKeys, exactSemanticEqual）；重试级仅按决定性 witness 聚合，顶层 union 仅兼容保留。
* `classifyRootRetryOfflineVerdict` 抽离导出；`compactRootRetryNoveltyAttribution` 每 comparison 增加 `missingFields`, `decisiveEarlierCallOrdinals`, 精简 `pairwiseComparisons`，禁止 raw rootCompileEvents/candidate pools，`JSON.stringify(compact).length < 20000`。

### 重新冻结的真实结果（frozen corpus 未变）

```text
repeatGroupCount = 1, #2/#4 仍 EVIDENCE-INCOMPLETE, #5 FIRST-SEEN, offline verdict 仍 EVIDENCE-INCOMPLETE

#2 (callOrdinal 2, vs #1):
  pairwise[vs #1]: EVIDENCE-INCOMPLETE, missingFields=[survivalMargin,damage],
  decisiveEarlierCallOrdinals=[1], pairwiseComparisons 压缩保留

#4 (callOrdinal 4, vs #1,#2):
  pairwise[vs #1]: EVIDENCE-INCOMPLETE, improved=[attackMargin,sourceTerminalProgressScore],
                   missing=[survivalMargin,damage], exact=false
  pairwise[vs #2]: EVIDENCE-INCOMPLETE, 同上
  retry-level: EVIDENCE-INCOMPLETE, decisiveEarlierCallOrdinals=[1,2]
  missingFields 顶层=[damage,survivalMargin] 直接锁定
```

若修复后真实结果意外变化，将先报告 pairwise details 而不修改指标合同迎合预期。

交付摘要：从 `9967abb` 起实际共有 closure/implementation/evidence 三段历史（9967abb→7830aa9→cb06e66→45143c0），Repair 提交另计。

## Review State

Implementation and observation are `VERIFIED`；Repair 2 focused verification is `VERIFIED` and waiting review. D2 remains `OPEN`, D3 remains `NOT AUTHORIZED`。

## Repair 2 — Verdict Audit Persistence + Fail-Closed Evaluation Linkage + Checker Integrity（P2_ONLY）

本轮只修 verdict 审计持久化、post-search evaluation linkage 与 checker 完整性；不修改搜索行为，不创建 5.19w。

### Contract

- attribution schema 从 `motapathfinder.strategic-root-retry-novelty-attribution.v1` 升为 `v2`。pairwise evidence、decisive witnesses 与 verdict audit fields 均属于正式 schema，不向 v1 继续追加字段。
- `classifyRootRetryOfflineVerdict` 无论最终 verdict 为 PRODUCTIVE、EVIDENCE-INCOMPLETE、TRACE-LOCAL 或 NO-PRECHARGE，都保留实际检测到的 `productiveFlaggedCallOrdinals`、`nonProductiveFlaggedCallOrdinals`、`incompleteCallOrdinals`、`missingEvaluationCallOrdinals`。
- 每条 non-improving comparison 必须链接到唯一且 `productive` 为 boolean 的 `postSearchEvaluation`；缺失、重复或非 boolean 都加入 `missingEvaluationCallOrdinals` 并触发 fail-closed `EVIDENCE-INCOMPLETE`。
- verdict 优先级固定为 `PRODUCTIVE-ROOT-WOULD-BE-FLAGGED > EVIDENCE-INCOMPLETE > TRACE-LOCAL-NONPRODUCTIVE-DOMINATED-RETRY-OBSERVED > NO-PRECHARGE-NONIMPROVING-RETRY`。PRODUCTIVE 优先时仍保留 incomplete、missing 与 nonproductive audit 数组。
- search-end 和 `compactRootRetryNoveltyAttribution` 同步输出 verdict 与四组 ordinal 字段；compact 继续保留 pairwise `missingFields`，禁止 raw pools，长度 guard 仍为 `<20000`。

### Frozen Result

真实 frozen trace 未改变：

```text
schema = motapathfinder.strategic-root-retry-novelty-attribution.v2
verdict = EVIDENCE-INCOMPLETE
productiveFlaggedCallOrdinals = []
nonProductiveFlaggedCallOrdinals = []
incompleteCallOrdinals = [2, 4]
missingEvaluationCallOrdinals = []

#2/#4 pairwise missingFields = [damage, survivalMargin]
```

真实 #4 仍只表示 incomplete pairwise evidence；dominance + unrelated incomplete、missing evaluation、non-boolean evaluation、duplicate evaluation、productive-priority 与完整 nonproductive witness 均为 checker synthetic evidence，不能混入 frozen trace 结论。

### Checker Integrity

- missingFields 排序均使用副本，不原地改变 attribution。
- 删除恒真断言，明确断言 `rootCompileEvents` 不存在。
- compact 构造前后比较 attribution 稳定序列化结果，锁定 checker assertion 不修改生产数据。
- synthetic gates 覆盖 nonproductive 保留、missing/duplicate/non-boolean linkage fail-closed、PRODUCTIVE 最高优先级与 TRACE-LOCAL 完整 witness。

Implementation and focused checker verification are `VERIFIED`; Repair 2 remains pending review. D2 remains `OPEN`, D3 remains `NOT AUTHORIZED`.
