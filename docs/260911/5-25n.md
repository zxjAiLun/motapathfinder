# PR-5.25n — Frontier-Conditioned Resource-State Skyline Search

```ini
MILESTONE = PR-5.25n
STATUS = PHASE_0_PASS / PHASE_1_PRESSURE_ESTABLISHED / PHASE_2_LOCAL_GATE_FAILED
DATE = 2026-09-11
BASE = 9511416 (PR-5.25m Repair 1, review = APPROVED_WITH_NARROW_P2_MEASUREMENT_NOTES)
REVIEW = PR_5_25N = AUTHORIZED; PRIORITY_ONLY_LINE = COMPLETED_NEGATIVE
```

## 0. 动机与架构假说

5.25m 证明：从目标 `floorReached(MT3)` 出发自动推导的拓扑依赖前置，能自主发现目标楼梯（`MT2:6,12`）
并将搜索重心极其强力地推入 MT2（MT2 扩展数暴增 31~60 倍，战略深度翻倍）。
然而，单纯把搜索推入 MT2 仍未能打通通往 MT3 的前进楼梯。

评审指出关键机制假说：
> 在自主前置区域已被锁定的前提下，搜索在 MT2 停滞是因为不同的动作排列组合产生了大量**结构完全等价、但保留资源各异的衍生状态**；
> 如果算力大量被浪费在“结构相同但资源更劣”的状态上，则基于非标量 Pareto Skyline 的资源质量排序
> 可以在不引入人工权值/阈值的前提下，让高潜力状态优先推进，进而攻克解锁序列。

## 1. Phase 0 预检清理与硬门禁

本轮彻底解决了 5.25m 留存的 P2 度量问题，并在执行任何搜索前严格断言：
1. **Pre-Goal 战略宇宙修正**：
   目标为 `floorReached(MT3)`，进入 MT3 即任务完成。Denominator 严格排除 MT3 内部的 Post-Goal 内容，
   `ALL_STRATEGIC_POIS` 精确计算为 MT1 与 MT2 走廊上的全部战略 POI 及跨层楼梯，总计 **108** 个（彻底杜绝 Denominator 人为虚胖）。
2. **统一突变标识符**：
   突变节点统一使用规范 `mutation:hook:floor:at`，杜绝 `undefined,undefined` 坐标折叠。
3. **动态相对楼层解析**：
   `poiToSemanticIdentity` 与 `actionToSemanticIdentity` 统一调用 `resolveRelativeFloor`，零 MT 特判。
4. **自主目标楼梯推导**：
   仅提供起点 `initialState` (CHAOS MT1) 与终点 `terminalGoal` (`{type: "floorReached", floorId: "MT3"}`)，
   由 `buildPlanningFloorEnvelope` 推出 `MT1 -> MT2 -> MT3`，由 `buildFloorTransitionGraph` 推出目标楼梯 `MT2:6,12 -> MT3`。
5. **硬门禁断言**：
   ```ini
   TARGET_TRANSITION_DISCOVERY = DERIVED_FROM_PROJECT_AND_TERMINAL_GOAL
   NO_AUTHORED_TARGET_COORDINATE = true
   ORACLE_VALID = true
   OBSERVED_PRE_UNLOCK_RECALL = 1.0 (13/13 决策入口全覆盖)
   FRONTIER_FRACTION = 63 / 108 = 58.33% (压缩 41.7% < 1.0)
   PHASE_0_GATE = PASS
   ```

## 2. Phase 1 资源状态压力遥测（实测证据）

在 5.25m 基线自主搜索（CONTROL）中接入严格非标量 Pareto 支配度量：
- **严格结构键（Structural Key）**：
  保留位置（floor, x, y, direction）、装备/随从、游戏 flag、`visitedFloors`、地图像素突变、触发事件。
  **完全剔除所有数值资源**。同结构键下的所有状态，其世界拓扑与未来动作合法性完全等价。
- **资源向量（Resource Vector）**：
  包含 HP、HPMAX、MANA、MANAMAX、ATK、DEF、MDEF、MONEY、EXP、LV 及全部道具数量。
- **Pareto 支配判定（无标量权重）**：
  A 支配 B 当且仅当 A 在所有资源维度上均 `>= B`，且在至少一个维度上 `> B`。维度互有胜负时判定不可比，双方均保留为非支配状态。

### 2.1 MT2 扩展状态压力实测表（180s 权威运行）

| 遥测指标 | 测量值 | 物理含义与解释 |
| :--- | :--- | :--- |
| `MT2_EXPANDED_STATES` | **1,293** | 搜索实际扩展的 MT2 战略节点总数 |
| `STRUCTURAL_GROUP_COUNT` | **201** | 其中仅包含 201 个不同的结构等价类 |
| `MULTI_VARIANT_GROUP_COUNT` | **171** | 拥有 2 个及以上资源变种的结构类数量 |
| `RESOURCE_VARIANT_STATE_COUNT`| **1,263** | 属于多变种结构类的状态总数 |
| `MAX_VARIANTS_IN_GROUP` | **103** | 单个结构类中最多衍生出 103 个不同资源变种 |
| `PARETO_NONDOMINATED_COUNT` | **201** | 处于 Pareto 最前沿的高质量变种 |
| **`PARETO_DOMINATED_COUNT`** | **1,092** | **被同一结构类中更优状态完全支配的劣质变种** |
| **`DOMINATED_FRACTION`** | **84.45%** | **MT2 上 84.45% 的扩展算力花在劣质变种上！** |
| `MULTI_VARIANT_FRACTION` | **97.68%** | MT2 上 97.68% 的节点存在同类资源竞争 |

### 2.2 Phase 1 裁定

```ini
RESOURCE_VARIANT_PRESSURE = STRONGLY_OBSERVED (84.45% DOMINATED)
HYPOTHESIS_STAGE_1 = CONFIRMED
PHASE_2_PRIORITY_PROBE = TRIGGERED
```

数据压倒性地证实了假说：MT2 上绝大多数算力确实被浪费在“结构进展完全相同、只是资源保留更差”的冗余衍生状态上。

## 3. Phase 2 资格赛对比（Pareto Skyline 优先队列）

预算：180s 墙钟 / 2048MB RSS / 120,000 战略扩展上限。单次搜索独立子进程隔离。

- **CONTROL**：5.25m 自主依赖前置双队列搜索（不区分资源变种优劣，凡命中 frontier 均入 guided heap）。
- **TREATMENT**：完全相同的搜索内核 + 在同类结构状态内维护在线 Pareto Skyline。
  - **规则**：Pareto 非支配状态（高质量变种）进入 `guidedHeap`；被支配状态（劣质变种）**不入导向堆，仅保留在 `neutralQueue`**。
  - **零剪枝约束**：`PRUNING = NONE`、合法动作集逐字相同、Exact 注册表完全相同、中立队列定期轮询保底机制完全相同。

### 3.1 实测结果表

| 指标 | CONTROL (5.25m Dual-Queue) | TREATMENT (+ Pareto Skyline) | 机制对比与现象 |
| :--- | :--- | :--- | :--- |
| `found` | `false` | `false` | 两臂均未在 180s 内抵达 MT3 |
| `deepestFloorOrdinal` | 2 (MT2) | 2 (MT2) | deepest 均停留在 MT2 |
| `strategicExpansions` | 11,017 | **17,233** | treatment +56.4% 战略探索吞吐 |
| `guidedExpansions` | – | 8,677 (50.4%) | Skyline 精选变种为主导 |
| `neutralExpansions` | 11,017 (100%) | 8,556 (49.6%) | 中立保底正常运转 |
| **`deepestFloorHistogram`** | **MT1: 9724, MT2: 1293** | **MT1: 8402, MT2: 8831** | **MT2 关注度暴增 6.8 倍！** |
| **`deepestStrategicDepth`** | **20** | **31** | **探索深度大幅跃升 11 层！** |
| `stoppedReason` | `time-limit` (180.0s) | `time-limit` (180.0s) | 均跑满 180s 墙钟 |
| `peakRssMb` | 1094.0MB | 1990.4MB | 均处于 2GB 限制内 |

```ini
PRIMARY_GATE = CHAOS_MT1_TO_MT3 == FOUND && STRICT_REPLAY_VALID  ->  FAIL
verdict = NEITHER_ARM_FOUND_LOCAL_GATE_FAILED
```

## 4. 机制分析与全家族总结

### 4.1 机制现象分析

1. **Pareto Skyline 展现出了强大的深入推进能力**：
   - 当劣质变种被移出导向堆、仅由高质量资源变种领跑时，搜索在 MT2 上的扩展量从 1,293 飙升至 **8,831**。
   - 战略探索深度从 20 深度大幅推进到了 **31**（创下全系列实验的最深记录）。
2. **为什么战略深度达到 31 仍未通关 MT3**：
   - 战略深度 31 已经超过了成功 witness 的解锁步数（witness 在第 26 步解锁）。
   - 然而，在**零剪枝、纯靠优先队列调度（先扩谁）**的框架下，MT2 内部的战斗分支组合依然过于庞大：
     即使每一步都倾向于探索 Pareto 占优的状态，累积的同级非支配变种数量仍随深度指数扩散；
   - 在 180s 有限墙钟内，搜索虽然打到了深度 31，但探索前沿仍散布在 MT2 的不同战斗尝试与血量权衡中，未能闭合通往 `(6,12)` 的精确解锁钥匙/数值约束链。

### 4.2 “排序器/优先级（Priority-Only）”家族证据链正式闭环

至此，围绕“不剪枝、仅通过优先级/启发排序引导前向搜索”的科学探索全部完成，构成了完整的不可辩驳证据链：

```text
1. 局部学习动作优先级 (PR-5.25c~h)      -> 无楼层进展 (Max MT2)
2. 蒙特卡洛图搜索分配 (PR-5.25b/j/k)      -> 无楼层进展 (Max MT2)
3. 纯运输局部折叠 (PR-5.25l)             -> 改善分支/内存，无楼层进展 (Max MT2)
4. 自主拓扑前置双队列 (PR-5.25m)          -> 强力转向 MT2，无楼层进展 (Max MT2)
5. 资源 Pareto Skyline 优先队列 (PR-5.25n) -> MT2 深度推进至 31，仍无楼层进展 (Max MT2)
```

**终审裁决**：
所有“仅改变扩展先后顺序、不剪枝、平铺状态搜索”的排序器方法，在真实预算下均无法单靠软引导完成跨楼层长程解锁序列。
**“Priority-Only”研究线正式全面关闭。**
这为进入真正的**结构性资源子问题规划（Frontier-Conditioned Resource Subproblem Planning）**提供了极其完备的先决证明。
