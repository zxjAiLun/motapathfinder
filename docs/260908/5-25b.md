# PR-5.25b — Feedback-Allocated Monte Carlo Graph Search (MCGS)

```ini
MILESTONE = PR-5.25b
STATUS = COMPLETED__HYPOTHESIS_LINE_STOPPED (2 capability iterations exhausted)
DATE = 2026-09-08
PREVIOUS_MILESTONE = PR-5.25a (CLOSED: hypothesis line stopped after 3 iterations)
SCOPE = 检验"真实长程采样得到的后果，经过反馈分配以后，能否提高自主
        找到路线的能力"——一个与 5.25a 手工 heuristic 方向实质不同的
        credit-allocation hypothesis。
```

## 0. 核心假设（一级，唯一）

```text
Given the same exact simulator, rollout policy, rollout horizon,
random seeds, terminal goal and real-work budget:

does trajectory-outcome feedback used to allocate future sampling
find a valid route more effectively than sampling allocation
that does not use outcome feedback?
```

**Causal A/B**:

```text
CONTROL   = NO-FEEDBACK graph sampling (uniform existing-edge selection)
TREATMENT = UCT-FEEDBACK graph sampling (edge-local UCT selection)
```

5.25a 只作历史 reference（其手写 heuristic 在同等预算下 NOT_FOUND）。

**Product gate**: `FOUND + STRICT_REPLAY_VALID`。

## 1. Graph contract

全局 exact registry: `exactKey → StateNode`（identity + expansion metadata）。
统计放在 **edges** 上：`(parentExactKey, actionIdentity) → EdgeStats`。

```text
EdgeStats: { N_edge, W_goal, W_aux, Q_goal, Q_aux, childExactKey }
```

**Selection 不使用 child node 的总访问数替代 incoming edge visits**——
防止不同前史因共享 child 而互相污染探索项。

父节点 denominator: `N_parent = sum(outgoing edge visits)`。

## 2. CONTROL isolation

两臂完全共享：exact graph / transposition lookup / legal action enumeration /
expansion rules / rollout policy / rollout horizon / cycle handling / random seed /
terminal predicate / budgets。

唯一不同：

```text
if node has untried legal actions:
    select one untried action using the same seeded rule
else:
    CONTROL   → uniform existing edge
    TREATMENT → UCT existing edge
```

## 3. Selection formula (TREATMENT)

```text
score(edge) = Q_goal(edge)
            + λ × Q_aux(edge)
            + c × sqrt(log(1 + N_parent) / (1 + N_edge))
```

Iteration 1 固定: `λ = 0.10`, `c = 1.0`。不 sweep。

`Q_goal` 与 `Q_aux` 分开保存、分开报告，不写 "success probability"。

## 4. Trajectory-only backup

每次 simulation 保存实际访问序列 `(node0, edge0, ..., nodeK, edgeK)`。
回传只沿 **reverse(trajectory)** 每条实际经过的 edge 更新一次。

不允许因 transposition 顺便更新所有指向同一 child 的 incoming edges。

单人确定性环境：same rollout outcome → same sign backed up。

## 5. Cycles: 全局 graph 可有环；单次 trajectory 不允许有环

```text
GLOBAL_SEARCH_STRUCTURE = directed graph (NOT DAG — MT2→MT1→MT2 合法)
```

每次 simulation 维护 `pathExactKeys`。若 next exact key 已在当前 trajectory：
`termination = CYCLE_TRUNCATED`。非 DEAD_END / UNSOLVABLE / FAILURE_PROVEN。

同理 `ROLLOUT_HORIZON_REACHED` → `TRUNCATED`。

## 6. Rollout & reward

```text
ROLLOUT_HORIZON = 32 decisions (frozen, no sweep)
```

每次 rollout 返回双通道：

```text
goalReward  = 1 if terminal goal reached, 0 otherwise
auxProgress = normalized deepest actual floor progress (MT1=0.0 .. MT5=1.0)
```

Selection: `Q_goal + λ × Q_aux`（λ=0.10——辅助信号最多贡献 terminal reward 十分之一量级）。

日志永远分别报告 `goalHitRate` / `meanAuxProgress`，不写混合 "win probability"。

## 7. L1 micros（4 个）

```text
A. delayed-benefit:
   immediate-good branch → dead; temporary-loss branch → terminal.

B. irreversible recovery:
   wrong key/resource spend vs delayed correct branch.

C. transposition:
   A→B and B→A converge to same exact state;
   one StateNode, separate incoming edge stats.

D. deep-dead-vs-shallow-solvable:
   branch 1 quickly reaches deeper floor but cannot terminal;
   branch 2 stays shallower longer but eventually reaches terminal.
```

通过条件（bounded stochastic evidence, fixed seed）：

```text
after sufficient fixed simulations:
  solvable branch visit share / Q_goal overtakes deep-dead branch
```

## 8. Real benchmark solvability witness（L3 前必须具备）

在相同 chaos initial state / simulator / MT1..MT5 region / blueKing@MT5 terminal
goal 下，必须已有一条 `STRICT_REPLAY_VALID` known witness route。该 route：

```text
MUST NOT be passed to search
MUST NOT influence action ordering
MUST NOT be used as milestone / prefix / threshold
```

它只是 qualification oracle（证明 benchmark 在当前精确规则模型下确实可解）。

**若无此证据，先补 witness 再跑 MCGS。**

## 9. Budget & telemetry

```text
primary:
  wall = 180 s
  RSS  = 2048 MB

metered:
  searchIterations, applyActionCalls, expandedEdges,
  rolloutCount, rolloutDecisionSteps,
  completeDeadEndRollouts, horizonTruncatedRollouts,
  cycleTruncatedRollouts, terminalRollouts,
  uniqueExactStates, transpositionHits,
  timeToFirstGoal, applyActionsToFirstGoal, iterationsToFirstGoal
```

`applyActionCalls` 设很高的共同 safety cap。主预算 = wall + RSS。

## 10. Seeds

```text
SEEDS = [52501, 52502, 52503, 52504]
```

每个 seed 成对运行 CONTROL / TREATMENT。

**Capability**: 任意 TREATMENT run FOUND + STRICT_REPLAY_VALID。

**Feedback evidence**: paired seeds 比较 goal hit / time-to-goal /
applyActions-to-goal / terminal discovery rate。

## 11. ROLLOUT_RETURN_DIVERSITY（Iteration 1 首要诊断）

```text
goal reward distribution
aux reward distribution
unique return buckets
percentage all-zero
deepest floor distribution
```

若 99.9% rollout 都是 goal=0 / aux=0.25 → UCT 无信息可学，失败原因在
sampling horizon / rollout reachability，不在 c。

## 12. Iteration limits

```text
MAX_CAPABILITY_ITERATIONS = 2
```

Iteration 2 只修 Iteration 1 暴露的**一个**明确 deficiency（四选一：
rollout horizon / reward sparsity / UCT over-exploration / progressive widening）。

## 13. 禁止

```text
milestone / authored route / known event order / resource threshold
repair protocol / subgoal decomposition
buildStateKey / exact key 修改
per-floor semantic projection
segment-dp / dominance / state merging
```

（witness route 不进入搜索，不构成 route hint。）

---

## 14. Iteration 1 — Implementation + Qualification Results (2026-09-08)

### 14.1 Implementation closure conditions (all PASSED)

```ini
C1_PHASE_BOUNDARY = PASSED
  Graph selection ends at first node with untried actions;
  expansion creates exactly one new edge;
  default rollout starts from the expanded child and is UNIFORM
  (no heuristic); rollout actions are NOT inserted into the graph.
  Total 32 decision actions per simulation (selection+expansion+rollout).

C2_ACTION_IDENTITY_STABILITY = PASSED
  actionIdentity = fingerprintAction(action) from route-store (canonical
  semantic fingerprint). Same exact state enumerated twice yields identical
  identity sets, no duplicates among distinct legal actions (3/3 at chaos
  root; the production fingerprint covers all action kinds).

C3_RNG_STREAM_ISOLATION = PASSED
  Three independent seeded RNG streams:
    untried-selection: (seed, parentExactKey, simIndex) — stable per state
    control-selection: (seed, 0xC077701, simIndex, exactKey) — CONTROL only
    default-rollout: (seed, 0x0B011, simIndex, step)
  Feedback selection (UCT) consumes NO random numbers → the rollout stream
  cannot be shifted by the CONTROL/TREATMENT difference.
```

### 14.2 L1 Micro (MCGS functional on real project)

```ini
L1_DELAYED_BENEFIT = PASSED
  Goal: hero.exp >= 2 (two battles needed for level-up).
  MCGS found route in 1 iteration (goalHitRate 94.8%, meanAux 0.60).
  Confirms MCGS core + rollout + backup + goal detection all functional.
```

### 14.3 Witness Audit

```ini
WITNESS = AVAILABLE
witnessRoute = routes/latest/mt5-blueking-kill.route.json
start = chaos MT1 (hp 201, atk 2, def 0, mdef 10)
goal = bossDefeated blueKing@MT5
decisions = 92
final = MT5, hero hp 4464
MUST NOT be passed to search / influence ordering / used as hint.
```

### 14.4 L3 Real A/B — 4 Paired Seeds (180s/2GB each arm)

```text
seed 52501: CONTROL 1,985 iter / 180.0s | TREATMENT 2,921 iter / 180.0s
seed 52502: CONTROL 2,612 iter / 180.1s | TREATMENT 1,878 iter / 180.0s
seed 52503: CONTROL 2,242 iter / 180.0s | TREATMENT 1,877 iter / 180.1s
seed 52504: CONTROL 2,015 iter / 180.0s | TREATMENT 1,947 iter / 180.0s

Both arms: found=false across ALL seeds. Both arms: time-limit.
```

### 14.5 ROLLOUT_RETURN_DIVERSITY (Iteration 1 primary diagnostic)

```ini
GOAL_REWARD_DISTRIBUTION = { 0.0 } (ALL ZERO across all seeds, both arms)
AUX_PROGRESS_DISTRIBUTION = { 0.625 } (nearly constant, MT2-3 range)
UNIQUE_GOAL_REWARD_BUCKETS = ["miss"] (single bucket)
UNIQUE_AUX_PROGRESS_BUCKETS = [0.6, 0.65] (two buckets)
ALL_ZERO_PERCENTAGE = 0.0 PERCENT (aux > 0 but goal always 0)
DEEPEST_FLOOR_DISTRIBUTION = { "0.6": ~50%, "0.65": ~50% }
TERMINAL_ROLLOUTS = 0 (across ALL seeds, both arms)
```

### 14.6 Iteration 1 verdict

```ini
PR_5_25b = IN_PROGRESS
ITERATION_1 = COMPLETED__NO_CAPABILITY_WINNER

C1/C2/C3 closure conditions = ALL PASSED
WITNESS = AVAILABLE
L3_VERDICT = NO_CAPABILITY_WINNER
  CONTROL   = NOT_FOUND (4/4 seeds, time-limit)
  TREATMENT = NOT_FOUND (4/4 seeds, time-limit)

PRIMARY_DEFICIENCY = INSUFFICIENT_ROLLOUT_HORIZON
  ROLLOUT_RETURN_DIVERSITY diagnostic result: 32-step uniform rollouts from
  chaos MT1 NEVER reach MT5 blueKing. goalReward = 0 everywhere across all
  4 seeds and both arms. auxProgress ~0.625 constant (MT2-3 plateau).
  UCT has NO discriminative outcome signal to learn from.

  The witness route requires 92 decisions to reach blueKing. A 32-step
  rollout horizon cannot produce a terminal sample from ANY state in the
  reachable set (the deepest 32-step progress plateaus at MT2-3).

PER_REVIEW_CONTRACT: Iteration 2 may ONLY address this single deficiency
  (increase ROLLOUT_HORIZON or equivalent reachability improvement).
  NOT: tune c / progressive widening / better reward mixing.
```

### 14.7 Verification record

```text
check-mcgs.js (C2 + C3 + L1 + witness + L3)      COMPLETED
check:manifest (152 modules, 188 tests)            PASSED
check:no-tower-solver-js                           PASSED
check:core                                         PASSED
```

### 14.8 Discipline

```text
5.25a evaluator inside rollout                    0 (rollout is uniform)
milestone / authored route / event order / threshold   0
witness route passed to search / ordering           0
repair / subgoal / dominance / state merge         0
segment-dp modification                           0
buildStateKey / exact key modification            0
```

---

## 15. Iteration 1 Repair 1 — Qualification Closure：全绿 + 固定工作复验

### 15.1 Cloud Review 终审（Iteration 1 v1）

MCGS 核心方向 APPROVED（edge-local UCT / phase boundary / RNG 实现方向）；qualification 有 5 个未闭合合同：
L1 只实现 1/4 micros / witness 未 strict replay / FOUND route 未 replay gate / graph selection 无 cycle 检测 /
auxProgress 合同未按 MT1=0→MT5=1 实现且未覆盖全 simulation deepest。

### 15.2 五项修复

```text
1. GRAPH SELECTION CYCLE DETECTION:
   graphSelection 携带 pathExactKeys；child exact key 已在当前 trajectory →
   CYCLE_TRUNCATED（非 horizon/dead-end）。

2. AUX PROGRESS CONTRACT:
   MT1=0.0 → MT5=1.0（idx / terminalFloorIndex，非 (idx+1)/(terminal+1)）；
   simulation-wide deepest = max(selection deepest, expansion+rollout deepest)。

3. ALL 4 L1 ADVERSARIAL MICROS:
   A delayed-benefit: exp>=2 goal, MCGS found in 1 iteration
   B irreversible-recovery: atk>=12 goal, MCGS found in 2 iterations
   C transposition: 2,591 transposition hits / 3,099 unique states confirmed
   D deep-dead-vs-shallow-solvable: Q_aux diversity check（当前 auxBuckets=1，
     在 32 步 horizon 从 MT2 fixture 无法产生 floor 分化——诚实验证：D 的
     前提条件尚未满足，与 ROLLOUT_RETURN_DIVERSITY 发现一致）

4. C3 TRUE RNG ISOLATION:
   纯推导断言（同 seed/simIndex/step → 同值；不同 step → 不同值）+
   两臂完成验证。

5. WITNESS + FOUND STRICT REPLAY:
   witness 92 decisions 逐步 replay 于 fresh simulator → blueKing 被击败 →
   STRICT_REPLAY_VALID = TRUE。
   L3 任意 FOUND route 必须通过 strictReplayRoute 才计入 capability verdict。
```

### 15.3 固定工作复验（horizon=32, 修正后 reward）

```ini
L3_REAL_AB (4 paired seeds, 180s/2GB, post-repair):
  VERDICT = NO_CAPABILITY_WINNER
  CONTROL   = NOT_FOUND (4/4, time-limit)
  TREATMENT = NOT_FOUND (4/4, time-limit)

  goalReward = 0.0 PERCENT (ALL seeds, BOTH arms — unchanged)
  meanAuxProgress = 0.581-0.583 (2 buckets, MT2-3 plateau)
  terminalRollouts = 0 (ALL seeds, BOTH arms — unchanged)

  WITNESS_STRICT_REPLAY = VALID (92 decisions, blueKing defeated)
```

### 15.4 修正后归因

```ini
PRIMARY_DEFICIENCY_CANDIDATE =
  INSUFFICIENT_ROLLOUT_REACHABILITY_AT_HORIZON_32

  证据（修复后仍成立）：
  - goalReward 全零（4 seeds × 2 arms，修正后 reward contract 下复验）
  - terminalRollouts = 0（所有 seeds × arms）
  - auxProgress 2 buckets（MT2-3 平台；32 步 uniform 无法到达 MT4+）
  - witness route 92 decisions > 32 step horizon
  - L1 micro D 的 Q_aux 多样化前提在 horizon 32 下不成立
    （MT2 fixture 处 rollout 无法产生 floor 分化）

  NOT_YET_PROVEN: 不存在更短路线（32 步内不可达 ≠ 数学不可达）；
  但修正后 reward 合同下，32 步 uniform rollout 确实无法产生 terminal
  feedback 这一现象已按合同验证。
```

### 15.5 Iteration 1（Repair 1 后）冻结

```ini
PR_5_25b = IN_PROGRESS
ITERATION_1 = COMPLETED__NO_CAPABILITY_WINNER

C1_PHASE_BOUNDARY = PASSED
C2_ACTION_IDENTITY_STABILITY = PASSED
C3_RNG_STREAM_ISOLATION = PASSED
L1_MICROS_A_B_C = PASSED (D prerequisite honestly reported as NOT_MET_AT_HORIZON_32)
WITNESS_STRICT_REPLAY = VALID
L3_VERDICT = NO_CAPABILITY_WINNER

PRIMARY_DEFICIENCY_CANDIDATE =
  INSUFFICIENT_ROLLOUT_REACHABILITY_AT_HORIZON_32
  (UCT 收到的 reward 严格按设计合同产生：MT1=0→MT5=1，deepest 覆盖全程，
   在此合同下 32 步 uniform rollout 仍无 terminal feedback)

ITERATION_2_AUTHORIZATION = PENDING_CLOUD_REVIEW
  (若授权：唯一变量 = ROLLOUT_HORIZON 32→96 decisions，不 sweep)
```

### 15.6 验证记录

```text
check-mcgs.js v2 (C2+C3+L1 A-D+witness+L3)         COMPLETED
check:manifest (152 modules, 188 tests)              PASSED
check:no-tower-solver-js                             PASSED
check:core                                           PASSED
```

---

## 16. Iteration 1 Repair 2 — Strict Replay Fail-Close + Expansion Cycle：全绿

### 16.1 Cloud Review 终审（Repair 1）

五项主修复 APPROVED（auxProgress 合同 / simulation-wide deepest / graph-selection cycle / RNG gate /
horizon-32 zero-terminal 观察）。两个 blocking P1：strict replay fail-open（witness 只查 floorId，
L3 verdict 用 `replayValid !== false` 使 null 被当 valid）+ expansion transition 无 cycle 检查。

### 16.2 三项修复

```text
P1-1 STRICT REPLAY FAIL-CLOSE:
  strictReplayRoute 增加 terminalPredicate 参数——replay 必须
  (a) 每步 re-enumerate 成功、(b) 每步 apply 成功、
  (c) terminalPredicate(finalState) === true 才算 ok。
  Witness 检查: finalFloorId === "MT5" AND floorStates.MT5.removed["6,7"] === true。
  L3 verdict: anyFoundValid = found === true && replayValid === true
  （不是 !== false——null replayValid 不再被计为 valid FOUND）。

P1-2 EXPANSION TRANSITION CYCLE:
  expansionAndRollout 在创建 edge 后检查 pathExactKeys.has(childKey)。
  若 expansion 的 child 已在当前 trajectory → termination = CYCLE_TRUNCATED，
  该 edge 仍为合法 graph edge（全局 graph 允许环），但不执行 rollout。
  设计合同 "ONE SIMULATION MAY NOT REVISIT AN EXACT STATE" 现在覆盖
  selection / expansion / rollout 全部三个阶段。

P2 L1 DOC WORDING:
  L1-A/B → FUNCTIONAL_SIGNAL；L1-C → TRANSPOSITION_OCCURRENCE_OBSERVED；
  L1-D → NOT_MET_AT_HORIZON_32（horizon diagnostic，非 gate failure）。
  不再声称 "all 4 adversarial micros passed"。
```

### 16.3 复验实测

```ini
C2 = PASSED | C3 = PASSED

L1 (graded honestly):
  A delayed-benefit          = FUNCTIONAL_SIGNAL (found in 1 iteration)
  B irreversible-recovery   = FUNCTIONAL_SIGNAL (found in 2 iterations)
  C transposition           = TRANSPOSITION_OCCURRENCE_OBSERVED
      (2,591 hits / 3,099 unique states; edge-local stats by construction)
  D deep-dead-vs-shallow    = NOT_MET_AT_HORIZON_32 (1 aux bucket)

WITNESS_STRICT_REPLAY = VALID
  (92 decisions replayed; blueKing@MT5:6,7 removed = TRUE via predicate)

L3_REAL_AB (4 paired seeds, 180s/2GB, post-repair):
  VERDICT = NO_CAPABILITY_WINNER
  goalReward = 0.0 PERCENT (ALL seeds, BOTH arms — unchanged)
  auxProgress = 0.580-0.583 (2 buckets, MT2-3 plateau — unchanged)
  terminalRollouts = 0 (ALL seeds, BOTH arms — unchanged)
```

### 16.4 Iteration 1 正式冻结

```ini
PR_5_25b = IN_PROGRESS
ITERATION_1 = COMPLETED__NO_CAPABILITY_WINNER

C1_PHASE_BOUNDARY = PASSED
C2_ACTION_IDENTITY_STABILITY = PASSED
C3_RNG_STREAM_ISOLATION = PASSED
L1_A = FUNCTIONAL_SIGNAL
L1_B = FUNCTIONAL_SIGNAL
L1_C = TRANSPOSITION_OCCURRENCE_OBSERVED
L1_D = NOT_MET_AT_HORIZON_32 (horizon diagnostic)
WITNESS_STRICT_REPLAY = VALID (fail-closed)
L3_VERDICT = NO_CAPABILITY_WINNER (fail-closed replay gate)

PRIMARY_DEFICIENCY_CONFIRMED =
  INSUFFICIENT_ROLLOUT_REACHABILITY_AT_HORIZON_32
  (reward contract correct; cycle handling complete across all phases;
   32-step uniform rollout from chaos MT1 still produces ZERO terminal
   feedback across all 4 paired seeds and both arms)

ITERATION_2 = PENDING_CLOUD_REVIEW
  PLANNED_ONLY_CHANGE = TOTAL_DECISIONS_PER_SIMULATION 32 → 96
  (96 > 92-decision witness; not a sweep; all else frozen)
```

### 16.5 验证记录

```text
check-mcgs.js v3 (C2+C3+L1 A-D graded+witness+L3)   COMPLETED
check:manifest (152 modules, 188 tests)               PASSED
check:no-tower-solver-js                             PASSED
check:core                                           PASSED
```

---

## 17. Iteration 2 — Final 96-Decision Capability Qualification: 5.25b 正式停止

### 17.1 Cloud Review 授权

Iteration 1 APPROVED_AND_CLOSED（NO_CAPABILITY_WINNER）。Iteration 2 authorized as
FINAL CAPABILITY ITERATION：唯一算法变量 = TOTAL_DECISIONS_PER_SIMULATION
32 → 96（selection+expansion+rollout 合计 96 decisions；96 > 92-decision witness，
非 sweep）。其余全冻结（UCT_C/LAMBDA/rollout policy/reward/seeds/budget）。

### 17.2 唯一变更

```js
// mcgs.js
TOTAL_DECISIONS_PER_SIMULATION: 32 → 96
```

### 17.3 L3 Real A/B 实测（4 paired seeds，96 decisions，180s/2GB）

```text
seed 52501: CTRL found=false iters=3,026 term=0 | TRT found=false iters=3,212 term=0
seed 52502: CTRL found=false iters=2,821 term=0 | TRT found=false iters=2,940 term=0
seed 52503: CTRL found=false iters=2,961 term=0 | TRT found=false iters=3,496 term=0
seed 52504: CTRL found=false iters=3,602 term=0 | TRT found=false iters=3,366 term=0
All arms: time-limit, terminalRollouts=0
deepestFloorDistribution: { 0.55 (MT2): ~2400, 0.65 (MT3): ~700 } across all runs
```

```ini
VERDICT = NO_CAPABILITY_WINNER

GOAL_REWARD_DISTRIBUTION = { 0.0 } (ALL seeds × BOTH arms — STILL zero at 96)
TERMINAL_ROLLOUTS = 0 (ALL seeds × BOTH arms — STILL zero at 96)
DEEPEST_FLOOR_DISTRIBUTION = { 0.55: MT2, 0.65: MT3 } (bimodal, no MT4/MT5)
UNIQUE_AUX_PROGRESS_BUCKETS = 2 (unchanged from horizon 32)
```

### 17.4 最终科学结论

即使把 sampling horizon 提升到 96 decisions（3× 之前 horizon，超过 92-decision
known valid witness 长度），uniform rollout 从 chaos MT1 仍无法产生任何 terminal
feedback：

- terminalRollouts = 0（4 seeds × 2 arms，全部 time-limit）；
- deepestFloor 始终二元分布（MT2=0.55 / MT3=0.65），无任何 rollout 到达 MT4 或 MT5；
- goalReward 恒为 0——feedback starvation 未被 horizon 96 解除。

按 review 预注册的解释表：

```ini
CASE = 两臂均 NOT_FOUND, terminal 仍全零
→ witness-length-scale uniform rollout 仍不能形成 terminal feedback
→ PR_5_25b STOP
```

### 17.5 PR-5.25b 正式冻结

```ini
PR_5_25b = COMPLETED__HYPOTHESIS_LINE_STOPPED

ITERATION_1 = NO_CAPABILITY_WINNER (horizon 32, zero terminal feedback)
ITERATION_2 = NO_CAPABILITY_WINNER (horizon 96, STILL zero terminal feedback)

MCGS_CAPABILITY = NOT_PROVEN
FEEDBACK_ALLOCATION_EVIDENCE = ABSENT
  (no terminal outcomes produced for either arm at horizon 32 or 96;
   UCT had zero discriminative reward signal to learn from)

RESEARCH_CONCLUSION =
  TRANSPOSITION_AWARE_MONTE_CARLO_GRAPH_SEARCH_WITH_UNIFORM_ROLLOUT
  _CANNOT_PRODUCE_TERMINAL_FEEDBACK_FROM_CHAOS_MT1
  _AT_WITNESS_LENGTH_SCALE_HORIZON_96_UNDER_REAL_BUDGET

  OBSERVED_FAILURE = no longer explained by horizon length alone;
    under the tested budget, uniform rollout failed to sample the
    coordinated cross-floor irreversible decision sequences needed to
    produce terminal feedback (deepest plateaus at MT2-3 bimodal
    regardless of horizon 32 or 96; the bottleneck is cross-floor
    progression, not simply horizon length)

WITNESS_STRICT_REPLAY = VALID (92 decisions, blueKing defeated)
PRIMARY_PRODUCT_GATE = FOUND + STRICT_REPLAY_VALID → NOT_ACHIEVED

NO_ITERATION_3 = TRUE
NO_HORIZON_SWEEP = TRUE
NO_MORE_5_25B_WORK = TRUE
```

### 17.6 下一步（超出 PR-5.25b 范围，需新授权）

既然 uniform rollout 在 witness-length horizon 下仍无法产生 terminal feedback，
下一候选方向应在 rollout policy / search guidance 层面而非 horizon 层面：

- heuristic-guided rollout（复用 production action ranking / progressive widening）
- learned rollout policy
- hybrid: graph MCGS backbone + heuristic leaf rollout
- 或完全不同的 search paradigm

### 17.7 验证记录

```text
check-mcgs.js v4 (C2+C3+L1+witness+L3 96-decision)    COMPLETED
check:manifest (152 modules, 188 tests)                 PASSED
check:no-tower-solver-js                               PASSED
check:core                                            PASSED
```

### 17.8 纪律

```text
唯一算法变更 = TOTAL_DECISIONS_PER_SIMULATION 32 → 96
其余全部冻结（UCT_C / LAMBDA / rollout / reward / seeds / budget / RNG / graph）
witness route 未进入搜索 / 未影响排序
```
