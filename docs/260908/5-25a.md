# PR-5.25a — Event-Level Forward Search with Multi-Step Resource Lookahead

```ini
MILESTONE = PR-5.25a
STATUS = IN_PROGRESS / ITERATION_2_REPAIR_1_COMPLETED__AWAITING_REVIEW
DATE_STARTED = 2026-09-08
PREVIOUS_MILESTONE = PR-5.24j (CLOSED: one-step CF determinately insufficient)
SCOPE = 一个统一的真实状态前向搜索 + 一个只负责排序的有限多步未来评价器；
        验证"有限多步资源推演能否在不改变合法性、去重与状态保留语义的
        情况下，提高真实事件级前向搜索找到可行路线的能力"
```

## 0. 方向来源与一级研究假设

Cloud Review（与方向制定 agent 对齐后）正式撤回上一轮 "Goal-Causal Strategic Search"
主线安排，改为本方向（`APPROVED AS THE NEXT RESEARCH HYPOTHESIS`）。PR-5.24k two-step
CF **不再作为主线**（CF/dependency planner/battle thresholds 降级为纯计算组件，禁止重新
成为控制循环）。

**一级研究假设（唯一）**：

> 有限多步资源推演（multi-step resource lookahead），能否在不改变合法性、去重与状态
> 保留语义的情况下，提高真实事件级前向搜索找到可行路线的能力？

## 1. 架构边界（三条，Cloud Review 已接受）

### 1.1 搜索节点必须是真实 exact state

```text
节点 = hero / inventory / flags / map mutations / floor / location /
       其他决定未来行为的真实状态
边   = 一次有决策意义的 simulator action
```

普通无意义移动由 reachability 折叠（现有 simulator 已提供）。搜索器结构：

```text
search core → existing simulator
```

**不是** `new planner → segment DP → repair → simulator`。不修改 segment-dp 主循环。

### 1.2 评价器只影响优先级（correctness boundary）

评价器可以输出：estimated terminal feasibility / resource plans considered /
estimated acquisition cost / useful thresholds / recommended action ranking /
uncertainty。

评价器**绝对不能**：宣布真实状态非法 / 合并两个 exact state / 证明状态无解 /
删除没看懂的 action / 替 simulator 计算最终状态。

"这个状态看起来没希望" = **晚点展开**，不是删除。因预算/cap 未展开 → incomplete search。

### 1.3 第一版不做跨状态 failure learning

真实结果 → 重新评价当前 state，仅此而已。failure abstraction / backjump learning 后置。

## 2. 探索契约（exploration contract，实现前写死）

```text
EVALUATOR_CAN_PRIORITIZE = TRUE
EVALUATOR_CAN_PERMANENTLY_STARVE_ACCEPTED_STATE = FALSE
```

实现选择（Iteration 1 冻结）：**age-term 双队列**——guided frontier（评价器排序）+
neutral frontier（FIFO），每 N 次 guided expansion 至少允许一次 neutral expansion
（N=8 冻结起始值；不 sweep）。低分动作随等待时间获得 age bonus，永不永久饥饿；
唯一允许的未展开原因是整体 budget/cap 截断（→ incomplete search 分类）。

## 3. 评价器（multi-step future evaluation）最低表达力

必须能表达：

```text
当前状态 → 候选资源取得序列 → 资源变化 → 后续关键战斗成本变化 → 终点通路
在简化模型中是否变得可行
```

例：打怪 X（-600 HP, +EXP → 升级 +3 ATK）→ enemy C damage -900 → predicted terminal
path feasible。这区别于 `HP 多=好 / ATK 高=好` 的普通 heuristic。

### 3.1 简化推演模型必须保留（首轮 review contract）

| 必须保留 | 原因 |
|---|---|
| 一次性资源消费 | 否则凭空重复吃宝石/经验/钥匙 |
| prerequisite | 否则假设拿得到被怪挡住的资源 |
| ATK/DEF/HP/MDEF/EXP/LV 关键变化 | 魔塔投资顺序核心 |
| 多步战斗成本重新计算 | 看到"先加攻、后面省血" |
| alternative route isolation | 不能把互斥路线资源全加 |

未知事件 → `UNKNOWN`（不是 `BLOCKED`）。

### 3.2 允许复用的旧模块（克制）

```text
允许 reuse（纯计算组件）:
  battle-thresholds.js（estimateBattleSurvivability）
  automatic-macro-graph.js（path topology / blocker extraction）
  battle resolver / reachability / resource calculations

禁止 reuse:
  automatic-dependency-planner 整体（其 topology alternatives/prerequisites
  是 bounded-topology-alternatives-not-proof）
  任何"选 subgoal → 执行 → repair → backtrack"控制循环
```

## 4. A/B 实验设计（同核因果对照）

```text
CONTROL:   future evaluator OFF（同 core）
TREATMENT: future evaluator ON（同 core）

严格相同: initial state / terminal goal / allowed region / simulator /
         action enumeration / exact state key / duplicate handling /
         frontier retention / termination / total resource budget
唯一变量: evaluator 开关
evaluator 的时间与内存计入 TREATMENT 预算
```

### 4.1 三方 reference

```text
old blind discovery（blind-discovery-baseline）= 整体位置 reference
new core / evaluator OFF                       = causal control
new core / evaluator ON                        = experiment
```

因果判断只以后二者为准。

## 5. 测试层级

| Level | 内容 | 作用 |
|---|---|---|
| **L1** | 4 个 strategic micro（ordering / threshold / synergy / irreversible investment） | 确认 evaluator 真在做 multi-step future reasoning（非 test coverage） |
| **L2** | controlled MT2→MT4（G32 fixture 起点） | diagnostic：one-step CF 已知不足的状态上 multi-step 是否改变行为 |
| **L3** | terminal-only real region（canonical start + region + terminal goal，无 milestone/threshold/route prefix） | **主结果**；成功标准 = FOUND + STRICT_REPLAY_VALID |

## 6. Progress 彻底降级

`deepest floor +1 / more states / better HP / better projection` 全部只是 diagnostic。
capability ranking：

```text
1. route found + strict replay
2. 若两组都找到：成本
3. 若两组都没找到：没有 capability winner（只能 PROMISING_SIGNAL，
   不能 CAPABILITY_GAIN_PROVEN）
```

## 7. 资源预算（第一轮）

```text
wall:        180 sec / arm / real case（micro 可以小得多）
RSS:         2 GB hard ceiling
expansions:  足够高但共同的 ceiling（wall/memory 进入实际竞争）
A/B 完全相同预算（evaluator 开销计入 TREATMENT）
```

## 8. 停止条件（严格）

最多 **3 个能力 iteration**：
- Iteration 1：实现 + micro 排序差异 + real case 可观察能力信号；
- Iteration 2：只根据 Iteration 1 的一个主要失败原因改 evaluator（禁止 repair framework）；
- Iteration 3：最后一次真实路线 qualification。

三轮后若 control/treatment 都 not found 且收益始终只有 projection/novelty/depth →
**停止此 heuristic 方向**（无 Repair 4/5/6）。

## 9. Iteration 1 范围

```text
做:  new search core（事件级前向搜索，双队列 age-term 探索契约）
     new future evaluator（简化推演模型，§3.1 五条契约）
     thin CLI / harness（A/B 开关）
     4 个 micro fixtures（L1）
     一个 real A/B qualification（L3；L2 diagnostic）
不做: 修改 segment-dp 主循环 / production dominance / CF orchestration /
     failure certificate / global backjump / 训练模型 / MCTS /
     大规模拆旧代码 / MT1→MT8
```

### 9.1 Iteration 1 交付 report（不是二十多个 gate）

```text
CONTROL:   found? / replay? / expansions / wall / peak memory
TREATMENT: found? / replay? / expansions / wall / peak memory /
           evaluator calls / evaluator wall / evaluated multi-step plans
ABILITY DIFFERENCE
+ 几个 evaluator trace（证明识别到"暂时损血→获得ATK→后续战斗大幅降伤"类多步关系）
```

## 10. 与旧体系的区别（防止滑回 D2）

旧体系（禁止回归）：

```text
识别 blocker → 选 subgoal → 调局部 solver → 失败 → repair/backtrack → 再生成 subgoal
```

本方向（必须保持）：

```text
真实状态 S 在 open frontier 中
evaluate(S) → 给 S / outgoing actions 排序
expand(S) → simulator 得到真实 S1/S2/S3
S1/S2/S3 都成为普通搜索节点 → exact dedup → frontier 继续
失败分支没有特殊身份，只是没走到终点的普通搜索分支
```

repair admission / continuation / rollback anchor / counterfactual wave / segment
recovery 全部从主控制结构中拿掉。

---

## 11. Iteration 1 — 实现 + qualification 实测（2026-09-08）

### 11.1 实现（3 个新模块，零 production solver 改动）

```text
lib/event-forward-search.js
  统一事件级前向搜索：单 exact registry（buildStateKey 去重，P1-2 无双展开，
  Iteration 1 无 reopen）+ guided/neutral 双队列调度视图（age-term 探索契约，
  NEUTRAL_EVERY=8 冻结）+ parent 对象引用链（route 重建）+ 共享预算
  （wall/expansions/RSS）+ goal check 先于展开（双臂一致）。
  P1-1：两臂生成完全相同的 legal action 集合——evaluator 只对已生成的
  successors 赋分排序，绝不筛选/增删。P1-4：exact key / dedup / registry /
  action generation / budget / retention / goal check 字面一致。

lib/multi-step-resource-lookahead.js
  有界多步资源推演评价器（frozen params：LOOKAHEAD_DEPTH=3 / MAX_PLANS=24 /
  MAX_ALTERNATIVES=4 / KEY_BATTLES_SAMPLED=6 / TIE_BREAK=enumeration-order）。
  五条契约实现：一次性资源消费（used-set）/ prerequisite（战斗死亡→plan 终止）/
  ATK-DEF-HP-MDEF-EXP-LV 关键变化（levelUpGains 等级引擎）/ 多步战斗成本重算
  （abstractBattleCost per-step）/ alternative isolation（每 plan 独立评估）。
  未知事件 → UNKNOWN（unknowns 列表 + unknownPlans 计数，非 BLOCKED）。
  输出：score / feasibility(FEASIBLE|UNRESOLVED|UNKNOWN) / plansConsidered /
  bestProjectedPlan（explanation only）/ usefulThresholds / uncertainty / trace。

check-event-forward-search.js
  qualification harness：P1 self-checks + L1 四 micro + L2 diagnostic + L3 real A/B。
```

### 11.2 P1 Self-Checks（P1-1/P1-2）

```ini
P1_1_LEGAL_ACTION_SET_IDENTITY = TRUE
   （构造性保证：core 在两臂生成完全相同 action 集合；evaluator 只排序）
P1_2_SINGLE_REGISTRY_NO_DOUBLE_EXPANSION = TRUE
   （expansions ≤ registrySize；stale entry 跨视图跳过：treatment 60 exp / 4 stale）
```

### 11.3 L1 四 Micro（全部通过，evaluator trace 验证多步推理）

| Micro | 验证内容 | 实测 |
|---|---|---|
| **ordering** | 多 plan 投影 + 投影 hero 与 baseline 不同 | 24 plans；projected atk 15→16、lv→2、exp→3（多步战斗+升级链真实发生） |
| **threshold** | atk 投资跨战斗轮数阈值 → damage 下降 | turns 30→24，damage 450→360（+3 atk 跨过 ceil 阈值） |
| **synergy** | A 单独死 / B 单独死 / A+B 活 | atk-only=false, def-only=false, both=true（抽象模型验证） |
| **irreversible** | 一次性资源不重复消费 | bestProjectedPlan 3 entries 全 unique（used-set 契约） |

### 11.4 L2 Controlled MT2→MT4 Diagnostic

| Arm | found | expansions | accepted | wall | stopped |
|---|---|---|---|---|---|
| CONTROL | false | 21,622 | 41,907 | 95.9s | rss-limit |
| TREATMENT | false | 4,976 | 8,305 | 18.8s | rss-limit（evaluator share 5.8%） |

诊断读法：两臂均在 2GB 内被 RSS 截断。Treatment 的 evaluator 只占 5.8% wall
（1,099ms / 9,456 calls），prioritization 让搜索在同等 RSS 内探索了更少的 accepted
states（8,305 vs 41,907）但同样没有找到 MT4——**诊断性结论：该 source 的事件级
全状态搜索在 2GB 内远未饱和，Iter 2 的候选主失败原因是内存约束下的全状态保留，
而非 evaluator 方向错误**。

### 11.5 L3 Terminal-Only Real Region A/B（主结果）

```text
输入: canonical chaos initial state + region MT1..MT5 + terminal goal
       (bossDefeated blueKing@MT5:6,7)——无 milestone/route/threshold/prefix
预算: 120,000 exp / 180s / 2GB（两臂完全相同；evaluator 开销计入 treatment）
```

| Arm | found | replay | expansions | accepted | wall | peak RSS | stopped |
|---|---|---|---|---|---|---|---|
| CONTROL | false | — | 369 | 1,145 | 3.5s | 2,052MB | rss-limit |
| TREATMENT | false | — | 1,176 | 2,633 | 6.0s | 2,050MB | rss-limit（evaluator 8.8% wall / 4,427 calls） |

```ini
VERDICT = NO_CAPABILITY_WINNER
```

诚实归因：两臂均在 2GB RSS 硬顶处被截断（CONTROL 仅 369 expansions）。全状态
事件级前向搜索的内存消耗（每 accepted state 保存完整 hero/inventory/flags/
mutations/floorStates）远超 segment DP 的 bucket 化保留——**第一轮的主要失败原因
是内存下的全状态保留约束，不是 evaluator 方向**。按 §6 纪律：progress 全部降级为
diagnostic（treatment 在同等 RSS 下 accepted 2.3×、expansions 3.2× CONTROL——
PROMISING_SIGNAL，不是 capability 证据）。

### 11.6 Iteration 1 冻结结论

```ini
PR_5_25a = IN_PROGRESS
ITERATION_1 = COMPLETED__NO_CAPABILITY_WINNER__MEMORY_CONSTRAINED

P1_1_LEGAL_ACTION_SET_IDENTITY = TRUE
P1_2_SINGLE_REGISTRY_NO_DOUBLE_EXPANSION = TRUE
P1_4_OFF_ON_RETENTION_IDENTICAL = TRUE（构造性：同 core 同 budget 同 dedup）
P1_3_FROZEN_PARAMS = LOOKAHEAD_DEPTH_3 / MAX_PLANS_24 / MAX_ALT_4 / KEY_BATTLES_6

L1_MICROS = 4/4 PASSED（ordering/threshold/synergy/irreversible 全过，
             trace 证明多步战斗→升级→未来成本变化推理）
L2_DIAGNOSTIC = NO_GOAL_BOTH_RSS_LIMITED（treatment evaluator share 5.8%）
L3_REAL_AB = NO_CAPABILITY_WINNER
  CONTROL   = NOT_FOUND (rss-limit @ 369 exp)
  TREATMENT = NOT_FOUND (rss-limit @ 1,176 exp)
  PROMISING_SIGNAL = treatment 同等 RSS 下 accepted 2.3×（仅 diagnostic）

PRIMARY_ITERATION_1_FAILURE_REASON =
  FULL_STATE_RETENTION_MEMORY_CONSTRAINT（非 evaluator 方向）

ITERATION_2_CANDIDATE_FOCUS =
  内存约束下的状态保留（per-floor 状态精简 / 已访问楼层 state 投影），
  或在保持 exact key 的前提下降低每 state 内存占用。
  禁止：repair framework / evaluator 重写（除非 Iter 2 诊断指向 evaluator）

PRODUCTION_SOLVER_CHANGES = NONE（3 个新模块 + harness，segment-dp 零改动）
```

### 11.7 验证记录

```text
check-event-forward-search.js (P1 + L1 + L2 + L3)       COMPLETED
check:manifest (151 modules, 187 tests)                 PASSED（新增 2 模块 + 1 check）
check:no-tower-solver-js                                 PASSED
check:core                                               PASSED
check:route-free-state                                   PASSED
G24 (counterfactual-repair, spot)                        PASSED
```

### 11.8 纪律遵守

```text
segment-dp 主循环修改        0
production dominance 修改    0
repair / CF orchestration    0
failure certificate          0
训练模型 / MCTS              0
MT1→MT8                      0
evaluator 参数临场调整       0（frozen params 全程未变）
```

---

## 12. Iteration 1 Repair 1 — Evaluator Contract Fidelity：全绿 + 重跑 A/B

### 12.1 Cloud Review 终审（Iteration 1 v1）

Search core `APPROVED`（P1-1/P1-2/P1-4 隔离全部成立，无 D2 回退）；NO_CAPABILITY_WINNER
观测接受。但 evaluator 有两个 contract gap（P1 prerequisite 未实现——map 扫描直接
加资源无 reachability gating；P1 alternative isolation 未实现——无互斥分组，
`alternatives` 变量实际计的是 unknown pickups）+ L1 未覆盖这两个缺口 + L2 heuristic
goal 与 search goal 不一致（MT5 blueKing vs MT4）。

### 12.2 修复内容

```text
P1-1 prerequisite:
  extractResourcesWithPrerequisites(project, simulator, state) 现在以 simulator 的
  enumeratePrimitiveActions 为权威 obtainable 信号：当前 action targets 内的
  battle/pickup = obtainable；map 上存在但不在 action targets 内 = blocked
  （prerequisite = 先击败 corridor guard——plan 内至少一个同组 battle 后 pickup 才
  可消费；blocked battle 可随时 seed plan——fight through corridor IS prerequisite
  satisfaction）。undeterminable → UNKNOWN。

P1-2 alternative isolation:
  obtainable 资源按 stance-proximity 聚类（CLUSTER_RADIUS=6 tiles）分组为
  route-alternative groups；每 plan 只从 ONE group 提取资源；blocked-after-guard
  也限于同组。MAX_ALTERNATIVES=4 现在真正参与（capping active groups）。stabilized
  corridor 状态（obtainable=0）时从 blocked battles 种子化替代组。

新增 L1 micros:
  prerequisite-order：blocked 资源绝不在 guard battle 之前出现于 plan
  alternative-isolation：best plan 只涉及 ≤1 个 group

P1-4 L2 goal:
  treatment evaluator 现在指向 MT4（skeletonCaptain@MT4:8,3）——与 search goal
  同域。
```

### 12.3 重跑实测

#### L1（6/6 micros 全过）

| Micro | 结果 |
|---|---|
| ordering | 24 plans；projected atk 15→16、lv→2、exp→3 |
| threshold | turns 30→24、damage 450→360 |
| synergy | false/false/true |
| irreversible | 3 unique resources |
| **prerequisite-order** | **24 blocked / 0 obtainable 状态下 plan 无违反** |
| **alternative-isolation** | **best plan groups = 0（单组）** |

#### L2（evaluator goal = MT4，diagnostic）

| Arm | found | expansions | accepted | wall | stopped |
|---|---|---|---|---|---|
| CONTROL | false | 17,782 | 37,110 | 72.0s | rss-limit |
| TREATMENT | false | 7,525 | 12,144 | 46.4s | rss-limit（evaluator 50% wall） |

#### L3（terminal-only real region A/B，主结果）

| Arm | found | replay | expansions | accepted | wall | peak RSS | stopped |
|---|---|---|---|---|---|---|---|
| CONTROL | false | — | 369 | 1,145 | 2.9s | 2,048MB | rss-limit |
| TREATMENT | false | — | 7,510 | 14,252 | 52.8s | 2,048MB | rss-limit |

TREATMENT evaluator：24,217 calls / 28,107ms / **53.2% wall share**。

```ini
VERDICT = NO_CAPABILITY_WINNER
```

### 12.4 修正后归因（Repair 1 指令 §5）

```ini
OBSERVED_BINDING_LIMIT = RSS / FULL_STATE_REPRESENTATION
   (两臂均在 2GB 处截断；CONTROL 369 exp，TREATMENT 7,510 exp)

EVALUATOR_HYPOTHESIS_STATUS =
   QUALIFIED_ITERATION_1（contract-faithful evaluator 已验证，
   但 capability 效果在当前内存绑定下不可判）

EVALUATOR_COST_SIGNAL = 53.2 PERCENT WALL SHARE
   (prerequisite-aware evaluation 昂贵——per review 指令：先跑出来，
    不在本轮优化缓存；若 Iter 2 后仍是主要 wall 消耗再判断
    "heuristic 有方向性但太贵")

TREATMENT_PROMISING_SIGNAL (diagnostic only) =
   同等 2GB RSS 下 accepted 12.4×（14,252 vs 1,145）、expansions 20.4×
   （7,510 vs 369）CONTROL——evaluator 的优先级让搜索在内存耗尽前
   探索更多 canonical states
```

### 12.5 Iteration 1（Repair 1 后）冻结结论

```ini
PR_5_25a = IN_PROGRESS
ITERATION_1 = COMPLETED_WITH_CONTRACT_FAITHFUL_EVALUATOR

EVENT_FORWARD_SEARCH_CORE = APPROVED
P1_1_LEGAL_ACTION_SET_IDENTITY = TRUE
P1_2_SINGLE_REGISTRY_NO_DOUBLE_EXPANSION = TRUE
P1_4_OFF_ON_RETENTION_IDENTICAL = TRUE
P1_3_FROZEN_PARAMS = DEPTH_3 / PLANS_24 / ALT_4 / KEY_BATTLES_6

EVALUATOR_CONTRACTS =
  PREREQUISITE = IMPLEMENTED (action-enumeration-gated + corridor-guard)
  ALTERNATIVE_ISOLATION = IMPLEMENTED (stance-cluster groups, one per plan)
  ONE_SHOT_CONSUMPTION = IMPLEMENTED
  MULTI_STEP_BATTLE_RECOMPUTE = IMPLEMENTED
  KEY_STAT_CHANGES = IMPLEMENTED (levelUpGains engine)
  UNKNOWN_NOT_BLOCKED = IMPLEMENTED (events)

L1_MICROS = 6/6 PASSED
L2_DIAGNOSTIC = NO_GOAL_BOTH_RSS_LIMITED (evaluator goal = MT4 aligned)
L3_REAL_AB = NO_CAPABILITY_WINNER

NEXT_AUTHORIZED_STEP = ITERATION_2
ITERATION_2_SCOPE =
  EXACT_SEMANTICS_PRESERVING_SEARCH_STATE_MEMORY_REDUCTION
  (release CLOSED full states / parentId only / queue lightweight handles /
   stale entry 不持有 full state)
  禁止: per-floor semantic projection / dominance / state merging /
  repair / heuristic rewrite
```

### 12.6 验证记录

```text
check-event-forward-search.js v2 (P1 + L1×6 + L2 + L3)   COMPLETED
check:manifest (151 modules, 187 tests)                   PASSED
check:no-tower-solver-js                                  PASSED
check:core                                                PASSED
```

### 12.7 纪律遵守

```text
内存优化（representation）   0（按指令：不在 Repair 1 做）
production solver 修改       0
repair / CF / MCTS          0
evaluator 参数调整          0（frozen params 全程一致）
```

### 12.8 文档归因修正（P1-5）

v1 文档 §11.6 的 `PRIMARY_ITERATION_1_FAILURE_REASON = FULL_STATE_RETENTION_MEMORY_
CONSTRAINT（非 evaluator 方向）` 已按 Repair 1 指令降级为 §12.4 的
`OBSERVED_BINDING_LIMIT = RSS / FULL_STATE_REPRESENTATION` +
`EVALUATOR_HYPOTHESIS_STATUS = QUALIFIED_ITERATION_1`（capability 效果在内存绑定下
不可判——不再声称排除了 evaluator 方向问题）。探索契约实现描述已从 "age-term" 修正为
"dual-view bounded-fair scheduling"（P2：无 age bonus，但 neutral FIFO 兜底满足核心契约）。

---

## 13. Iteration 1 Repair 2 — Prerequisite Identity + Group Integrity：全绿 + 重跑 A/B

### 13.1 Cloud Review 终审（Repair 1）

Search core 继续 APPROVED；L2 goal 对齐 APPROVED；但 prerequisite 只有"任意战斗解锁"而非
specific blocker identity，alternative isolation 被 floor-wide blocked 注入穿透，两个新 micro
fail-open（prerequisite-order 只检查有无 battle；alternative-isolation 只给 obtainable 建
group → groupsInBestPlan=0 为 vacuous green）。Iter 2 暂不授权。

### 13.2 修复内容（Repair 2）

```text
P1-1 PREREQUISITE IDENTITY:
  新增 computeBlockerTopology(project, state, floorId)：从 hero 位置 BFS 洪泛
  passable 格（alive enemies 为墙），识别 boundary enemies（可达区域边界的
  alive 敌人）；再从 boundary enemies 多源 BFS（穿透敌人格 + 其后方 passable
  格），为每个 blocked 资源分配 requiredBlockerKeys（具体的、挡在路径上的
  敌人坐标）。plan 内 blocked 资源 IFF 其 ACTUAL blocker 已在 plan 内被击败。
  unrelated battle 不能解锁。无法确定 → prerequisiteKnown=false →
  UNKNOWN（永不 assumable）。修复 h5mota tile-0 = 空地板语义（map 值
  0/falsy = walkable，非 wall）。

P1-2 GROUP INTEGRITY:
  blocked 资源加入其 blocker 所在的 alternative group（blockerKeyToGroup
  映射）。floor-wide blocked 注入彻底删除。每 plan 从 ONE group 内提取
  （含 blocked 成员）。MAX_ALTERNATIVES=4 界定全部 projected resource space。
  prerequisite-unknown 的 blocked 资源获得独立 group（永不混入他组）。

ADVERSARIAL MICROS（重写，非 fail-open）:
  prerequisite-order: 断言每个 prerequisiteKnown 资源携带 requiredBlockerKeys、
    每个 unknown 资源 requiredBlockerKeys=null、best plan 内 blocked pickup 的
    OWN blocker 已被击败（crossBlockerFalseUnlock=0）、UNKNOWN-prerequisite
    资源绝不出现于 plan
  alternative-isolation: 断言全部资源（obtainable + blocked）携带 groupIndex、
    多个 perturbed 状态的 best plan 均只触及 ONE group
    （crossGroupPlanCount=0 across ALL plans checked）
```

### 13.3 重跑实测

#### L1（6/6 micros，含两个 adversarial contract micros）

| Micro | 结果 |
|---|---|
| ordering | 4 plans；projected atk 2→2/lv→1/exp→2（多步战斗+经验链） |
| threshold | turns 30→24、damage 450→360 |
| synergy | false/false/true |
| irreversible | 2 unique resources |
| **prerequisite-order** | **crossBlockerFalseUnlock = 0；22 blocked / 22 unknown-prereq；plan 无违反** |
| **alternative-isolation** | **crossGroupPlanCount = 0 across 4 plans checked；23 groups 全带 groupIndex** |

#### L2（goal = MT4，10k exp / 120s 预算）

| Arm | found | expansions | accepted | wall | stopped |
|---|---|---|---|---|---|
| CONTROL | false | 10,000 | 25,022 | 77.4s | expansion-limit |
| TREATMENT | false | 10,000 | 13,404 | 49.4s | expansion-limit（evaluator 37% wall） |

#### L3（terminal-only real region A/B，180s/2GB/120k）

| Arm | found | expansions | accepted | wall | peak RSS | stopped |
|---|---|---|---|---|---|---|
| CONTROL | false | 23,708 | 41,582 | 123.8s | 2,049MB | rss-limit |
| TREATMENT | false | 12,196 | 21,845 | 80.2s | 2,052MB | rss-limit |

TREATMENT evaluator：37,769 calls / 39,880ms / **49.7% wall share**。

```ini
VERDICT = NO_CAPABILITY_WINNER
```

### 13.4 归因（Repair 2 后正式口径）

```ini
OBSERVED_BINDING_LIMIT = RSS / FULL_STATE_REPRESENTATION
   (两臂均在 2GB 处截断；同预算下 CONTROL 23.7k/41.6k，TREATMENT 12.2k/21.8k)

EVALUATOR_HYPOTHESIS_STATUS =
   ITERATION_1_COMPLETE（contract-faithful evaluator 已实现并经 adversarial
   micros 锁定；capability 效果在当前内存绑定下不可判）

NOTE_ON_TREATMENT_SELECTIVITY:
   Repair 2 后 treatment 探索 FEWER states（12.2k vs 23.7k——Repair 1 的
   optimistic scoring 已消除；prerequisite-faithful evaluator 更严格）。
   两种解释（heuristic 有效引导 vs heuristic 过度收缩）当前不可区分。
   这是 Iteration 2 memory repair 后需要重新检验的信号。
```

### 13.5 Iteration 1（Repair 2 后）冻结结论

```ini
PR_5_25a = IN_PROGRESS
ITERATION_1 = COMPLETE

SEARCH_CORE = APPROVED
EVALUATOR_CONTRACTS =
  SPECIFIC_PREREQUISITE_IDENTITY = IMPLEMENTED (BFS blocker topology +
    requiredBlockerKeys; unrelated battle never unlocks; UNKNOWN never assumed)
  ALTERNATIVE_GROUP_INTEGRITY = IMPLEMENTED (blocked join blocker group;
    floor-wide injection eliminated; MAX_ALTERNATIVES bounds full space)
  CROSS_BLOCKER_FALSE_UNLOCK = 0 (adversarial micro)
  CROSS_GROUP_PLAN_COUNT = 0 (adversarial micro, ALL plans)
  UNKNOWN_NEVER_ASSUMED_OBTAINABLE = TRUE
  MAX_ALTERNATIVES_ACTUALLY_BOUNDS_PLAN_GROUPS = TRUE

L2_GOAL_ALIGNED = TRUE (MT4)
L1_MICROS = 6/6 PASSED (adversarial)
L3_REAL_AB = NO_CAPABILITY_WINNER (both rss-limit)

NEXT_AUTHORIZED_STEP = ITERATION_2
ITERATION_2_SCOPE =
  EXACT_SEMANTICS_PRESERVING_SEARCH_STATE_MEMORY_REDUCTION
```

### 13.6 验证记录

```text
check-event-forward-search.js v3                       COMPLETED
check:manifest (151 modules, 187 tests)                 PASSED
check:no-tower-solver-js                                PASSED
check:core                                              PASSED
```

### 13.7 纪律遵守

```text
search core 修改       0
production solver 修改 0
内存优化              0（按指令不在 Repair 2 做）
evaluator 参数调整     0（frozen params 全程一致）
```

---

## 14. Iteration 1 Repair 2a — Current-State Topology Closure：全绿 + 最终 A/B

### 14.1 Cloud Review 终审（Repair 2）

Prerequisite identity 数据结构 / blocked→blocker group / floor-wide injection 删除 /
MAX_ALTERNATIVES 全空间界定 / L2 goal 对齐全部 APPROVED。唯一 blocking bug：
`computeBlockerTopology` 使用 `floorState.replaced` 而非当前真实 state 语义（`removed`
的敌人仍被当墙、`replaced` tile 未按替换语义参与 BFS）。另有 prerequisite micro 未
真正命中 known blocker identity（22/22 全 UNKNOWN 路径）与 alternative micro 的
"ALL plans"声称与 sampled best-plan 实现不一致。

### 14.2 修复内容（Repair 2a）

```text
P1 CURRENT-STATE TOPOLOGY:
  computeBlockerTopology 与 assignBlockers 的两个 BFS 全部改用生产
  getTileDefinitionAt(project, state, floorId, x, y)——完全遵循
  floorStates.removed（击败敌人→walkable）与 floorStates.replaced（替换
  语义），不再手写第三套 mutation 解释器。
  TOPOLOGY_TILE_VIEW == SIMULATOR_CURRENT_STATE_TILE_VIEW

REGRESSION: removed blocker 不再被当墙:
  micro 内先 applyAction 击败一个真实敌人，断言该 tile 在 current-state
  view 中不再是 enemy，且 BFS 从 1 cell 扩展到 85 cells。

PREREQUISITE MICRO 增强:
  在 afterBattle（真实 mutated state）上提取：20/20 known blockers
  （0 UNKNOWN 路径——不再全部走 UNKNOWN 兜底），crossBlockerFalseUnlock=0。

ALTERNATIVE MICRO 收紧:
  保留 sampled best-plan check + 新增 trace alternativeGroupIndex 验证
  （4/4 trace groupIndex 与 plan group 匹配）。文档口径修正为
  "structural group-integrity + sampled best-plan + trace verification"。
```

### 14.3 最终 A/B 实测

#### L1（6/6 micros）

| Micro | 结果 |
|---|---|
| ordering | 4 plans；多步战斗+经验链 |
| threshold | turns 30→24、damage 450→360 |
| synergy | false/false/true |
| irreversible | 2 unique |
| **prerequisite-order** | **20 known / 0 unknown；crossBlockerFalseUnlock=0；removedBlockerRegression=true** |
| **alternative-isolation** | **crossGroupPlanCount=0；traceGroupIndicesVerified 4/4；23 groups** |

#### L2（goal = MT4）

| Arm | found | expansions | accepted | wall | stopped |
|---|---|---|---|---|---|
| CONTROL | false | 9,835 | 24,612 | 120.0s | time-limit |
| TREATMENT | false | 10,000 | 13,404 | 119.1s | expansion-limit（evaluator 38% wall） |

#### L3（terminal-only real region，180s/2GB/120k）

| Arm | found | expansions | accepted | wall | peak RSS | stopped |
|---|---|---|---|---|---|---|
| CONTROL | false | 12,444 | 24,386 | 180.0s | 1,455MB | time-limit |
| TREATMENT | false | 11,417 | 20,559 | 177.8s | 2,049MB | rss-limit（evaluator 51.6% wall） |

```ini
VERDICT = NO_CAPABILITY_WINNER
```

### 14.4 Iteration 1 正式关账

```ini
PR_5_25a = IN_PROGRESS
ITERATION_1 = COMPLETE

TOPOLOGY_TILE_VIEW = SIMULATOR_CURRENT_STATE_TILE_VIEW (PROVEN)
REMOVED_BLOCKER_REGRESSION = PASSED
KNOWN_BLOCKER_IDENTITY_HIT = 20/20 (0 UNKNOWN fallback)
CROSS_BLOCKER_FALSE_UNLOCK = 0
CROSS_GROUP_PLAN_COUNT = 0
TRACE_GROUP_INDICES_VERIFIED = 4/4
UNKNOWN_NEVER_ASSUMED_OBTAINABLE = TRUE
MAX_ALTERNATIVES_BOUNDS_FULL_SPACE = TRUE
L2_GOAL_ALIGNED = TRUE

L3_REAL_AB = NO_CAPABILITY_WINNER
  CONTROL   = NOT_FOUND (time-limit @ 12,444 exp / 1,455MB)
  TREATMENT = NOT_FOUND (rss-limit @ 11,417 exp / 2,049MB)
  EVALUATOR_WALL_SHARE = 51.6 PERCENT

NO_EVALUATOR_REPAIR_3 = TRUE (contract line CLOSED per review instruction)
NEXT_AUTHORIZED_STEP = ITERATION_2
ITERATION_2_SCOPE =
  EXACT_SEMANTICS_PRESERVING_SEARCH_STATE_MEMORY_REDUCTION
  (release CLOSED full states / parentId only / queue lightweight handles /
   stale entries 不持有 full state)
  禁止: per-floor semantic projection / dominance / state merging /
  repair / evaluator rewrite
```

### 14.5 验证记录

```text
check-event-forward-search.js v4                       COMPLETED
check:manifest (151 modules, 187 tests)                 PASSED
check:no-tower-solver-js                                PASSED
check:core                                              PASSED
```

---

## 15. Iteration 2 — Exact-Semantics-Preserving Search-State Memory Reduction：全绿

### 15.1 Cloud Review 授权

Iteration 1 `APPROVED_AND_CLOSED`（`NO_CAPABILITY_WINNER`；evaluator contract line
CLOSED；no Repair 3）。Iteration 2 authorized：唯一变量 = 同一批 exact states 的
retained memory，不改变搜索语义。

### 15.2 实现（`event-forward-search.js` 内存表示重构）

```text
CLOSED nodes 释放 full state（state=null），仅保留:
  exactKey（去重身份）+ parentId（路线重建）+ compact replay action（summary string）
OPEN nodes 保留 full state。

guided heap / neutral queue 存储 lightweight nodeId handles（整数），
不再间接持有 node objects / full states。
neutral queue 改用 head index（避免 O(n) shift）。

路线重建走 parentId → node records（携带 compact replay action），
产出与 Iteration 1 完全一致的 route。

未触碰: buildStateKey / exact key / per-floor projection / evaluator / dominance /
state merging / repair / scheduler。
```

### 15.3 G33-REPR 等价门禁（representation equivalence）

```ini
G33_REPRESENTATION_EQUIVALENCE = PASSED
  determinism: 500-exp 双跑 expansions/accepted/duplicates 完全一致
  route reconstruction: 小 goal run found + route + finalState 正确
  memory contract: fullStatesRetained == openNodes（CLOSED 全部释放）
                   closedNodes == expansions
                   fullStatesRetained < accepted
```

### 15.4 Memory Qualification（fixed 2000 exp，两臂）

| Arm | accepted | wall | peak RSS | fullStatesRetained | RSS/accepted |
|---|---|---|---|---|---|
| CONTROL | 5,286 | 31.3s | 460.5MB | 3,287 (=open) | 0.087MB |
| TREATMENT | 4,141 | 40.9s | 512.3MB | 2,142 (=open) | 0.124MB（evaluator 51% wall） |

CLOSED states 全部释放（fullStatesRetained ≈ OPEN only，不是 ALL accepted）。

### 15.5 L2 Diagnostic

| Arm | found | expansions | accepted | wall | stopped |
|---|---|---|---|---|---|
| CONTROL | false | 10,000 | 25,022 | 110.1s | expansion-limit |
| TREATMENT | false | 5,556 | 7,894 | 55.0s | rss-limit（evaluator 38% wall） |

### 15.6 L3 Real A/B（180s/2GB/120k，主结果）

| Arm | found | replay | expansions | accepted | wall | peak RSS | stopped |
|---|---|---|---|---|---|---|---|
| CONTROL | false | — | 5,780 | 13,119 | 97.8s | 2,048MB | rss-limit |
| TREATMENT | false | — | **11,452** | **20,614** | 180.0s | 2,047MB | **time-limit** |

TREATMENT evaluator：35,530 calls / 92,811ms / **51.6% wall share**。

```ini
VERDICT = NO_CAPABILITY_WINNER
```

### 15.7 关键新信号

内存表示修复后，两个 arm 的 binding resource 发生了分离：

- **CONTROL → memory-bound**（rss-limit @ 5,780 exp / 2,048MB）：朴素 FIFO 搜索
  在 2GB 内只能接受 13k states；
- **TREATMENT → time-bound**（time-limit @ 11,452 exp / 2,047MB / 180s）：
  evaluator 引导的搜索利用释放的内存探索了 **2× 于 CONTROL 的 canonical
  states**（20,614 vs 13,119 accepted），但 evaluator 自身占 51.6% wall。

与 Iteration 1 对照：treatment 从 rss-limit 变为 time-limit——**同样的 2GB 内
evaluator 现在能多探索近一倍的状态空间**。但 51.6% evaluator wall share 意味着
下一轮的主要失败原因（若 treatment 仍 NOT_FOUND）将是 **evaluator cost /
selectivity**，不再是 memory representation。

### 15.8 Iteration 2 冻结结论

```ini
PR_5_25a = IN_PROGRESS
ITERATION_2 = COMPLETED_AWAITING_REVIEW

EXACT_SEMANTICS_PRESERVED = TRUE（G33 equivalence gate PASSED）
MEMORY_REPRESENTATION_REDUCTION = PROVEN
  (CLOSED states released; fullStatesRetained == OPEN only;
   RSS/accepted 0.087-0.124 MB vs Iteration 1 ~0.15+ MB)

L3_REAL_AB = NO_CAPABILITY_WINNER
  CONTROL   = NOT_FOUND (rss-limit @ 5,780 exp)
  TREATMENT = NOT_FOUND (time-limit @ 11,452 exp, evaluator 51.6% wall)

BINDING_RESOURCE_SHIFT =
  ITERATION_1: BOTH_ARMS_RSS_LIMITED
  ITERATION_2: CONTROL_RSS_LIMITED + TREATMENT_TIME_LIMITED

TREATMENT_EXPLORATION_GAIN = 1.76x ACCEPTED (20,614 vs 13,119)
  (vs Iteration 1 where both arms hit RSS at similar accepted counts)

NEXT_CANDIDATE_FOCUS =
  EVALUATOR_COST_AND_SELECTIVITY
  (per §14 review: if treatment now not memory-bound but 50%+ wall in
   evaluator, the main failure reason is evaluator cost/selectivity)

NO_EVALUATOR_CACHE_OPTIMIZATION_THIS_ROUND = TRUE
NO_PER_FLOOR_PROJECTION = TRUE
NO_BUILD_STATE_KEY_CHANGE = TRUE
```

### 15.9 验证记录

```text
check-event-forward-search.js v5 (G33 + memQual + L1 + L2 + L3)  COMPLETED
check:manifest (151 modules, 187 tests)                           PASSED
check:no-tower-solver-js                                          PASSED
check:core                                                        PASSED
```

### 15.10 纪律遵守

```text
buildStateKey / exact key 修改     0
per-floor semantic projection      0
evaluator 修改 / cache 优化       0（按指令：先跑出来不优化）
dominance / state merging / repair 0
segment-dp 修改                   0
```

---

## 16. Iteration 2 Repair 1 — True Compact Retention + Legacy Equivalence：全绿

### 16.1 Cloud Review 终审（Iteration 2 v1）

lightweight queue / parentId / state=null 机制 APPROVED；但 P1-1 G33 实为 compact
双跑（非 legacy vs compact 比较），P1-2 CLOSED nodes 仍通过 `action.travelState`
间接保留完整游戏状态（后门引用链）。

### 16.2 修复内容（Repair 1）

```text
TRUE COMPACT RETENTION:
  node records 不再存储完整 simulator action 对象。
  actionSummary (string) 是唯一的 replay descriptor。
  acceptChild 时即不保留 action（创建时剪掉，非 close 时）。
  legacy 模式（test-only）通过 config.representation="legacy" 保留 state+action。
  fullActionsRetained telemetry + G33 断言 = 0。

TRUE LEGACY vs COMPACT G33:
  同一 core，两种 representation mode，固定 500-exp：
  - expandedKeys 序列 deepStrictEqual
  - acceptedKeys 序列 deepStrictEqual
  - duplicateDecisionKeys 序列 deepStrictEqual
  - 小 goal case found + route deepStrictEqual
  - compact CLOSED state refs = 0, retained full action objects = 0
  - legacy CLOSED nodes 确实保留 state（验证测试真实 exercised 两种表示）

TELEMETRY 重命名:
  staleQueueEntries → queuedFrontierHandles（P2）
```

### 16.3 G33 实测（true legacy vs compact）

| Metric | LEGACY | COMPACT |
|---|---|---|
| expansions | 500 | 500 |
| accepted | 1,513 | 1,513 |
| duplicatesSkipped | 852 | 852 |
| fullStatesRetained | **1,514**（全部） | **1,014**（= openNodes only） |
| fullActionsRetained | **1,513** | **0** |

```ini
LEGACY_VS_COMPACT_EXPANDED_KEYS = IDENTICAL
LEGACY_VS_COMPACT_ACCEPTED_KEYS = IDENTICAL
LEGACY_VS_COMPACT_DUPLICATE_DECISIONS = IDENTICAL
FOUND_RESULT = IDENTICAL
ROUTE = IDENTICAL
COMPACT_CLOSED_STATE_REFS = 0
COMPACT_RETAINED_FULL_ACTION_OBJECTS = 0
```

### 16.4 最终 L3 Real A/B（true compact 后）

| Arm | found | expansions | accepted | wall | peak RSS | stopped |
|---|---|---|---|---|---|---|
| CONTROL | false | 11,835 | 23,463 | 180.0s | 1,718MB | **time-limit** |
| TREATMENT | false | **16,670** | **30,105** | 180.0s | 1,719MB | **time-limit**（evaluator 52.0% wall） |

```ini
VERDICT = NO_CAPABILITY_WINNER
```

### 16.5 关键归因变化（Repair 1 前后对比）

| 阶段 | CONTROL stopped | TREATMENT stopped | 隐藏引用链 |
|---|---|---|---|
| Iter 2 v1（action 保留） | rss-limit @ 5,780exp | time-limit @ 11,452exp | action.travelState 保留完整世界 |
| **Iter 2 R1（action 剪除）** | **time-limit @ 11,835exp** | **time-limit @ 16,670exp** | **0** |

action-travelState 剪除后，两个 arm 均从 memory-bound 转为 time-bound：

- CONTROL：5,780 → **11,835** exp（+2.05×，2GB→1,718MB）
- TREATMENT：11,452 → **16,670** exp（+1.46×，2,047→1,719MB）

```ini
BINDING_RESOURCE = WALL_TIME (BOTH ARMS)
HIDDEN_RETENTION_ELIMINATED = TRUE (fullActionsRetained = 0)
TREATMENT_EXPLORATION_GAIN = 1.41x ACCEPTED (30,105 vs 23,463)
EVALUATOR_WALL_SHARE = 52.0 PERCENT
```

### 16.6 Iteration 2（Repair 1 后）冻结结论

```ini
PR_5_25a = IN_PROGRESS
ITERATION_2 = COMPLETED_AWAITING_REVIEW

EXACT_SEMANTICS_PRESERVED = TRUE
  (G33 true legacy/compact: expanded/accepted/duplicate/GOAL/ROUTE all IDENTICAL)
MEMORY_REPRESENTATION_REDUCTION = PROVEN
  (fullActionsRetained=0; fullStatesRetained=openNodes only;
   both arms 2GB→1.7GB peak; both arms rss→time bound shift)

L3_REAL_AB = NO_CAPABILITY_WINNER
  CONTROL   = NOT_FOUND (time-limit @ 11,835 exp / 1,718MB)
  TREATMENT = NOT_FOUND (time-limit @ 16,670 exp / 1,719MB)

NEXT_MAJOR_DEFICIENCY = EVALUATOR_COST / SELECTIVITY
  (per review 情况 A: treatment not memory-bound but 52% wall in evaluator)
  → Iteration 3 candidate: selectivity/ranking quality over caching

NO_BUILD_STATE_KEY_CHANGE = TRUE
NO_PER_FLOOR_PROJECTION = TRUE
NO_EVALUATOR_CACHE_OPTIMIZATION = TRUE
```

### 16.7 验证记录

```text
check-event-forward-search.js v6 (G33 true + memQual + L2 + L3)   COMPLETED
check:manifest (151 modules, 187 tests)                           PASSED
check:no-tower-solver-js                                          PASSED
check:core                                                        PASSED
```

### 16.8 纪律遵守

```text
buildStateKey / exact key 修改     0
per-floor semantic projection      0
evaluator 修改 / cache 优化       0
dominance / state merging / repair 0
segment-dp 修改                   0
```
