# PR-5.32c — Resource-Aware Local Continuation

```ini
MILESTONE = PR-5.32c
STATUS = REJECTED
IMPLEMENTATION = IMPLEMENTED
VERIFICATION = VERIFIED (local contract/identity/durable/core/manifest gates; frozen same-commit 32k A/B and evidence audit)
ACCEPTANCE = NOT_PROMOTED
STATUS_NOTE = The bounded local resource-first hypothesis failed its frozen route-prefix gate and regressed exact-prefix service. No goal was found; close this comparator-tuning branch without sweeping K or weights.
DATE_STARTED = 2026-09-23
BASE_COMMIT = 9cc53b908ef0c73a7395720200494cf6aa3c9d1f
SCOPE = Keep global goal-relative + Fair4; change only the comparator used inside the bounded K32 continuation-slice heap: inherit (control) vs resource-first (treatment).
```

## Authorization and frozen comparison

Owner accepted 5.32a/b as directional evidence: longer goal-relative budget did not find TS14; global resource-first regressed. Owner explicitly authorized this next single-variable mechanism experiment and reviewed the implementation boundary.

- Control and treatment use the same source commit/bundle, seed `2-7f41f60b09a55951abd8931c`, tower, initial state, global `dpPriorityMode=goal-relative`, Fair4 (`hybrid-fair`, fairnessEvery=4, FIFO), slice K=32, total budget=32k, DP key/dominance/archive/action-generation/candidate limits.
- Control `continuationSlice.localPriorityMode=inherit`; treatment sets only this option to `resource-first`.
- Local comparator reads the already-built rank fields through an explicit mode argument. It must not write/temporarily override `rank.priorityMode`, mutate/rebuild ranks, or change global heap comparator. Global agenda remains goal-relative. Local survivors remain globally owned and retain ordinary global rank when the bounded slice ends.
- `localPriorityMode` is part of canonical `searchSemantics`, resume fingerprint, execution provenance, and is forwarded by durable `runAttempt`. Disabled slice normalizes local mode to null; enabled omitted mode normalizes to `inherit`.
- Supported local values in this change: `inherit` and `resource-first`; unsupported values fail closed.
- Search does not load/replay witness; post-run exact-checkpoint lifecycle matching is offline. No other comparator field, weight, K/fairness, rank creation, pruning, or action scope changes.

## Frozen outcome gate

Positive requires either (A) autonomous TS14 candidate materialized and original-start strict replay passes with protected green key unchanged, or (B) at identical 32k work, clearly passes the control's first exact winning-prefix blocker and generates/accepts a legal next-prefix successor, with lifecycle evidence. Merely higher HP/ATK, isolated exact-key hits, or a queue `accepted` label is insufficient. Record per-slice floor distribution, slice roots/work, goal distance at slice start/end, resource delta, exact blocker accepted/popped/evicted/rejected lifecycle, frontier, wall, RSS, trims/errors, and full normalized outcome. Both arms are rerun on the same commit; no historical arm is silently substituted.

If local resource-first fails this gate, close this local-priority-mode branch rather than sweep modes/weights; next design would be semantic feasibility/blocker subgoals using existing progressive-planning facilities. No production deployment occurs without verified full route.

## Implementation and local verification (2026-09-23)

Implemented in `shared-solver/lib/dp-search.js` and `lib/durable-search.js`:

- Global comparator and rank construction stay unchanged. The existing comparator body now accepts an optional explicit mode through `compareDpAgendaRankForMode(left,right,mode)`; normal global calls use the original `rank.priorityMode` rules. The slice heap gets `compareContinuationSliceRank` and uses explicit `resource-first` only when configured; `inherit` uses the exact existing single-root comparator.
- No rank writes, temporary `priorityMode` override, rank reconstruction, global heap change, or re-enqueue. New local diagnostic states configured effective local mode. Mode is only `inherit` or `resource-first`; unsupported enabled values fail closed.
- `searchSemantics()` records enabled omitted local mode as `inherit`, disabled slice local mode as `null`; it is included in fingerprint/provenance. Durable `runAttempt` forwards that same canonical value to `searchDP`.
- Extended continuation-slice contract: local mode can reverse an intentionally conflicting shared rank pair, global comparator result remains identical before/after, frozen rank objects stay unchanged, K=1 local resource-first leaves global pop/result projection invariant, real local resource-first obeys K/global budget/fair trigger, reports global rank modes unchanged and is deterministic.
- Extended search-semantics identity contract: inherit/resource-first fingerprint differently; changing disabled local mode is inert; recovery refuses local-mode drift; provenance records inherit.
- Targeted PASS: `check:continuation-slice`, `check:search-semantics-identity`, `check:durable-search`, `check:core`, `check:manifest` (174 modules / 231 graded tests), `check:no-tower-solver-js`; `node --check` for changed JS files. Differential check compared old committed global comparator against its refactored compatibility path across 20,000 generated rank pairs (including mixed modes): exact match. At implementation-verification time the cloud pair had not yet run; its frozen result is appended below.
- Broader `npm run check:static` was attempted and **failed** in untouched `shared-solver/check-region-entry-contract.js:199` at `full report rebuild must be deterministic`. Isolating normalized report diffs shows the mismatch is limited to `inputSha256` for six generated negative-control fixtures under `shared-solver/routes/generated/region-entry-contract/negative-inputs/`; no search/solver outcome fields differ. These fixture hashes are stale relative to their current generated inputs; this gate remains a fixture/report hygiene blocker, not claimed fixed. The 5.32c contract/identity and core/manifest checks pass.

### Frozen A/B contract (frozen before run)

After committing/pushing this source version, package the same immutable bundle for both arms, fresh output directories, actual config `dpPriorityMode=goal-relative` and `continuationSlice={enabled:true,budget:32,localPriorityMode:inherit|resource-first}`, Fair4/FIFO. Run Control and Treatment serially at 32,000 expansions from the same seed. Capture per-slice floor distribution and start/end goal-distance/resource deltas as well as exact blocker lifecycle and resource/outcome diagnostics. No K/mode sweep; evaluate the frozen success gate above.

## Iteration log

### Iteration 1 — 2026-09-23 — Frozen cloud pair

- **Change:** No solver change after source commit `7de00f27d13312cf26f45db92512a4bf68250a48`; serially ran Control (`localPriorityMode=inherit`) and Treatment (`resource-first`) from fresh run directories, each on the same 1,262-file bundle, exact seed/initial, and 32,000-expansion budget. Search was global `goal-relative + Fair4/FIFO` in both.
- **Preflight:** `problemFingerprint` matched prior 5.32a (`0ca7d0…dec78`); solver digest matched between arms; normalized search contracts differed only in local mode and had distinct resume fingerprints. Seed strict replay passed for 29 decisions with protected-item checks. Witness route was absent from the search bundle and was joined to exact checkpoints only after both processes completed.
- **Control:** 32,000 expansions; `foundGoal=false`, no goals, `budgetExhausted=true`, `frontierExhausted=false`, `searchComplete=false`; frontier 44,885; wall 325.218s; peak RSS 2,286.6 MiB; action trims / observer errors / run errors all zero. 1,633 slices, 29,890 local expansions, each at most K=32. Slice expansion floors: TS13 23,212 / TS12 5,917 / TS11 761; root floors TS13 787 / TS12 774 / TS11 72. Exact contiguous prefix reached P13: P7 pop 843 (wait 831), P8 845, P12 31,355 (wait 30,505), P13 31,356. P14 `battle:skeletonCaptain@TS13:5,6` was rejected twice by dominance (candidate HP 2,667, ATK 8, DEF 1, LV3, EXP8); it was never accepted or popped. P15 also popped at 31,574 but is non-contiguous and not a bypass of P14.
- **Treatment:** 32,000 expansions; `foundGoal=false`, no goals, `budgetExhausted=true`, `frontierExhausted=false`, `searchComplete=false`; frontier 38,331; wall 253.917s; peak RSS 2,007.8 MiB; action trims / observer errors / run errors all zero. 2,380 slices, 27,493 local expansions, each at most K=32. Slice expansion floors: TS13 8,428 / TS12 13,066 / TS11 5,999; root floors TS13 630 / TS12 1,346 / TS11 404. Exact contiguous prefix stopped at P7: P7 was accepted at 466 but not popped until 14,127 (wait 13,661, `fair-oldest`); it generated/accepted exact P8 at 14,128, but P8 was not popped by budget end and P9 was not accepted. Treatment did not accept or pop control blocker P14 and emitted no exact next-prefix successor from it.
- **Per-slice direction:** Control mean goal-distance delta (root→last local pop) −4.288; 722/1,633 windows improved, 744 were flat, 167 worsened. Treatment mean −0.641; 486/2,380 improved, 1,450 flat, 444 worsened. Treatment shifted local expansion toward TS12/TS11 and away from TS13; its best observed HP was lower (4,945 vs 5,451), and ATK did not improve (both max ATK 9). The shorter wall time and smaller frontier are not success evidence.
- **Gate:** Autonomous goal + original-start route strict replay was not reached. Gate B also failed: Treatment regressed from Control's contiguous P13 to P7 and did not pop P14 or produce its accepted successor. Verdict: reject the bounded local resource-first priority hypothesis; do not sweep K, comparator modes, or weights; do not promote the feature/configuration to production.
- **Evidence correction:** An initial offline prefix-join draft reused one mutable checkpoint accumulator across arms. The issue was detected before publication (it made the arms appear to share lifecycle rows); the analyzer was corrected to allocate independent checkpoint tables per arm and rerun against unchanged raw event streams. Only the corrected per-arm result is used here and in the result artifact.
- **Isolation:** Search service was inactive and the old production ledger remained paused (`activeTask=null`, 43 nodes, `STOP` present) before/after. Both runs used independent fresh directories. Bundle post-run drift was zero. Full event streams remain on cloud with hashes; a compact evidence archive is retained under the ignored generated directory.

## Final result and evidence

- **Status:** `VERIFIED / NOT_PROMOTED`; the resource-first local-comparator hypothesis is rejected. This is a bounded negative result, not evidence that the tower is unsolvable.
- Tracked compact result: [`5-32c-result.json`](5-32c-result.json). It contains arm/run identities, normalized outcomes, slice/floor summaries, exact-prefix findings, and evidence hashes.
- Cloud evidence root: `/home/ubuntu/motapath-solver/probes/pr532c-7de00f2-r1/`; both raw JSONL streams are retained there. Local compact bundle: `shared-solver/routes/generated/260923-pr532c/evidence-pr532c-32k-compact.tar.gz` (SHA-256 `686e0ca279a38c23c16d25ab675355c77e6ac9aff15855a0cf33b4dc5f640ecf`). Detailed extracted evidence is in the sibling ignored `evidence-r1/` directory. The witness was used only by post-run analysis.
- Focused checks rerun after the pair: `check:continuation-slice`, `check:search-semantics-identity`, `check:durable-search`, `check:core`, `check:manifest` (174 modules / 231 graded tests), and `check:no-tower-solver-js` all PASS. `npm run check:static` remains blocked by the six pre-existing stale generated negative-input fixture hashes described above; no fixture was modified.
- No autonomous goal candidate exists, so full-route strict replay and terminal protected-key closure could not be claimed. Production release remains unauthorized; old production ledger remains paused/read-only.

## Next research gate

Close the local comparator / priority-mode tuning branch. The next direction is a **design-only** general semantic feasibility/blocker-subgoal proposal grounded in the observed P8 accepted-but-unpopped and Control P14 dominance-rejection lifecycle. Freeze a general, non-route-authored objective and a bounded gate before implementation. Do not change K, sweep comparator modes/weights, resume the old production ledger, or deploy this local mode.
