# PR-5.24i — Terminal Positive Progress Counterfactual Work-Conservation

```ini
MILESTONE = PR-5.24i
STATUS = IN_PROGRESS / ITERATION_1_COMPLETED_AWAITING_REVIEW
DATE_STARTED = 2026-09-07
BASE = bfd437d4ef64098281f83d4a40ca9bcc4e61caef + promotion commit e0bee7d
SCOPE = 只修 CF admission 的 work conservation：\"曾发生过局部 positive progress\"
        不应永久屏蔽 counterfactual；只有仍存在可执行 normal work 的
        positive ticket 才阻止 CF。
```

## 1. 问题定义（来自 5-24h 9.2s Formal Run 的实锤轨迹）

PR-5.24h Real Run 1（9.2s 提前自然返回）暴露的 admission 缺陷：

- depth-1 两个 normal primary ticket 均 `progressClass=SEGMENT_ADVANCE`（positive）；
- 但两个 ticket 均 **determinately exhausted**：`status=PROBE_COMPLETE_OR_GOAL`、`stopReason=exhausted`、`continuationEligible=false`、`goalReached=false`、零 pending；
- 全局还有 20.8s wall 未用；
- 旧规则 `positiveInitial > 0 ⇒ CF blocked`（reason=`normal-primary-progress`）把 CF 永久屏蔽。

这正是 solver 战略能力的核心缺口：**省血路线确实推进过一段，但最终确定走死时，solver 没有把剩余预算拿去尝试资源投资型替代历史**。

## 2. 实现（`shared-solver/lib/segment-dp.js`）

### 2.1 Pure helper `assessCounterfactualAdmission`（B1）

从 `tryAdaptiveCheckpointRepair` 的 inline admission 逻辑抽出，单一判定源，导出供 G31 独立测试。输入：`normalDepthTickets / deferredNormalDescriptors / depthGoalReached / globalStopReason / isTrustedCompleteFailure / failureExecutionDeterminateComplete / counterfactualAlreadyTriggered / intentGeneratorAvailable / normalFirstRoundComplete / realNormalProbedTickets`。输出：`trigger / reason / positiveTicketCount / actionablePositiveTicketCount / terminalPositiveTicketCount / hasPendingNormalWork / hasDeferredNormalWork / hasContinuationEligiblePositive / blockedBy{...} / deferredNormalPresent`。

### 2.2 ACTIONABLE vs TERMINAL positive（B2/B3）

```text
ACTIONABLE positive（仍阻止 CF）——任一：
  continuationEligible = true
  status = PROBE_PENDING
  stopReason = probe-limited
  lastProgress.goalReached = true

TERMINAL positive no-goal（不再单独阻止 CF）：
  progressClass ∈ {WITHIN_SEGMENT_PROGRESS, SEGMENT_ADVANCE}
  AND goalReached = false
  AND status ≠ PROBE_PENDING
  AND stopReason ≠ probe-limited
  AND continuationEligible ≠ true
```

### 2.3 新 admission 条件（B4）

```text
trusted normal tranche complete
AND no goal
AND no ACTIONABLE positive ticket
AND global stop clean
AND trusted failure class
AND failure determinately complete
→ CF triggers
```

reason 分开：`primary-normal-exhausted-no-progress`（历史 no-progress 路径）与 **`terminal-positive-no-goal`**（新路径）。

### 2.4 Deferred normal authority（B8/G31-D 修正）

初版实现曾把 `deferredNormalDescriptors.length > 0` 作为 blocker——**G24-K1 立即证伪**：PR-5.24e 的契约是 deferred secondary 是 *fallback* 工作（CF 先行，CF 无用时恢复 deferred，G24-K3），不是 blocker。已修正为保留原语义：`deferredNormalPresent` 仅作 telemetry 观测字段。

### 2.5 Telemetry（B12）

`counterfactualAdmission` 新增：`positiveTickets / actionablePositiveTickets / terminalPositiveTickets / pendingNormal / deferredNormal / continuationEligible`；`counterfactualRepair.admissionReason` 记录 helper 的 reason。

## 3. G31 门禁（`check-terminal-positive-cf-admission.js`，全部通过）

| Gate | 内容 | 实测 |
|---|---|---|
| **G31-A** | 历史 no-progress CF parity | all-NO_PROGRESS → `trigger=true, reason=primary-normal-exhausted-no-progress` ✓ |
| **G31-B** | **Terminal positive no goal 触发 CF（核心）** | 2 terminal positive → `trigger=true, reason=terminal-positive-no-goal, actionable=0, terminal=2` ✓ |
| **G31-C** | Actionable positive 仍阻止 CF | probe-limited / continuation-eligible / pending-status 三案例全部 `trigger=false, reason=normal-primary-progress` ✓ |
| **G31-D** | Deferred normal authority | deferred 存在不阻止 CF（fallback 语义保留，K1 parity 同测）；`deferredNormalPresent` telemetry ✓ |
| **G31-E** | Global/resource stop | `globalStopReason=time-limit` → `trigger=false, reason=global-stop`（即便 terminal positives 存在）✓ |
| **G31-F** | Goal-reached 排除 | goalReached ticket → `terminal=0, actionable=1`（永不分类 terminal）✓ |
| **B11** | **9.2s Formal Run admission replay** | 真实 artifact 的 2 个 ticket 逐字段重放：旧 `normal-primary-progress` → 新 **`terminal-positive-no-goal, CF-eligible=true`** ✓ |

## 4. 验证记录

```text
G31-A/B/C/D/E/F + B11 (check-terminal-positive-cf-admission.js)  PASSED (7/7)
G24 A-K7 (check-counterfactual-repair.js, 20 gates)               PASSED (含 K1 CF-before-secondary、K2 primary-progress-preserved、K3 deferred-resume)
G25 (isolated-batch)                                              PASSED
budget scheduling (--fast)                                        PASSED
G26 + G27 (check-dp-hot-path.js)                                  PASSED
G28-A/F/G/H/I                                                     PASSED
G29-A..K                                                          PASSED
G30-A..J（含 G30-I default/rollback 扩展）                         PASSED
route-free-state                                                  PASSED
MT1 real-route gate                                               PASSED
check:core                                                        PASSED
check:manifest (149 modules, 185 tests)                          PASSED (新增 G31 套件)
check:no-tower-solver-js                                          PASSED
```

## 5. 禁止项遵守

未修改 CF intent generator、未新增 intent 类型、无 multi-step CF、无 scheduler/multi-root 修改、无 budget 调参、无 candidateLimit/DP-key/dominance 修改、无 OnlyUp hint、未跑 Real MT1→MT4。

## 6. 下一步（Cloud Review 裁决）

- G31 全绿 + 全回归通过；9.2s 轨迹在新 admission 下 CF-eligible 已由 B11 证明（test-only replay，未真塔执行 CF）。
- 若通过：授权一次 Real MT1→MT4 运行，验证真实塔上 terminal-positive-no-goal 路径是否实际产生并执行 CF intents（对照 PR-5.24e Run 2 的 CF 执行先例）。
