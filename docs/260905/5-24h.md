# PR-5.24h — MT3→MT4 Downstream Canonical Search Cost Reduction

```ini
MILESTONE = PR-5.24h
STATUS = IN_PROGRESS / ITERATION_2_COMPLETED_AWAITING_REVIEW
DATE_STARTED = 2026-09-05
BASE_COMMIT = 3c4ae80a8c704b996aa4b2f202aa0962a339dcfd
PRODUCTION_BASELINE = db2e6c120f3855a97e62694a4a2946772e56bb8a
SCOPE = 在保持 auto-battle、battle damage、reachability、DP key、dominance 及调度语义 100% 不变的前提下，对真实 MT3→MT4 下游 workload 构建固定工作量 profiler，归因并攻坚其最大通用 canonical hotspot
```

## 目标与背景

在 PR-5.24g Combined Formal Run 2 中证实：
- 图块索引记忆化（Iteration 1）+ 战斗估算管道优化（Iteration 2）使 30s 预算内的有效扩展量从 3,025 提升至 4,642 (+53.5% OBSERVED)；
- 首探批处理耗时缩短 35.8%（14.77s → 9.48s），释放约 5.3 秒墙钟；
- 释放的墙钟被全额注入 MT3→MT4 主线探索，使得 MT3→MT4 扩展数翻倍（1,365 → 2,764 扩展），耗时达 13.74 秒；
- 瓶颈已明确转移至 **MT3→MT4 下游规范搜索本体成本**。

PR-5.24h 的目标是：直接针对 **MT3→MT4 这一真实下游瓶颈** 建立固定工作量测试基准，下钻分析其计算开销构成，实施单项安全通用优化。

---

## 1. 真实 MT3→MT4 固定工作量测试基准

### 测试夹具 (`fixtures/perf/onlyup-mt3-qualified-state.json`)
- **来源**: PR-5.24g Formal Run 2 真实产出的规范修复后 MT3 候选状态；
- **状态属性**: `floorId = "MT3"`, `hero.loc = { x: 6, y: 12 }`, `HP = 12782, ATK = 97, DEF = 65, MDEF = 490, LV = 4, EXP = 12`；
- **严格约束**: 测试专用夹具，仅供性能基准测试读取，禁止生产求解器逻辑读取。

### 确定性完成基准 (`shared-solver/check-mt3-mt4-hot-path.js`)
- **运行方式**: `maxExpansions: 1000`, `maxRuntimeMs: 600000`（无墙钟阶段截断）；
- **完成状态**: **在前沿耗尽（Frontier Exhausted）下确定性完成于恰好 897 expansions**；
- **确定性指标**: 连续运行 expansions (897)、acceptedStates (1152)、generated (2285)、goalSkyline (0)、前 100 次扩展 DP state keys 100% 逐项完全一致。generated 的权威值为门禁实测 2285（初期文档曾误写 4568，已在 Iteration 1 Repair 1 中修正，以门禁实际输出为准）。

---

## 2. 当前生产基线 Profile 测量 (5 次测量取中位数)

```text
medianWallMs:           6,111 ms
medianCpuMs:            5,868 ms
expansions:               897
medianExpansionsPerSec:   147 exp/s
```

### Top-Level Self-Time 分布
| 阶段 Bucket | 耗时 (ms) | 占比 (%) |
|---|---|---|
| **stabilization** | **2,761** | **57.9%** |
| **primitiveEnumeration** | **767** | **16.1%** |
| **walkReachability** | **555** | **11.6%** |
| **applyAction** | **284** | **6.0%** |
| **stateKeyAndDominance** | **278** | **5.8%** |
| otherExpansionOverhead | 92 | 1.9% |
| frontierQueue | 29 | 0.6% |
| actionEvaluation | 2 | 0.0% |

### Stabilization 内部子阶段分解
| 子阶段 | 耗时 (ms) | 占 stabilization 比例 |
|---|---|---|
| **battleScan** | **1,128** | **40.9%** |
| **hazardBuild** | **792** | **28.7%** |
| **pickupScan** | **720** | **26.1%** |
| applyStep | 45 | 1.6% |
| pickupApply | 33 | 1.2% |
| battleApply | 10 | 0.4% |
| autoEvent | 2 | 0.1% |

---

## 3. MT2→MT3 与 MT3→MT4 每扩展工作量对比与核心归因

对比两者每扩展（per expansion）的结构化指标与耗时分布：

| 指标 (每扩展平均) | MT2→MT3 (520 exp) | MT3→MT4 (897 exp) | 对比与归因 |
|---|---|---|---|
| **Wall 耗时 / expansion** | **21.26 ms** | **11.77 ms** | **MT3 单步扩展反而比 MT2 快近一倍** |
| **CPU 耗时 / expansion** | **22.60 ms** | **12.52 ms** | 同样显著更低 |
| **Battle Resolver 调用 / expansion** | 105.4 次 | 36.6 次 | MT3 上怪物战斗密度相对较低 |
| **Hazard 扫描单元 / expansion** | 3,855 cells | 2,258 cells | MT3 单步危害扫描更少 |
| **Stabilization 通道 / expansion** | 19.7 次 | 11.9 次 | MT3 稳定化震荡更少 |
| **Stabilization 耗时 / expansion** | 14.63 ms | 6.84 ms | 单步稳定化耗时减半 |
| **Primitive 枚举耗时 / expansion** | 2.53 ms | 1.96 ms | 单步枚举耗时更少 |
| **Reachability 耗时 / expansion** | 2.07 ms | 1.42 ms | 单步可达性耗时更少 |

### 核心科学结论（Iteration 1 Repair 1 修正后口径）
- **不是“每步扩展变慢”**：candidate-0 的单位扩展计算成本（11.77 ms/exp）远低于 MT2→MT3（21.26 ms/exp）；
- **candidate-0 单状态搜索并非无法收敛**：在无墙钟限制下，candidate-0 在 897 expansions 即前沿耗尽、确定性完成（`searchComplete = true`）；
- **真实 Run 的 13.74 s / 2,764 expansions 发生在包含多条 MT3 repaired histories 的真实下游 workload**，不能从单一 candidate 的 ms/exp 反推：
  - `CANDIDATE_0_PER_EXP_COST = 11.77ms OBSERVED`；
  - `REAL_MULTI_CANDIDATE_PER_EXP_COST = NOT_DERIVED_FROM_CANDIDATE_0`；
  - `REAL_MT3_MT4_AGGREGATE_EXPANSIONS = 2764 OBSERVED`；
  - 真实 wall 分解应以 worker/process telemetry 为准，而非以单 candidate 的 ms/exp 外推（初稿中“2764 × 11.77 ms ≈ 32.5 s 经多进程压缩至 13.74 s”的推导不成立，已删除）；
- **首要新假设**：`MULTI_CANDIDATE_DOWNSTREAM_WORK_MULTIPLICATION` —— 多条 repaired MT3 histories 使下游重复执行多个规范搜索。该冗余度已在 Iteration 1 Repair 1 中实测（见第 7 节 G28-G）；
- **单 candidate 视角下的优化杠杆参考**（保留作为 per-exp hotspot 参考，不再作为当前主攻方向）：在 candidate-0 的 11.77 ms/exp 中，`stabilization` 占 57.9% (6.84 ms/exp)，其中 `battleScan` (3.28 ms/exp)、`hazardBuild` (1.74 ms/exp) 与 `pickupScan` (1.49 ms/exp) 占比最高。

---

## 4. Iteration 1 优化实现与 G28 门禁实测数据（历史记录）

> **状态（Iteration 1 Repair 1）**: 本节记录的 fastCollectTargets 优化与静态图块定义缓存已被 **REJECTED_FOR_PRODUCTION** 并在 Repair 1 中全部撤回。G28-D 性能结果降级保留为 `EXPLORATORY_SUBTHRESHOLD_RESULT`。Cloud Review 裁决及撤回理由见第 5 节。

### 4.1 优化实现 (`enableFastCollectTargets`)
- **文件**: `shared-solver/lib/auto-actions.js`, `shared-solver/lib/simulator.js`, `shared-solver/lib/isolated-segment-executor.js`, `shared-solver/segment-worker.js`
- **机制**:
  1. 使用紧凑定长数组与代际计数器重构 BFS 目标遍历 (`collectTargetsFast`)：
     `bfsQueueX`, `bfsQueueY`, `bfsQueueDist`, `bfsVisited`（`Uint16Array`），代际递增 `bfsGeneration`；
     彻底消除 2.1 万次 BFS 中数以百万计的 `new Set()`、`coordinateKey` 字符串与 `{ x, y, distance }` 临时对象分配；
  2. 独立保留 legacy `collectTargetsLegacy`，提供 `enableFastCollectTargets` 独立开关（默认 `true`）；
  3. `state.js` 内部对静态图块定义执行冻结缓存 (`getTileDefinitionByNumber`)，消除全图近百万次 `{ number, ...definition }` 包装分配。

### 4.2 G28 门禁套件实测报告 (`check-mt3-mt4-hot-path.js`)

在 MT3→MT4 确定性 897-exp 固定工作量基准下执行：

```json
{
  "schema": "motapathfinder.mt3-mt4-hot-path.v1",
  "contractStatus": "below-threshold-reported-to-cloud",
  "iteration": "PR-5.24h Iteration 1 (Auto-Action BFS Traversal Flat-Buffer Optimization)",
  "fixture": {
    "tower": "OnlyUp",
    "segment": "mt3-to-mt4 (from canonical repaired MT3 state)",
    "completion": "frontier-exhausted (deterministic)",
    "expansions": 897,
    "pairsCount": 4
  },
  "gates": {
    "G28-A": {
      "fixtureDeterminismVerified": true,
      "expansions": 897,
      "frontierSize": 0,
      "acceptedStates": 1152,
      "generated": 2285,
      "first100ExpansionKeysMatched": true,
      "searchComplete": true
    },
    "G28-B": {
      "optimizationExactParityVerified": true,
      "expansions": 897,
      "acceptedStates": 1152,
      "generated": 2285,
      "first100ExpansionsMatched": true,
      "cacheOffAndOnIdentical": true
    },
    "G28-C": {
      "targetSpecificCollectionCorrectnessVerified": true,
      "statesTested": 3,
      "allTargetsMatchedExactly": true
    },
    "G28-D": {
      "performanceMeasured": true,
      "pairsCount": 4,
      "acceptanceThresholdMet": false,
      "acceptanceThresholdRequired": "overall median >= 5.0% OR target bucket median >= 20.0%",
      "acceptanceThresholdStatus": "below-threshold-reported-to-cloud",
      "armA_off": {
        "medianWallMs": 4745,
        "medianCpuMs": 5180,
        "medianExpansionsPerSec": 189,
        "stabilizationMs": 2675,
        "pickupScanMs": 698,
        "battleScanMs": 1107,
        "individualWallMs": [4552, 4798, 4833, 4691]
      },
      "armB_on": {
        "medianWallMs": 4659,
        "medianCpuMs": 4835.5,
        "medianExpansionsPerSec": 193,
        "stabilizationMs": 2443,
        "pickupScanMs": 626,
        "battleScanMs": 1009,
        "individualWallMs": [4592, 4726, 4756, 4342]
      },
      "pairImprovements": [
        { "pair": 0, "wallGain": -0.9, "targetGain": 3.1 },
        { "pair": 1, "wallGain": 1.5, "targetGain": 2.7 },
        { "pair": 2, "wallGain": 1.6, "targetGain": 3.3 },
        { "pair": 3, "wallGain": 7.4, "targetGain": 9.4 }
      ],
      "overallWallImprovementPercent": 1.8,
      "targetBucketMedianImprovementPercent": 3.2,
      "targetBucketLastPairImprovementPercent": 9.4,
      "faster": true
    },
    "G28-E": {
      "generalizationVerified": true,
      "mt2Expansions": 520,
      "mt2AcceptedStates": 839,
      "mt2GoalSkylineCount": 8,
      "noRegression": true
    }
  }
}
```

### 4.3 性能归因与诚实上报 (遵守 Cloud Review 纪律)
- **正确性契约 100% 达成**: G28-A、G28-B、G28-C、G28-E 均 100% 通过，897 次扩展及其前 100 次 DP keys 完全一致，MT2 泛化无回归；
- **真实子阶段收益确实存在**:
  - `pickupScanMs`: 从 698 ms 降至 626 ms (**-72 ms, -10.3%**)；
  - `battleScanMs`: 从 1,107 ms 降至 1,009 ms (**-98 ms, -8.9%**)；
  - `stabilizationMs`: 从 2,675 ms 降至 2,443 ms (**-232 ms, -8.7%**)；
- **整体中位数提升**: +1.8%（未达到 `>= 5.0%` 阈值，未达 `>= 20%` 目标 bucket 阈值）；
- **按纪律诚实上报**:
  严格遵守 Cloud Review 指导（*“如果没达到：不要为了性能放松编码安全，如实报告给 Cloud”*），将 `acceptanceThresholdMet: false` 与 `contractStatus: "below-threshold-reported-to-cloud"` 如实上报，不为了凑数据降低门禁要求，交由 Cloud Review 裁决。

---

## 5. Cloud Review 裁决与 Iteration 1 Repair 1（撤回未达门槛优化）

### 5.1 Cloud Review 正式裁决（REQUEST_CHANGES）

```ini
PR_5_24h = IN_PROGRESS

ITERATION_1 = REQUEST_CHANGES

MT3_MT4_FIXED_WORK_BENCHMARK_DIRECTION = APPROVED
CANDIDATE_0_DETERMINISM = PROVEN
CANDIDATE_0_COMPLETE_EXHAUSTION = PROVEN

PER_EXPANSION_COST_HYPOTHESIS =
MT3_CANDIDATE_0_IS_NOT_MORE_EXPENSIVE_THAN_MT2

FAST_COLLECT_TARGETS =
REJECTED_FOR_PRODUCTION

FAST_COLLECT_TARGETS_PERFORMANCE =
BELOW_ACCEPTANCE_THRESHOLD

STATIC_TILE_DEFINITION_CACHE =
UNQUALIFIED / REJECTED_FOR_THIS_ITERATION

P0 = NONE

P1_1 =
FIXED_1024_BFS_BUFFER_BREAKS_GENERIC_LARGE_FLOORS

P1_2 =
STATIC_TILE_CACHE_WAS_NOT_INCLUDED_IN_OFF_ON_QUALIFICATION

P1_3 =
PERF_FIXTURE_CONTAINS_MATERIALIZED_ROUTE_DESPITE_CANONICAL_STATE_CONTRACT

P2_1 =
DOC_GENERATED_COUNT_IS_INCONSISTENT_4568_VS_2285

P2_2 =
CANDIDATE_0_PER_EXPANSION_COST_CANNOT_BE_MULTIPLIED_ACROSS_REAL_MULTI_CANDIDATE_WORKLOAD

REAL_MT1_MT4 = NOT_AUTHORIZED
```

### 5.2 P1 问题明细与处理

| 编号 | 问题 | 处理 |
|---|---|---|
| **P1-1** | `collectTargetsFast` 写死 `BFS_MAX_CELLS = 1024`，无 `width*height <= 1024` 检查、无 fallback。任何 `width×height > 1024` 的楼层都会发生 TypedArray 越界写入、visited/queue 失真。非 OnlyUp 专用项目不可接受 | **直接撤回整个 fastCollectTargets production change**（不为 1.8% 收益增加 fallback/动态扩容） |
| **P1-2** | 本轮实际同时落地了两个优化（fastCollectTargets + static tile-definition cache），但 G28-B/G28-D 的 OFF/ON 只切换 `enableFastCollectTargets`。tile cache 的 object identity / top-level mutability 契约变化未纳入任何 OFF/ON 资格化 | **直接撤回 static tile-definition cache**（不专门花一轮资格化） |
| **P1-3** | MT3 fixture 声称 "only the canonical state"，但实际含 34 条 materialized route 历史。benchmark wall timer 包含整条历史 route 的 JSON clone 污染 | **fixture 规范化为 route-free canonical representation**，新增 G28-F 门禁（见第 6 节） |

### 5.3 撤回执行（Repair 1）

Production 撤回（恢复到 `db2e6c120f3855a97e62694a4a2946772e56bb8a` 的字节级原状）：

```text
shared-solver/lib/auto-actions.js       ← 撤回 collectTargetsFast / BFS_MAX_CELLS / bfsQueue* / bfsVisited / bfsGeneration / enableFastCollectTargets；恢复 canonical collectTargets()
shared-solver/lib/state.js              ← 撤回 project.__cachedTileDefinitions / Object.freeze tile cache；恢复 fresh-object getTileDefinitionByNumber
shared-solver/lib/simulator.js          ← 撤回 enableFastCollectTargets 透传
shared-solver/lib/isolated-segment-executor.js ← 撤回 profile 字段
shared-solver/segment-worker.js         ← 撤回 worker 透传
```

验证：`git diff db2e6c1 -- <上述5文件>` 为空（字节级一致）。

保留（G28 profiler infrastructure）：

```text
shared-solver/check-mt3-mt4-hot-path.js        ← 保留并升级为 v2（G28-A/F/G；删除针对已撤回优化的 G28-B/C/D/E acceptance 结论）
shared-solver/fixtures/perf/onlyup-mt3-qualified-state.json ← 保留并规范化（见第 6 节）
solver-manifest.json                           ← 保留 manifest entry
docs/260905/5-24h.md                           ← 本文档
```

### 5.4 撤回理由冻结

```ini
FAST_COLLECT_TARGETS =
REJECTED_BELOW_PERFORMANCE_THRESHOLD

OVERALL_GAIN = 1.8%
TARGET_GAIN = 3.2%

GENERIC_LARGE_FLOOR_SAFETY =
NOT_SATISFIED
```

---

## 6. Fixture 规范化与 G28-F（Route-Free Canonicality）

### 6.1 单 candidate fixture 规范化

`fixtures/perf/onlyup-mt3-qualified-state.json` 已转换为真正 canonical representation：

- `state.route = []`（删除全部 34 条 materialized route 历史）；
- 保留 `state.meta`（`rawRouteLength=91`, `decisionDepth=30`, `autoStepCount=61`, `autoPickupCount=50`, `autoBattleCount=11`）与 `provenance` 元数据不变；
- **状态键不变性验证**: `buildStateKey(strip前) == buildStateKey(strip后) == provenance.stateKey`（route 不参与 state key，剥离不改变 canonical 语义）。

### 6.2 G28-F 门禁断言

```text
G28-F — Fixture Route-Free Canonicality
  fixture.state.route.length = 0
  fixture 无任何 action/battle/pickup route 对象
  16-candidate frontier fixture 每个候选 route.length = 0
  stored stateKey == buildStateKey(fixture.state)（单 candidate 与全部 16 个 frontier 候选）
  meta/provenance 元数据完整保留
```

实测：**PASSED**（`singleFixtureRouteFree=true, frontierFixtureRouteFree=true, canonicalStateKeyPreserved=true, metadataPreserved=true, candidatesChecked=16`）。

---

## 7. 真实 Multi-Candidate MT3 Input Frontier 与 G28-G（Workload Characterization & Overlap）

### 7.1 Frontier fixture (`fixtures/perf/onlyup-mt3-qualified-frontier.json`)

从 **PR-5.24g Formal Run 2 已保存 artifact**（本地 qualification 结果文件，`finalCandidates`，源头 qualification commit `db2e6c1`、record commit `3c4ae80`）提取了**完整 16-candidate canonical MT3 input frontier**：

```text
FULL_REAL_FRONTIER_ARTIFACT = AVAILABLE
sourceQualificationSha = db2e6c120f3855a97e62694a4a2946772e56bb8a
sourceRecord = 3c4ae80a8c704b996aa4b2f202aa0962a339dcfd
inputFrontierCount = 16
```

- 每个候选仅含 `candidateId` + `canonical state` + `stateKey`（`buildStateKey` 实算校验一致）；
- **不含** materialized route / correct route / manual labels / MT4 hint（route 全部剥离，剥离前后 stateKey 不变）；
- 测试专用夹具，禁止生产求解器逻辑读取。

### 7.2 初始状态去重确认

16 个候选的初始 `stateKey` 互不相同（16 unique / 16 total），初始 `buildDpStateKey`（region 模式）同样互不相同 —— 下游冗余不是"相同初始状态被重复送入"，而是**不同初始状态在下游扩展中收敛到相同状态**。

### 7.3 G28-G 实测数据（每 candidate 独立 bounded 搜索）

单 candidate 完整工作量特性（300-exp bound 仍全部自然收敛，验证 `CANDIDATE_0_SEARCH = DETERMINATELY_EXHAUSTED` 的代表性；G28-G 门禁内使用 100-exp bound 做重叠统计）：

| Candidate | 初始 HP/DEF | Expansions (自然收敛) | searchComplete | found | accepted | generated |
|---|---|---|---|---|---|---|
| candidate-0 | 12782/65 | 897 | true | false | 1152 | 2285 |
| candidate-1 | 11563/60 | 649 | true | false | 856 | 1677 |
| candidate-2 | 10997/70 | 404 | true | false | 465 | 961 |
| candidate-3 | 12782/65 | 897 | true | false | 1152 | 2285 |
| candidate-4 | 10997/70 | 404 | true | false | 465 | 961 |
| candidate-5 | 10197/60 | 1001 | true | false | 1365 | 2609 |
| candidate-6 | 12047/65 | 898 | true | false | 1154 | 2293 |
| candidate-7 | 12047/65 | 898 | true | false | 1154 | 2293 |
| candidate-8 | 10617/60 | 978 | true | false | 1325 | 2531 |
| candidate-9 | 10617/60 | 978 | true | false | 1327 | 2531 |
| candidate-10 | 10370/65 | 358 | true | false | 437 | 755 |
| candidate-11 | 10370/65 | 358 | true | false | 434 | 755 |
| candidate-12 | 10197/60 | 1001 | true | false | 1366 | 2609 |
| candidate-13 | 9956/65 | 288 | true | false | 337 | 669 |
| candidate-14 | 9956/65 | 288 | true | false | 337 | 669 |
| candidate-15 | 9891/70 | 268 | true | false | 297 | 615 |

（300-exp bound 下 16/16 全部 `searchComplete=true` 且 `found=false`；单 candidate 合计自然收敛工作量 ≈ 10,633 expansions，均独立可完成。）

### 7.4 跨 candidate 重叠统计（每 candidate 前 100 expansions，共 1,600 次扩展）

```json
{
  "stateKeyOverlap": {
    "totalExpanded": 1600,
    "uniqueStateKeys": 1265,
    "duplicateStateKeys": 335,
    "duplicateStateKeyRatio": 20.94
  },
  "dpKeyOverlap": {
    "totalExpanded": 1600,
    "uniqueDpKeys": 345,
    "duplicateDpKeys": 1255,
    "duplicateDpKeyRatio": 78.44
  },
  "pairwiseDpJaccard": {
    "pairsAnalyzed": 120,
    "averageJaccardPercent": 29.94,
    "maxJaccardPercent": 92.59,
    "maxOverlapPair": "mt2-to-mt3:candidate-0 & mt2-to-mt3:candidate-6"
  }
}
```

解读：

- **exact canonical DP state 重叠率 = 78.44%**，远高于 20% 判定线：16 条 repaired histories 的下游搜索在前 100 扩展窗口内有近 4/5 的 canonical DP 状态是**跨候选重复扩展**的；
- 最极端的 candidate-0 与 candidate-6 初始 HP/DEF 不同（12782/65 vs 12047/65），但前 100 扩展的 DP key Jaccard 重叠高达 92.59%；
- 完整 300-exp bound 窗口的 stateKey 级冗余为 25.55%（4,744 次扩展中 1,212 次跨候选重复），趋势一致。

---

## 8. 修正后的科学口径与下一 Iteration 方向

### 8.1 冻结口径

```ini
MT3_MT4_PER_EXPANSION_COST_HYPOTHESIS =
NOT_ABNORMALLY_HIGH_FOR_CANDIDATE_0

CANDIDATE_0_SEARCH =
DETERMINATELY_EXHAUSTED_AT_897_EXPANSIONS

CANDIDATE_0_REPRESENTS_FULL_MT3_MT4_WORKLOAD =
FALSE

REAL_MT3_MT4_AGGREGATE_WORKLOAD =
LARGE

REAL_MT3_MT4_AGGREGATE_EXPANSIONS =
2764 OBSERVED

CANDIDATE_0_PER_EXP_COST = 11.77ms OBSERVED
REAL_MULTI_CANDIDATE_PER_EXP_COST =
NOT_DERIVED_FROM_CANDIDATE_0

MULTI_CANDIDATE_DOWNSTREAM_WORK_MULTIPLICATION =
PRIMARY_HYPOTHESIS

MULTI_CANDIDATE_REDUNDANCY =
MEASURED (DP-KEY DUPLICATE RATIO 78.44% IN FIRST-100 WINDOW; STATE-KEY 20.94%)

SINGLE_CANDIDATE_FIXTURE_ROLE =
SINGLE_CANDIDATE_PROFILE_FIXTURE
```

### 8.2 下一 Iteration 方向判定（按 overlap 判据）

Cross-candidate duplicate ratio（78.44% DP-key 级）**明显 > 20%**，按既定判据：

```ini
NEXT_ITERATION_PRIORITY =
MULTI_ROOT / CROSS_CANDIDATE_EXACT_WORK_REUSE
```

目标：多条 repaired MT3 histories 到达同一个 exact canonical DP state 时，不重复做同一 downstream expansion。硬约束：

- `exact buildDpState
Key authority` 不变；
- origin/provenance 保留；
- search order / candidate semantics 保留；
- 不做 approximate merge。

### 8.3 本轮验证记录

```text
G28-A (fixture determinism)                  PASSED
G28-F (fixture route-free canonicality)      PASSED
G28-G (multi-candidate workload & overlap)   PASSED
G24 (check-counterfactual-repair)            PASSED
budget scheduling (--fast)                   PASSED
route-free-state                             PASSED
MT1 real-route gate                          PASSED
isolated-batch (G25)                         PASSED
dp-hot-path (G26/G27, incl. G27-G/G27-H)     PASSED
check:core                                   PASSED
check:manifest (149 modules, 182 tests)      PASSED
check:no-tower-solver-js                     PASSED
```

已撤回优化的 G28-B/C/D/E performance acceptance 不再执行（fastCollectTargets 已 REJECTED_FOR_PRODUCTION；G28-D 历史数据保留为 EXPLORATORY_SUBTHRESHOLD_RESULT）。

---

## 9. Iteration 1 Repair 1 复审（Cloud Review REQUEST_CHANGES：仅针对 G28-G 科学解释）与 Repair 2

### 9.1 Cloud Review 对 Repair 1 的裁决

生产清理本身 **APPROVED**（P1-1/P1-2 生产风险关闭，G28-F route-free fixture APPROVED，16-candidate frontier fixture ACCEPTED）。但 G28-G 的统计口径与解释存在三个问题：

```ini
ITERATION_1 = SUPERSEDED
ITERATION_1_REPAIR_1 = REQUEST_CHANGES
PRODUCTION_REVERT = APPROVED
FAST_COLLECT_TARGETS = REJECTED_AND_REMOVED
STATIC_TILE_CACHE = REJECTED_AND_REMOVED
G28_F_ROUTE_FREE_CANONICAL_FIXTURE = APPROVED
REAL_16_CANDIDATE_FRONTIER_FIXTURE = ACCEPTED
G28_G_FIRST_100_OVERLAP_SIGNAL = POSITIVE

P1_1 = DP_KEY_OVERLAP_IS_MISCLASSIFIED_AS_EXACT_CANONICAL_STATE_OVERLAP
P1_2 = GLOBAL_DUPLICATE_RATIO_MIXES_WITHIN_CANDIDATE_AND_CROSS_CANDIDATE_DUPLICATES
P1_3 = REPORT_CLAIMS_16_OF_16_SEARCH_COMPLETE_BUT_G28_G_ONLY_RUNS_100_EXPANSIONS
P2 = ORIGINAL_REAL_RUN_FRONTIER_EXTRACTION_ARTIFACT_NOT_COMMITTED

NEXT_DIRECTION_CANDIDATE = MULTI_ROOT_SHARED_DP_AUTHORITY
PRODUCTION_MULTI_ROOT_IMPLEMENTATION = NOT_YET_AUTHORIZED
```

### 9.2 术语修正（绑定）

- `buildStateKey` = **CANONICAL_STATE_KEY**（exact canonical state 身份）；
- `buildDpStateKey` = **PRODUCTION_DP_EQUIVALENCE_BUCKET_KEY**：当前隐式模型下**刻意不含 HP**；同一 DP bucket 内的状态通过 `isBetterForSameDpKey`（HP → decisionDepth → rawRouteLength）竞争；
- **same DP bucket ≠ same exact canonical state ≠ successor 计算必然可复用**；禁止设计 `same DP key → cache/reuse successor list`；
- 旧的 "CROSS_CANDIDATE_EXACT_CANONICAL_DP_STATE_DUPLICATE = 78.44%" 表述撤回；78.44% 重命名为 GLOBAL_DUPLICATE_OCCURRENCE_RATIO（混合 within + cross，不再是 cross-candidate 口径）。

### 9.3 P1-3 修正（completion claim）

G28-G 窗口冻结为 `FIRST_100_EXPANSIONS_PER_CANDIDATE`，`FULL_COMPLETION = FALSE`；初稿中 "300-exp 下 16/16 searchComplete" 的表述删除（G28-G 从未测量）。完整 completion 由新增 **G28-H** 测量。

---

## 10. G28-G 修正后口径（Repair 2 实测）

### 10.1 within / cross 拆分（first-100 窗口，1600 occurrences）

```json
{
  "withinCandidateDuplicates": { "stateKey": 0, "dpBucket": 289 },
  "canonicalStateKeyOverlap": {
    "sumUniquePerCandidate": 1600, "globalUnionUnique": 1265,
    "crossCandidateSharedOccurrences": 335,
    "crossCandidateOverlapRatioPercent": 20.94,
    "globalDuplicateOccurrenceRatioPercent": 20.94
  },
  "dpBucketKeyOverlap": {
    "sumUniquePerCandidate": 1311, "globalUnionUnique": 345,
    "crossCandidateSharedBucketOccurrences": 966,
    "crossCandidateOverlapRatioPercent": 73.68,
    "globalDuplicateOccurrenceRatioPercent": 78.44
  },
  "dpMultiplicity": { ">=2cand": 206, ">=4cand": 138, ">=8cand": 57, "all16": 10 },
  "maxCandidateMultiplicity": 16
}
```

关键读数：

- **candidate 内部从不存在 exact canonical state 重复**（within stateKey dup = 0，DP 本身不重复扩展同一 exact state）；
- 旧 78.44% 的构成 = 289 within-bucket churn + 966 cross-candidate shared；**修正后真正的 cross-candidate DP bucket 重叠 = 73.68%**（仍然极高，方向信号保持 POSITIVE）；
- **10 个 DP bucket 被全部 16 个 candidate 共享**；57 个 bucket 被 >=8 个 candidate 共享 —— 直接证明不同 HP / route-quality 的 roots 大量落入相同 production DP bucket。

---

## 11. G28-H — Full Candidate Completion Characterization（每 candidate 独立子进程）

架构：每个 candidate 一个全新子进程（顺序执行，杜绝单进程 16 全量搜索 OOM），`maxExpansions = 2000`（全部自然收敛，未触界）、runtime non-binding、keys-only capture。实测 **16/16 全部自然前沿耗尽完成**：

| Candidate | 初始 HP/DEF | Expansions | searchComplete | found | generated | wall |
|---|---|---|---|---|---|---|
| candidate-0/3 | 12782/65 | 897 | true | false | 2285 | ~5.0s |
| candidate-1 | 11563/60 | 649 | true | false | 1677 | ~3.7s |
| candidate-2/4 | 10997/70 | 404 | true | false | 961 | ~2.0s |
| candidate-5/12 | 10197/60 | 1001 | true | false | 2609 | ~5.7s |
| candidate-6/7 | 12047/65 | 898 | true | false | 2293 | ~4.9s |
| candidate-8/9 | 10617/60 | 978 | true | false | 2531 | ~5.5s |
| candidate-10/11 | 10370/65 | 358 | true | false | 755 | ~1.7s |
| candidate-13/14 | 9956/65 | 288 | true | false | 669 | ~1.5s |
| candidate-15 | 9891/70 | 268 | true | false | 615 | ~1.3s |

全部 `found = false`（bounded 2000 内无一 candidate 打通 MT4）。

### 11.1 Full-work overlap（每 candidate 完整 bounded workload，10,565 occurrences）

```json
{
  "withinCandidateDuplicates": { "stateKey": 0, "dpBucket": 3474 },
  "canonicalStateKeyOverlap": { "sumUniquePerCandidate": 10565, "globalUnionUnique": 6990, "crossShared": 3575, "crossOverlapPercent": 33.84 },
  "dpBucketKeyOverlap": { "sumUniquePerCandidate": 7091, "globalUnionUnique": 938, "crossShared": 6153, "crossOverlapPercent": 86.77,
    "multiplicity": { ">=2cand": 778, ">=4cand": 747, ">=8cand": 515, "all16": 123 } }
}
```

完整工作量下：**跨 candidate DP bucket 重叠 86.77%**（7,091 个 per-candidate unique bucket 只覆盖 938 个全局 union bucket）；123 个 bucket 被全部 16 个 candidate 共享。

---

## 12. G28-I — Test-Only Multi-Root Shared-DP Feasibility Probe

触发条件：修正后 `CROSS_CANDIDATE_DP_BUCKET_OVERLAP_RATIO = 86.77% >= 20%` → gate 执行（test-only，production 未接线）。

### 12.1 机制（test-only，默认关闭）

- `dp-search.js` 新增 **默认关闭** 的 `dpSeedAuthority` 测试钩子：将外部提供的已定局 DP bucket（state + originTag）注册进 `bestByKey` 作为 dominance blocker（不入 agenda、不展开、只参与同 bucket HP/depth/route 支配判定）；production 搜索从不设置该 config，缺省时代码路径完全一致；
- `segment-dp.js` 仅增加该 config 的透传（默认 null）；
- **hook-off parity precheck**：钩子缺省时 candidate-0 复现 G28-A 权威基线（897 exp / 2285 generated）—— 实测一致；
- 共享臂：16 roots 顺序注入同一 progressive authority（每 run 结束将其新扩展 bucket 以生产等价同 bucket 胜者规则 HP → depth → route 合并），单专用子进程执行，峰值 RSS 480.6 MB（安全）。

### 12.2 Reference contract 对比（16 independent vs 1 shared authority）

```json
{
  "INDEPENDENT_TOTAL_EXPANSIONS": 10565,
  "SHARED_MULTI_ROOT_EXPANSIONS": 2346,
  "EXPANSION_REDUCTION_RATIO_PERCENT": 77.79,
  "INDEPENDENT_WALL_MS_TOTAL": 57714,
  "SHARED_WALL_MS": 21006,
  "INDEPENDENT_WALL_OBSERVATIONAL_ONLY": true,
  "UNIQUE_DP_BUCKETS_EXPANDED_SHARED": 938
}
```

### 12.3 语义 / 完备性 / 血统验证

```ini
INDEPENDENT_FOUND = FALSE / SHARED_FOUND = FALSE
GOAL_MULTISET_MATCHED = TRUE (双方均为空集)
BEST_GOAL_MATCHED = TRUE (双方均无 goal)
COMPLETION_AUTHORITY_PARITY = TRUE (independent 16/16 complete 且无 goal；shared 16/16 complete 且无 goal)
ORIGIN_PROVENANCE_VERIFIED = TRUE (条件满足：本 workload 无 goal，provenance 断言为空真；goal 存在场景的血统验证留待 production 资格轮)
SHARED_ARM_COVERAGE_NOTE = shared final authority buckets (938) == independent union buckets (938) 完全一致
```

扩展顺序不作要求（shared 的价值正是让 roots 共同竞争）；要求的是语义结果等价 + 完备性权威正确 + origin 正确。

共享臂逐 run：candidate-0 (无 seed) 897 exp 完整执行；后续 roots 逐级吸收（candidate-2/4/6/8/9/10/11/13/15 仅 4 exp 即被既有更优 bucket 完全支配）；candidate-1 (541)、candidate-5 (413)、candidate-3 (158)、candidate-7 (132)、candidate-12 (135)、candidate-14 (34) 部分吸收。

### 12.4 判定（按 review §13 判据）

```ini
SHARED_SEMANTIC_PARITY = TRUE
ORIGIN_PROVENANCE_CONTRACT = CONDITIONALLY_TRUE (NO_GOALS_IN_WORKLOAD)
COMPLETION_AUTHORITY = TRUE
EXPANSION_REDUCTION = 77.79% (>= 20% 判据，大幅超出)

RECOMMENDED_NEXT_ITERATION = PRODUCTION_MULTI_ROOT_SHARED_DP
PRODUCTION_DECISION_AUTHORITY = CLOUD_REVIEW
```

---

## 13. Repair 2 冻结口径汇总

```ini
PR_5_24h = IN_PROGRESS
ITERATION_1_REPAIR_2 = COMPLETED_AWAITING_REVIEW

CANONICAL_STATE_KEY_VS_DP_BUCKET_KEY_TERMINOLOGY = FIXED
WITHIN_VS_CROSS_DUPLICATE_SPLIT = MEASURED (BOTH WINDOWS)
G28_G_WINDOW = FIRST_100_EXPANSIONS_PER_CANDIDATE (FULL_COMPLETION = FALSE; 由 G28-H 单独测量)
G28_H_FULL_COMPLETION_16_OF_16 = PROVEN (NATURAL EXHAUSTION, NO GOALS, BOUNDED 2000)

CORRECTED_CROSS_CANDIDATE_DP_BUCKET_OVERLAP_RATIO = 86.77% (FULL WORK) / 73.68% (FIRST-100)
CORRECTED_CROSS_CANDIDATE_STATE_KEY_OVERLAP_RATIO = 33.84% (FULL WORK) / 20.94% (FIRST-100)
GLOBAL_DUPLICATE_OCCURRENCE_RATIO_LEGACY = 78.44% (FIRST-100, MIXED, NON-AUTHORITATIVE)

MULTI_ROOT_SHARED_DP_AUTHORITY_FEASIBILITY = PROVEN_TEST_ONLY
TEST_ONLY_EXPANSION_REDUCTION = 77.79%
PRODUCTION_MULTI_ROOT_WIRING = NOT_AUTHORIZED_THIS_ROUND
SUCCESSOR_CACHE = PROHIBITED (DP BUCKET KEY EXCLUDES HP; NOT A SUCCESSOR IDENTITY)

P0 = NONE
P2_REPAIR1 = ORIGINAL_RUN_ARTIFACT_NOT_COMMITTED (NOTED; FIXTURE STATEKEY/PROVENANCE CONSISTENCY VERIFIED INSTEAD; NON-BLOCKING)
REAL_MT1_MT4 = NOT_AUTHORIZED

NEXT_ITERATION_CANDIDATE = PRODUCTION_MULTI_ROOT_SHARED_DP (PENDING CLOUD REVIEW)
```

### 13.1 本轮验证记录

```text
G28-A (fixture determinism)                        PASSED (897/2285 权威基线)
G28-F (route-free canonicality)                    PASSED
G28-G v2 (within/cross split + multiplicity)       PASSED
G28-H (16/16 full completion, child-per-candidate) PASSED
G28-I (test-only shared-DP feasibility)            PASSED (hook-off parity + semantic parity + 77.79% reduction)
G24 (check-counterfactual-repair)                  PASSED
budget scheduling (--fast)                         PASSED
route-free-state                                   PASSED
MT1 real-route gate                                PASSED
isolated-batch (G25)                               PASSED
dp-hot-path (G26/G27)                              PASSED (覆盖 dp-search/segment-dp 变更回归)
check:core                                         PASSED
check:manifest (149 modules, 182 tests)            PASSED
check:no-tower-solver-js                           PASSED
```

### 13.2 生产语义保护声明

- `dp-search.js`/`segment-dp.js` 本轮仅新增默认关闭的 `dpSeedAuthority`/`dpSeedAuthorityDiagnostics` 测试钩子与其诊断透传；production 路径（config 缺省）行为字节级不变（G28-A 权威基线 897/2285 复现 + G26/G27 全量回归佐证）；
- 未触碰：DP key 语义、state key、dominance、candidate ordering、scheduler、CF、candidateLimit、预算分配；
- 未运行 Real MT1→MT4；未实现 successor cache；multi-root 仅存在于 test-only 原型。

---

## 14. Iteration 1 Repair 2 终审（APPROVED_AS_TEST_ONLY_FEASIBILITY）与 Iteration 2 授权

### 14.1 Cloud Review 裁决（Repair 2）

```ini
ITERATION_1_REPAIR_2 = APPROVED_AS_TEST_ONLY_FEASIBILITY
G28_G_CORRECTED_OVERLAP = APPROVED
G28_H_FULL_WORK_CHARACTERIZATION = APPROVED
G28_I_COMPLETE_NO_GOAL_FEASIBILITY = APPROVED
CROSS_ROOT_DP_BUCKET_OVERLAP_FULL_WORK = 86.77%
TEST_ONLY_EXPANSION_REDUCTION = 77.79%
MULTI_ROOT_SHARED_DP_DIRECTION = APPROVED
SUCCESSOR_CACHE = PROHIBITED

PRODUCTION_BLOCKER_1 = INCOMPLETE_ROOT_CANNOT_BE_REDUCED_TO_SETTLED_BLOCKERS_ONLY
PRODUCTION_BLOCKER_2 = GOAL_BEARING_ORIGIN_PROVENANCE_NOT_YET_PROVEN
PRODUCTION_BLOCKER_3 = BUDGET_LIMITED_SHARED_COMPLETION_AUTHORITY_NOT_YET_PROVEN

P2_1 = dpSeedAuthority_IS_DEFAULT_OFF_BUT_IS_STILL_A_RUNTIME_CONFIGURABLE_SEMANTIC_HOOK
P2_2 = BYTE_IDENTICAL_PRODUCTION_PATH_IS_INACCURATE__HOOK_OFF_BEHAVIORAL_PARITY_IS_THE_CORRECT_CLAIM

NEXT_ITERATION = PRODUCTION_MULTI_ROOT_SHARED_DP
```

### 14.2 Iteration 2 实现摘要（production multi-root，one live authority）

核心结构（`shared-solver/lib/dp-search.js` / `search-nodes.js` / `segment-dp.js`）：

- **`searchDPMultiRoot(simulator, initialRoots, options)`**：一次真实的多根 DP 搜索 —— 所有 roots 共享同一 `bestByKey / SkylineSet / agenda / nodes registry / budget & completion authority`；roots 注册走**生产注册规则**（`buildDpStateKey` + `isBetterForSameDpKey` + `SkylineSet.add`），不在外面复制比较器；
- **root provenance 放在 search node 上**（`rootCandidateId` / `rootIndex`，`createChildNode` 从 parent 自动继承），绝不进入 canonical state / 任何 state key；
- **per-root route context**：每个 root 携带自己的 prefixRoute/prefixTrace，route 物化按 `node.rootIndex` 解析正确 prefix —— 杜绝 root-A state + root-B route prefix；
- **root-ordered agenda**：multi-root 搜索按 root 顺序 pop（root 0 先耗尽再 root 1……），单一 root 搜索完全不变（rootIndex 恒 0）；
- **`searchSegmentDPMultiRoot`**：segment 级多根入口，复用全部 production segment 机制（actionProvider/goalPredicate/dominanceConfig/goalSkyline 投影），per-goal 返回 `rootCandidateId`；
- **Arm B 生产集成**：`runSegmentAgainstFrontierLocal` 新增 `enableMultiRootSharedDp`（**默认 OFF**）+ 激活守卫（candidates >= 2、stopOnFirstGoal=false、best-first agenda、无 probe caps，否则回退 legacy Arm A）；
- **`dpSeedAuthority` 原型已删除**（dp-search 钩子 + segment-dp 透传 + G28-I 旧 worker），不留第二套 runtime-configurable dominance 入口；G28-I 改用新 multi-root API 做 feasibility regression；
- 表述修正：不再声称 byte-identical production path，正确口径为 **hook-off behavioral parity**（G28-A 权威基线 897/2285 复现 + G26/G27 全量回归佐证）。

### 14.3 关键实证发现：自由竞争序 vs root-ordered 活跃 agenda

实现过程中实测（16-root MT3 workload，直接 searchDP 管道）：

- **自由竞争序（所有 roots 的状态按统一 rank 混排）**：同一 live authority 下扩展数膨胀到 30,000+（cap 截断、未完成），unique DP bucket 2,561-2,763 —— winner 跨 lineage 反复替换引发重扩展级联（47k+ same-HP / 44k+ lower-HP churn）；
- **root-ordered 活跃 agenda**：扩展数回到 2,346（与 G28-I 顺序原型完全一致），同时**保住 live-authority 硬契约** —— incomplete root 的 pending agenda 留在共享搜索内，budget 截断绝不伪报 exhaustion；
- **重要教训（基线错配）**：直接 `searchDP`（默认 action 枚举）与 segment 管道（segment actionProvider）的 action 空间不同（candidate-0 独立完成分别需要 12,896 vs 897 expansions）。多根实现的最终对照一律走 segment 管道；修正后验证：**4-root shared 的 unique bucket 集合 == 独立 union 集合（零多余领域）**，live 实现语义正确。

### 14.4 G29 门禁实测（check-multi-root-shared-dp.js，v1）

```json
{
  "G29-A": { "completeNoGoalParityVerified": true, "legacyTotalExpansions": 10565, "sharedExpansions": 2346, "expansionReductionPercent": 77.79, "mechanismThresholdPercent": 50, "mechanismThresholdMet": true, "sharedRootCount": 16 },
  "G29-B": { "nonVacuousGoalProvenanceVerified": true, "segment": "mt1-to-mt2 (generic, non-MT3)", "sharedWithinLegacyUnionMultiset": true, "attributionVerified": true, "strictReplayVerified": true, "replayedGoalCount": 8, "bestGoalMatched": true, "sharedExpansions": 186, "legacyTotalExpansions": 338 },
  "G29-C": { "crossRootDominanceReplacementVerified": true, "laterBetterRootStillExpands": true, "orders": { "weak-then-strong": { "exp": 1079, "byRoot": { "weak": 267, "strong": 812 } }, "strong-then-weak": { "exp": 991, "byRoot": { "strong": 896, "weak": 95 } } } },
  "G29-D": { "budgetLimitedFailCloseVerified": true, "direct": { "expansions": 300, "frontierSize": 132, "pendingByRootTotal": 132, "searchComplete": false, "expansionBudgetExhausted": true, "neverExhausted": true }, "armB": { "finalPending": 16, "finalFound": 0, "finalComplete": 0, "searchComplete": false, "candidatesInLedger": 16 } },
  "G29-E": { "candidateCompletionLedgerVerified": true, "armB": { "finalComplete": 16, "finalPending": 0, "searchComplete": true, "initialAttempts": 1, "candidatesInLedger": 16, "multiRootSharedExpansions": 2346 }, "armALegacy": { "finalComplete": 16, "searchComplete": true, "initialAttempts": 16 } },
  "G29-F": { "goalSkylineParityVerified": true, "comparisonLevel": "merged segment output (mergeMilestoneFrontier byKey dedup)", "multisetMatched": true, "bestMatched": true, "limitConfig": { "goalSkylineLimit": 4, "candidateLimit": 4 } },
  "G29-G": { "isolatedWorkerIntegrationVerified": true, "inputStateKeysVerified": 4, "stateRoundTripIdentity": true, "simulatorProfileIdentity": true, "executionMode": "multi-root-shared-dp" },
  "G29-H": { "legacy": { "expansions": 10565, "wallMs": 104801, "peakRssMb": 423.4 }, "shared": { "expansions": 2346, "wallMs": 24048, "peakRssMb": 438.1 }, "expansionReductionPercent": 77.79, "wallObservationalOnly": true, "mergeConditionMet": true },
  "G29-I": { "generalizationVerified": true, "note": "G29-B/F 在 MT1→MT2 segment 上经同一通用 multi-root 管道运行；生产代码无 MT3/floorId/candidateId 特化" }
}
```

G29-B goal-bearing fixture 说明：以确定性 MT1 初始状态（`createInitialState({rank:"chaos"})`）合成 3 个 root 变体（HP 30 无法到达目标 / HP 201 正常到达 / HP 150 较差到达），无任何 route hint；8/8 共享 goal 全部从其报告 root 严格重放至 exact stateKey 一致；全部 goal 归因到独立可达同一 state 的 root（root-b-normal；root-c 的较弱 lineage 被跨 root 支配正确吸收）。

### 14.5 Iteration 2 冻结口径

```ini
PR_5_24h = IN_PROGRESS
ITERATION_2 = COMPLETED_AWAITING_REVIEW

PRODUCTION_MULTI_ROOT_SHARED_DP = IMPLEMENTED (ARM B, DEFAULT OFF)
PROGRESSIVE_SEED_BLOCKERS = REMOVED (dpSeedAuthority DELETED)
ONE_LIVE_MULTI_ROOT_AUTHORITY = PROVEN
INCOMPLETE_ROOT_PENDING_AGENDA_PRESERVED = PROVEN (G29-D)
GOAL_BEARING_ORIGIN_PROVENANCE = PROVEN_NON_VACUOUS (G29-B, 8/8 STRICT REPLAY)
BUDGET_LIMITED_COMPLETION_AUTHORITY = PROVEN_FAIL_CLOSE (G29-D)
CANDIDATE_COMPLETION_LEDGER_COMPATIBILITY = PROVEN (G29-E, 16 candidates NOT collapsed)
CROSS_ROOT_DOMINANCE_REPLACEMENT = PROVEN (G29-C, both orders)
ISOLATED_WORKER_ROUND_TRIP = PROVEN (G29-G)
FIXED_WORK_EXPANSION_REDUCTION = 77.79 PERCENT (G29-A/G29-H MERGE CONDITION MET)

P0 = NONE
P2_NOTES = FREELY_INTERLEAVED_AGENDA_ORDER_EXPLODES_WORK_30K_PLUS_EXP__ROOT_ORDERED_POP_IS_THE_ITERATION_2_DEFAULT__PRODUCTION_DEFAULT_FLIP_PENDING_CLOUD_REVIEW
REAL_MT1_MT4 = NOT_AUTHORIZED (NEXT REVIEW ONLY)
```

### 14.6 本轮验证记录

```text
G28-A/F/G/H (profiler suite)                   PASSED (G28-I 改用 production multi-root API, 77.79% 一致)
G29-A..I (multi-root gate suite)               PASSED
G24 / G25 / G26 / G27                          PASSED
budget scheduling (--fast)                     PASSED
route-free-state                              PASSED
MT1 real-route gate                           PASSED
check:core                                    PASSED
check:manifest (149 modules, 183 tests)       PASSED (新增 check-multi-root-shared-dp.js)
check:no-tower-solver-js                      PASSED
```
