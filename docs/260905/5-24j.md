# PR-5.24j — One-Step Counterfactual Exhaustiveness Qualification

```ini
MILESTONE = PR-5.24j
STATUS = APPROVED_AND_CLOSED (ONE_STEP_EXPRESSIVENESS_LINE = CLOSED)
DATE_STARTED = 2026-09-07
BASE = 49f5146 (PR-5.24i closure)
SCOPE = 在不受 30s global wall 抽样影响的条件下，完整评估当前 production
        one-step counterfactual generator 的真实历史集合与 downstream 能力，
        判断 one-step CF 表达空间 SUFFICIENT / PARTIALLY_SUFFICIENT / INSUFFICIENT。
        本轮不改 production solver。
```

## 1. 问题定义

PR-5.24e 的真实结果是：16 条 one-step CF histories，只有 6 条被 probe（37.5% coverage），10 条 global-wall 前未 probe，0 条已 probe 有 measurable progress。这**不足以证明** one-step 本身不足——coverage 只有 37.5%。本轮在非 binding 条件下做完整评估。

## 2. 方法（G32 harness: `check-one-step-cf-exhaustiveness.js`）

### B1 Source authority（复用 5.24e 真实 CF source condition）

```text
Source artifact:  $TEMP/diagnostic-result.json（PR-5.24e Real Run 2 原始 artifact）
sourceCandidate:  mt2-to-mt3:candidate-0（canonical rollback anchor 输出）
                  MT3 floor, HP 10820 / ATK 97 / DEF 70 / MDEF 490
failureClass:     floor-progress-blocked（trusted）
failedSegment:    mt3-to-mt4
无 OnlyUp hint、无人工资源目标
```

### B2 Production generator（未调 generator）

当前 production `buildCounterfactualRepairIntents` 生成 **3 个 intents**：

| intentId | kind | structuralDelta（关键项） | goal |
|---|---|---|---|
| cf-d0-path_unlock | path/unlock | pathOpportunity+1 | MT2 floor |
| cf-d1-exp_level | exp/level | HP-698, exp+2 | MT3, exp>=16, remove (8,11) |
| cf-d2-exp_level | exp/level | HP-5194, mdef+20, exp+2 | MT3, exp>=16, mdef>=510, remove (4,11) |

**与 5.24e 的身份比较**：5.24e 历史为 2 intents（path/unlock + exp/level 单一变体）；当前 production 生成 3（多一个更强的 exp/level 变体 cf-d2）。生成集合已发生变化（记录，未调）——当前集合是历史集合的**超集方向**（覆盖 5.24e 的两个 kind，且 exp/line 多一个变体），评估覆盖不小于历史空间。

### B3 Full materialization（production segment machinery，非 binding）

每 intent 走 production 管道：synthesized intent segment（`stopOnFirstGoal=false`，16k exp，非 binding wall）→ intent.startCandidate → realized frontier；再走 anchor segment（mt2-to-mt3）expansion → `buildRepairedHistoryHypotheses` history split。

```text
3/3 intents REALIZED（0 UNREALIZABLE / 0 INCOMPLETE / 0 RESOURCE_LIMITED）
```

### B4 Dedup（production keys）

```text
RAW_REALIZED_HISTORY_COUNT   = 20
UNIQUE_CANONICAL_STATE_COUNT = 20  (buildStateKey)
UNIQUE_DP_BUCKET_COUNT       = 19  (buildDpStateKey region)
intentId → realized root provenance 全保留
```

### B5 Multi-root shared DP full downstream（本轮核心）

20 个 unique one-step CF histories 作为 roots → production isolated pipeline（`runSegmentAgainstFrontier` → `executeIsolatedSegment` → worker → `runSegmentAgainstFrontierLocal`）→ mt3-to-mt4 segment，`enableMultiRootSharedDp` UNSET（production default ON）、root-sliced、非 binding（60k exp cap 远非 binding）。

```text
executionMode        = multi-root-shared-dp
policy               = root-sliced (quantum 64)
rootsInput/trie      = 20/20   searchInvocations = 1
inputStateKeysVerified = 20/20  simulatorProfileIdentity = true
searchComplete       = true（自然前沿耗尽）
expansions           = 439     frontierSize = 0   stoppedReason = null
worker peak RSS      = 283.9 MB
19 scheduler switches, 68 stale skipped, 139 crossRootReplacements
```

## 3. 结果（B6-B8）

```ini
FOUND_GOAL_ROOTS = 0
EXPLORED_ROOTS = 18   DOMINANCE_ELIMINATED_ROOTS = 2
   (2 roots 的 bucket territory 在第一轮即被更强 cross-root 状态消费——
    合法 work elimination，非饥饿)
DETERMINISTIC_POSITIVE_DOWNSTREAM_PROGRESS = FALSE
   (goal skyline 为空：没有任何 root 的后代在 mt3-to-mt4 segment goal 上
    取得 completion > 0 或 floor 进展)
PENDING_ROOTS = 0

INTENTS_GENERATED = 3   INTENTS_REALIZED = 3   INTENTS_FAILED = 0
RAW_HISTORIES = 20   UNIQUE_EXACT_HISTORIES = 20
ROOTS_PROBED = 20   ROOTS_COMPLETE = 20   ROOTS_GOAL = 0
ONE_STEP_EVALUATION_COVERAGE_RATIO = 1.0 (100%)
```

Per-root expansion 分布（root-sliced，完整耗尽）：path_unlock roots 0-13：46/81/0/9/8/9/53/70/5/61/5/5/5/5；exp_level roots 14-19 见 artifact。18 roots 真实探索（5-81 exp），2 roots dominance-eliminated（0 exp + 0 pending，bucket 被更强 root 消费）。

## 4. 判决（B7）

```ini
ONE_STEP_CF_EXPRESSIVENESS =
INSUFFICIENT

ONE_STEP_COUNTERFACTUAL_CAPABILITY =
DETERMINATELY_INSUFFICIENT_ON_THIS_FAILURE_FRONTIER

依据（Case C，全部满足）:
  all realized one-step roots (20/20, 100% coverage)
  + shared downstream search complete (frontier exhausted, 0 pending)
  + no goal
  + no deterministic positive downstream progress (empty goal skyline)

COMPOSITION_NEED =
PROVEN_REQUIRED (one-step 不足以单步打通该 failure frontier)

MULTI_STEP_CF = NOT_AUTHORIZED_THIS_ROUND
  (按 B13：本轮只完成 qualification；是否开 bounded multi-step 由
   Cloud Review 裁决——本轮已提供其所需的硬证据前提)
```

## 5. 与 5.24e 的对照（诚实口径）

| 维度 | PR-5.24e Real Run 2 | PR-5.24j（本轮） |
|---|---|---|
| 条件 | 30s global wall（生产抽样） | 非 binding 完整评估 |
| Intents | 2 | 3（production 现状，含历史超集） |
| Histories | 16 raw（17 tickets 含 1 placeholder） | 20 unique exact |
| Probed | 6/16（37.5%） | **20/20（100%）** |
| 正向 progress | 0/6 已 probe | 0/20 全量（含 goal-skyline 空验证） |
| 结论 | NOT_PROVEN（coverage 不足） | **DETERMINATELY_INSUFFICIENT** |

Fixed-work metrics（B9，观察性）：20 roots shared = 439 exp；同额度 legacy per-candidate 对照未跑（本轮非性能 milestone，不重开 5.24h）；wall/RSS 仅为运行环境记录。

## 6. 验证记录（B12）

```text
G32 harness (check-one-step-cf-exhaustiveness.js)          COMPLETED (Case C)
G24 (counterfactual-repair, 20 gates)                      PASSED
G31 (terminal-positive-cf-admission, 7 gates)              PASSED
G29 (multi-root-shared-dp)                                 PASSED
G30 (root-sliced-scheduling, 含 default/rollback)           PASSED
route-free-state                                           PASSED
check:core                                                 PASSED
check:manifest (149 modules, 186 tests)                    PASSED (新增 G32)
check:no-tower-solver-js                                   PASSED
```

## 7. 禁止项遵守

未修改 generator / 未加 intent / 未调排序 / 未动 candidateLimit / dominance / multi-root / scheduler / budget；无 OnlyUp hint；未跑完整 MT1→MT4 / 30s lottery / 第二次资格抽样。Production 零改动（G32 为 qualification harness + fixture-free 直读 source artifact）。

## 8. 下一步（Cloud Review 裁决）

按 B13 判据，Case C 已达成：只有现在才有资格讨论 **bounded multi-step / composed counterfactual**。候选方向（未授权）：
- 两步组合（如 path_unlock → exp_level）是否覆盖 single-step 不可达的历史空间；
- 组合的 budget-aware 搜索空间控制（避免爆炸）；
- 或先审 20 个 realized histories 的结构（哪些资源维度被 one-step 覆盖、哪些维度组合缺失）。

---

## 9. Iteration 1 Repair 1 — Exact CF Source Authority：STOP 报告（EXACT_SOURCE_UNAVAILABLE）

### 9.1 Cloud Review Repair 1 指令（摘要）

Iteration 1 的 G32 使用了错误的 CF source stage：`src.finalCandidates /
mt2-to-mt3:candidate-0 / MT3 state` 作为 generator source。Production authority 是：

```js
buildCounterfactualRepairIntents({ startCandidates: anchor.inputFrontier, ... })
```

必须恢复**实际触发 CF 的 depth-1 anchor 的 inputFrontier**；若 artifact 未保存该完整 state，
禁止用 finalCandidates 代替，报告 `EXACT_5_24e_CF_SOURCE_STATE = UNAVAILABLE` 并 STOP。

### 9.2 源结构核实（production 语义逐层确认）

5.24e 轨迹的 repair-time history 结构（代码路径核实，`segment-dp.js`）：

```text
forward path: mt1-to-mt2 ✓ → mt2-to-mt3 ✓ → mt3-to-mt4 ✗（失败，触发 backtrack）
repair-time history（失败 segment 不入 history）:
  [ {segment: mt1-to-mt2, inputFrontier: [initial#0], merged: [mt1-to-mt2 输出]},
    {segment: mt2-to-mt3, inputFrontier: [mt1-to-mt2 merged], merged: [mt2-to-mt3 输出]} ]

depth=1 → anchorHistoryIndex = 1 → anchor = mt2-to-mt3 条目
anchor.inputFrontier = mt1-to-mt2 segment 的 merged 输出 = [mt1-to-mt2:candidate-0]
                                      ↑ 这是 MT2-floor 状态（CF 的真实 source stage）
```

与 artifact 交叉验证（`diagnostic-result.json`）：

```text
bt.attempts[0]: depth=1, anchorSegmentId=mt2-to-mt3,
                anchorInputCandidateIds=["mt1-to-mt2:candidate-0"]   ← 确认唯一 source candidate
```

### 9.3 Artifact 可恢复性核查（穷尽）

对 `diagnostic-result.json`（及全部现存 artifact）做深度扫描：

```text
full-state objects by floor (diagnostic-result.json):  { MT3: 1 }
  （唯一完整 state = finalCandidates[0] = mt2-to-mt3:candidate-0，MT3 floor——
   是 anchor 的 merged 输出，不是 anchor.inputFrontier）

mt1-to-mt2:candidate-0 完整状态对象：0 个
MT2-floor 完整状态对象：0 个

其它 artifact 核查：
  qualification-5-24g-run-result.json   → full states: { MT3: 10 }
  qualification-5-24g-run2-result.json  → full states: { MT3: 16 }
  qualification-run2-result.json        → （同结构，无 MT2 完整状态）
  batch-diagnostic-result.json          → 无 MT2 完整状态、无 mt1-to-mt2 候选
  5-24i-run1 artifact                   → 含 MT2 状态，但属不同轨迹（5.24i 前向），
                                           不可跨轨迹冒用（provenance 违规）
  logs/generated/、routes/generated/    → 无相关 artifact
```

5.24e diagnostic runner（`run-mt1-mt4-cf-diagnostic.js` 源码核实）只序列化
`finalCandidates`（MT3）与 telemetry 摘要——`anchor.inputFrontier` 的完整 state
**从未被保存**。

### 9.4 冻结结论

```ini
EXACT_5_24e_CF_SOURCE_STATE =
UNAVAILABLE

RECOVERY_FROM_ARTIFACT = IMPOSSIBLE (穷尽核查：唯一完整状态为 anchor merged 输出，
                                  非 anchor.inputFrontier；MT2-floor source 从未保存)
CROSS_TRAJECTORY_BORROWING = PROHIBITED (5-24i 等其它轨迹的 MT2 状态 provenance 不符)
DETERMINISTIC_FORWARD_SEGMENT_REPLAY = NOT_ATTEMPTED (超出本轮授权：指令限定
                                  恢复必须来自 artifact 内部；重放属 production
                                  re-derivation 而非 artifact recovery，需 Cloud 裁决)

ITERATION_1_VERDICT = SUPERSEDED_BY_WRONG_SOURCE_STAGE
  (G32-v1 的 3 intents / 20 histories / Case C 判决全部基于错误 source stage，
   按 Repair 1 指令 §3 视为 SUPERSEDED，不再引用)

QUALIFICATION = STOPPED_PER_INSTRUCTION
```

### 9.5 交 Cloud 裁决的选项（未自行选择）

1. **授权确定性重放恢复**：从标准 chaos 初始状态对 mt1-to-mt2 segment 做 production
   确定性搜索（非 binding），以其 merged 输出中 `id=mt1-to-mt2:candidate-0` 的候选为
   source——并用 5.24e artifact 的 `bt.attempts[0].anchorInputCandidateIds` 做身份锚定。
   注意：5.24e 运行中 forward mt1-to-mt2 执行可能受 30s wall 时序影响，重放结果与
   原轨迹的 buildStateKey 是否逐项一致需要先行验证（不一致则仍不可用）；
2. **换用已保存完整 source 的其它授权轨迹**：如 5-24i Run 1 artifact 中的
   `mt1-to-mt2:candidate-0@MT2`（HP 3250），其 CF source 阶段完整可恢复——但它是
   不同日期/代码状态的轨迹，失败 frontier 需重新确认（该轨迹 failedSegment 亦为
   mt3-to-mt4 / floor-search-incomplete，但 failureClass 不同：untrusted for CF，
   无法直接作为 CF source condition）；
3. **重新定义 qualification source 策略**：不再依赖历史 artifact，而是在一次性受控
   运行中现场生成并保存完整 anchor.inputFrontier（需新的运行授权）。

### 9.6 验证记录

本轮为 docs-only STOP 报告（零 production 改动、零新 harness 运行、零重放尝试）。
源结构核实与 artifact 穷尽扫描均为只读操作。

---

## 10. Iteration 2 — Historical Source Re-Derivation：Witness 口径 STOP 报告

### 10.1 执行摘要

按指令在 5.24e production commit `675997112781f8bbcd1eea0294993c00d491334f` 的 clean worktree
上完成了 MT2 source 的确定性 prefix re-derivation（chaos initial → mt1-to-mt2 only，未跑完整
MT1→MT4）与 downstream witness 链（mt2-to-mt3 from re-derived source），并完成 4× fresh-process
determinism 检查。**字面 witness 契约（vs archived finalCandidates[0]）未匹配**——按指令 §3 必须
STOP。但分析发现了重要的证据结构：re-derivation 与 archived run 自身的 **depth-1 repair wave
anchor 完全一致**。以下为完整记录。

### 10.2 Re-derivation 结果（4 次独立 fresh process，全部一致）

```text
Worktree:   C:/Users/81489/AppData/Local/Temp/base-524e @ 6759971（PR-5.24e final production）
Prefix:     chaos initial → mt1-to-mt2（forward config：adaptive-feasible / isolated-process /
            candidateLimit 8 / resource diversity / 30s/50k/2048MB；仅此 prefix，未继续）

REDERIVED_SOURCE_CANDIDATE_ID = mt1-to-mt2:candidate-0
REDERIVED_SOURCE_STATE_KEY   = {"floorId":"MT2","progressSig":"1:MT1,MT2:MT1,MT2:2",
                               "hero":{..."hp":3250,"atk":21,"def":17,"mdef":130,"lv":3,"exp":8}...}
mt2CandidateCount = 8（candidate-0..7，HP 3250/2770/2503/3250/2770/2023/1970/1970）

Downstream witness（mt2-to-mt3 from full 8-candidate set，同 5.24e spec/config）：
WITNESS_CANDIDATE = mt2-to-mt3:candidate-0
WITNESS_STATE_KEY = ..."hp":12782,"atk":97,"def":65,"mdef":490,"lv":4,"exp":12...}

PREFIX_REDERIVATION_DETERMINISM = PASSED（4 次 fresh process，source key 与 witness key 逐项一致）
```

### 10.3 Witness 对比（指令 §3 字面契约）

| 维度 | re-derived witness | archived finalCandidates[0] | 一致？ |
|---|---|---|---|
| candidateId | mt2-to-mt3:candidate-0 | mt2-to-mt3:candidate-0 | ✓ |
| floor | MT3 | MT3 | ✓ |
| hero | HP 12782 / DEF 65 / EXP 12 | HP 10820 / DEF 70 / EXP 14 | **✗** |
| buildStateKey | （12782 键） | （10820 键） | **✗** |

```ini
HISTORICAL_DOWNSTREAM_WITNESS_MATCHED = FALSE
PREFIX_REDERIVATION_DETERMINISM = PASSED

EXACT_5_24e_CF_SOURCE_STATE =
UNRECOVERABLE_BY_DETERMINISTIC_PREFIX_REDERIVATION
   (按指令 §3 字面口径)
QUALIFICATION = STOP
```

### 10.4 根因分析（未调参数追匹配，按纪律）

Archived run 自身就包含**同一个 source 的两个不同 mt2-to-mt3 输出**：

```text
同一个 mt1-to-mt2:candidate-0 输入：
  forward 执行（30s shared globalBudget 已被 mt1-to-mt2 消耗后的剩余额度 +
    stopOnFirstGoal=true goal-directed 模式）
    → 10820/70/14（archived finalCandidates[0]）
  depth-1 repair wave anchor expand（fresh probe 预算 200exp/2000ms +
    stopOnFirstGoal=false backtrack overrides）
    → 12782/65/12（archived h-d1w0h0 anchorOutputStateKey）
```

我的 re-derivation（fresh budget + forward config）得到的 witness **恰好等于 archived run 自己
的 repair-wave 输出**（逐字节 buildStateKey 一致）：

```ini
REDERIVED_WITNESS_KEY == ARCHIVED_h_d1w0h0_ANCHOR_OUTPUT_STATE_KEY = TRUE
```

原因：forward 输出（10820）是 **execution-context-dependent 的轨迹 artifact**——它与 repair-wave
执行在多个维度同时不同（预算状态、stopOnFirstGoal、priority/search mode、backtrack overrides），
当前授权的 fresh-budget deterministic re-derivation 不复现该 mid-run forward witness。
归因口径：FORWARD_WITNESS_CONTEXT_DEPENDENT = ESTABLISHED；BUDGET_STATE_ISOLATED_CAUSE =
NOT_ESTABLISHED（未做隔离归因，非主线）。这不是 source 错误，而是 witness 目标本身的性质。

### 10.5 Source identity 的实际证据链（供 Cloud 裁决）

虽然字面 witness 失败，但 source 身份有一条**更强的直接证据链**：

```text
archived run 的 depth-1 repair wave 从 mt1-to-mt2:candidate-0 重扩 mt2-to-mt3
→ 得到 h-d1w0h0.anchorOutputStateKey（12782）
我的 re-derivation 从 re-derived mt1-to-mt2:candidate-0 扩 mt2-to-mt3
→ 得到完全相同的 buildStateKey（12782）
→ mt2-to-mt3 段代码对同一输入确定性
→ archived 的 mt1-to-mt2:candidate-0 状态 == re-derived 的 candidate-0 状态（身份传递证明）
```

即：**CF 真正消费的 source（anchor.inputFrontier[0]）的身份已由 repair-wave 等价性证明**；
未匹配的只是 forward-path 输出（finalCandidates[0]），它从来不是 CF source 链的一部分。

### 10.6 交 Cloud 裁决

```ini
OPTION_1 = 维持 STOP：接受 UNRECOVERABLE 判定，one-step exhaustiveness
           qualification 需要新的 source authority 策略
OPTION_2 = 重锚 witness 契约到 h-d1w0h0（depth-1 repair wave anchor，CF source
           的直接下游）：re-derivation 已逐字节匹配，source fixture 可立即落盘，
           G32-v2 qualification 可继续
OPTION_3 = 接受 identity-chain 证明（§10.5）作为 source 身份的充分证据，
           无需任何 witness 重跑
```

### 10.7 纪律遵守

```text
完整 MT1→MT4          0（仅 prefix + 单 segment witness）
参数调整追匹配        0（发现 mismatch 后立即停止分析，未调 budget/config）
借用 5.24i MT2 source 0
手工修改 state        0
production 修改       0（全部在 5.24e 只读 worktree 与 $TEMP 脚本内）
source fixture 落盘   未执行（witness gate 未过，按指令 §5 前置条件不满足）
```

Artifacts：`$TEMP/524e-rederive-run{1,2,3,4}.json`（4 次 re-derivation 完整输出）。

---

## 11. Iteration 2 Repair 1 — Repair-Wave Source Identity Isolation：GATE STOP 报告

### 11.1 执行摘要

按 Cloud Review 指令在 5.24e worktree（`6759971`）完成 8-root isolation gate：将确定性重推导的 8 个
MT2 candidates 逐个单独输入 mt2-to-mt3，在**两种执行条件**下运行并记录
`input candidate id + buildStateKey → output buildStateKey`：

```text
Arm A（字面指令条件）= 历史 h-d1w0h0 repair-wave 复刻：
  stopOnFirstGoal=false（backtrack overrides）+ probe cap 200 exp + probe wall 2000ms
  + goalSkylineLimit=16 + maxRuntimeMs=30000
Arm B（对照）= forward 配置（goal-directed / stopOnFirstGoal=true / 无 probe caps）
```

### 11.2 结果（8 roots × 2 arms，fresh process，repair arm 3× 重复一致）

| Input | Arm A (repair 复刻) | Arm B (forward) |
|---|---|---|
| `mt1-to-mt2:candidate-0` | hp **12769**，**不匹配** witness | hp **12782**，**完整 buildStateKey 匹配** witness ✓ |
| candidate-1 | 未达 MT3（merged 空） | 未达 MT3 |
| candidate-2 | 未达 MT3 | 未达 MT3 |
| candidate-3 | hp 12769，不匹配 | hp 12782（同 HP），**完整 key 不匹配**（状态不同） |
| candidate-4..7 | 未达 MT3 | 未达 MT3 |

### 11.3 字面 gate 判定：FAILED → STOP

```ini
ISOLATION_GATE_LITERAL (repair-wave 条件复刻) = FAILED
   (candidate-0 在复刻条件下产出 12769 ≠ archived 12782；
    且 8 个 root 无一在复刻条件下产出 archived key)

REPAIR_ARM_DETERMINISM = PASSED (3× fresh process，12769 稳定)
FORWARD_ARM_UNIQUENESS = OBSERVED
   (8-root 域内，仅 candidate-0 的 forward 单根执行产出与 archived h-d1w0h0
    完整 buildStateKey 一致的 12782；candidate-3 同 HP 但完整 key 不同；
    其余 6 个 root 均无法到达 MT3)
```

### 11.4 根因（未调参追匹配）

Archived h-d1w0h0 anchor expand 运行于 **mid-run live context**（live globalBudget 对象，
forward 路径已消耗 expansions；`waveStartExpansions` 为非零累计值；probe caps 锚定在
该累计基线上）。fresh re-derivation（globalBudget 从零开始）与该 mid-run context 不等价；
observed 12769/12782 divergence 与该 context difference 共现。具体由 globalBudget 的哪个
字段或调度分支造成，NOT_ESTABLISHED（未做隔离归因，非主线）。

口径（修正后，吸收 Cloud P2）：

```ini
ARCHIVED_REPAIR_WAVE_OUTPUT_IS_EXECUTION_CONTEXT_DEPENDENT = ESTABLISHED
   (fresh 复刻不复现；具体差异维度含 live globalBudget 累计状态等，
    未做隔离归因——非本轮主线)
FORWARD_WITNESS_IS_ALSO_EXECUTION_CONTEXT_DEPENDENT = ESTABLISHED
   (5.24e forward finalCandidates[0]=10820 亦不复现)
```

### 11.5 证据现状与诚实结论

两条 witness 路径的字面复刻均失败：

```ini
HISTORICAL_FORWARD_WITNESS_MATCHED (10820)     = FALSE (Iteration 2)
HISTORICAL_REPAIR_WAVE_WITNESS_MATCHED (12782) = FALSE (本 Repair 1, 复刻条件)
```

但 8-root 域内的**唯一性信号**存在：forward 单根执行下，8 个重推导 candidate 中
**只有 candidate-0 产出与 archived h-d1w0h0 完整一致的 12782**（candidate-3 同 HP 不同状态，
其余全部无法到达 MT3）。这是强相邻证据（adjacent evidence），但**不满足指令的字面
gate 契约**（要求在 repair-wave 复刻条件下命中）。

```ini
SOURCE_IDENTITY_CHAIN = NOT_YET_PROVEN
   (forward-config 唯一性 ≠ repair-wave 条件复刻；反推链仍有逻辑洞:
    forward 执行条件与 archived repair wave 执行条件不同,
    forward-config唯一命中不能直接证明 archived repair wave 的输入就是 candidate-0)
QUALIFICATION = STOP (RETURN TO CLOUD)
```

### 11.6 交 Cloud 裁决（未自行选择）

```ini
OPTION_A = 接受 forward-uniqueness 作为有限域 source identity 证据
           (8-root 域内唯一命中 + candidate-3 同HP异state 的区分度证明；
            但执行条件 arm 与 archived repair wave 不同)
OPTION_B = 授权 mid-run context 复刻: 构造带已消耗 globalBudget 的运行环境
           (如先跑 forward prefix 消耗与 archived 一致的 expansions,
            再在 live context 中触发 repair wave) —— 工程复杂度中等
OPTION_C = 放弃历史 source forensics, 改用 forward-config 单根重推导的
           candidate-0 (12782 lineage) 作为新 source authority 并冻结新 provenance
           (接受其与 archived run 的 source 不保证逐字节一致,
            换取 qualification 主线继续)
```

### 11.7 纪律遵守

```text
参数调整追匹配        0（发现 12769≠12782 后停止，未调 budget/config/probe 值）
完整 MT1→MT4          0（仅 prefix 重推导 + 单 segment isolation）
production 修改       0（全部在 5.24e 只读 worktree）
source fixture 落盘   未执行（gate FAILED）
G32-v1 harness        未改动（等 Cloud 裁决后一并处理）
```

Artifacts：`$TEMP/524e-isolation-gate-result.json`、`$TEMP/524e-isolation-all.json`、
`$TEMP/524e-isolation-debug` 3× 运行记录。

---

## 12. Iteration 3 — Controlled Source Qualification / G32-v2：Case C 达成

### 12.1 Cloud Review 裁决（Iteration 2 Repair 1 终审）

Iteration 2 Repair 1 终审 `Approved as negative-result gate`（字面 isolation gate FAILED 的
记录正确，未包装成成功证明）。Cloud 决策 **Option C**：停止 historical exact-source
forensic（Option A 拒绝——forward-uniqueness 是 strong provenance evidence 而非 exact
identity proof；Option B 拒绝——mid-run context 复刻是 archaeology rabbit hole）。Arm A
口径修正吸收：字面复刻实为 repair-wave local-option replica under fresh globalBudget
context（live globalBudget 本就是 repair-wave execution contract 的组成部分）。

### 12.2 Controlled Source Fixture（`fixtures/perf/onlyup-524e-cf-source.json`）

```ini
sourceAuthority = controlled-rederived
originCommit = 675997112781f8bbcd1eea0294993c00d491334f
derivationMethod = chaos initial -> mt1-to-mt2 prefix (5.24e production code)
candidateId = mt1-to-mt2:candidate-0
buildStateKey = <exact full key（fixture 内完整保存）>
prefixCandidateCount = 8
freshProcessDeterminism = 4/4
historicalExactIdentity = NOT_ESTABLISHED
historicalAdjacentEvidence = forward-8-root-unique-match-to-archived-12782
EXACT_5_24e_HISTORICAL_SOURCE = NOT_CLAIMED
```

Fixture 4,684 bytes；route=[] 前后 buildStateKey 不变（已断言）。

### 12.3 Controlled Lineage Verification（当前 dev production，非 binding）

```text
MT2 source → mt2-to-mt3:  found=true, 8 MT3 candidates (OBSERVED; gate = frontier_count > 0), complete (520 exp)
            (candidate-0 = HP 12782 —— 与 5.24e/5.24g 历史多源出现的 12782 一致)
MT3 frontier → mt3-to-mt4: found=false, complete (1,772 exp, multi-root shared 8 roots),
            failureClass = floor-progress-blocked（trusted）, searchComplete=true
```

```ini
CONTROLLED_CF_SOURCE = ESTABLISHED
CONTROLLED_FAILURE_FRONTIER = ESTABLISHED
```

### 12.4 G32-v2 实测（`check-one-step-cf-exhaustiveness.js` 重写为 v2）

**v1 → v2 的 authority 变更**：删除 `finalCandidates/MT3` source（wrong stage），
改读 controlled MT2 source fixture；triggerFailure 使用 controlled lineage 现场验证的
floor-progress-blocked；generator 从 MT2 source 出发。v1 判决维持
`SUPERSEDED_BY_WRONG_SOURCE_STAGE`。

| 阶段 | 结果 |
|---|---|
| **B2** generator | **3 intents**（path/unlock + 2×exp/level；5.24e 历史为 2——当前为超集方向，已记录未调） |
| **B3** materialization | **3/3 REALIZED**（1 + 8 + 4 realized candidates，0 UNREALIZABLE/INCOMPLETE/RESOURCE_LIMITED） |
| **B4** dedup | 32 raw histories → **16 unique canonical states**（16 DP buckets） |
| **B5/B6** per-root independent evaluation | **16/16 roots，全部 searchComplete=true**；每 root 读 production `att.bestProgressProjection`（isolated worker 内计算、与 production repair scheduler 同字段） |
| **B6** comparator | production `compareProgressProjections(start, best)`：**16/16 = 0（严格相等）**——每 root 的 bestProgress 投影与 start 投影完全一致，无一 root 有 deterministic positive downstream progress |
| **B9** shared cross-check | multi-root-shared-dp 16 roots，complete，no goal（2,123 exp），`semanticRegression = false` |
| **B8** coverage | `INDEPENDENT_ROOTS_COMPLETE/UNIQUE = 16/16 = 100%` |

### 12.5 Case C 判决（per-root hard authority）

```ini
Case C 条件（全部满足）:
  all realized roots determinate complete     = TRUE (16/16)
  shared search complete                      = TRUE
  100% evaluation coverage                    = TRUE
  goal roots                                  = 0
  per-root positive-progress roots            = 0 (comparator = 0 on all 16)

ONE_STEP_CF_EXPRESSIVENESS = INSUFFICIENT
CURRENT_ONE_STEP_CF_GENERATOR =
DETERMINATELY_INSUFFICIENT_ON_THIS_CONTROLLED_REAL_FAILURE_FRONTIER

NEXT_EXPRESSIVENESS_EXPANSION = REQUIRED
BOUNDED_COMPOSITION = AUTHORIZED_AS_NEXT_CANDIDATE
MULTI_STEP_CF = NOT_AUTHORIZED_THIS_ROUND
```

注意（口径）:
- 只冻结 **current generator insufficient**，不写 `MULTI_STEP_REQUIRED = PROVEN`；
- 本轮 source 为 controlled re-derived（`EXACT_5_24e_HISTORICAL_SOURCE = NOT_CLAIMED`），
  historical exact identity 正式关闭（不再欠历史 artifact 的账）；
- 16 个 realized histories 全部来自 cf-d1-exp_level（path_unlock/cf-d2 的 anchor
  expansion 产物 dedup 进 cf-d1 相同 canonical 状态——即当前 generator 的 3 个 intent
  在该 source 上收敛到同一个 16-state 历史空间）。

### 12.6 与 G32-v1 的关系

```ini
G32_V1 = SUPERSEDED_BY_WRONG_SOURCE_STAGE_AND_INCOMPLETE_PROGRESS_MEASUREMENT
G32_V2 = AUTHORITATIVE (controlled source + per-root production progress authority)
```

### 12.7 验证记录

```text
G32-v2 (check-one-step-cf-exhaustiveness.js v2)           COMPLETED (Case C, 100% coverage)
G24 (counterfactual-repair, 20 gates)                     PASSED
G31 (terminal-positive-cf-admission, 7 gates)             PASSED
G29 (multi-root-shared-dp)                                PASSED
G30 (root-sliced-scheduling, 含 default/rollback)           PASSED
route-free-state                                           PASSED
check:core                                                 PASSED
check:manifest (149 modules, 186 tests)                   PASSED
check:no-tower-solver-js                                   PASSED
```

### 12.8 纪律遵守

```text
完整 MT1→MT4          0（controlled lineage 仅 2 段）
production 修改       0（G32-v2 为 harness 重写 + 新 fixture；solver 零改动）
generator/intent/candidateLimit/dominance/scheduler/budget  0 修改
OnlyUp hint           0
现场 repair           0
```

### 12.9 下一步（Cloud Review 裁决）

若本轮通过（Case C + Option C source authority），5.24j 可关闭：

```ini
HISTORICAL_5_24e_EXACT_SOURCE_IDENTITY = NOT_RECOVERABLE / NOT_REQUIRED
CONTROLLED_REPRODUCIBLE_SOURCE_AUTHORITY = ESTABLISHED
ONE_STEP_CF_ON_CONTROLLED_REAL_FAILURE_FRONTIER = DETERMINATELY_INSUFFICIENT
```

下一主线进入 **bounded two-step / composed counterfactual**（不再授权第三轮 historical
witness archaeology）。优先依据 recovered intents / realized histories 的资源维度结构
决定 depth-2 composition vs richer compound one-step intent。

---

## 13. Iteration 3 Repair 1 — G32-v2 Fail-Closed Qualification Closure：全绿

### 13.1 Cloud Review 终审（Iteration 3）

Iteration 3 实验结果 `Approved`（controlled source authority、历史 forensic 关闭、
production progress comparator 用法、Case C 科学结果全部认可）；但 PR-5.24j 最终关闭
需补 fail-closed closure（P1-1 failure-frontier 未在 G32 内自验证、P1-2 materialization
completeness 未锁定、P1-3 progress-null 可能退化成 no-progress；P2 shared cross-check
单向）。四项一次性修复。

### 13.2 四项修复（`check-one-step-cf-exhaustiveness.js` v2.1）

```text
F1  B1 INLINE controlled lineage gate：
    fixture MT2 source → mt2-to-mt3（require found + determinate complete；
    frontier count > 0 gate，具体数量为 observed 不锁死）
    → full MT3 frontier → mt3-to-mt4（require no-goal + determinate complete +
    trusted failure classification）。triggerFailure 由本次实际运行的失败证据
    构造（不再硬编码 floor-progress-blocked——inline gate 断言它）。

F2  B3 per-intent determinate-complete bits：
    intentRealizationComplete + anchorExpansionComplete；只有
    REALIZED + realization-complete + anchor-complete 的 intent 的 histories
    进入 Case-C coverage authority。

F3  B7 Case C 增加 allMaterializationComplete + allProgressComparable：
    ROOTS_PROGRESS_COMPARABLE === UNIQUE_CANONICAL_STATE_COUNT（null comparator
    = unknown progress，永不退化成 no-progress）。

F4  B9 symmetric semantic cross-check：
    Boolean(shared.found) !== Boolean(independent goals) 双向判 regression。
```

### 13.3 v2.1 重跑实测（全部 fail-closed 条件通过）

```ini
F1_INLINE_LINEAGE_GATE:
  mt2_found = TRUE    mt2_complete = TRUE    mt3_frontier_count = 8
  mt3_found = FALSE   mt3_complete = TRUE
  failure_class = floor-progress-blocked (TRUSTED，来自实际运行证据)
  outcome_class = goal-not-found-search-complete

F2_MATERIALIZATION:
  3/3 intents REALIZED
  intentRealizationComplete = TRUE ×3
  anchorExpansionComplete = TRUE ×3
  allMaterializationComplete = TRUE

F3_CASE_C_HARD_AUTHORITY:
  ROOTS_REALIZED = 16
  INDEPENDENT_ROOTS_COMPLETE = 16/16
  ROOTS_PROGRESS_COMPARABLE = 16/16 (comparator = 0 on all，严格相等)
  INDEPENDENT_ROOTS_GOAL = 0
  INDEPENDENT_ROOTS_POSITIVE = 0
  SHARED_SEARCH_COMPLETE = TRUE
  allMaterializationComplete = TRUE
  allProgressComparable = TRUE

F4_SYMMETRIC_CROSS_CHECK:
  shared.found = FALSE == independent goals 0 → semanticRegression = FALSE
  (双向判断)

VERDICT:
  caseLabel = C
  ONE_STEP_CF_EXPRESSIVENESS = INSUFFICIENT
  CURRENT_ONE_STEP_CF_GENERATOR =
    DETERMINATELY_INSUFFICIENT_ON_THIS_CONTROLLED_REAL_FAILURE_FRONTIER
  NEXT_EXPRESSIVENESS_EXPANSION = REQUIRED
  BOUNDED_COMPOSITION = AUTHORIZED_AS_NEXT_CANDIDATE
  MULTI_STEP_CF = NOT_AUTHORIZED_THIS_ROUND
```

### 13.4 PR-5.24j 最终冻结

```ini
PR_5_24j =
APPROVED_AND_CLOSED_WITH_ONE_STEP_CF_DETERMINATELY_INSUFFICIENT_ON_CONTROLLED_REAL_FAILURE_FRONTIER

FINAL_PRODUCTION_COMMIT =（无 production 改动；本轮全部为 qualification harness/fixture/docs）
CONTROLLED_SOURCE_FIXTURE = fixtures/perf/onlyup-524e-cf-source.json
QUALIFICATION_HARNESS = check-one-step-cf-exhaustiveness.js (v2.1)

HISTORICAL_5_24e_EXACT_SOURCE_IDENTITY = NOT_RECOVERABLE / NOT_REQUIRED
CONTROLLED_REPRODUCIBLE_SOURCE_AUTHORITY = ESTABLISHED
CONTROLLED_FAILURE_FRONTIER = ESTABLISHED (INLINE GATE)

ONE_STEP_CF_ON_CONTROLLED_REAL_FAILURE_FRONTIER = DETERMINATELY_INSUFFICIENT
   (16/16 roots complete + 16/16 comparator=0 + 0 goals + 100% coverage +
    all materialization complete + all progress comparable)

INTENT_CONVERGENCE_STRUCTURE = 3 intents → 32 raw → 16 unique states
   (当前 generator 的全部 intent 在该 source 上收敛到同一 16-state 空间；
    继续增加相同形态的 one-step intent 暂无已观察到的表达增益)
NEXT_HYPOTHESIS = bounded composition / compound interaction
COMPOSITION_AS_ACTUAL_BOTTLENECK = NOT_YET_PROVEN

NEXT_EXPRESSIVENESS_EXPANSION = REQUIRED
BOUNDED_COMPOSITION = AUTHORIZED_AS_NEXT_CANDIDATE

P0 = NONE
P1_1 = RESOLVED (inline lineage gate)
P1_2 = RESOLVED (materialization complete bits)
P1_3 = RESOLVED (progress comparable gate)
P2 = RESOLVED (symmetric cross-check)

COUNTERFACTUAL_ADMISSION_LINE = CLOSED (PR-5.24i)
ONE_STEP_EXPRESSIVENESS_LINE = CLOSED (PR-5.24j)
```

### 13.5 验证记录

```text
G32-v2.1 (含 F1-F4 fail-closed gates)                PASSED (Case C, 全条件)
G24 (counterfactual-repair, 20 gates)                PASSED
G31 (terminal-positive-cf-admission, 7 gates)        PASSED
G29 (multi-root-shared-dp)                           PASSED
G30 (root-sliced-scheduling, 含 default/rollback)      PASSED
route-free-state                                      PASSED
check:core                                            PASSED
check:manifest (149 modules, 186 tests)              PASSED
check:no-tower-solver-js                              PASSED
```

### 13.6 纪律遵守

```text
production solver 修改     0（本轮仅 harness/fixture/docs）
完整 MT1→MT4               0（inline lineage 仅 2 段，复用 controlled source）
generator/intent/candidateLimit/dominance/scheduler/budget  0 修改
OnlyUp hint                0
```

### 13.7 下一主线（Cloud Review 裁决）

**Bounded two-step / composed counterfactual**——按 Cloud 指引的最窄起步：先只验证
depth=2 是否产生真正新的 canonical states（`TWO_STEP_UNIQUE_STATE_GAIN > 0 ?`）：

```text
controlled MT2 source
→ first CF intent realization → canonical state
→ 再运行一次 production one-step generator → compose second intent
→ canonical materialization
```

不先跑完整 MT4 downstream；若 depth-2 连新 exact state 都不产生，说明 generator
primitive vocabulary 太窄（考虑 richer compound intent）；若能产生新 canonical states，
再做第二阶段 downstream qualification。

---

## 14. Iteration 3 Repair 2 — Literal Fail-Closed Hardening：全绿（PR-5.24j 关账）

### 14.1 Cloud Review 终审（Repair 1）

Case C 科学结果继续 Approved（F2/F3 修复确认通过）；但 harness 有 3 个 fail-open 残留：
P1-1 F1 的三个答案型 fallback（`failureClass || "frontier-exhausted"`、
`outcomeClass || "goal-not-found-search-complete"`、fabricated MT3→MT4 missingGoalFields）、
P1-2 completion ledger 缺失时 `executionDeterminateComplete` 返回 complete、
P1-3 F4 只检测 semanticRegression 但 Case C 不看它。另两个 P2：8 candidates 应为
OBSERVED 而非锁死；composition 是 next hypothesis 而非已证明瓶颈。

### 14.2 修复（v2.1 → v2.2）

```text
P1-1  删除全部三个答案型 fallback：
      - lineageFailure 必须存在且 failureClass 为非空 string（assert，无 || 默认）
      - lineageOutcome.outcomeClass 必须存在且 === "goal-not-found-search-complete"（assert）
      - missingGoalFields 只保留实际运行证据（|| []，不制造 MT3→MT4 evidence）
P1-2  executionDeterminateComplete：candidateSliceTelemetry 缺失 → false
      （与 helper 注释一致：no pending/terminal-incomplete in the ledger）
P1-3  Case C 条件增加 && semanticRegression === false
P2-1  docs：8 MT3 candidates → OBSERVED（gate = frontier_count > 0，不锁死数量）
P2-2  docs：表达瓶颈=组合资源变化 → NEXT_HYPOTHESIS = bounded composition；
      COMPOSITION_AS_ACTUAL_BOTTLENECK = NOT_YET_PROVEN
```

### 14.3 v2.2 重跑实测（fail-closed 全条件通过）

```ini
F1 (actual evidence, no fallback):
  failureClass = floor-progress-blocked（来自实际运行证据，无 fallback）
  outcomeClass = goal-not-found-search-complete（来自实际运行，assert 通过）
  mt2Found = TRUE    mt2Complete = TRUE    mt3FrontierCount = 8 (OBSERVED)
  mt3Found = FALSE   mt3Complete = TRUE

F2: 3/3 REALIZED, realization-complete ×3, anchor-complete ×3,
    allMaterializationComplete = TRUE
F3: 16/16 complete, 16/16 progress comparable, comparator = 0 on all,
    0 goals, 0 positive
F4: semanticRegression = FALSE（双向，shared.found=false == independent goals 0）

Case C（含 semanticRegression gate）:
  ONE_STEP_CF_EXPRESSIVENESS = INSUFFICIENT
  CURRENT_ONE_STEP_CF_GENERATOR =
    DETERMINATELY_INSUFFICIENT_ON_THIS_CONTROLLED_REAL_FAILURE_FRONTIER
```

### 14.4 验证记录

```text
G32-v2.2 (含 P1-1/2/3 fail-closed hardening)        PASSED (Case C)
check:manifest (149 modules, 186 tests)             PASSED
check:no-tower-solver-js                             PASSED
check:core                                           PASSED
```

（按 Repair 2 指令：只跑 G32 + manifest + no-tower + core，不重跑 G24/G31/G29/G30）

### 14.5 PR-5.24j 正式关账

```ini
PR_5_24j =
APPROVED_AND_CLOSED_WITH_ONE_STEP_CF_DETERMINATELY_INSUFFICIENT_ON_CONTROLLED_REAL_FAILURE_FRONTIER

FINAL_QUALIFICATION_HARNESS = check-one-step-cf-exhaustiveness.js (v2.2, fail-closed)
CONTROLLED_SOURCE_FIXTURE = fixtures/perf/onlyup-524e-cf-source.json

HISTORICAL_5_24e_EXACT_SOURCE_IDENTITY = NOT_RECOVERABLE / NOT_REQUIRED
CONTROLLED_REPRODUCIBLE_SOURCE_AUTHORITY = ESTABLISHED
CONTROLLED_FAILURE_FRONTIER = ESTABLISHED (INLINE GATE, actual evidence)

ONE_STEP_CF_ON_CONTROLLED_REAL_FAILURE_FRONTIER = DETERMINATELY_INSUFFICIENT

INTENT_CONVERGENCE_STRUCTURE = 3 intents → 32 raw → 16 unique states
NEXT_HYPOTHESIS = bounded composition / compound interaction
COMPOSITION_AS_ACTUAL_BOTTLENECK = NOT_YET_PROVEN

BOUNDED_COMPOSITION = AUTHORIZED_AS_NEXT_CANDIDATE
COUNTERFACTUAL_ADMISSION_LINE = CLOSED (PR-5.24i)
ONE_STEP_EXPRESSIVENESS_LINE = CLOSED (PR-5.24j)
```
