# PR-5.20b — Static Core Scale Gate + Goal Pareto Archive

```ini
MILESTONE = PR-5.20b
STATUS = VERIFIED
KIND = IMPLEMENTATION
ITERATION_STANDARD = LEAN
DATE_STARTED = 2026-08-21
BASE_COMMIT = 9cc4db9311f63bce8561a819555264d026c4c370
IMPLEMENTATION_COMMIT = 2009cfb
REVIEW_STATUS = AWAITING USER REVIEW
FINAL_COMMIT = <提交实际存在后再填>
SCALE_GATE = PASSED
ACCEPTANCE = H1 PASS / H2 PASS / H3 PASS / H4 PASS / H5 PASS / H6 PASS
SCOPE = scale gate for the static combat-economy core (24-32 interaction cases inside 50000 expansions and 256MB), dual-heap agendas, tightened mode switching, and a goal Pareto archive
```

采用 Lean Iteration Standard：无 authorization-only commit、无 Repair 子轮、未运行任何 5.19 长检查。文件范围为既有 core / checker / 微型 fixture + 新 scale fixture；`package.json` 与 `solver-manifest.json` 均未改动（无新增 JS 文件）。

## 授权与已有结论

PR-5.20a 已 `APPROVED / CLOSED`（FINAL_COMMIT=`9cc4db9`，阻塞 0 项）。已确立：限制域内 solver 能自动找路线并与无剪枝 oracle 对齐。已暴露：13 个交互即 4096 状态，RSS 随环境波动明显（787ms/114.2MB vs 复核 1.26s/230.5MB）。

因此本轮先做规模闸门，而不是直接接 H5Mota。

## 本轮范围

### 1. 合并修复 5.20a 三项非阻塞 finding

```text
finding 1  validateStaticCombatEconomyProblem 改为严格 allowlist：
           problem / start / goal 拒绝一切未知字段（unknown-problem-field:*、
           forbidden-start-field:*、forbidden-goal-field:*）；
           grid 只允许 '#' 与 '.'（grid-illegal-character:y,x:ch）；
           interaction id 必须非空且全局唯一（不再从坐标默认生成）。
           微型 fixture 的 S/M/G/P/K 装饰字符全部改为 '.'，
           地形只表达地形，交互一律由 interactions 显式列出。

finding 2  applyStaticMacroAction 自校验前置条件：interactionIndex 必须位于
           当前 frontierInteractions（不可达交互无法被消费），
           并拒绝越界/非整数 index。

finding 3  applyStaticMacroAction 与 replayStaticCombatEconomyRoute 同时核对
           index + kind + id。replay 新增 reason：malformed-step、
           unknown-interaction、step-identity-mismatch。
```

三项均新增 targeted regression（未知顶层字段、非法 grid 字符、不可达消费、伪造 kind/id）。generous H2 补上 `adaptiveGenerous.status === "SOLVED"`。

### 2. 双 agenda BinaryHeap

`O(frontier)` 线性扫描替换为两个二叉堆：`CONSERVE_HP` 与 `BREAK_BOTTLENECK`。

```text
每个合法 Pareto state 同时进入两个 heap；
pending map 是"是否仍可扩展"的唯一真相，保证每个 state 只被扩展一次；
pop 时跳过 stale entry（已扩展、已被 Pareto 淘汰、已失效）；
insertStaticParetoState 返回 evictedNodeIds，调用方据此 retire 其 agenda entry；
peakFrontier 统计 active pending state 数量，不统计含 stale entries 的 heap 长度。
```

`retiredStates` 作为新指标输出，用于确认淘汰确实生效。

### 3. 收紧 mode switch

```text
只有当前确实存在非零 blocker deficit 时，连续 4 次未改善才允许进入
BREAK_BOTTLENECK；deficit 为 [0,0] 或没有 blocker 时 stagnation 归零，
不得周期性切换。切换仍只影响 agenda，不影响动作合法性、Pareto 或状态 key。
```

效果：5.20a 的 invest 案例完整搜索从 **512 次** switch 降到 **1 次**。周期性 fallback 的行为消失。

该收紧暴露并修正了 5.20a invest fixture 的一个设计缺陷：原地图把 `armoured-wall` 放在 `toll-guard` 之后，因此囤血期间 frontier 上根本没有 blocker、deficit 恒为 `[0,0]`，收紧后不再切换、H2 失效。新地图把 `armoured-wall` 移到起始可达区域（把目标挡在身后、一开始就看得见），atk 宝石仍在 toll-guard 之后。这更贴近真实情形：**看得见的墙，打不过的墙**。

### 4. Goal Pareto archive

```text
新增 insertStaticGoalParetoState：在所有到达目标的状态之间按
[hp,atk,def,mdef] 保留非支配集合。
solveStaticCombatEconomy 返回 goalArchive，每项含 hero / nodeId / route。
route 与 finalHero 保持兼容，定义为 archive 中 HP 最大项。
RESOURCE_LIMIT 可携带未证明最优的 partial archive；
SOLVED 仅在 agenda 耗尽后才表示 archive 完整。
exhaustive oracle 同样输出 goalArchive，checker 逐向量比对两者集合相等。
```

### 5. Scale fixture

新建 `shared-solver/fixtures/static-combat-economy-scale.json`，两个确定性中型静态案例，只定义地图与数值，**不含**任何预期打怪顺序 / checkpoint / 目标拆分。

关键设计：每个 pocket 是**强制链**（第 k 项挡住第 k+1 项），所以搜索决定的是"推进哪个 pocket"而不是"取哪个子集" —— 这是 24–32 个交互能落在真实预算内的原因。def/mdef 收益使消费顺序改变后续 guard 的代价，因此同一 consumed set 经不同顺序到达时 hp 确实不同，真实 dominance 会发生。

## Verified Result

```text
STATIC CORE SCALE GATE — PASSED
H1 PASS / H2 PASS / H3 PASS / H4 PASS / H5 PASS / H6 PASS
```

### H6 — Scale Gate（本轮的决定性闸门）

```text
案例                        交互  并列  status   finalHp  archive  route  expanded/50000  dominated  retired  peak/10000  switches
scale-six-pocket-ladder      25    6    SOLVED      393        1     25     18286          66221     1071      1533         2
scale-two-phase-mdef-gate    26    6    SOLVED      186        1     26     13031          48743      179       337         0
```

两案例均 `SOLVED`（非 `RESOURCE_LIMIT`）、均未用尽预算、peakFrontier 均远低于 10000、dominance 均真实发生、所有 goal archive route 逐条 strict replay 通过。四种属性资源（hp/atk/def/mdef）在两个案例中都出现，且 checker 断言 ≥6 个并列合法动作（案例 A 在第 0 步即有 6 个；案例 B 在第 10 步出现 6 个）。

### H1 — 固定案例 vs 无剪枝 oracle

5 个案例接受 oracle 对照，`found`、最优 final HP 与 **goal archive 集合**全部一致：

```text
案例                          status       finalHp  oracleHp  archive  expanded  dominated  peak
optional-potion-before-gate   SOLVED           60        60        1         4          1     2
atk-gem-unblocks-guard        SOLVED           86        86        1         3          0     1
detour-for-def-beats-direct   SOLVED           64        64        1         6          2     3
goal-tradeoff-archive         SOLVED          150       150        2         6          1     3
permanently-attack-blocked    UNSOLVABLE     null      null        0         2          0     1
invest-before-conserve        SOLVED（无 oracle 对照，交互 13 个超出 oracle 实用范围）
```

### H2 — invest-before-conserve（同预算 32，只切调度）

```text
conserveOnly : RESOURCE_LIMIT  foundGoal=false  switches=0
adaptive     : RESOURCE_LIMIT  foundGoal=true   switches=1
```

充裕预算下两模式均 `SOLVED`，最优 finalHp 与 goal archive 集合完全相同 —— 调度不改变可证明的结果集。

### H3 — 32 个固定 seed vs oracle

```text
seeds=32  checked=32  skipped=0  solvable=27  unsolvable=5  maxInteractions=7
oracleAgreement = found-and-optimal-final-hp-identical
```

### H5 — Goal trade-off archive

```text
案例 goal-tradeoff-archive：archive = 2
  hp=150 atk=10 def=0 mdef=0   route: big-potion
  hp= 90 atk=30 def=0 mdef=0   route: big-potion -> side-guard -> atk-gem
```

两者互不支配（150>90 但 10<30）。oracle 的 archive 逐向量相同。checker 另断言 archive 内任意两项互不支配，并逐条 strict replay。

### H4 — 预算

```text
wallMs         = 2146    （限 10000）
peakRssMb      = 155.0   （限 256）
compact stdout = 3,486 bytes （限 5,120）
singleProcess = true   workerPool = false
replayedRoutes = 36
```

RSS 已知随环境波动（5.20a 复核方观测 230.5MB）。本轮只冻结上限与语义结果，**不**冻结 expanded / switches / RSS 的精确值。

## 冻结的验收语义（只锁上限与语义，不锁具体数值）

```text
5.20a 三个 oracle 正例 + 一个反例   found 与最优 HP 一致
32 个微型 seed                      与 exhaustive oracle 一致
H2                                  conserve-only 未找到、adaptive 找到、预算相同
goal trade-off archive              ≥2 个互不支配结果
两个 24–32 interaction scale case   均 SOLVED、expanded<50000、peakFrontier<=10000
整个 checker                        单进程、无 worker、wall<10s、RSS<256MB、stdout<5KB
```

## 验证记录

```text
npm run check:static-combat-economy-core   PASS（wall 2146ms，RSS 155MB，stdout 3,486B）
node --check shared-solver/lib/static-combat-economy-core.js       PASS
node --check shared-solver/check-static-combat-economy-core.js     PASS
check:manifest                             PASS（145 modules，153 graded tests）
check:no-tower-solver-js                   PASS
git diff --check                           clean
```

未运行 5.19v / 5.19w / 5.19x 的长 frozen suite（授权范围内）。`solver-manifest.json` 未运行 refresh、未修改：本轮无新增 JS 文件，scale fixture 是 JSON。因此 manifest 中该 module/test 的 notes 仍是 5.20a 的 H1–H4 描述，未包含 H5/H6 —— 属于已知的描述滞后，需要单独授权才改。

## 规模现状（如实记录，供 5.20c 判断）

```text
全并列形态在 50000 expansions 下的实测上限约为 6 个 pocket × 4 项（25 个交互，
18286 expansions）。再加一个 pocket（7×4=28 个交互）会使 consumed-set 组合
从 5^6 升到 5^7，超出本轮预算 —— 案例 B 因此改用两阶段形态（mid gate 先只
开放三个 pocket），以 26 个交互取得同等规模而 expanded 更低。

结论：闸门通过，但"全并列 + 24 个交互"确实接近当前算法在 256MB/10s 下的边界。
状态数随同时可选交互数指数增长这一根本问题没有被消除，只是被强制链结构绕开。
```

## 下一方向

Scale gate `PASSED`，因此下一轮固定为 **PR-5.20c Native H5Mota Static Adapter**。本轮未实现，未创建该文档。

## Review State

Implementation 与 H1–H6 verification 为 `VERIFIED`，待复核。`FINAL_COMMIT` 留空，不自行 `APPROVED / CLOSED`。
