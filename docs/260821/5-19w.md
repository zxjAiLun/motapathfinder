# PR-5.19w — Battle-Stage-Conditional Root Retry Comparability Attribution

```ini
MILESTONE = PR-5.19w
STATUS = VERIFIED
DATE_STARTED = 2026-08-21
BASE_COMMIT = fafb1c0
IMPLEMENTATION_COMMIT = 4a9b012
REVIEW_STATUS = AWAITING USER REVIEW
FINAL_COMMIT = <提交实际存在后再填>
RESEARCH_VERDICT = TRACE-LOCAL-STAGE-CONDITIONAL-NONIMPROVING-RETRY-OBSERVED
SCOPE = OBSERVATION_ONLY attribution of whether the damage/survivalMargin absence found in PR-5.19v is unexpected missing evidence or a battle-stage-determined NOT-APPLICABLE, and whether stage-conditioned metrics expose a charge-time identifiable non-improving retry among root calls #1-#4
```

## 授权与已有证据

PR-5.19v 已 `APPROVED / CLOSED`（FINAL_COMMIT=`fafb1c0`，P0=0/P1=0/P2=0）。冻结证据：

```text
schema  = motapathfinder.strategic-root-retry-novelty-attribution.v2
verdict = EVIDENCE-INCOMPLETE
incompleteCallOrdinals = [2, 4]，其余三组 audit ordinals 为空
repeatGroupCount = 1（e9c03049d436f6f2|MT5|evilHero|9|10 → callOrdinals [1,2,4]）
#2/#4 pairwise missingFields = [survivalMargin, damage]
#4 improvedFields = [attackMargin, sourceTerminalProgressScore]
```

5.19v 的正式结论是：统一要求 damage/survivalMargin 的 retry 比较无法在 attack-blocked → damageable 边界上完成，因为 **attack-blocked 状态下 damage 本来就没有定义**（`strategic-battle-viability.js`：`damage == null` 且 `attackMargin < 0` 才判 attack-blocked，`survivalMargin = heroHp - damage` 因此同时为 null）。这不是遥测漏采。

## 本轮研究问题

```text
Q1  5.19v 的 damage/survivalMargin 缺失，究竟是 unexpected missing evidence，
    还是由 battle stage 决定的 NOT-APPLICABLE？
Q2  改用 stage-conditioned metrics 之后，#1/#2/#4 是否出现可在 charge 时
    识别的 non-improving retry？
```

只生成 observation。**不能**由此证明 suppression 安全。

## 不可触碰的前置合同

`shared-solver/lib/strategic-battle-viability.js` 的现有语义不得修改。该模块已明确规定 attack-blocked 时 damage/survivalMargin 为 `null`。本轮禁止：

- 假想提高 ATK 后重新计算伤害；
- 把 `null` 转成 `0`、`Infinity` 或任何估算值；
- 修改 hero state 做 counterfactual probe；
- 重新调用 battle resolver（只复用既有 root charge records）。

## 本轮范围（observation-only）

- `shared-solver/lib/strategic-d2-search.js` 新增 opt-in `enableRootRetryMetricApplicabilityAttribution` 与纯 helper `classifyStageConditionalRootRetryComparability(rootCalls)`。
- 复用既有 root charge records；不修改 5.19v 的 schema v2，也不修改其 frozen classifier。
- `rootAttemptSeparabilityCalls` 的采集条件扩展为 separability / retry novelty / metric applicability 三者任一启用。
- `beforeStage` 从 context 字段移出，成为正式 stage axis；`sourceDepth`/`sourceFloor`/`stageGoal`/`compiledCandidateRank`/`compiledCandidateCount` 继续只作 context。

### Stage 合同

```text
supported stages 有序：attack-blocked < lethal < viable
unsupported 不参与有序比较 → INCOMPARABLE-UNSUPPORTED

attack-blocked 同阶段：只要求 attackMargin
lethal/viable 同阶段：要求 attackMargin、damage、survivalMargin
通用指标（所有可比较阶段）：sourceTerminalProgressScore、reachableAtCompileTime

damage/survivalMargin 在任一 side 为 attack-blocked 时记录为 notApplicableMetrics，
不得进入 missingFields / unexpectedMissingMetrics
```

### 每 pair 输出字段

```text
earlierStage / currentStage / stageRelation
applicableMetrics / notApplicableMetrics / unexpectedMissingMetrics
improvedFields / regressedFields / equalFields
contextDifferenceKeys / exactObservableVectorEqual
```

### Pairwise classification（固定 9 类）

```text
EXACT-OBSERVABLE-RETRY
STAGE-PROGRESS
STAGE-REGRESSION
SAME-STAGE-PRIOR-DOMINATES
SAME-STAGE-CURRENT-IMPROVES
SAME-STAGE-METRIC-TIE-CONTEXT-ONLY
SAME-STAGE-MIXED-TRADEOFF
INCOMPARABLE-UNSUPPORTED
UNEXPECTED-MISSING-APPLICABLE-METRIC
```

判定顺序：exact 优先 → 任一 side 为 unsupported 则 incomparable → 跨 supported stage 先按 `stageRelation` 分类（progress / regression）→ 同阶段才比较该阶段的 applicable metrics（applicable 但缺失即 `UNEXPECTED-MISSING-APPLICABLE-METRIC`）。

### Retry-level 聚合

保留 pairwise provenance 与 `decisiveEarlierCallOrdinals`。语义分组：

```text
pre-charge non-improving witness:
  EXACT-OBSERVABLE-RETRY
  STAGE-REGRESSION
  SAME-STAGE-PRIOR-DOMINATES
  SAME-STAGE-METRIC-TIE-CONTEXT-ONLY

不是 suppression candidate:
  STAGE-PROGRESS
  SAME-STAGE-CURRENT-IMPROVES

必须 fail-closed:
  UNEXPECTED-MISSING-APPLICABLE-METRIC
  INCOMPARABLE-UNSUPPORTED
```

retry-level 优先级固定（延续 5.19v 的"报告决定性 witness 种类"两层设计）：

```text
EXACT-OBSERVABLE-RETRY > SAME-STAGE-PRIOR-DOMINATES > STAGE-REGRESSION >
SAME-STAGE-METRIC-TIE-CONTEXT-ONLY > UNEXPECTED-MISSING-APPLICABLE-METRIC >
INCOMPARABLE-UNSUPPORTED > SAME-STAGE-MIXED-TRADEOFF > STAGE-PROGRESS >
SAME-STAGE-CURRENT-IMPROVES
```

### Search-end 输出与 offline verdict

`stats.rootRetryMetricApplicabilityAttribution`，schema `motapathfinder.strategic-root-retry-metric-applicability-attribution.v1`。

- `preChargeComparisons` 严禁 post-hoc label（productive/directSatisfied/satisfied/materialized/continuation）。
- `postSearchEvaluation` 独立连接 productive/directSatisfied；每条 non-improving comparison 必须链接到唯一且 `productive` 为 boolean 的条目，否则进 `missingEvaluationCallOrdinals` 并 fail-closed。
- offline verdict 固定四类，优先级如下：

```text
PRODUCTIVE-ROOT-WOULD-BE-FLAGGED
  > STAGE-CONDITIONAL-EVIDENCE-INCOMPLETE
  > TRACE-LOCAL-STAGE-CONDITIONAL-NONIMPROVING-RETRY-OBSERVED
  > NO-STAGE-CONDITIONAL-NONIMPROVING-RETRY
```

- 四组 ordinals 在**每个** verdict 分支都反映真实检测：`productiveFlaggedCallOrdinals`、`nonProductiveFlaggedCallOrdinals`、`incompleteCallOrdinals`、`missingEvaluationCallOrdinals`。

## Output contract

```text
compactRootRetryMetricApplicabilityAttribution:
  schema（...root-retry-metric-applicability-attribution.v1）
  rootCallCount / repeatGroupCount
  精简 pairwise applicability（stage、stageRelation、applicable/notApplicable/
    unexpectedMissing、improved/regressed、classification、decisive ordinals）
  postSearchEvaluation
  productive/nonProductive/incomplete/missingEvaluation 四组 ordinals
  verdict
  JSON.stringify(compact).length < 20000
```

禁止输出 raw `rootCompileEvents` / candidate pools。顺手把既有 `compactRootRetryNoveltyAttribution` 块按两空格规范整理缩进——**只做格式调整**，不改其 schema v2 或任何值。

## Checker 与运行预算

- 新增 flag `--root-retry-metric-applicability`；新增 package script `check:strategic-root-retry-metric-applicability`，启用 qualification-1000、residual recovery、second residual、hierarchy attribution/chronology 和本轮 flag，**不**同时启用 `--root-attempt-separability` / `--root-retry-novelty`（避免触发其额外 isolation runs）。
- 新增专用 isolation runner：candidate 与 control 的搜索 / second-residual / hierarchy 配置完全相同，只切 metric applicability flag；复用 generic candidate，使本轮 checker 控制在 **3 次 1000-work run**。

### Synthetic gates（必须覆盖）

```text
attack-blocked 同阶段 damage/survival 为 NOT-APPLICABLE 而非 missing
attack-blocked → lethal/viable  = STAGE-PROGRESS
lethal → attack-blocked         = STAGE-REGRESSION
lethal 同阶段 dominance / improvement / mixed / tie
damageable 阶段真正缺 damage    = UNEXPECTED-MISSING-APPLICABLE-METRIC
unsupported                     = INCOMPARABLE-UNSUPPORTED
productive false-positive verdict 优先
missing / duplicate / non-boolean post evaluation → fail-closed
```

## 假设检查（不预设必然为正结果）

真实 run 后再冻结结果。必须明确检查但**不得**修改合同迎合：

```text
H1  #2 是否从 EVIDENCE-INCOMPLETE 变成 SAME-STAGE-METRIC-TIE-CONTEXT-ONLY
H2  #4 是否因 attack-blocked → damageable 变成 STAGE-PROGRESS
H3  #5 是否仍为 FIRST-SEEN
```

若结果不同，报告真实 stage / pairwise applicability，不改合同。

## 不在本轮 / 未授权

- 不修改 `selectFeedbackAwareDependencyAttempts`、`dependencyAttemptDedupe`、lazyWork/agenda、`hierarchyPriority`、reservation、`connectorMaxCalls`/`maxOutstanding`/`lazyDrainEvery`。
- 不实现 skip/suppress/reorder；不做 fixed-8、8→9、O5、reroot、第 9 次 search。
- D3 = NOT AUTHORIZED。
- 不修改 `strategic-battle-viability.js` 现有语义。

## 实施顺序

1. 实现提交只含 `shared-solver/lib/strategic-d2-search.js`、checker、`shared-solver/package.json`（例如 `Add stage-conditional root retry comparability attribution`）。
2. 串行验证：`node --check` 两个修改文件；`check:manifest`；`check:strategic-battle-viability`；`check:strategic-root-retry-metric-applicability`；`check:strategic-root-retry-novelty`；`check:strategic-root-attempt-separability`；`check:strategic-hierarchy-call-chronology`；`check:strategic-second-residual-recovery`；`check:no-tower-solver-js`；`git diff --check`。
3. 真实 frozen evidence 追加到本文件并更新 handoff，形成 docs commit（例如 `docs: record PR-5.19w stage-conditional retry evidence`）。
4. 不自行标记 `APPROVED / CLOSED`，等待 review。

## 下一授权 gate

stage-conditional 证据仍只作未来 shadow counterfactual 的输入。任何真实 skip/suppress/reorder 或 allocation 改动必须 review 后另行授权。

## 迭代日志

### Iteration 1 — 2026-08-21 — observation implementation

- Change: `strategic-d2-search.js` 增加 opt-in `enableRootRetryMetricApplicabilityAttribution`，新增并导出两个纯 helper `classifyStageConditionalRootRetryComparability(rootCalls)` 与 `classifyStageConditionalRootRetryOfflineVerdict(preChargeComparisons, postSearchEvaluation)`。复用既有 root charge records 与既有 `beforeStage`；未重新调用 battle resolver，未修改 `strategic-battle-viability.js`，未修改 `buildRootAttemptSemanticVector`，未修改 5.19v 的 v2 schema 或其 classifier。
- `rootAttemptSeparabilityCalls` 采集条件扩展为 separability / retry novelty / metric applicability 三者任一启用。
- 既有 post-search label 计算（materialization/directSatisfied 派生）抽为局部 `buildLabeledRootCalls()`，由 5.19v 与 5.19w 两个 search-end 块共用。这是**纯代码搬移**：5.19v frozen 结果在验证中逐字节不变（compact 仍 3,357 字符，verdict 仍 `EVIDENCE-INCOMPLETE`，incomplete 仍 `[2,4]`）。
- Checker：`--root-retry-metric-applicability` + 专用 isolation runner（candidate 复用 generic candidate，control 只切本轮 flag，其余搜索/second-residual/hierarchy 配置逐项对齐 → 本轮 checker 共 3 次 1000-work run）。package script `check:strategic-root-retry-metric-applicability`（不启用 `--root-attempt-separability` / `--root-retry-novelty`）。
- stdout 只输出 `compactRootRetryMetricApplicabilityAttribution`；顺手把既有 `compactRootRetryNoveltyAttribution` 块按两空格规范整理缩进（纯格式，schema v2 与所有值未变）。
- Outcome: implementation commit `4a9b012`。

### Iteration 2 — 2026-08-21 — real observation

- Command:

```text
NODE_OPTIONS=--max-old-space-size=4096 npm run check:strategic-root-retry-metric-applicability --prefix shared-solver
```

- Frozen result（stdout 1,216 行 / 44,811 bytes；compact 3,754 字符）：

```text
schema           = motapathfinder.strategic-root-retry-metric-applicability-attribution.v1
rootCallCount    = 5
repeatGroupCount = 1
repeat group     = e9c03049d436f6f2|MT5|evilHero|9|10
                   callOrdinals [1, 2, 4]
                   stages       [attack-blocked, attack-blocked, lethal]

真实 stage（按输出顺序）:
  #1 attack-blocked   #2 attack-blocked   #4 lethal   #3 attack-blocked   #5 lethal

comparisons（输出顺序）:
  #1 [attack-blocked] FIRST-SEEN
  #2 [attack-blocked] SAME-STAGE-METRIC-TIE-CONTEXT-ONLY  decisive=[1]
       vs #1  attack-blocked → attack-blocked (same)
         applicableMetrics        = [attackMargin, sourceTerminalProgressScore,
                                     reachableAtCompileTime]
         notApplicableMetrics     = [damage, survivalMargin]
         unexpectedMissingMetrics = []
         improved=[]  regressed=[]  contextDifferenceKeys=[sourceDepth]
         exactObservableVectorEqual=false
  #4 [lethal]         STAGE-PROGRESS  decisive=[1, 2]
       vs #1 与 vs #2 均 attack-blocked → lethal (progress)
         applicableMetrics        = [attackMargin, sourceTerminalProgressScore,
                                     reachableAtCompileTime]
         notApplicableMetrics     = [damage, survivalMargin]
         unexpectedMissingMetrics = []
         improved=[attackMargin, sourceTerminalProgressScore]  regressed=[]
         contextDifferenceKeys=[sourceDepth]
  #3 [attack-blocked] FIRST-SEEN
  #5 [lethal]         FIRST-SEEN

postSearchEvaluation            = #1-#4 productive=false，#5 productive=true

verdict                         = TRACE-LOCAL-STAGE-CONDITIONAL-NONIMPROVING-RETRY-OBSERVED
productiveFlaggedCallOrdinals   = []
nonProductiveFlaggedCallOrdinals= [2]
incompleteCallOrdinals          = []
missingEvaluationCallOrdinals   = []

整条真实 trace 的 unexpectedMissingMetrics 全为空。
```

### 假设检查结果

```text
H1  CONFIRMED  #2：EVIDENCE-INCOMPLETE → SAME-STAGE-METRIC-TIE-CONTEXT-ONLY
H2  CONFIRMED  #4：attack-blocked → lethal，判为 STAGE-PROGRESS
H3  CONFIRMED  #5 仍为 FIRST-SEEN（其 stage 为 lethal，但属另一组）
```

三个假设都成立，合同未为迎合假设做任何修改。

## Verified Result

```text
STAGE-CONDITIONAL ROOT RETRY COMPARABILITY ATTRIBUTED

Q1 已回答：5.19v 的 damage/survivalMargin 缺失是 battle stage 决定的
   NOT-APPLICABLE，不是 unexpected missing evidence。
   真实 trace 中 unexpectedMissingMetrics 处处为空；#1/#2 处于 attack-blocked，
   该阶段 damage 本无定义，因此这两个指标从未进入 applicable 集合。

Q2 已回答：改用 stage-conditioned metrics 后，#2 成为可在 charge 时识别的
   non-improving retry —— 与 #1 同 prerequisite、同 boundary、同 stage，
   该阶段全部 applicable metrics（attackMargin / sourceTerminalProgressScore /
   reachableAtCompileTime）完全相等，唯一差别是 context 的 sourceDepth。
   #2 事后为 non-productive，linkage 完整 → nonProductiveFlagged=[2]。

   #4 不是 suppression candidate：它跨过 attack-blocked → lethal，
   阶段进展本身即真实进展（且 attackMargin 与 sourceTerminalProgressScore 同时改善）。

   productive root #5 在其组内仍 FIRST-SEEN，不会被任何 pre-charge 分类标记。

→ verdict = TRACE-LOCAL-STAGE-CONDITIONAL-NONIMPROVING-RETRY-OBSERVED
  仅说明当前 trace 存在唯一一个 shadow-counterfactual 候选（#2）；
  不证明 suppression 安全，不授权任何 skip/suppress/reorder。
```

这是 observation-only 结果。5.19v 的 `EVIDENCE-INCOMPLETE` 与本轮的 trace-local 正结果并不冲突：前者是"统一指标要求"下的诚实负结果，后者说明该负结果的成因是 metric applicability，而不是证据缺失。

## 验证记录

```text
node --check shared-solver/lib/strategic-d2-search.js                     PASS
node --check shared-solver/check-strategic-survival-opportunity-prerequisite.js  PASS
check:manifest                                    PASS（144 modules，152 graded tests）
check:strategic-battle-viability                  PASS
check:strategic-root-retry-metric-applicability   PASS（1,216 行 / 44,811 bytes）
check:strategic-root-retry-novelty                PASS（5.19v frozen 逐字节未变，compact 3,357）
check:strategic-root-attempt-separability          PASS（1,312 行 / 48,308 bytes）
check:strategic-hierarchy-call-chronology          PASS
check:strategic-second-residual-recovery           PASS
check:no-tower-solver-js                           PASS
git diff --check                                   clean
```

Isolation：candidate 与 control 的 outcome（去 `wallMs`）、`bestTerminalBlocker`、`totalSearchExpansions`/`expansions`/`battleAccessPrerequisiteCalls`/`rootLevelCalls`/`continuationDerivedCalls`、charged witness attemptIds、residual/materialization 结果全部相同；control 的 `stats.rootRetryMetricApplicabilityAttribution` 为 `null`。

## Review State

Implementation and observation are `VERIFIED`; PR-5.19w is pending review. D2 remains `OPEN`, D3 remains `NOT AUTHORIZED`。suppression 仍未授权。
