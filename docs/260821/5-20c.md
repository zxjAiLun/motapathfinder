# PR-5.20c — Native H5Mota Static Adapter

```ini
MILESTONE = PR-5.20c
STATUS = VERIFIED
KIND = IMPLEMENTATION
ITERATION_STANDARD = LEAN
DATE_STARTED = 2026-08-22
BASE_COMMIT = 9602d69e6600055894c7c2429198800547242c20
IMPLEMENTATION_COMMIT = 6e3b306
REVIEW_STATUS = AWAITING USER REVIEW
FINAL_COMMIT = <提交实际存在后再填>
ADAPTER_GATE = PASSED
ACCEPTANCE = A1 PASS / A2 PASS / A3 PASS / A4 PASS
SCOPE = map ONE event-free native H5 floor into the static combat-economy core problem, with cell-level provenance and fail-closed rejection of everything outside the subdomain
```

采用 Lean 两段提交：第一段 implementation，第二段 docs/handoff，无 authorization-only commit，无 observation/repair 小轮。基线 `dev / HEAD=origin/dev=9602d69 / clean`。

## 这一轮证明什么、不证明什么

**证明**：一个语义已经落在静态子域内的原生 H5 楼层，可以被**机械地、逐格可审计地**映射成 `static-combat-economy-core.js` 的受限问题，并由 core 自动求出路线。

**不证明**：与任意 H5 `functions.js` 的语义等价。因此 manifest 中 adapter 记为 `experimental` 且 `correctnessSource=false`。这一点是本轮的边界，不是遗留的待办。

## 严格的数据边界

新增 `shared-solver/lib/static-combat-economy-h5-adapter.js`，只做数据映射：

```text
只接收已在内存中的 H5-shaped project object
不读文件、不 require project-loader、不 loadFunctionsRuntime
不调用 battle-resolver / simulator / event-resolver
不 eval、不用正则去猜某个 effect 字符串在做什么
```

三个导出：

```text
inspectH5StaticFloorEligibility(project)  只读探针，一次列出全部不支持原因
adaptH5StaticFloor(project)               成功返回 problem + provenance；否则 problem=null
mapStaticRouteToH5Steps(adaptation, route) 把 core 路线翻回 H5 格子步骤
```

`floorId` / `hero` / `start` 全部自动取自 `project.data.firstData.floorId` / `hero` / `hero.loc`；`goal` 自动取该层**唯一**的 `changeFloor` 坐标。不接受人工 checkpoint、目标拆分、路线或怪物顺序提示——checker 直接对 fixture 文本断言不存在 `route` / `checkpoint` / `order` / `sequence` 等键。

## 分类判据：`tile.trigger` 而不是猜测

```text
无 trigger + canPass === true    → 可通行地形
无 trigger + canPass === false   → 墙
trigger "battle"   + enemys 类   → monster（须为 plain）
trigger "getItem"  + items 类    → resource（须在 canonical allowlist 内）
trigger "changeFloor"            → 仅当它就是唯一登记出口时作为终止 goal
其它任何 trigger                 → 整层 fail-closed
canPass 既非 true 也非 false     → 整层 fail-closed（未知 tile 绝不当空地或墙）
```

interaction id 为 `floorId:x,y:tileId`，按构造唯一（每格至多一个 interaction），checker 另行断言唯一性与该构造式本身。

### 资源：精确 canonical string contract

只接受白名单内的标准宝石/药水，且 `itemEffect` 必须**逐字符**等于两种 canonical 形式之一：

```text
core.status.hero.<attr> += core.values.<key>
core.status.hero.<attr> += core.values.<key> * core.status.thisMap.ratio

redJewel→atk  blueJewel→def  greenJewel→mdef
redPotion / bluePotion / yellowPotion / greenPotion → hp
```

gain 只由 `project.values[key]` 与 `floor.ratio` 构造，必须是正安全整数。带 `useItemEffect` / `canUseItemEffect` / `equip` 的物品即使 id 在白名单内也拒绝——同一个 id 下的可用/可装备物品是另一套机制。

### 普通怪物

`hp/atk/def` 必须是安全整数（hp 为正），`special` 缺失/0/空数组，`money`/`exp`/`point` 为 0 或缺失，并对 19 个特殊能力字段（`firstAttack`、`vampire`、`counter`、`zoneSquare` …）逐一查空。

## Fail-closed：整层拒绝，绝不改写成障碍

任一不支持内容让**整个 floor** 返回 `eligible=false` / `problem=null`，并给出按 `floorId → y → x → reason` 稳定排序、每条带 `floorId/x/y/reason` 的列表。**不会**把出问题的格子改成 obstacle 后继续求解——那等于悄悄回答了另一个问题。

`changeFloor` 格是**终止** goal：本轮不执行下一层，`goalArchive` 也不得被当作跨层 continuation archive。地图上出现第二个未登记的楼梯 tile 会被拒绝（`unregistered-change-floor-tile`），不会被当成普通地形。

## Fixture

新增 `shared-solver/fixtures/static-combat-economy-h5-native.json`，使用原生 H5 字段名：`data.firstData`、`values`、`mapTilesByNumber`、`itemsById`、`enemysById`、`floorsById`。

```text
单层 MT1，15x9，event-free，唯一出口 (13,1)，hero 起点 (1,1)
22 个 interactions = 11 plain monsters + 11 标准宝石/药水
resource 属性覆盖 atk / def / mdef / hp 四种；floor.ratio = 2（宝石乘 ratio，药水不乘）
```

`expectedStaticProblem` 由**同一份 layout spec 独立推导**（不是 adapter 输出的转储），所以 A1 的 `deepStrictEqual` 是真正的交叉校验：adapter 的分类或 gain 数学一错就会失败。文件内无任何路线。

## 验收 A1–A4

新增 `shared-solver/check-static-combat-economy-h5-adapter.js`（`unit-plus-micro`），单进程、无 worker、无磁盘缓存、不加载真实塔。

```text
A1  原生 fixture 自动适配成功，problem 与 expectedStaticProblem 完全相同
    22 interactions；provenance 逐条比对 floor.map[y][x] 与 mapTilesByNumber 的真实 tile id
    每个 resource gain 必须等于 values[key] 或 values[key] * ratio

A2  solver 自行产出路线与 goal archive，archive 每条都通过 strict replay，
    mapStaticRouteToH5Steps 的每一步 坐标/tileNumber/tileId/id/kind 与 provenance 一致
    伪造 id 与越界 index 都必须映射失败

A3  41 个 mutation 覆盖每一种不支持语义，全部 eligible=false / problem=null / 无 route
    并断言 unsupported 列表稳定排序、每条带 floorId/x/y/reason

A4  单进程、无 worker/磁盘缓存，wall 256ms（限 10s）、峰值 RSS 96.9MB（限 256MB）、
    compact stdout 1,913B（限 5KB）
```

### A2 冻结结果

```text
status = SOLVED    finalHp = 1056    routeLength = 15
goalArchive = 3    非支配终局向量 hp/atk/def/mdef:
                     1056/40/16/18
                      968/40/22/18
                      776/40/22/28
expanded = 7757    dominated = 25417    peakFrontier = 1458    agendaRebuilds = 1
mappedH5Steps = 51（三条 archive 路线映射步数合计）
```

### A3 的 41 个 mutation

```text
floor 脚本钩子   events / autoEvent / firstArrive / eachArrive / parallelDo /
                 beforeBattle / afterBattle / afterGetItem
机制外内容       door / key / shop / equipment / skill
经济             hero-money / hero-exp / hero-level / hero-items / hero-equipment /
                 enemy-money / enemy-exp / enemy-point
特殊怪           special-monster-number / special-monster-array / special-monster-field
物品效果         custom-item-effect / item-effect-with-extra-expression / unknown-item-value
tile             tool-sensitive-tile / tile-script / unknown-tile-passability
出口             multiple-exits / missing-exit / cross-floor-continuation / exit-tile-mismatch
未知数据         unknown-map-number / unknown-item / unknown-enemy
结构             non-rectangular-map / missing-floor / missing-hero / interaction-on-start
```

全部 41 个 fail-closed。若其中任何一个被适配成可解问题，即为 `FAILED_ADAPTER_GATE`，不得通过增加审计字段掩盖。本轮无此情况。

## 核心模块与 manifest

`static-combat-economy-core.js` 仅做一处**注释**清理：bitset 编码措辞改为"对固定长度 bitset 单射"（同一张图上所有 state 共享格数）。key、Pareto、mode policy、fixture、预算、规模均未动，`git diff` 确认无非注释行变更。

`package.json` 只新增 `check:static-combat-economy-h5-adapter`。`solver-manifest.json` 手工增加 adapter 与 checker 两项（adapter `experimental` / `correctnessSource=false`），并把既有 static core module 与 checker 的 notes 从 5.20a 更新到 5.20b（H1–H6、goal archive、ticket agenda、scale gate）。未运行 `manifest:refresh`，无全表 churn。计数 145/153 → **146 modules / 154 graded tests**。

未修改 `strategic-d2-search.js`、5.19 checker、既有 DP、`project-loader`、battle/event runtime 与 worker pool。

## 验证记录

```text
node --check shared-solver/lib/static-combat-economy-h5-adapter.js     PASS
node --check shared-solver/check-static-combat-economy-h5-adapter.js   PASS
check:static-combat-economy-h5-adapter   PASS（A1–A4，41 mutations，wall 256ms，RSS 96.9MB，stdout 1,913B）
check:static-combat-economy-core         PASS（H1–H6，H6 SOLVED/SOLVED，wall 1509ms，RSS 116.0MB）
check:manifest                           PASS（146 modules，154 graded tests）
check:no-tower-solver-js                 PASS
git diff --check                         clean
```

未运行 5.19 长检查，未运行 `manifest:refresh`。

## Review State

等待复核。`FINAL_COMMIT` 留空，未经授权不得标记 `APPROVED/CLOSED`。

需要复核者留意的两点：

1. **adapter 的价值边界。** 它只覆盖 event-free、单层、plain-combat 的静态子域。真实塔的绝大多数楼层都不在这个子域内，会被整层拒绝。这是设计意图，但也意味着本轮**没有**推进任意 H5 楼层的可解性。
2. **5.20b 的余量缺口仍然成立。** 复核实测 235.5–240.6MB 高于 230MB 目标线；本轮 adapter fixture 规模小（22 interactions、96.9MB），并没有重新测这条线。任何进一步扩大静态规模的轮次都必须重测，不能引用本轮数值。
