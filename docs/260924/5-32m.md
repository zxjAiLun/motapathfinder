# PR-5.32m — Explicit Boss-removal goal distance and corrected-seed bounded search

```ini
MILESTONE = PR-5.32m
STATUS = NOT_PROMOTED
DATE_STARTED = 2026-09-24
BASE_COMMIT = 7610f42400123a8780558a8400c0ff23a9e0e739
SCOPE = Fix projection of an existing removal goal into goal-relative distance, then test one corrected TS15 seed within32k; no scheduler tuning or production resume.
```

## Owner request and starting evidence

Owner requests repair and an explanation of why Boss remains uncompleted, then says “继续”. Starting `dev == origin/dev ==7610f42`, clean. [5.32l](5-32l.md) established a real browser74/74 route to TS15 HP36397/ATK34/DEF1/LV3/EXP41, lifetime greenKey spent1. [5.32k](5-32k.md) old Boss run used subsequently invalidated inflated seeds and was stopped; it is not a no-route result.

Inspection finds a separate contract/projection bug: configured Boss goal is `{floorId:"JC19",removed:{floorId:"TS15",x:6,y:11}}`, but `estimateGoalRelativeDistance` ignores `removed` and only seeks a `:next` stair. TS15 only has a `:before` stair; Boss afterBattle event itself returns toJC19. Thus TS15 distance=Infinity while lower floors have finite next-stair distances. Old stopped-run observation:52/16000 expansions onTS15,13421 onTS11. That documents pathological allocation, not a valid corrected-code counterfactual.

Boss soldier is HP3444/ATK111/DEF44/special33“牵制”: per-round damage scaled by monsterDEF/heroDEF. ATK34 cannot penetrate; resources and level-ups remain. Do not infer impossibility from entry stats or inject an authored resource order into search.

## Frozen repair and gates

1. Pending, valid `stageGoal.removed` is an explicit goal anchor. On that floor, use Manhattan distance to its configured coordinate; on other floors project toward its floor using existing transit logic. Once removed, fall back to the remaining floor goal. Preserve all behavior for goals without a removal anchor. No hardcoded TS/JC names or Boss IDs.
2. Regression: terminal event floor different from target floor; target floor with no next stair; pending/removed state; invalid/out-of-bounds anchor; normal floor-only behavior unchanged. Demonstrate the broken old rule with a negative control. Existing goal-priority/agenda/core/search-identity checks remain passing.
3. Freeze source commit and fresh immutable bundle, same tower manifest, original initial, only the corrected/revalidated TS15 state and its existing prefix. No teacher/witness or manually specified resource goals. Single arm: total32k expansions /900000ms wall /6144MiB heap /8192MiB RSS; unchanged Fair4/FIFO/K32/inherit,16 skyline candidates,stopOnFirstGoal=false. This is a goal-contract repair, not a restart of the stopped scheduler/K/weight experiments.
4. Save full goal states before reconstruction. Full original-initial exact/strict replay, all campaign-floor snapshots, monotone spend counter≤1; representative highest configured score gets real browser replay with independent gross-consumption observation. Goal-found, search-complete and actual Boss closure remain distinct.
5. If budget misses or replay fails: retain diagnostics and bounded evidence, no automatic larger run. Check failureClass/actionTrimmed/frontier/budget/provider errors and actual expanded-floor distribution. Production stays paused; new round is NOT_PROMOTED and not owner acceptance of prior milestones.

## Publication

Generated scripts/bundles/logs/routes: ignored `shared-solver/routes/generated/260924-pr532m/` and cloud probes. Track code/tests, this milestone, concise result and handoff; do not alter tower runtime. Follow [documentation system](../project-documentation-system.md) and the route/state/provenance contracts from5.32k/l. Full cloud qualification is not duplicated locally.

## Iterations

### Iteration 1 — implementation and focused checks

- `score.js` now projects a pending valid removal target to its configured floor/coordinate. No map/Boss ID, stat target, resource route or scheduler parameter is hardcoded. Actual corrected seed distance changes Infinity→11; stageGoal itself and canonical success predicate are unchanged.
- `check-removal-goal-distance` PASS; stripping the removal component in a process-local negative control fails as expected (Infinity vs4). `check-agenda-policy-evaluation`19/19, search-semantics-identity, durable-search and syntax checks PASS. Remaining pre-freeze manifest/core checks and finite search follow.
- Combat-only sensitivity (modified copies, never search inputs): at60ATK/1DEF Boss damage1040600, while60ATK/4DEF damage253055. Natural next level needs75EXP; corrected entry has41, and the level grants+3ATK/+3DEF. This explains why merely penetrating44DEF is insufficient, but does not prescribe a resource order or prove a route exists.
- An initial diagnostic call used the wrong `evaluateBattle` argument shape and then read `.damage` instead of `.damageInfo.damage`; those empty/unknown diagnostic outputs were corrected, never used as search evidence.

### Iteration 1 result — valid bounded MISS

- `c06f9fe`32k finished: `goalFound=false`, `budgetExhausted=true`, `searchComplete=false`, frontier121464, wall736253ms. TS15 received12054 expansions; TS14 19210, TS13 728, TS12 8. Max observed TS15 HP98433/ATK55/DEF4/LV4 (marginal maxima, not necessarily one state). ActionTrimmed0, provider/observer errors0, uniqueBattleTargets37, uniquePortalEntries7, peakheap3658MiB/RSS3871.7MiB; no memory/time stop. This is not impossibility and not a completed search.
- Local one-step diagnostic from the actual verified seed exposed an incomplete fix: root distance11; legal retreat toTS14(6,0) distance0; legal redPriest attack distance10. The existing cross-floor estimate counted reaching the final room, not the additional distance from its entrance to the explicit removal coordinate.

### Iteration 2 — complete the missing terminal-room leg

- For pending removal goals on a later floor, add the minimum Manhattan leg from that floor's declared `:before` entrance(s) to the removal coordinate. On the actual route, retreat distance becomes11 rather than0, while in-room advancement remains10. No extra floor-priority weight, new quota or new resource target.
- Extended microcheck covers previous-stair nonzero distance, multiple declared entries, absent-entry fallback and unchanged ordinary floor-only goals. Missing entrances are not invented; this remains a heuristic over declared stair topology, not a general event/teleport graph or exact path distance. Focused goal/agenda19/core/identity/manifest235 checks PASS.
- Before seeing the next result, freeze **one final fresh32k/15-minute validation arm**, same original initial and exact corrected seed, same Fair4/FIFO/K32/inherit/candidate16 configuration. This is a source-correction comparison, not a64k continuation. No third arm or budget ladder in this round. Runner additionally preserves already-returned best/progress/deepest states after search, only for diagnosing a MISS; this changes output retention, not search inputs or ordering.
- Original32k negative evidence and source remain intact. Second-arm source freeze/preflight and result follow.

### Iteration 2 result — corrected distance; valid bounded MISS

- Final arm used source `9967151f03a55e3d79cbeff1d084132e4c1a3b4c`, exact same original-initial-verified seed (`seedSha256=5e49fce5…55954c`), new bundle manifest `487c38fe…d4008`,1729 bound files, prior1728-file bundle rehashed with no drift. Preflight PASS: original-initial strict replay74 decisions, seed TS15 HP36397/ATK34/DEF1/LV3/EXP41, cumulative greenKey spend1. After run bundle drift0; production journal/status/STOP hashes unchanged, service inactive; no run process remains.
- One bounded arm finished at32000/32000 expansions in893603ms (within900000ms): no goal, budget exhausted, search incomplete, frontier107917. TS15 had31356 expansions, TS14 627, TS13 17. No action trim, provider error, observer error, memory stop, or time stop; peak heap3717.9MiB/RSS3933.7MiB. This is a valid bounded MISS, not frontier exhaustion or an impossibility result.
- Corrected projection did its intended scheduling job: practically all observed expansion service stayed on TS15. Actual seed/one-step audit: TS15 distance11; legal retreat to TS14 stair also distance11; redPriest action advances to10. Thus the remaining failure is no longer explained by the old Infinity/zero retreat projection.
- Post-run diagnostics identify the immediate observed blocker in preserved states: bestSeen HP180208/ATK53/DEF4, Boss predicted damage449614; deepest expanded HP24224/ATK53/DEF4, same damage. Both attacks are lethal and Boss is absent from their enumerated actions. Raising HP **only in a diagnostic clone** makes the Boss action enumerable; synthetic stat sensitivity at DEF4 gives damage449614@ATK53,253055@60,213037@63,191851@65,168311@68. This supports a survivability/stat-acquisition gap as the current hypothesis; it does not show all pending/frontier states lack a viable Boss attack, since107917 remain.
- Actual expanded-state marginal maxima: HP143794, ATK63, DEF4, LV4 (not necessarily one state); `bestSeen` is a separately preserved generated diagnostic and must not be conflated with expansion maxima. No winning route means no original-initial route replay and no browser replay.
- Residual productization gate discovered offline: TS15 Boss `afterBattle` returns toJC19 but also invokes `unloadEquip` and `insert("清空状态")`; simulator reports those event types unsupported. Synthetic goal-predicate plumbing is not full terminal-event parity. Any future winning route requires event-parity review and real browser replay before claiming TS15/Boss closure.
- Final tracked summary: [5-32m-result.json](5-32m-result.json). Raw report, contract, seed verification and preserved states are under ignored `shared-solver/routes/generated/260924-pr532m/r2-evidence/`; diagnostic `boss-gate-diagnostic.json`. No more search arms were run.

## Final implementation and limits

- `score.js` uses explicit pending removal goals on their target floor and includes the terminal floor's declared `:before` entrance-to-anchor leg for lower-floor projections. It remains a topology heuristic, not exact pathfinding or a general event-graph solver. `check-removal-goal-distance.js` covers old-rule negative control, invalid/completed anchors, same-floor distance, lower-floor entrance leg, multiple/absent entrances, and floor-only compatibility.
- `9967151` is pushed to `dev`; focused removal-distance, agenda19, core, search-identity, manifest174/235, syntax checks passed before final bundle. `git diff --check` and documentation/link reconciliation follow this result update.
- Milestone status is `NOT_PROMOTED`: implementation and checks are real, but the bounded Boss gate missed and there is no route/real replay/owner acceptance. Production remains paused. The first and second valid negative results stay preserved.

## Next authorization gate

No automatic third search, larger budget, heuristic/stat target injection, or production resume. Next, offline-only: (1) resolve/test terminal `unloadEquip`/`清空状态` event parity; (2) quantify whether legal resources from the verified one-key seed can produce a state with survivable Boss combat, without turning an authored route into search input. Then freeze a new explicit bounded-run contract and obtain owner authorization before any additional expansion budget. No route/no impossibility claim until then.

Iteration1 preflight record (historical): source `c06f9fea16d0ccadd41e6aba870c98b9db37d0c8` committed/pushed; `pr532m-c06f9fe-r1` fresh bundle,1729 bound files, prior1728-file bundle rehashed with drift0, productionSHA unchanged/service inactive. Bundle manifestSHA `232f78f1a217798fb676446786d9acb5664ab42770d1d1e1f30881199ff40ebb`; corrected seedSHA `5e49fce57cbdb14e6d696defbde092cb427b2f129fe6a933c31184c43855954c`. Cloud preflight original-initial74-decision strict replay PASS, seed distance11, cumulativeSpent1. `manifest`174 modules/235 tests, core and no-tower checks also PASS. Search used its fresh `runs/boss-32k`; the completed negative result is recorded above.
