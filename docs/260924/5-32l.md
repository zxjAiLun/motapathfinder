# PR-5.32l — Directional movement parity exposed by one-key route replay

```ini
MILESTONE = PR-5.32l
STATUS = VERIFIED
PROMOTION = NOT_PROMOTED
DATE_STARTED = 2026-09-24
BASE_COMMIT = fbe134d43642bbe1dae61d5c7c2be253d96fc92e
SCOPE = Repair movement/automatic-target direction checks; revalidate existing autonomous sequences without new search or witness guidance.
```

## Evidence and initial hypothesis

[5.32k](5-32k.md) found 16 internal-strict TS15 candidates, but goal0 browser replay fails at decision70 `brownWizard@TS14:7,3`: expected HP83556, actual57956 (−25600). Key gross spend remains exactly1. Internal checkpoints alone do not certify the runtime.

Actual tower has I619(+25600) at TS14(3,1), west of `arrowRight` at (4,2). Runtime `libs/maps.js::_canMoveHero_checkCannotInOut` checks source `cannotOut[direction]` and target `cannotIn[opposite(direction)]`. Solver reachability, safe/legacy walking and automatic target collection currently contain no `cannotIn` / `cannotOut` checks. Hypothesis: automatic pickup illegally reaches the western supply through the one-way arrow. Preserve old candidates and failing browser evidence; do not erase the negative result.

## Initial bounded repair design and gates

- A shared directed-edge predicate for tile cannotIn/cannotOut and floor cannotMove/cannotMoveIn, separate from tile occupancy. Respect removed/replaced tile lookup; check before frontier insertion, not after marking visited.
- Use the predicate in static reachability, safe-fast and exact transit steps, automatic-target BFS, and primitive adjacent-action generation. No goal heuristic, dominance, comparator, keys or budgets tuning; no tower runtime changes.
- Small executable regression: one-way forward/reverse, source/target direction convention, alternate approach after rejected edge, auto pickup behind arrow, safe/legacy parity, blocked direct step; existing movement/auto/core checks. New search-semantics identity is inherited from changed code hashes; frozen 5.32k bundle remains untouched.
- Re-simulate the already discovered action sequence from original initial with corrected solver, requiring each original action's travel identity. Save a new route with **all campaign floor snapshots**, full internal exact replay, and real browser replay plus independent gross-spend hook. Do not fake a PASS by subtracting HP or ignoring mismatches.
- No new tower search in this repair iteration. Old Boss experiment was stopped when runtime mismatch was observed; its partial run is neither no-route proof nor a completed32k result. Zero-spend same-code Control remains an isolated 5.32k check.

## Non-goals and limitations

Not a complete audit of every h5mota movement/plugin rule, dynamic bg/fg restrictions, teleport semantics or generic script net-zero item consumption. No full Rust rewrite, feasibility planner, scheduler experiments, production resume, independent review or owner acceptance. Generated reproduction/replay evidence stays ignored.

## Iterations

### Iteration 1 — implementation and actual route parity

- Shared `canTraverseEdge` in `reachability.js`: source cannotOut and target opposite-side cannotIn, plus floor cannotMove/cannotMoveIn. Integrated into static flood-fill, automatic near/BFS targets, safe walk skeleton, exact/direct transit and primitive adjacent-action generation. No tower runtime edit or heuristic change.
- New `check-directional-movement.js` checks the one-way supply microcase, forward/backward direction convention, removed-arrow behavior, floor restrictions, forbidden direct step, safe/exact walking, primitive action exclusion and legal alternate approach after rejecting an edge. Negative control overriding the exported edge predicate to always allow **fails on automatic pickup**: 25700 vs100; ordinary test passes. The override is process-local, not a file mutation.
- `check:directional-movement`, `check:green-key-budget`, `check:auto-stabilize`, `check:walk-reachability-fast-path`, `check:core`, `check:durable-search`, `check:search-semantics-identity`, `check:manifest` (174 modules/234 tests), `check:no-tower-solver-js`, all changed script syntax checks PASS. Full qualification suite not duplicated locally; old6 stale generated static fixtures remain a separate limitation.
- No new tower search. Replayed the previously discovered74-action sequence under corrected code, dropping old expected outcomes but **requiring the same fingerprint, path and travel identity**. Initial exact state matches original record; every action remains legal/alive. First changed result is exactly decision70:83556→57956, delta−25600; subsequent HP differences stay−25600. Final TS15 HP36397/ATK34/DEF1/LV3/EXP41/greenKey33.
- Rebuilt route with `snapshotFloors=config.allowedFloors` rather than the default start/end-only scope. Browser strict snapshot verification **74/74 PASS**, all campaign floor mutations included. A getter/setter observer on actual `core.status.hero.items.tools.greenKey` recorded every decrease/increase and checked the hook remained attached after each decision: only one decrease, decision43 TS11 inventory34→33; initial pickup30→34 is logged separately. No state/HP patch or ignored mismatch.
- Original invalid TS15 records and failed browser reports remain archived. Corrected artifact `shared-solver/routes/generated/260924-pr532k/direction-corrected-ts15-r1/route.json`, state/result alongside; browser `live-direction-corrected-ts15.json`. Hashes and concise evidence: [5-32l-result.json](5-32l-result.json).

## Result and next gate

One actual original-start one-green-key route to **TS15** is VERIFIED. Boss completion and global optimality are **not** established. The faulty Boss attempt was stopped; no new Boss search was performed after this repair. Fresh corrected-source Boss search with revalidated input requires its own bounded execution contract; do not reuse the old inflated states. Independent review/owner acceptance pending, production NOT_PROMOTED.
