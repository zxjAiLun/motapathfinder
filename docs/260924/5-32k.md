# PR-5.32k — One-green-key bounded autonomous search

```ini
MILESTONE = PR-5.32k
STATUS = VERIFIED
PROMOTION = NOT_PROMOTED
DATE_STARTED = 2026-09-24
BASE_COMMIT = 37a5368da550233886659f42b53ad6428d3f3961
SCOPE = Permit at most one cumulative greenKey spent from the original initial state; fresh isolated bounded solver search; no production release.
```

## Owner authorization and precise boundary

Owner requests to change the old no-green-key constraint to **at most one green key consumed**, and start solver search; reports an independently tried no-key attempt near the TS14 lower-left green NPC but hundreds of HP short. This is a hypothesis/empirical clue, not a route oracle. Old production was paused and must not be silently resumed. This round may perform **new, finite, isolated search**, superseding 5.32i's no-search boundary only to that extent. Multi-day/unbounded run and production publication are not authorized by “启动搜索”.

At initial TS11 configured inventory `greenKey=30`; verified TS13 trial seed has more green keys due acquisitions. **Budget means lifetime gross spend ≤1 from campaign start**, not final count≥seed−1 and not inventory capped at 1. A green door requiring 3 and event consuming 10 must remain forbidden. Existing `protectedItems=["greenKey"]` default zero-spend semantics remain backward-compatible unless opt-in `protectedSpendLimits.greenKey=1`. Other protected items stay zero-spend. A solver-only cumulative-spend state component must be included in the canonical key, inherited from seed/stage transitions and strictly verified in replay. Do not simulate a free key or subtract it from initial inventory.

## Initial design and gates

1. Inspect door enumerator/travel simulation and event consumers. Track every permitted green-door consumption via per-state explicit cumulative spent counter, guard before action/reachability and after transition against cap; fail closed for unexplained inventory loss or event consumption. Ensure option absent uses original code path and state-key/identity.
2. `searchSemantics`/resume identity must include the effective one-key option; problem fingerprint must change if this campaign constraint changes. Validate nonnegative integer cap, reject unsupported protected items or multi-item combinations rather than silently allowing cost. Independent zero-spend and cap=1 tests: first green door legal, second green door blocked, 3-key door blocked, spending+pickup cannot reset cap, alternate path rejoin keeps distinct budget states, replay/status/route metadata not incorrectly claiming `zeroSpendItems=["greenKey"]`.
3. Freeze new source commit and immutable tower/seed/initial bundle. Same-code fresh 32k Control (zero) vs Treatment (one) at TS14 stage, goal-relative/Fair4/K32/inherit, no witness search input, bounded RSS/runtime; treatment must include a concrete count of green spend and legal actions if possible. No K/comparator/fairness/action-weight tuning. Search result `foundGoal`, `frontierExhausted`, `budgetExhausted`, `searchComplete` are distinct. Historical Control may be referenced but is not a same-code counterfactual.
4. Any autonomous goal candidate must reconstruct the full original-start route and strict replay with **gross** green-key spend ≤1 and all other protected items unchanged; do not treat lower final key inventory as a proof of gross consumption. Without a goal, report the actual budget/frontier; do not automatically run longer or resume old production.
5. Keep scripts/logs/large results ignored at `shared-solver/routes/generated/260924-pr532k/`, track concise result, milestone and handoff. 5.32j profiling remains separate; no Rust port or feasibility implementation in this round. P2 static fixture hashes not part of this change.

## Iterations

### Iteration 1 — initial 32k Treatment and recovery (2026-09-24)

- Frozen solver `fbe134d43642bbe1dae61d5c7c2be253d96fc92e`; `pr532k-fbe134d-r1` manifest 1,728 files rehashed with zero drift. Git `dev == origin/dev` verified at the same commit; existing observer-tool directory preserved.
- Treatment: 32,000 expansions, 16 TS14 skyline candidates, frontier 47,851, `goalFound=true`, `budgetExhausted=true`, `searchComplete=false`; original runner saved zero verified routes because it used the TS13 seed as replay start for a full-prefix route. Its `completed` status meant search process finished, not route verification.
- Takeover found 16 subsequently created full route files, but empty `all-verified-goals.json` / `strict-replay-results.json`. Their generating command is not attested; they were not trusted without independent replay and search-lineage checks.
- Offline recovery `pr532k-recovery-r3`: 16/16 original-initial strict replays PASS, each pre/post exact key checked, 29-decision seed prefix and 16–28 suffix edges exactly joined to accepted search parent chains. All spend exactly one green key at `openDoor:greenDoor@TS11:11,3` (inventory 34→33); original initial inventory stays 30. Representative final HP/ATK: 27708/13, 27594/14, 26867/17. No new TS14 search was needed.
- Recovery r2 retained as failed diagnostic: 8/16 failed only when re-saving replay-produced summary routes, which discarded alternate travel evidence. r3 passes original structured decisions and raw route to reconstruction; all 16 pass without ignoring state differences. These are internal simulator checks, not browser/live replay.
- Loaded actual trial floors TS11–TS14 contain no direct greenKey event references; TS15 only reads it in victory text; JC19 has a +2 grant. This narrows actual-route audit, not a blanket proof against arbitrary scripts consuming and replenishing keys.

### Iteration 2 — bounded continuation authorized by owner “接手继续研究”

Freeze before execution: same immutable solver/tower, all 16 verified TS14 states as canonical multi-root seeds (no teacher/witness input), existing Fair4/FIFO/K32/inherit and 16-candidate preservation unchanged. **Total 32k expansions / 15 minute wall cap** for TS14→TS15, not 32k per root. If and only if goals fully replay from original initial, another **total 32k / 15 minute** stage may target the configured Boss-clear goal. This is at most 64k new continuation expansions, not a budget ladder. Lifetime spent counter remains 1 in all seeds; no second green key. Save full goal states before any route reconstruction; search and replay status separate; original production untouched. Same-code zero-spend Control32k still pending. Multi-root continuation is not the TS14 A/B counterfactual.

Generated scripts and evidence remain ignored. Stop after these finite stages; a budget MISS is not impossibility. Browser replay, independent review, owner acceptance, and promotion are separate and have not been asserted.

### Iteration 2 results and correction

- TS14→TS15 multi-root run completed 32,000 expansions / 593,037ms, 16 goals, frontier80,318, search incomplete. Full raw goal states were saved before verification.
- First post-reconstruction spend audit incorrectly used public snapshots, which omit private solver counters. Offline `pr532k-ts15-verified-r1` instead replays full states: 16/16 internal exact/strict PASS, cumulative spend1. Original candidate0 HP61997/ATK34/74 decisions.
- **Runtime veto:** browser candidate0 fails decision70 `brownWizard@TS14:7,3`, HP83556 expected vs57956 actual. Internal-strict TS15 candidates are not runtime-qualified. Boss run `pr532k-boss-r1` was stopped by the parent (not an owner acceptance/rejection) after the mismatch; last observed16000 expansions, actual stop count not recovered, no completed32k outcome. Do not use these old TS15 seeds or treat this as no-route evidence.
- [5.32l](5-32l.md) fixes a directional-movement defect: the one-way arrow was ignored, permitting automatic pickup of I619(25600HP) across TS14(4,2). Revalidation of exactly the original74 actions and travel yields TS15 HP36397/ATK34; **full campaign-floor browser snapshots74/74 and independent gross-consumption1 PASS**. This replaces the invalid oldHP state, not the historical search result. One corrected TS15 route is qualified; the other15 are not yet revalidated under corrected source.
- Initial TS14 representative separately passed browser45/45 with cumulative gross1. Loaded local Neko project files/assets matched all1078 tower manifest entries; the earlier project-only copied release lacked21 assets and was not used for browser execution. First browser launcher attempt got403 due forward-slash root vs native Windows path; normalizing only the probe's projectRoot fixed setup, no runtime changes.
- Same frozen-code Control32k finally completed: no goal, frontier44885, 318667ms, `goal-not-found-search-incomplete`; events SHA `da02e10e4c66ace160b9c9b94306fbc249321b0ca7aa1b5b36d99b6d92a237d7` exactly matches the historical Control. Treatment's one-key TS14 improvement is established at this fixed budget; this does not prove zero-key impossibility or certify the old solver after the new movement correction.
- Source bundle drift remained0; productionjournal/status/STOP SHA unchanged and service inactive. Final evidence: [5-32k-result.json](5-32k-result.json), [5-32l-result.json](5-32l-result.json). Independent review/owner acceptance pending; production NOT_PROMOTED.

## Next gate

Freeze corrected-source continuation separately and use only revalidated states for a new finite Boss search. No further search was launched in the direction-repair iteration. Do not continue the stopped Boss process or reuse its inflated inputs.
