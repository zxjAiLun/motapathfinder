# PR-5.32a — Bounded Continuation Slice (Fair4 temporal-locality causal probe)

```ini
MILESTONE = PR-5.32a
STATUS = NOT_PROMOTED
VERIFICATION = VERIFIED (local contract and one completed cloud causal pair)
STATUS_NOTE = Partial locality benefit; P7 regressed and P12 retained a 30k wait. Full gate not passed; no automatic 64k or production release.
DATE_STARTED = 2026-09-22
BASE_COMMIT = 5a383a6
ROUND_KIND = search-mechanism causal probe (NOT a production deployment)
SCOPE = Optional single-root Fair4 continuation slice, triggered only by fair-oldest pops; subtree shares K expansions including the root, within the global budget. Default OFF. Independent causal probe, not a production release.
```

## 问题与已有证据

接手审计与云端诊断已确立单根病理（[takeover](cloud-progress-takeover.md)、[cloud-node-route-audit](../260921/cloud-node-route-audit.md)）：neko 生产搜索为**单根** durable 搜索，Fair4（`hybrid-fair` + `fairnessEvery=4` + `fifo`）虽已明确压过 best-first，但每推进一个 teacher-prefix step 仍要把约十万个兄弟节点重新排队展开——缺少 **temporal locality**。133 attempts / 8,320,697 expansions 后最深 popped prefix 仍停在 Step 11，P12 插入 @49,945 却从未 popped；Fair4@128k 相比 64k 未推进（deepest popped prefix 均为 11）。

代码调研（committed HEAD `5a383a6` `dp-search.js`）确认：

- **Fair4 是 hybrid-fair agenda**：子节点 enqueue 时同时进 `heap`（best-first 比较器）与 `fairEntries`（FIFO）。`popNext` 每 `(expansions+1)%fairnessEvery===0` 弹出 `fairEntries` 最老未展开节点，否则弹 `heap` 最优。
- **单根**：`runAttempt` 调 `searchDP(sim, state, …)` 传单个 state ⇒ `multiRootSearch===false` ⇒ PR-5.24h 的 root-sliced quantum scheduler（`multiRootExpansionQuantum` / `popRootSliced`）在 neko 路径**是死码**，不是本轮的 prior art，也不与之竞争。
- **5.31g 已预留** `searchSemantics.continuationSlice{enabled,mode,budget}` 并纳入 `resumeSearchFingerprint`（enabled=false ⇒ mode/budget=null）。

病理机制：fair-pop 给某个饥饿的深层 winning ancestry 一次服务机会后，**紧接着的展开立即回到全局竞争**，把该方向的十万级兄弟重新排一遍，promising lineage 无法连续前进。

## 唯一因果问题

> 给一个刚通过 fair pop 获得服务的方向**一点连续的局部工作量**，是否真的能消除“每走一步重新排十万节点”的病理？

这是一个 **temporal-locality** 假设检验，不是再造 heuristic。

## 初始设计（v1 语义 — LOCKED）

### 机制（fair-pop-only,锁定原因见下）

```
fair pop 一个 root（***仅*** fair-oldest pop 触发；best-first/fifo pop 不触发）
        ↓
以 N 为界创建一个 local continuation slice
        ↓
整个局部子树共享一个总预算 K
（root 自身的 expansion 消耗单位 #1；K 次为界的局部 expansion 中，下一个
  展开对象从「本 slice 内新产生且存活」的局部前沿里按现有同一比较器选取）
        ↓
K 次 expansion 用完（或局部前沿枯竭）
        ↓
仍存活的状态正常回 global agenda（heap + fairEntries，一如既往）
```

补充（fair-pop-only 裁定）：best-first pop 本就沿最优 rank 连续深入，没有「被服务一次后立刻丢回全局竞争」的病；"被服务一次后立刻掉回全局" 只发生在 fair-oldest pop 把某个看似饥饿的深层 winning ancestry 弄出来一次之后。切片只钩在 fair pop 上，是最对症、改动最小的 causal test；两臂的 best-first pop 路径逐位一致。

### 硬约束（防止重演 5.31d/e）

1. **subtree 共享总预算 K**，不是“每个 child 各自获得 K”。否则 branching factor 会把额度乘起来。
2. **不继承永久优先级**：slice 结束后存活状态回到全局 agenda 的正常位置，没有任何跨 slice 的持久优先权。
3. **K 从同一全局 `maxExpansions` 预算扣**，不是额外预算。这样 Control 与 Treatment 在**相同总工作量**下比较，A/B 才是单变量。
4. **slice 内沿用现有搜索语义**：同一 DP key、同一 dominance、同一 goal-relative 比较器、同一 action generation、同一 archive/skyline。第一版**禁止**新增：resource score、TS12 特判、回楼奖励、ATK/宝石阈值。测的是 temporal locality，不是另造启发式。
5. **默认 OFF**：`continuationSlice.enabled` 未开时，搜索路径必须与当前逐位一致（behavior-preserving）；child 处理（注册/skyline/dominance/goal archive/heap/fairEntries）在开/关两态下都完全相同，slice 只改变“下一个展开谁”。

### K 首值

不做参数扫表。由代码结构选一个**小而自然**的首值，让一次 slice 机会有**几十次**局部 expansion（不是几千）。第一轮只允许 one K / one treatment / one 32k run。观察的是机制是否成立，不是找“最佳 K”。

## 第一轮 causal control（LOCKED）

Control 不回 best-first（已被 Fair4 压过）。第一轮单变量对照：

```
Control    : Fair4  (hybrid-fair + fairnessEvery=4 + fifo)
Treatment  : Fair4  +  bounded continuation slice (one K)
```

其它全部一致：same exact durable seed / DP key / dominance / goal-relative comparator / action generation / archive / expansion budget（32k）。

### 预算：32k（不是 128k）

Fair4@32k 已知基线：`P7=143, P8=1467, P9=10651, P10=queued`。

## 预冻结判据（三态）

**成功信号（→ 授权 64k 复算）**：
- P7 / P8 / P9 不明显退化；
- P10 在 32k 内被 **popped/展开**；
- P11 / P12 不再重新产生数万级等待；
- deepest exact prefix 明显 **> Step 9**。
- 最好直接 `foundGoal=true` → materialize actual route → strict replay PASS。

**负信号（→ 记负结果，停止，不扫 K）**：
- 只是多展开了大量局部兄弟；
- frontier / wall-time 暴涨；
- teacher prefix 没明显推进。

**中间（inconclusive）**：deepest prefix 有小幅推进但未达 P10 展开 → 记录，交 owner 判是否值得一次 64k，不自动扫 K。

## 隔离与部署边界

- 第一轮是**独立 probe**（用 5.31g 建立的执行隔离契约），**不是 production deployment**。
- probe 用 **fresh run-dir / fresh provenance / fresh searchSemantics fingerprint**；**绝不 resume** 已 `SEARCH_SEMANTICS_DRIFT` 的旧 neko 账本；旧 paused neko run 只读保留。
- 只有 probe **明确正收益**（相同工作量下恢复更多连续 winning ancestry，最好 foundGoal + strict replay PASS）后，才把 5.31g + 5.31h + 5.31i + 5.32a 一起制作**全新 production release**。
- probe 负向：5.32a 不需为此发生产 release；5.31g/h/i 已在 GitHub，等下一次真实搜索能力变化时一并部署。

## 非目标

不改 Fair4 本身、不改 DP key / dominance / 比较器 / action 生成 / archive；不引入任何地图或资源特判；不做 K 扫表；本轮不发 production release。

## 门禁计划

- 新 `check-continuation-slice-contract.js`（manifest graded）：
  - slice 关闭 ⇒ 与基线逐位 behavior-preserving（同一 seed/输入下 expansions、候选、stoppedReason 完全一致）；
  - slice 开启 ⇒ subtree 共享总预算 K（不 per-child）、K 用完存活状态回 agenda、无跨 slice 持久优先权、无新 heuristic；K 计入同一全局预算；
  - `searchSemantics.continuationSlice` fingerprint 敏感（5.31g 已具备，补断言）。
- 回归：`check:durable-search` / `check:core` / `check:manifest` / `check:no-tower-solver-js` / `node -c` 全过。

## 迭代日志

### Iteration 1 — 2026-09-22 实现与契约验证
- Change: `lib/dp-search.js` 新增 bounded continuation slice（关开启默认 OFF）：
  - 配置解析 `continuationSliceEnabled/continuationSliceBudget`（`enabled===true && fairnessEnabled` 门控；默认 OFF，budget 默认 32）；
  - 公平 pop 触发 `beginContinuationSlice`（两个 fair-pop 路径都钩）；slice 开启期间 popNext 只从本地前沿取；
  - enqueue 时节点照常注册全局 agenda（heap/fairEntries），激活 slice 时额外推入本地边结堆（**view-only，node 归全局 agenda 管辖**）；
  - 主循环每次 slice-window expansion 计入同一份 `expansions`（`expansions += 1` 后 `continuationSliceRemaining -= 1`，满了关 slice）；
  - slice 存活 `endContinuationSlice` 在运行终结时也强制关、计数幸存者。
  - 诊断随 `result.diagnostics.dp.agendaFairness` 输出：`continuationSliceEnabled/Budget/Opened/LocalExpansions/Survivors/MaxDepthReached`。
  - `lib/durable-search.js` `runAttempt` 从同一 `searchSemantics` 提取器传入 `continuationSlice`（fingerprint==执行语义）。
- Reason: temporal-locality causal probe。owner 定性方向是「给一个刚被 fair pop 救起的深层 ancestry 一次局部连续工作量」。
- Evidence:
  ```text
  node -c lib/dp-search.js lib/durable-search.js → OK
  npm run check:continuation-slice → PASS（K=1 逐位恒等 vs OFF vs {enabled:false}；subtree 共享 K；
    slice pop 只出现在 fair pop 之后、每长度 ≤K-1；两臂同预算同 expansions；
    K=100000 时 fairPops===1 且其后全 continuation-slice；确定性；searchSemantics 归一化）
  npm run check:search-semantics-identity → PASS（continuationSlice 指纹敏感；禁用等价默认；
    recovery 跨 semantics fail-closed）
  npm run check:durable-search → PASS（默认行为 unchanged）
  npm run check:manifest → PASS (174 modules / 231 graded)
  npm run check:core → exit 0；npm run check:no-tower-solver-js → PASS
  ```
- Outcome: 实现 + 本地契约验证完成。**因果验证（云端 probe）是实际意义所在，待 #9。**

### 遗留边界与下轮（PR-5.32a → probe）
- 第一轮云端 probe：Control=Fair4（hybrid-fair/fairnessEvery=4/fifo），Treatment=Fair4+slice（K=32，默认 budget），32k expansions，同 seed/DP key/dominance/comparator/action gen/archive；**fresh run-dir / fresh provenance / fresh fingerprint**，不 resume 旧漂移账本；**非 production deployment**。
- 预冻结判据（设了 STATES 的那三色）：P7/8/9 不退化 + P10 在 32k 内被展开 + P11/P12 不重建数万级等待 + deepest exact prefix > Step 9 → PROMISING & 授权 64k；foundGoal=true ⇒ materialize + strict replay；反之记负结果，不扫 K。
- 只有 probe 明确正收益才把 5.31g/h/i+5.32a 一起打 **production release**（fresh run-dir，旧 paused neko run 只读保留）。probe 负向则和其并存为经典的 experiment-only 代码，不单独发 release。

## 接手预检更正 — 2026-09-22（32k 尚未启动）

- 已核验 `dev == origin/dev == 8e08e6afd38608596d5ef08905cc411906ddde33`；旧 `neko-zero-key` paused、求解服务 inactive，原 `probes/pr532a/solver` 只有上传 bundle，无报告/运行日志。
- 原待跑 harness 存在证据缺口：对原始 profile 调 `executionProvenance`，没有带入 Fair4/slice/32k 的实际配置；bundle 缺 `run-durable-search.js`，`identityOf` 会在搜索后报 ENOENT。**尚未执行真实 probe，不能把它记为负结果。**
- 修复限于 experiment harness：身份在搜索前计算并持久化；两臂从同一个规范化配置生成执行选项与 provenance，仅 slice 配置不同；另记 seed/initial/runner/bundle hash 和 direct-DP 执行合同（不冒充 durable journal）。
- 新建独立目录（不覆盖旧上传物），复制冻结 tower/seed/initial；每臂 fresh run-dir，串行运行。`K=32`、32,000 expansions、现有 comparator/key/dominance/action/archive 均保持不变；首轮仅一对 Control/Treatment。
- 搜索进程不解析 witness 路线：记录通用 accepted/popped/evicted 节点的 exact-key SHA256 与队列指标，**搜索结束后**才与见证 postExactStateKey 对照；无 witness 预热 simulator。删除错误的 candidateGenerated-parent-key 生成时间推断，保存 raw stoppedReason 与 normalized searchOutcome、observer errors；无 pop 不冒称仍 active。
- 只订阅观测事件，不改变搜索机制。若 foundGoal 则保存实际 candidate、从原始 initial 构建路线并用既有 strict replay 验证。新控制臂需复现历史 P7=143/P8=1467/P9=10651；不能用旧报告代替当前同 bundle 对照。
- 文档更正：早期设计提交 `16e767c` 的 “any agenda pop” 超出 owner 的 fair-pop-only 约束；实现 `8e08e6a` 已是 fair-only，本段显式保留更正记录。multi-root quantum 只是在 Neko 单根路径**不激活**，不宣称全项目是死码；无误导的“best-first 序列在 ON/OFF 下逐位相同”能力断言。

## Iteration 2 — 2026-09-22 独立云端 causal probe（已完成）

### 执行与隔离

- 代码：`8e08e6afd38608596d5ef08905cc411906ddde33`（本轮未改 solver）；主机会话 Node v22.23.2 / ARM64，绝对路径 `/home/ubuntu/.local/share/motapath-solver/node/bin/node`。
- 独立 bundle：`/home/ubuntu/motapath-solver/probes/pr532a-8e08e6a-r1/`；两臂分别写 `runs/control-32k/`、`runs/treatment-32k/`，各独立新进程、串行执行、禁止复用已有 arm 目录。
- 冻结同一 seed `2-7f41f60b09a55951abd8931c`（SHA256 `68c230b811e9d763f1f2e672b6707233f1a8b395c477d34406c931105c2c8022` 的完整值见下方 artifact JSON），同一 initial/tower/config；仅 `continuationSlice` 不同。无生产服务切换、账本恢复、配置迁移。
- 每臂 direct `searchDP`：hybrid-fair、fairnessEvery=4、FIFO、goal-relative、location DP key、candidateLimit=16、maxActionsPerState=4096、32,000 expansions、runtimeMs=0、RSS8192MiB、heap6144MiB。Treatment 显式 `K=32`，包含 fair root 自身 expansion。两臂均 stopOnFirstGoal=false。
- 自有临时 systemd user units：`motapath-pr532a-r1-control`、`motapath-pr532a-r1-treatment`；两臂同为 MemoryMax=9G、Nice=10、CPUQuota=150%、RuntimeMaxSec=1800。均 exit 0 / success，未触及运行时或内存上限。
- 执行模式：`node run-slice-probe.js preflight`（不启动搜索，seed 与 witness strict replay）；随后分别 `node --max-old-space-size=6144 run-slice-probe.js run <arm>`，完成后 `node run-slice-probe.js analyze <arm>`。
- 搜索进程**不解析或重放 witness 路线**；通用 exact-key hash 事件写入 JSONL。独立离线分析才解析 witness，取每个 exact checkpoint 的首个插入/pop。Control **精确复现**历史 P7=143/P8=1467/P9=10651，消除了新旧 harness 可比性疑问。
- provenance字段 `witnessReadDuringSearch:false` 口径更正：pre-search `verifyBundle` 会读取包括witness在内的文件字节核验哈希；该字段应理解为“搜索不解析/使用见证”，不能理解为进程从未read该文件。无见证目标进入selection/rank/action/dominance，也无见证重放预热搜索simulator；保留已执行artifact不倒改。

### 固定工作量结果

| 指标 | Control Fair4 | Treatment Fair4 + K32 slice |
| --- | ---: | ---: |
| Expansions | 32,000 | 32,000 |
| FoundGoal / goal candidates | false / 0 | false / 0 |
| 连续 exact checkpoint 覆盖 | Step 9 | **Step 13** |
| P7 首次 pop（completed-before-pop，沿用历史零基口径） | 143 | **843** |
| P8 首次 pop | 1,467 | **845** |
| P9 首次 pop | 10,651 | **847** |
| P10 首次 pop | 未观测到（10,652 插入） | **848** |
| P11 首次 pop | 未插入 | **849** |
| P12 首次 pop / wait | 未插入 | **31,355 / 30,505** |
| P13 首次 pop | 未插入 | **31,356** |
| P14 exact state | 未插入 | 未插入 |
| Search wall time | 298.878s | 308.421s（+3.2%） |
| Active frontier | 33,695 | 44,885（+33.2%） |
| Peak RSS / heap (MiB) | 1933.8 / 1768.6 | 2233.5 / 2063.5 |
| Global fair pops | 8,000 | **1,633** |
| Global best pops | 24,000 | **2,110** |
| Fair cursor | 13,181 | 2,694 |

两臂 raw `stoppedReason=null`，同时 normalized `searchOutcome` 为 `goalFound=false / frontierExhausted=false / budgetExhausted=true / searchComplete=false`；`expansionBudgetExhausted=true`，`actionTrimmed=0`、observer errors=0、action provider errors=0。因此是有效的有界 MISS，不是基础设施失败，也不是无解证明。无独立发现的 goal candidate，实际路线 materialize/strict replay gate **未触发**（preflight 的59步 witness 通过不能代替它）。

### 真实事件流机制检查

- Treatment 共开 **1,633** 个 slice；逐窗口重建证明 **max=32** expansions，891 个用满32，663 个仅1步；其余窗口提前耗尽局部前沿或遇全局停止。
- 通过 accepted node 的 parentId 链验证，**每个 continuation-slice pop 都是当次 fair root 的后代**。没有 per-child K 放大或永久继承优先级。
- slice-window expansions 合计 **29,890（93.4%）**，其中含1,633个fair root；其余2,110次为全局best pop。局部burst消耗全局预算与fair服务份额的代价明确可见。
- 首个 P7 slice：node49，pop 843→874，恰32次。P8/P9/P10/P11在其中被连续服务，证实 temporal locality 可消除这一小段重复排队。
- P12 虽在850插入，但未在该 slice 内取得服务，直到31,355才由全局fair队列弹出；该次窗口2步，接着P13=31,356。**局部连续工作确实发生，但并未消除下一段数万级等待。**
- “Step13”是连续 exact checkpoint **覆盖**，不是完整同一 parent-chain 路线闭环。Treatment 对部分 exact key 有多次插入/pop，报告同时保留所有事件；P15另有命中但P14缺失，不把最深非连续命中冒充连续前缀。未pop的 accepted 状态仍标 `liveness-unproven`，不由“没见eviction”推导终止时active。

### 门禁裁决与停止边界

| 预冻结条件 | 结果 |
| --- | --- |
| P7/P8/P9 不明显退化 | **未满足**：P7首次服务143→843（5.90倍）；P8/P9改善 |
| P10 在32k内展开 | **满足**：848 |
| P11/P12 不再数万级等待 | **未满足**：P12 wait=30,505 |
| 连续 exact prefix 明显 > Step9 | **满足**：Step13 |
| 自主 foundGoal + strict replay | **未满足**：两臂foundGoal=false |

**结论：`NOT_PROMOTED`，局部机制正信号 + 公平服务回归的混合结果。** 这不是“只多展开兄弟而毫无前缀推进”的纯负信号，也不是可自动晋级的完整正收益。按owner锁定门禁停在32k；**没有跑64k、没有扫K、没有发production release**。下一步仅提交owner审阅该mixed result；任何预算升级、调度语义变化或部署都需另行裁定。本轮不把未通过全门包装成成功收口，也不把这一K值的失败推广成所有temporal-locality机制无效。

### Artifact / provenance

- 本轮显式提升一个小型**纯结果摘要**为tracked证据：[5-32a-result.json](5-32a-result.json)。包含固定参数、两臂指标、30个exact checkpoint首见时刻、哈希与gate判断；不含完整状态或可执行路线，不作为搜索输入。
- 本地完整生成物：`shared-solver/routes/generated/260922-pr532a-probe/evidence-r1/`；同目录的 `run-slice-probe.js`、`prepare-probe.js`、`summarize-probe.js` 与 `paired-summary.json` 可供复核；云端bundle保留相同runner及完整events。
- 原始证据包 `evidence-r1.tar.gz` SHA256：`3c842e748d642ca1ec3727786af2a09afee393db31aaa3d7b177635c5926d950`（post-run-integrity.json另附，不倒改此包）。
- Control report SHA256：`2c62f297923e504d9429c805ecc2158627d4a11f923fdb5d3dc739ff14de1147`；Treatment：`4fa3c11b4ed28d58f8b117f790ebc36b310142e2d27a151db2038a15e2189eb8`。
- 相同solverDigest：`82b167d8afa3c3aa0be6397f2b5fa775b468e7636c186d80d8ef053e7fba3ee5`；相同problemFingerprint：`0ca7d0b09e97dac015a8dc36e14988764b52185ae2ed5d63e3db8a3d6e0dec78`。
- 两个独立resume fingerprints（均为direct probe真实配置，不用于旧账本恢复）：Control `82db5039acbd3806bfbb550b082ab08ac878e6d40cd23660df12481388b998b9`；Treatment `6e7921f42bb917e4e21903f8974b9ab1c799bbd5cf8b7796eeb0ee9ef04bb537`。probeExecutionId另绑定seed/initial/runner/exact options。
- 云端后验完整性检查：**1266个bundle/input文件零漂移**；旧生产 `journal.json`、`status.json`、`STOP` SHA256 全部与启动前一致；求解服务仍inactive、进度UI仍active。

## 最终状态

机制与本轮独立32k A/B证据 `VERIFIED`；production promotion **`NOT_PROMOTED`**。完整正收益门未过，预算升级与部署均不自动执行，等待owner裁定。
