# PR-5.32b — Autonomous Route Closure Continuation

```ini
MILESTONE = PR-5.32b
STATUS = AUTHORIZED
DATE_STARTED = 2026-09-22
BASE_COMMIT = 769eb34fdc6b05b2aac21b9109ba9300e842724b
SCOPE = Continue bounded cloud search/tuning toward one complete zero-protected-key route, using actual route discovery and strict replay rather than witness coverage as the gate.
```

## Authorization and preserved history

Owner explicitly requested continued modification/tuning until the solver finds a complete winning route after reviewing [5.32a](5-32a.md). This authorizes further bounded experiments and adjustment; it does not retrospectively promote the mixed 5.32a result. Keep old production journal read-only, immutable experiment bundles and fresh run directories, no drift bypass, no witness actions in search, and automatic commit/push for completed rounds.

## Initial design and gates

1. First extend the promising but incomplete slice run to **64,000 expansions, K=32 unchanged**, from the same durable seed. Reason: P12 first received service at 31,355, immediately before the 32k stop, with 44,885 live frontier entries. This is a budget-horizon check, not a K sweep or a claim that P7/P12 regression disappeared.
2. Capture all discovered stage-goal candidates, raw outcome, doctor-relevant fields and strict replay of each exported route. No oracle/witness loading in the search runner. Teacher exact-key absence (e.g. P14) is not equivalent to no valid successor; dominance can retain another HP/route representative.
3. If TS14 is discovered, continue from independently discovered candidates through the existing profile stages (TS15 then JC19 + TS15 Boss removal). The campaign seed's prefix is inherited from the old durable search, never from manual witness actions; final route verification starts from the original configured initial state.
4. If 64k is still a clean bounded MISS, examine stage/progress/diagnostic evidence before changing scheduler, candidate preservation or a further budget. No blind unbounded loop. Each job has a hard host runtime/memory ceiling and no production service control.
5. Only a materialized route that satisfies the full existing profile terminal goal and original-start strict replay with no protected-key decrease counts as completion. Intermediate TS14/TS15 goals are progress, not full closure. Production release remains conditional on verified actual search capability.

## Scope boundaries

Initially no DP-key, dominance, action-generation, comparator, archive or K change. Search runs on cloud1 (not local thermal-risk hardware). Experiments may add bounded telemetry/probe orchestration, not tower-local solver files. Generated code/data stay under ignored `shared-solver/routes/generated/260922-pr532b/`; code/schema changes, if required by evidence, are documented and tested before further experiments.

## Iterations

### Iteration 1 — 64k horizon check (completed bounded MISS)

- New generic single-attempt job harness: explicit config/stage/seed/initial, immutable solver bundle, pre-search execution provenance and seed strict replay; save all goal candidates and per-goal route records. This is not a durable journal migration.
- Fixed first job: same seed `2-7f41f60b09a55951abd8931c`, hybrid-fair/FIFO/fairnessEvery4, goal-relative, K32, maxActions4096, candidateLimit16, 64k expansions.
- Cloud `probes/pr532b-route-closure-r1/runs/ts14-64k`: 64,000 expansions, foundGoal=false, candidate=0, normalized budgetExhausted=true/searchComplete=false; frontier=74,302, wall=608,749ms, peakRSS=3567.8MiB, actionTrimmed=0, provider/observer errors=0. Host unit exited 0, no swap, memory peak3.5G.
- Accepted maximum ATK9/DEF1/HP6257; expanded floors TS13=49,420, TS12=12,490, TS11=2,090; unique battle targets25/portal entries4. More horizon did not discover TS14. No 128k escalation.

### Iteration 2 — existing generic resource-first comparator (frozen before execution)

Evidence: goal-relative ranks bestFloor/distance before combat resources and retains the preparation-detour starvation documented in [5.31a](../260921/5-31a.md). Even 64k accepted no ATK above9. Test the **existing `resource-first` comparator** at **32k** using the identical original seed, Fair4/FIFO/K32 and all DP/dominance/action/archive settings. This changes one config field, not solver code, map rules or witness guidance. Baseline for 32k comparison is 5.32a's Fair4+K32 arm (telemetry harness differs, so timing is descriptive, not a precise performance claim).

### Iteration 2 result — bounded negative result (cloud completed)

- Completed under owner continuation; `jobs/ts14-resource-first-32k.json` hash `a4c5d5b613cc01b3b6a45fdff83aab9675cd12f4eb48a7715249c007b25dd010`, report `4fb6a58eb5f945dfd4d8d6c116d3f73c0dcb9c8800e52d5b5c2e0b165838cbac`, runner unchanged `879bafbdc0f5942e43d1d17aceb8fdab4792d0c281f20d3d81ffbc578ac2d1db`.
- 32,000 expansions, foundGoal=false, no goal candidates or route exports; normalized budgetExhausted=true/searchComplete=false, 159,627ms, frontier=19,915, peakRSS=1,384.8MiB, actionTrimmed=0 and observer/provider errors=0.
- Compared with identical Fair4+K32 telemetry arm, accepted ATK max dropped 9→**8** and HP max dropped 6,257→3,764. It did service the TS12 slimeman stat-gain early (first ATK gain exp37), but then concentrated 7,179 of 32,000 expansions on TS11, 16,025 on TS12 and only 8,796 on TS13. This did not solve autonomous TS14 and shows a strong lower-floor/stat bias.

 No custom gem/location/floor heuristics are introduced. Gate remains discovered TS14 route + original-state strict replay; if positive, continue existing later stages using only discovered candidates. **Non-goal outcome is diagnosed before any independent follow-up; this does not license automated source/K/agenda sweeps.** Each new job is hashed separately and uses a fresh output directory; original immutable bundle files are not overwritten.
