# PR-5.25y - CP9 Causality and Rank20 Saturation Diagnostic

```ini
MILESTONE = PR-5.25y
STATUS = IMPLEMENTED / PHASE_1_CP9_SUFFIX_CRITICAL_MULTI_STEP_CHAIN_ESTABLISHED / PHASE_2_CP9_FATE_NOT_OBSERVED_WITHIN_2000_STOP
DATE_STARTED = 2026-09-13
BASE_COMMIT = 0550892 (5.25x closure on c2c63e4)
FINAL_COMMIT = this milestone commit (see handoff)
SCOPE = Answer two specific questions about the new first loss: is cp#9 (battle:redBat@MT1:10,1) causally required by the recorded MT4 suffix, and what exactly crowded it out of the rank-20 retention class; pure replay/counterfactual plus one observational 2,000-expansion diagnostic run; no search policy change
```

## Authorization

Owner review of `c2c63e4`:

```ini
493d0e6 = APPROVED
c2c63e4 = APPROVED_AS_MECHANISM_VALIDATION / NOT_YET_MT4_CAPABILITY_WINNER
PR_5_25X = CLOSED_WITH_POSITIVE_MECHANISM_RESULT
COMBAT_PROGRESS_RANK20 = KEEP_FOR_NOW
CP8 = AUTONOMOUSLY_CLASSIFIED / SURVIVED / EXPANDED
FIRST_ORACLE_LOSS = MOVED_FROM_CP8_TO_CP9
CP9 = NEXT_LOST_ORACLE_PREFIX / COMBAT_PROGRESS_FLAGGED / PLAUSIBLY_NEXT_RESOURCE_CHAIN_LINK
CP9_CAUSAL_CRITICALITY = NOT_YET_DIRECTLY_PROBED
CHAIN_DEPTH_PRIORITY_YET = NO
NEXT = PR_5_25Y_CP9_CAUSALITY_AND_RANK20_SATURATION_DIAGNOSTIC
SEARCH_POLICY_CHANGE = NONE
```

Framing: rank20 is a WIDE class (58% of winner-trajectory successors; 8k+ members in real runs). cp#9 being dropped despite rank20 first shows rank20 HAS ITS OWN CAPACITY COMPETITION. Adding chain-depth priorities before knowing what crowds cp#9 out would walk toward hand-authored reward design - so this round measures first.

## Phases

### Phase 1 - cp#9 deletion probe (reuses the 5.25w method)

Replay decisions 0..8 (WITH cp#8), SKIP decision #9 (battle:redBat@MT1:10,1), then attempt decisions #10..end verbatim with the current `resolveRecordedAction()`. Record:

```ini
CP9_IMMEDIATE_DELTA   # the redBat battle's pre/post world diff:
                      # HP cost, ATK/DEF/MDEF gain, EXP, LV, flags, mutation
CP9_SKIPPED_SUFFIX    # REACHES_MT4 | FAILS(step, reason)
FIRST_FAILURE
WORLD_DEFICIT         # probe failure state vs the recorded oracle pre-state
STEP10_SKELETON_VIABILITY  # direct evaluateBattle at the failing step
```

```ini
CP9_SUFFIX_CRITICAL = FALSE -> stop the chain direction (the survival audit
                               moving to cp#9 does not mean retention should
                               keep chasing this prefix)
CP9_SUFFIX_CRITICAL = TRUE and the same mechanism (redBat stat gain ->
                      skeleton viability) -> CP8 -> CP9 =
                      OBSERVED_MULTI_STEP_RESOURCE_PREPARATION_CHAIN
```

### Phase 2 - the drop-trim rank composition (observational only)

The `dropped` lifecycle event gains pure observation fields (never used for sorting):

```ini
pendingRank0Count / pendingRank10Count / pendingRank20Count / pendingRank30Count
keptRank0Count / keptRank10Count / keptRank20Count / keptRank30Count
nodePendingSeq
rank20CutoffPendingSeq   # the largest pendingSeq among kept rank-20 candidates
```

The diagnostic harness (2,000-expansion MT4-configured run, same policy) captures the trim that drops cp#9's exact key and classifies:

```text
Case A: rank10 fills most of the cap (guidedAdmitted ~900+)
        -> capacity allocation between frontier-guided and resource investment
Case B: rank20 itself exceeds the remaining capacity and cp#9 sits beyond the
        rank-20 cutoff
        -> BINARY_COMBAT_PROGRESS_CLASS = TOO_COARSE_FOR_RETENTION_SELECTION
Case C: cp#9 entered rank20 early (within the cutoff) yet was dropped
        -> retention lifecycle implementation problem; investigate first
```

```ini
DIAGNOSTIC_BUDGET = 2,000 strategic expansions (fixed; cp#9 is a shallow
                    MT1-depth breakpoint)
CP9_KEY_AFFECTS_SEARCH_DECISIONS = FALSE   # filter logs only
IF_NO_CP9_FATE_WITHIN_2000 = return to review; do not auto-raise to 5k/10k
```

## Non-goals

```ini
NO = CHAIN_DEPTH_PRIORITY_YET, COMBAT_PROGRESS_MAGNITUDE_WEIGHT,
     HP_TO_STAT_RATIO, RANK20_PARETO, THRESHOLD_TUNING,
     EXP_ONLY_PROMOTION, CAP_OR_BUDGET_INCREASE, FRONTIER_PATCH,
     PLANNER, BACKTRACKING, MT5_OR_MT8
```

## Implementation

Changed (planned):

- `shared-solver/lib/transport-collapse.js` (dropped event gains trim composition fields; observational only)
- `shared-solver/audit-pr525y-cp9-causality-saturation.js` (new)
- `20260804handoff.md`

## Results

### Phase 1 - cp#9 deletion probe: SUFFIX CRITICAL, the multi-step chain is now causal

The redBat transition itself is the single largest investment in the prefix:

```ini
CP9_IMMEDIATE_DELTA =
  HP   775  -> 1559   (+784)
  ATK   15  ->   19   (+4)
  DEF    6  ->   10   (+4)
  MDEF 110  ->  130   (+20)
  EXP    2  ->    5   (+3)
```

Deletion counterfactual (replay 0..8, skip #9, attempt #10..end verbatim):

```ini
CP9_SKIPPED_SUFFIX = FAILS at step 10 (recorded-action-not-matched)
FAILING_DECISION   = battle:skeleton@MT1:8,1
WORLD_DEFICIT      = probe hp 775 / atk 15 / def 6 / mdef 110 / exp 2
                     vs oracle hp 1559 / atk 19 / def 10 / mdef 130 / exp 5
DIRECT_VIABILITY   = probe state: skeleton damage 17,086 >= hp 775 -> LETHAL
                     oracle state: skeleton damage 1,558 <  hp 1559 -> viable
```

```ini
CP9_SUFFIX_CRITICAL = TRUE (same mechanism class as cp#8:
                       attribute investment -> skeleton battle viability)
CP8 -> CP9 = OBSERVED_MULTI_STEP_RESOURCE_PREPARATION_CHAIN
  (both links now directly probed; no longer "plausibly next")
```

### Phase 2 - cp#9 fate NOT observed within the 2,000-expansion budget

The observational run (frozen MT4 configuration, 2,000-expansion cap):

```ini
FOUND = FALSE, STOPPED = expansion-limit (2,000), 1,775 drops
LIFECYCLE_EVENTS = 34,815
CP9_EVENTS = 0                      # cp#9's exact state was never generated
CP9_FATE = NOT_OBSERVED_WITHIN_BUDGET
ORACLE_KEY_DROPS_WITHIN_2000 = 0    # no oracle checkpoint key was dropped yet
```

Per the predeclared rule, this is a STOP: the rank-20 saturation snapshot for cp#9's drop trim cannot be taken within the authorized budget, and escalating to 5k/10k is not self-authorized. What the budget DID establish: cp#9's exact state is not even GENERATED within the first 2,000 expansions under the current policy - its drop happens later in the run, so the composition of that later trim remains unmeasured.

## Findings and boundaries

```ini
CP8 -> CP9 = OBSERVED_MULTI_STEP_RESOURCE_PREPARATION_CHAIN (both links
             causally probed with the same mechanism: attribute investment
             -> skeleton battle viability)
CP9_CAUSAL_CRITICALITY = ESTABLISHED (upgrade from NOT_YET_DIRECTLY_PROBED)
RANK20_SATURATION_SNAPSHOT = PENDING (requires a longer observational run;
                              fresh authorization needed)
```

- The first loss has now moved FORWARD through two causally-probed resource investments (cp#8, cp#9), both flagged by the general combat-progress signal, both still dropped-by-cap in uninstrumented runs before the rank-20 class could save cp#9 within the observed budget.
- The reviewer's Case A/B/C question (what crowds cp#9 out of rank20) remains open: the answer requires observing cp#9's actual drop trim, which occurs beyond the 2,000-expansion diagnostic budget.
- No search policy change; the production search contract is untouched.

## Next step (STOP_AND_RETURN)

Return for owner review. The decision on the table: authorize a longer observational run (e.g., the full 180s diagnostic with the trim-composition observer) to capture cp#9's drop-trim composition (Case A/B/C), or take a different direction. No escalation was self-authorized.
