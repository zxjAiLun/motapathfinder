# PR-5.19j — Lethal Child Survival Failure Attribution

```ini
MILESTONE = PR-5.19j
STATUS = APPROVED / CLOSED
RESEARCH_VERDICT = CLASS-B-SURVIVAL-PROGRESS-BUT-NOT-VIABLE
REVIEW_STATUS = APPROVED / CLOSED
OBSERVATION_RESULT = CLASS-B-SURVIVAL-PROGRESS-BUT-NOT-VIABLE
DATE_STARTED = 2026-08-16
BASE_COMMIT = 6704e81126684ce040a20f0cae8f7d1d9f36987c
FINAL_COMMIT = <提交实际存在后再填>
SCOPE = observation-only：归因 hierarchy depth 2 的 evilHero lethal generic child 在现有 50-expansion connector 内是否发现任何真实 survival progress。不新增 connector，不改 predicate，不改 budget。
```

## 问题

PR-5.19i 已关闭。完整 hierarchical chain 已真实执行到 depth 2：

```text
make-damageable satisfied
→ attack-blocked → lethal
→ anchor expansion
→ resume
→ existing generic make-viable
→ ACTUAL connector call
→ 50 expansions
→ budget-exhausted / not-satisfied
```

depth-2 child 真实状态：

```text
boundary = evilHero@MT5:2,8
hierarchy = 2
HP = 32691
ATK = 927
enemy DEF = 1050
damage = 650524
survivalMargin = -617833
```

现在需要知道这 50 次 connector expansion 中发生了什么，而不是只看 `budget-exhausted`。

## 观察内容

只对真实 depth-2 lethal child 挂 read-only observer：

```text
expansion / depth / exactStateFingerprint
stage / HP / ATK / DEF / MDEF / damage / survivalMargin
deltaFromConnectorSource: HP / ATK / DEF / MDEF
chainSummary / availablePrimitiveActionCount
```

aggregate：

```text
statesObserved
stageCounts

bestSurvivalMargin
bestSurvivalStateFingerprint
bestSurvivalChain
bestSurvivalResourceDelta

maxHP / minDamage
survivalMarginImprovedStateCount
damageReducedStateCount
hpImprovedStateCount
viableStateObserved
```

`bestSurvivalMargin` 只用于 attribution，不得成为搜索 priority 或 completion predicate。

## 三种失败形态

| Class | Evidence | Next direction |
| --- | --- | --- |
| A | best margin 未改善 | action scope 未接触 survival opportunity |
| B | margin 明显改善但仍 lethal | discrete survival resource prerequisite |
| C | 接近 viable 但 budget 先耗尽 | frontier/bounded-search attribution |

## Budget completeness

同时输出 connector 自身：

```text
generated / frontierSize / frontierTrimmed / maxDepthReached / depthHistogram / applyErrors
```

不把 budget exhaustion 写成 infeasible。

## Frozen gate

复用 5.19i candidate：

```text
total=1000 / calls=8 / per-call=50 / lazyDrain unchanged
```

必须复现：

```text
make-damageable satisfied
attack-blocked → lethal
depth-2 generic child actual call
not-satisfied / budget-exhausted
best=-903
```

attribution ON/OFF 的 solver semantic结果完全一致。

## 迭代日志

### Iteration 0 — 2026-08-16

- Change: 建立 PR-5.19j 文档，冻结范围与 BASE_COMMIT。
- Reason: 5.19i closure 后，用户授权 observation-only 归因 depth-2 lethal child 的真实 survival progress。
- Outcome: `AUTHORIZED`。

### Iteration 1 — 2026-08-16 — observation result: Class B

- Change: 新增 `shared-solver/lib/strategic-lethal-survival-observer.js` 与 `check-strategic-lethal-survival-attribution.js`。只对 `hierarchyLevel>0`、`prerequisite.kind=battle-access-prerequisite`、`beforeStage=lethal` 的 connector 挂 read-only observer；记录每 expansion 的 stage/HP/ATK/DEF/MDEF/damage/survivalMargin、source delta、chain、action count，并汇总 best margin / max HP / min damage / improved counts / frontier completeness。`bestSurvivalMargin` 仅 attribution，不进搜索。
- Reason: 回答 depth-2 lethal child 在 50 expansion 中到底有没有真实 survival progress。
- Evidence: 本机 attribution ON/OFF frozen 1000 语义完全一致（total/calls/child/stateCreated/best/goal 均同）。真实 depth-2 child：50 states 全部 lethal；source margin `-617833` → best margin `-150104`；43/50 states margin 改善，26 damage-reduced，25 HP-improved；max HP `214597`，min damage `168451`；viable=false；maxDepthReached 4；frontier 47 / generated 96 / applyErrors 0。Best chain：`battle:skeletonKing@MT5:8,11` → `battle:devilWarrior@MT5:11,11` → `changeFloor@MT5:6,12` → `battle:skeletonKing@MT4:8,3`，best resource delta HP +88604 / DEF +100。
- Outcome: `IMPLEMENTED`；`OBSERVATION_RESULT = CLASS-B-SURVIVAL-PROGRESS-BUT-NOT-VIABLE`。connector 已接触显著 survival opportunity，但 generic “直接 viable” 目标仍远。
- Next decision: 等 review 决定下一轮是否授权 discrete survival-resource prerequisite 或进一步机会归因；不自动加预算，不新增 make-survivable 名称。

### Iteration 2 — 2026-08-16 — Closure

- Change: 冻结 5.19j 为 `APPROVED / CLOSED`，research verdict `CLASS-B-SURVIVAL-PROGRESS-BUT-NOT-VIABLE`；并授权 PR-5.19k Survival Opportunity Edge Attribution。
- Reason: 用户复核 Class B 结论：observer ON/OFF 语义一致，50 states 真实改善明显但未 viable，frontier 仍有 47，不能解释为 infeasible。
- Evidence: fast regression `31957793105` success；本地 frozen evidence 已在 Iteration 1 写入。
- Outcome: `APPROVED / CLOSED`。下一轮 observation-only 归因 connector 实际 transition edges 的逐边 survival 因果贡献。
