# PR-5.19f — Battle Stage Prerequisite Decomposition

```ini
MILESTONE = PR-5.19f
STATUS = APPROVED / CLOSED
RESEARCH_VERDICT = MECHANISM POSITIVE / NOT PROMOTED
REVIEW_STATUS = APPROVED / CLOSED
DATE_STARTED = 2026-08-16
BASE_COMMIT = 129b7c064e2eb0430a55eadd472c50128091902e
IMPLEMENTATION_COMMIT = 7475b37
QUALIFICATION_MARKER = 9fe1d24
REMOTE_CI_RUN = 31950565107
REMOTE_JOB = 95173284026
FINAL_COMMIT = <提交实际存在后再填>
SCOPE = 第一版只把 continuation-derived 的 attack-blocked child 拆成 make-damageable discrete prerequisite。不新增 connector capability，不改 budget，不做 make-survivable。
```

## 问题与已有证据

PR-5.19e 已 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`。当前 frozen evidence：

```text
child boundary:
evilHero@MT5:2,8

stage:
attack-blocked

hero ATK = 777
enemy DEF = 1050
attackMargin = -273
damage = null

generic goal:
make battle viable

result:
50 expansions / budget-exhausted / not-satisfied
```

因此 generic `make-viable` 目标过粗。本轮把它分解为：

```text
attack-blocked
→ make-damageable
```

## 第一版冻结语义

新增 discrete prerequisite：

```text
kind: battle-stage-prerequisite
stageGoal: damageable
boundary: exact battle B
parentDependency: 原来的 T

completionPredicate(state):
  battle stage analyzer 返回
  lethal 或 viable

等价于:
  supported
  AND damage != null
```

注意：

- completion 不是 `ATK >= enemy DEF`
- completion 不是 `attackMargin >= 0`
- `attackMargin` 只用于 attribution
- `unsupported / unresolved-no-damage` 不算完成

## 本轮只改 continuation-derived child

- root-level 5.19b generic battle-access 行为保持原样。
- 只在 continuation resume 后编译下一层时，若 child boundary 是 `attack-blocked`，编译 `battle-stage-prerequisite` 而不是 generic battle-access。
- 不顺手把 root-level battle-unsurvivable 全部改 stage decomposition。

## Identity 必须与 generic battle-access 分开

```text
battle-stage-prerequisite|
parentDependencyId|
floorId,x,y|
enemyId|
stageGoal=damageable
```

不能复用 `battle-access-prerequisite|...`，否则 dedupe 会把 make-viable 与 make-damageable alias。

## 满足后不得宣称 B 已解决

若 `make-damageable` satisfied：

```text
attack-blocked → lethal
```

那么 5.19f 到此为止；不顺手编译 `make-survivable`。

正确链仍为：

```text
T
→ B attack-blocked
→ make-damageable
→ satisfied + strict replay
→ preserve parent T intent
→ resume / re-evaluate world
```

## Telemetry

```text
stagePrerequisitesCompiled
stagePrerequisitesScheduled
stagePrerequisitesExecuted
stagePrerequisitesSatisfied

stageGoal
beforeStage / afterStage
beforeAttackMargin / afterAttackMargin
beforeDamage / afterDamage
sourceExactStateFingerprint
hierarchyLevel
originContinuationId
```

成功时继续记录：

```text
chainActions / chainSummary / resourceDelta / finalCreated
parentContinuationId
```

## 验收与拒绝门

| Gate | Evidence | Pass condition | Meaning |
| --- | --- | --- | --- |
| Compile correctness | synthetic + real | kind / stageGoal / boundary / completionPredicate 正确 | decomposition 语义正确 |
| Identity | synthetic | stage prerequisite 与 generic battle-access id 不同 | 不 alias |
| Continuation-derived only | real A/B | root-level 5.19e 行为不变；只有 resumed child 被替换 | 实验可解释 |
| Mechanism | frozen 1000 A/B | continuation resume → attack-blocked child → make-damageable compiled/scheduled/executed | decomposition 接上 hierarchy |
| Strong positive | 同上 | make-damageable satisfied 且 afterStage ∈ {lethal, viable} + strict replay | battle stage 真推进 |
| Promotion | same A/B | best > -903 或 terminalActionGenerated > 0 或 goalFound + strict replay | 默认 D2 晋级 |

## 实施顺序

1. 冻结本文件。
2. `strategic-access-prerequisite.js` 或新模块：compile battle-stage prerequisite（damageable only）。
3. `strategic-d2-search.js`：continuation resume 的 attack-blocked child 改走 stage compiler；其余保持。
4. checker：identity + synthetic completion + real frozen A/B。
5. npm scripts / manifest / marker-only CI job。

## 迭代日志

### Iteration 0 — 2026-08-16

- Change: 建立 PR-5.19f 文档，冻结范围与 BASE_COMMIT。
- Reason: 5.19e closure 后，用户正式授权第一版 battle-stage decomposition，且明确只做 continuation-derived attack-blocked → make-damageable。
- Outcome: `AUTHORIZED`。

### Iteration 1 — 2026-08-16

- Change: 新增 `shared-solver/lib/strategic-battle-stage-prerequisite.js`（仅 `stageGoal=damageable`；completionPredicate 用 battle stage analyzer，要求 lethal/viable，不用 ATK 阈值）。`strategic-d2-search.js` 增加 opt-in `enableBattleStagePrerequisiteDecomposition`：仅在 continuation resume 且 child boundary 为 attack-blocked 时编译 stage prerequisite，root generic 行为不变；identity 与 generic battle-access 分离。新增 stage compiled/scheduled/executed/satisfied telemetry 与 checker/npm/manifest/CI。
- Reason: 5.19e 已让真实 child 执行 generic make-viable 但 50 expansions budget-exhausted；现在把 attack-blocked 拆成 make-damageable，复用同一 connector 与 hierarchy machinery。
- Evidence: 本机 `npm run check:strategic-battle-stage-prerequisite` PASS（synthetic kind/stageGoal/identity distinct/completion false→true/strict replay valid；lethal compile 返回 null）。`npm run qualification:strategic-battle-stage-prerequisite` PASS：control 5.19e candidate 保持 644+356 / calls 8 / generic child `cb70ef61` not-satisfied；5.19f candidate total 1000 / strategic 785 + battle-access 215 / calls 6 / root 5 / child 1 / stage compiled+scheduled+executed+satisfied = 1 / best `-903`。真实 stage child：`evilHero@MT5:2,8`，before attack-blocked（ATK 777 / DEF 1050 / margin -273 / damage null）→ satisfied in 9 expansions → after lethal（ATK 927 / DEF 1050 / margin -123 / damage 650524），chain 2 actions，`finalCreated=false`，`parentDependencyContinuationId` 存在。
- Outcome: `IMPLEMENTED`；**mechanism gate PASS，strong positive PASS**（attack-blocked → lethal battle-stage transition）；promotion gate 未通过。
- Next decision: 推 implementation commit，再用 `docs: record ...` marker 触发远端 full qualification；绑定远端 run/job 后收口。

### Iteration 2 — 2026-08-16 — qualification marker

- Change: 创建 qualification marker commit（`docs: record PR-5.19f battle stage prerequisite decomposition qualification`），用于触发 full qualification。
- Reason: 按项目流程，本地 synthetic + 5.19e/5.19f frozen A/B 对照 PASS 后，需要远端独立复现 stage transition evidence。
- Evidence: 本地 qualification PASS：control 5.19e 644+356 / generic child fail；candidate 5.19f 785+215 / calls 6 / stage compiled+scheduled+executed+satisfied 1 / attack-blocked → lethal。
- Outcome: marker 已就绪；远端 run/job 待绑定。
- Next decision: push 后绑定远端 run/job，并以非触发 docs commit 收口。

### Iteration 3 — 2026-08-16 — remote binding

- Change: 绑定远端 run `31950565107` / job `95173284026`，状态更新为 `VERIFIED`。
- Reason: 远端 full qualification 已成功复现 5.19f stage decomposition contract；需要按项目文档制度固定远端证据。
- Evidence: full run `31950565107` success；`qualification-strategic-battle-stage-prerequisite` job `95173284026` success。
- Outcome: `VERIFIED`（mechanism gate + strong positive 本地 + 远端双 PASS；promotion gate 仍未通过）。
- Next decision: 等待 review 判定是否 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`。

### Iteration 4 — 2026-08-16 — Closure

- Change: 冻结最终 verdict 为 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，并授权 PR-5.19g Retroactive Exact-Merge Continuation Re-entry。
- Reason: 用户复核 5.19f 远端 evidence：same source / same boundary 下 generic make-viable 失败而 make-damageable 9 expansions satisfied，attack-blocked → lethal；strong positive gate PASS，promotion 未通过。
- Evidence: 远端 run `31950565107` / job `95173284026` success；P0=0 / P1=0 / P2=0 blocking。
- Outcome: `APPROVED / CLOSED`。下一轮不做机械的 make-survivable，而先观察/修复 exact-merged continuation 的历史合法 descendant re-entry。
