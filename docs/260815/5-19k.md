# PR-5.19k — Survival Opportunity Edge Attribution

```ini
MILESTONE = PR-5.19k
STATUS = APPROVED / CLOSED
RESEARCH_VERDICT = EDGE-LEVEL-NAMED-SURVIVAL-OPPORTUNITIES-IDENTIFIED
REVIEW_STATUS = APPROVED / CLOSED
OBSERVATION_RESULT = EDGE-LEVEL-NAMED-SURVIVAL-OPPORTUNITIES-IDENTIFIED
DATE_STARTED = 2026-08-16
BASE_COMMIT = 186758547d14dc159b2fe40ab25c2d1b3b6d9890
FINAL_COMMIT = <提交实际存在后再填>
SCOPE = observation-only：归因 depth-2 lethal connector 真实 generated primitive edge 的逐边 survival 因果贡献，识别可命名的 discrete survival opportunity。不新增 connector，不改 predicate，不改 budget。
```

## 问题

PR-5.19j 已关闭为 Class B：

```text
source margin -617833
best margin   -150104
43/50 states improved
viable false
```

但 best chain 只能证明整条链的净效果，不能证明哪一个 primitive opportunity 是主要贡献。甚至 maxHP / minDamage / bestMargin 也未必来自同一个 state。

## 本轮只观察 actual generated edges

需要 `runDependencyConnector()` 增加纯观察 `edgeObserver`：

```text
成功 applyAction 后
seenExact suppression 之前调用
```

不影响 enqueue / seenExact。

接口：

```text
{
  expansion,
  depth,
  preState,
  action,
  postState,
  preExactStateKey,
  postExactStateKey,
  postAlreadySeen,
  chainBefore,
}
```

## 每条 edge 计算

```text
before/after:
  stage, HP, damage, survivalMargin

deltaHP
deltaDamage
deltaSurvivalMargin
```

```text
ΔsurvivalMargin =
(afterHP - afterDamage)
-
(beforeHP - beforeDamage)
```

并保存：

```text
action.kind / summary / fingerprint / floor / x / y / enemyId / tileId
resourceDelta: HP / ATK / DEF / MDEF / EXP
pre/post fingerprints
postAlreadySeen
```

## Aggregate

```text
edgesObserved
positiveSurvivalEdges
neutralEdges
negativeSurvivalEdges

positiveByActionKind
positiveUniqueActionTargets
topPositiveEdges
positiveOpportunityTargetsSeenAcrossMultipleSources
```

特别要分解当前 best chain 的逐边：

```text
source margin -617833
edge1 → margin ? delta ?
edge2 → margin ? delta ?
edge3 → margin ? delta ?
edge4 → margin -150104 delta ?
```

## Gate

继续复用 5.19j frozen candidate：

```text
total=1000 / calls=8 / per-call=50
depth-2 child 50 expansions
best=-903 / goal=false
```

edge attribution ON/OFF 语义完全一致。

机制 gate：

```text
至少一个真实 primitive edge deltaSurvivalMargin > 0
```

更强 gate：

```text
best chain 可逐边解释完整
```

## 迭代日志

### Iteration 0 — 2026-08-16

- Change: 建立 PR-5.19k 文档，冻结范围与 BASE_COMMIT。
- Reason: 5.19j closure 后，用户授权 observation-only 把 Class B 聚合结果进一步分解为真实 primitive edge 因果贡献。
- Outcome: `AUTHORIZED`。

### Iteration 1 — 2026-08-16 — observation result: named edge opportunities identified

- Change: `runDependencyConnector()` 增加只读 `edgeObserver`（成功 applyAction 后、seenExact suppression 前调用，不影响搜索）。新增 `shared-solver/lib/strategic-survival-edge-observer.js` 与 checker；对同一 depth-2 lethal child 记录每条 generated primitive edge 的 before/after battle stage、HP/damage/survivalMargin delta、resourceDelta、action identity，并汇总 positive/neutral/negative、by action kind、unique action targets、multi-source targets、top positive edges 与 best chain 逐边分解。新增 npm scripts / manifest。
- Reason: 回答哪些真实 primitive opportunity 对 evilHero survivalMargin 产生了离散正因果贡献。
- Evidence: 本机 ON/OFF frozen 1000 语义完全一致。真实 depth-2 child：edgesObserved 124；positive 42 / neutral 82 / negative 0；positiveByActionKind `battle=42`；positiveUniqueActionTargets 6。Multi-source stable targets：`skeletonPresbyter@MT5:3,10`（10 sources）、`devilWarrior@MT4:10,5`（9）、`skeletonKing@MT4:8,3`（8）、`devilWarrior@MT5:11,11`（6）、`skeletonKnight@MT5:1,11`（5）、`skeletonKing@MT5:8,11`（4）。Top edge：`devilWarrior@MT5:11,11` Δmargin `+279323`（HP -99802 / damage -379125 / DEF +100）。Best chain 已逐边分解：`skeletonKing@MT5:8,11 +90953` → `devilWarrior@MT5:11,11 +279323` → `changeFloor 0` → `skeletonKing@MT4:8,3 +97453`，最终 best margin `-150104`。
- Outcome: `IMPLEMENTED`；`OBSERVATION_RESULT = EDGE-LEVEL-NAMED-SURVIVAL-OPPORTUNITIES-IDENTIFIED`。最佳贡献不是单一机会，而是两个 HP-positive skeletonKing battle 与一个 damage-reduction devilWarrior battle 的复合。
- Next decision: 等 review 决定是否授权基于这些 named edge opportunities 的 discrete survival prerequisite compiler（PR-5.19l candidate）。

### Iteration 2 — 2026-08-16 — Closure

- Change: 冻结 5.19k 为 `APPROVED / CLOSED`，research verdict `EDGE-LEVEL-NAMED-SURVIVAL-OPPORTUNITIES-IDENTIFIED`；并授权 PR-5.19l Witness-Backed Discrete Survival Opportunity Prerequisite。
- Reason: 用户复核 edgeObserver timing、semantic target identity、best-chain decomposition 均正确；不再需要额外 full qualification。
- Evidence: fast regression `31960854561` success；本地 frozen evidence 已在 Iteration 1 写入。
- Outcome: `APPROVED / CLOSED`。下一轮把 failed generic connector 内部已经发现的 discrete positive opportunity 提升为可 strict-replay 的 prerequisite，不新增 connector call。
