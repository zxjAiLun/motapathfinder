# PR-5.19e — Hierarchical Call Allocation

```ini
MILESTONE = PR-5.19e
STATUS = APPROVED / CLOSED
RESEARCH_VERDICT = MECHANISM POSITIVE / NOT PROMOTED
REVIEW_STATUS = APPROVED / CLOSED
REPAIR_1_STATUS = VERIFIED
REPAIR_1_REVIEW = 2026-08-16 cloud REQUEST_CHANGES：released continuation 可能因 semantic merge 被重新 activate 成 orphan priority
REPAIR_1_COMMIT = 8bebf13
REPAIR_1_QUALIFICATION_MARKER = 6c5cbcc
REPAIR_1_REMOTE_CI_RUN = 31945583749
REPAIR_1_REMOTE_JOB = 95160936893
DATE_STARTED = 2026-08-16
BASE_COMMIT = 92c5c39f3035a1373ba73978cdafdccba5aff196
IMPLEMENTATION_COMMIT = 6a07169
QUALIFICATION_MARKER = 292a030
REMOTE_CI_RUN = 31942473357
REMOTE_JOB = 95153538807
FINAL_COMMIT = <提交实际存在后再填>
SCOPE = 在不增加 maxCalls=8 / per-call=50 / total-work=1000 的前提下，让成功 hierarchy 产生的 deeper prerequisite 能竞争现有 connector call budget。不新增 connector capability，不做 stage-specific prerequisite。
```

## 问题

PR-5.19d 已 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`。当前 frozen evidence 表明：

```text
P1 satisfied
→ continuation resume
→ P2 = cb70ef61 已编译
→ statusReason = call-cap-exhausted
→ P2 一次 call 都没获得
```

原因不是 attribution 或 connector 能力，而是：

```text
固定 8-call budget 被第一层 sibling attempts 全部耗尽；
hierarchy deeper child 没有获得执行机会。
```

## 本轮目标

在同样预算下改变 call 分配顺序：

```text
普通 sibling attempt
↓
某次 P(B) SATISFIED
↓
激活 active hierarchy priority
↓
普通新 sibling connector attempt 暂停抢占 call slot
↓
strategic search 继续推进
↓
lineage-safe descendant 返回 parent floor
↓
resume
↓
编译出 deeper prerequisite
↓
deeper prerequisite 获得下一个可用 call
↓
deeper feedback 后释放 priority
```

不是 `8 → 9/16`，也不是固定 `6+2` 切分，更不是永久预留第 8 个 call。

## 冻结范围

- `maxTotalSearchExpansions = 1000`、`connectorMaxCalls = 8`、`connectorMaxExpansions = 50`、`lazyDrainEvery` 均不调整。
- hierarchy priority 由真实 `P(B) SATISFIED` 动态激活；没有 success 时不预留 call。
- priority 只暂停新的 sibling connector call，不暂停 strategic expansion、lazy drain、continuation waiting/resume。
- release 条件：
  1. deeper prerequisite 得到一次真实 connector feedback
  2. parent 已 completable/reachable，无下一 prerequisite
  3. continuation blocked/unsupported
  4. 搜索整体 budget exhaustion
- 不新增 stage-specific connector。
- 不新增 budget、不读 known route、不碰 DP key / dominance / replay semantics。
- D3 仍 NOT AUTHORIZED。

## Telemetry

```text
rootLevelCalls
continuationDerivedCalls

hierarchyPriorityActivations
rootAttemptsDeferredForHierarchy

childPrerequisitesCompiled
childPrerequisitesScheduled
childPrerequisitesExecuted
childPrerequisitesSatisfied

maxHierarchyDepthAttempted
```

## 验收与拒绝门

| Gate | Evidence | Pass condition | Meaning |
| --- | --- | --- | --- |
| Synthetic scheduler | maxCalls=4 synthetic | S0 FAIL → S1 SATISFIED → child P2 获得 call 3；unrelated S2 不得抢占 | active hierarchy priority 生效 |
| Starvation boundary | synthetic | priority 激活时 strategic search 继续，continuation 可 resume | 不因 wait 卡死 |
| Release | synthetic | child feedback 后 release | 不永久 DFS |
| Real mechanism | frozen 1000 A/B | resume 后的 P2 获得真实 connector call | deeper prerequisite 可执行 |
| Promotion | same A/B | best > -903 或 terminalActionGenerated > 0 或 goalFound + strict replay | 默认 D2 晋级 |
| Frozen 5.19b/c/d | default scheduling | 关闭 hierarchy priority 时原数字不漂移 | 旧 frozen evidence 保持可复现 |

## 实施顺序

1. 冻结本文件。
2. `strategic-d2-search.js`：battle-access scheduler 增加 active hierarchy priority。
3. Telemetry 字段与 witness。
4. checker：maxCalls=4 scheduler synthetic + real 1000-work frozen contract。
5. npm scripts / manifest / marker-only CI job。

## 迭代日志

### Iteration 0 — 2026-08-16

- Change: 建立 PR-5.19e 文档，冻结范围与 BASE_COMMIT。
- Reason: 5.19d closure 后，用户正式授权 Hierarchical Call Allocation，并明确不通过增加 call/budget 解决。
- Outcome: `AUTHORIZED`。

### Iteration 1 — 2026-08-16

- Change: 新增 `shared-solver/lib/strategic-hierarchy-priority.js`（active hierarchy priority controller）。`strategic-d2-search.js` 在 battle-access mode 增加 opt-in `enableHierarchicalCallAllocation`：P(B) success 创建 continuation 后 activate priority；active 期间 strategic expansion 仍正常推进，但新 root/sibling connector attempt 暂缓；lineage-safe resume 编译出的 deeper prerequisite 优先获得剩余 call；child 获得真实 connector feedback 后 release。新增 root/child call、deferral、child compiled/scheduled/executed/satisfied、maxHierarchyDepthAttempted telemetry。新增 `check-strategic-hierarchical-call-allocation.js`、npm scripts、manifest 与 marker-only CI job。
- Reason: 5.19d 已证明 hierarchy 能续上并发现 P2，但 P2 在 8-call 全被 sibling 耗尽时从未执行；需要让 deeper child 在固定预算内获得一次真实 call。
- Evidence: 本机 `npm run check:strategic-hierarchical-call-allocation` PASS（synthetic priority：root success activate → sibling defer → child next call → feedback release → sibling resume）。`npm run qualification:strategic-hierarchical-call-allocation` PASS：candidate total 1000 / strategic 644 + battle-access 356 / calls 8 / rootLevelCalls 7 / continuationDerivedCalls 1 / childPrerequisitesCompiled 1 / Scheduled 1 / Executed 1 / Satisfied 0 / maxHierarchyDepthAttempted 1 / rootAttemptsDeferredForHierarchy 360 / best `-903` / terminal action 0 / goal false。child witness `hierarchyLevel=1`、`prerequisiteId=cb70ef61`、`status=not-satisfied`、`sourceExactStateFingerprint` 16 位。
- Outcome: `IMPLEMENTED`；**mechanism gate 本地 PASS**（deeper prerequisite 真实获得一次 connector call）；promotion gate 未通过。
- Next decision: 推 implementation commit，再用 `docs: record ...` marker 触发远端 full qualification；绑定远端 run/job 后收口。

### Iteration 2 — 2026-08-16 — qualification marker

- Change: 创建 qualification marker commit（`docs: record PR-5.19e hierarchical call allocation qualification`），用于触发 full qualification。
- Reason: 按 5.19b/5.19c/5.19d 流程，本地 mechanism gate PASS 后需要远端独立复现同一 frozen 1000-work contract。
- Evidence: 本地 qualification PASS：baseline 1000 / best `-903`；candidate 644+356 / calls 8 / rootLevelCalls 7 / continuationDerivedCalls 1 / childPrerequisitesExecuted 1 / maxHierarchyDepthAttempted 1 / best `-903` / terminal action 0。
- Outcome: marker 已就绪；远端 run/job 待绑定。
- Next decision: push 后绑定远端 run/job，并以非触发 docs commit 收口。

### Iteration 3 — 2026-08-16 — remote binding

- Change: 绑定远端 run `31942473357` / job `95153538807`，状态更新为 `VERIFIED`。
- Reason: 远端 full qualification 已成功复现 hierarchical call allocation frozen contract；需要按项目文档制度固定远端证据。
- Evidence: full run `31942473357` success；`qualification-strategic-hierarchical-call-allocation` job `95153538807` success。
- Outcome: `VERIFIED`（mechanism gate 本地 + 远端双 PASS；promotion gate 仍未通过）。
- Next decision: 等待 review 判定是否 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`。

### Iteration 4 — 2026-08-16 — Repair 1 Continuation Lifecycle-Safe Priority Activation

- Change: `createParentDependencyContinuation` 返回 `{continuation, created, lifecycle}`，不再返回裸 continuation。新 continuation：enqueue work + activate priority。merge 到 existing 时用 `shouldReactivateMergedParentContinuation(activeIds, parkedIds)` 判定：active/parked → 保留原 lifecycle，只做幂等 activate；completed/released → 只 merge，不 enqueue work，不 activate，并记录 `parentDependencyContinuationMergeReactivationBlocked`。caller 不再无条件 activate。checker 增加 completed-no-reactivation / active-preserved / parked-preserved lifecycle synthetic。
- Reason: review 指出已 release 的 continuation 若被另一个 source success merge 回来，会在没有 continuation work / child work 的情况下被重新激活，导致 root connector allocation 被 orphan priority 永久饿死。
- Evidence: 本机 `npm run check:strategic-hierarchical-call-allocation` PASS（lifecycle-safe merge 3/3）。`npm run qualification:strategic-hierarchical-call-allocation` PASS：frozen 1000-work 主机制仍保持 root 7 / child 1 / child executed 1 / best `-903`；real run 未出现 duplicate merge，`parentContinuationMergeReactivationBlocked=0`，由 synthetic 锁定该 branch。
- Outcome: `REPAIR_1_STATUS = IMPLEMENTED`；主 allocation 证据保持 `MECHANISM POSITIVE`；promotion 仍未通过。
- Next decision: 推 Repair 1 implementation commit，再用 `docs: record ...` marker 触发远端 full qualification；绑定远端 run/job 后收口。

### Iteration 5 — 2026-08-16 — Repair 1 qualification marker

- Change: 创建 Repair 1 qualification marker commit（`docs: record PR-5.19e Repair 1 lifecycle-safe hierarchy priority qualification`），用于触发 full qualification。
- Reason: 按项目流程，本地 lifecycle-safe merge synthetic 与 frozen 1000-work 主机制均 PASS 后，需要远端独立复现。
- Evidence: 本地 Repair 1 qualification PASS：candidate 644+356 / root 7 / child 1 / child executed 1 / maxHierarchyDepthAttempted 1 / best `-903`；lifecycle synthetic completed-no-reactivation / active-preserved / parked-preserved 3/3。
- Outcome: Repair 1 marker 已就绪；远端 run/job 待绑定。
- Next decision: push 后绑定远端 run/job，并以非触发 docs commit 收口。

### Iteration 6 — 2026-08-16 — Repair 1 remote binding

- Change: 绑定 Repair 1 远端 run `31945583749` / job `95160936893`，`REPAIR_1_STATUS = VERIFIED`。
- Reason: 远端 full qualification 已成功复现 lifecycle-safe priority contract；需要按项目文档制度固定远端证据。
- Evidence: full run `31945583749` success；`qualification-strategic-hierarchical-call-allocation` job `95160936893` success。
- Outcome: `REPAIR_1_STATUS = VERIFIED`；主 mechanism gate 与 lifecycle repair 均本地 + 远端双 PASS；promotion gate 仍未通过。
- Next decision: 等待 review 判定 5.19e 是否 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`；下一候选方向是 5.19f Battle Stage Prerequisite Decomposition。

### Iteration 7 — 2026-08-16 — Closure

- Change: 冻结最终 verdict 为 `MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，并授权 PR-5.19f Battle Stage Prerequisite Decomposition。
- Reason: 用户复核 `dev@129b7c0`：Repair 1 lifecycle-safe priority 与远端证据一致，主 frozen evidence 未漂移，真实 child 仍为 evilHero attack-blocked 且获得真实 50-expansion call。
- Evidence: Repair 1 远端 run `31945583749` / job `95160936893` success；P0=0 / P1=0 / P2=0 blocking。
- Outcome: `APPROVED / CLOSED`。下一轮 5.19f 第一版只做 attack-blocked → make-damageable，继续复用同一 hierarchy machinery 与同一 connector。
