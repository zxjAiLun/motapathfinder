# PR-5.19a — Dependency Access Attribution

```ini
MILESTONE = PR-5.19a
STATUS = IMPLEMENTED
LOCAL_CHECK = PASS（focused + qualification-1000）
REPAIR_1_STATUS = IMPLEMENTED
REPAIR_1_COMMIT = d064946240784a90e9a19a60c5045ac9b61697f5
REPAIR_1_REVIEW = 2026-08-15 cloud REQUEST_CHANGES：evilHero causal relevance NOT YET ESTABLISHED
REMOTE_CI_RUN = <Repair 1 qualification requested at d064946240784a90e9a19a60c5045ac9b61697f5；first-slice run 31876506225 job 94992941450 success>
DATE_STARTED = 2026-08-15
BASE_COMMIT = cedfdaffd5737c1e94f9b040d39376b18e9ca1fb
IMPLEMENTATION_COMMIT = d59ae4a9ffd11783cd90aed4e721da9a21bfaa9a
QUALIFICATION_MARKER = 8ff008a7f60b4091c4c72610b7604055a7534e40
FINAL_COMMIT = <提交实际存在后再填>
SCOPE = observation-only：对 PR-5.18e Repair 2 的 8 个 dependency attempts 做结构化 access attribution，解释 terminal-improving target T 为什么从未被 completionPredicate 满足；不修改搜索顺序、预算、vocabulary 或任何 solver 语义。
```

## 问题与已有证据

PR-5.18e 已关闭为 `VERIFIED / NOT_PROMOTED`。当前最强的可观察事实是：

```text
1000 total search work
8 feedback-aware dependency attempts
semantic dependency 只有 1 个
8 个 distinct source exact states
satisfied 0 / stateCreated 0 / globalBlockerAdvanced 0
```

因此 5.18e 排除的是“更努力地连接 T”这条支线在当前条件下的价值，而不是数学证明 T 不可达。现在需要回答的问题不是：

> 怎么更努力地连到 T？

而是：

> 为什么 dependency discovery 反复坍缩到同一个 T？  
> 从不同 source state 到 T 的第一个可解释 access boundary 是什么？

当前这仍是一个 **strong hypothesis**，不是已归因事实：

```text
T 的可达性依赖于当前 dependency model 没有显式表达的 access prerequisite。
```

5.19a 的任务就是把该假设变成可复核的 per-attempt evidence，或证伪它。

## 初始设计

只做观察器，不做 planner：

```text
runDependencyConnector
        ↓ 每个 expansion 只读采样
bestApproachStates（最接近完成 dependency 的 state）
        ↓ connector 停止后，仅对这些 state 做一次 reachability/battle/door/event 分析
firstUnresolvedAccessBoundary
        ↓
per-attempt attribution
```

目标 T 的接近度只用纯观察 distance，不参与搜索排序：

```text
same floor?
floor distance（project.floorOrder）
Manhattan distance（同 floor）
target POI visible?
target adjacent?
target action currently enumerable?
primitive chain depth
```

停止后对 top-K bestApproachStates 输出：

```text
attempt
source exact state
target T

best observed approach:
  floor / position
  primitive depth
  remaining distance if known

first unresolved access boundary:
  kind
  target
  evidence

classification:
  battle-unsurvivable
  missing-key
  closed-door
  event-condition
  flag-condition
  topology/changeFloor
  no-path-under-current-action-scope
  budget-incomplete
  unknown

proof strength:
  proven
  observed
  hypothesis
```

`budget-exhausted` 的尝试最多只能给 `observed` 或 `hypothesis`，不得写成根因；只有 frontier-exhausted 且边界证据完整时才能考虑 `proven`。

## 范围与非目标

### 本轮范围

- 给 `runDependencyConnector` 增加只读 observer hook，不改变 expansion 顺序、budget、predicate 或任何搜索语义。
- 实现 `buildDependencyAccessAttribution`：bestApproachStates 采样、boundary 分类、proof strength。
- 将 attribution 并入 `dependencyAttemptWitnesses` 或独立 `dependencyAccessAttribution` 输出。
- 新增 checker：synthetic distance/boundary 负控 + real 8-attempt attribution contract。
- 保持 blind：不读 known route、manual milestone、authored event order 或资源阈值。
- 若本轮产出明确 blocker，再考虑下一轮 5.19b；本轮不写 Access-Prerequisite Compiler。

### 不在本轮 / 未授权

- 不写 5.19b compiler，不产生新的 connector objective。
- 不修改 agenda、dominance、DP key、production segment solver、replay 语义。
- 不增加 total work、connector per-call budget、calls 或第三 vocabulary。
- 不把 known/manual route 作为搜索输入；只允许在 attribution 完成后作为事后 oracle 对照，并显式标记。
- 不进入 D3；D3 = NOT AUTHORIZED。

## 合同与不变量

- observation-only：attribution 必须对 `runStrategicD2Search` 的搜索图、统计、verdict 完全无影响。
- 所有 distance 是诊断量，不得进入 agenda、frontier 排序或 completionPredicate。
- attribution 必须逐 attempt 绑定 `attemptId / sourceExactStateFingerprint / semanticDependencyId / target`。
- `proof strength` 必须诚实：`budget-exhausted` 不能升级成 access root cause。
- 生成物默认 ignored；tracked 只放 checker、schema 与 milestone 文档。

## 验收与拒绝门

| Gate | Evidence | Pass condition | Meaning |
| --- | --- | --- | --- |
| No-semantic-change A/B | 同配置开/关 attribution | 搜索 stats、best、verdict byte-equivalent | 纯观察 |
| Distance correctness | synthetic controls | same-floor/manhattan/target-action 分类正确 | 诊断可信 |
| Boundary classification | synthetic door/battle/event/changeFloor controls | 每种 boundary 至少一个负控分类正确 | 分类可信 |
| Proof strength | synthetic budget/frontier cases | budget → observed/hypothesis；frontier+完整边界 → observed/proven | 不夸大 |
| Real 8-attempt attribution | frozen 1000-work dependency A/B | 8 条 attempt attribution、source/target 绑定、每 attempt 有 bestApproach 与 boundary | 产出可复核归因 |
| Blind contract | inputContract | knownRouteUsed=false，无 milestone/order/threshold | 不泄露 oracle |

## 实施顺序

1. 冻结本文件与 BASE_COMMIT。
2. 在 `strategic-dependency.js` 增加只读 observer 回调与 bestApproachStates 采样。
3. 新增 `strategic-dependency-attribution.js`：distance、boundary classifier、per-attempt report。
4. 在 D2 dependency mode 中挂接 observer，输出 `dependencyAccessAttribution`。
5. 新增 `check-strategic-dependency-attribution.js`：synthetic + real 8-attempt contract。
6. 重跑 frozen 1000-work A/B，确认 attribution 不影响搜索。
7. 汇总归因，决定是否授权 5.19b。

## 迭代日志

### Iteration 0 — 2026-08-15

- Change: 建立 PR-5.19a 轮次文档，尚未修改 solver。
- Reason: 用户接受 5.18e closure 并授权 5.19a observation-only attribution；在实现前冻结“strong hypothesis vs proven fact”的边界与输出合同。
- Evidence: BASE_COMMIT `cedfdaffd5737c1e94f9b040d39376b18e9ca1fb`；handoff 当前活动轮已更新。
- Outcome: `AUTHORIZED`；implementation/verification 尚未发生。
- Next decision: 从 bestApproachStates observer 开始，先证明纯观察不改变 frozen A/B。

### Iteration 1 — 2026-08-15

- Change: 新增 `shared-solver/lib/strategic-dependency-attribution.js`：observation-only `createDependencyAccessObserver`（top-K bestApproachStates，distance vector 不参与搜索）、`buildDependencyAccessAttribution`、`classifyFrontierBoundary`（battle-unsurvivable / missing-key / closed-door / event-condition / topology/changeFloor / no-path / budget-incomplete）。`strategic-dependency.js` 的 `runDependencyConnector` 增加只读 observer 回调；`strategic-d2-search.js` 在 dependency-derived 模式挂接 observer 并输出 `dependencyAccessAttributions`，任何 attribution 异常都被吞掉且不得影响搜索。新增 `check-strategic-dependency-attribution.js`；注册 npm scripts、solver manifest 与 marker-only qualification job。
- Reason: 按 5.19a 冻结合同，先给 Repair 2 的 8 个 feedback-aware attempts 做结构化 access attribution，并证明 observation-only 不改变 frozen A/B。
- Evidence: `check:strategic-dependency-attribution` PASS：synthetic distance/budget/battle/door/event/changeFloor 分类全过；真实 64-expansion on/off A/B 的 calls/total/stats/best 完全一致。`qualification:strategic-dependency-attribution` PASS：1000-work frozen A/B 产出 8 条 attribution，8 distinct source fingerprints、1 semantic dependency、calls 8。`check:strategic-d2-search`、`check:strategic-dependency-connector`、`check:manifest` PASS。
- Outcome: observation-only 实现完成。真实 8-attempt 归因初版为：5 个 `budget-incomplete`（proofStrength hypothesis）、3 个 `battle-unsurvivable`（proofStrength observed，boundary 为 `evilHero@MT5:9,10`）。还不能把 battle boundary 推广到全部 8 次尝试；budget-exhausted 尝试仍保持 hypothesis。待远端 qualification 后绑定。
- Next decision: 推远端 qualification；若复现，则按本轮 gate 进入 5.19b 设计授权讨论，不直接写 compiler。

### Iteration 2 — 2026-08-15

- Change: 推送 qualification marker `8ff008a` 并绑定远端 CI run `31876506225` / job `94992941450`。
- Reason: 需要远端独立复现 observation-only no-semantic-change 与 8-attempt attribution contract。
- Evidence: 远端 full run `31876506225` success；`qualification-strategic-dependency-attribution` 实际执行并 success。远端日志复现 on/off calls 4/4、total 144/144，以及 qualification：total 1000 / strategic 701 + dependency 299 / calls 8 / semantic 1 / distinct sources 8 / boundaryKinds 5×budget-incomplete + 3×battle-unsurvivable。
- Outcome: PR-5.19a observation-only 本地+远端双证据完成；本轮仍不宣称 T 不可达，也不直接授权 compiler。
- Next decision: 等待 review 确认归因是否足以进入 5.19b 设计。

### Iteration 3 — 2026-08-15

- Change: PR-5.19a Repair 1（Target-Relevance / Access-Cut Attribution）。`strategic-dependency-attribution.js` 增加 `analyzeBoundaryTargetRelevance`：对每个 bestApproach state，用当前真实 walk-reachable component 作为 seeds，在只读 static-grid 拓扑观察图上做 0-1 BFS（普通格 cost 0，enemy/door/event/changeFloor cost 1），输出 `minAccessBlockers / targetSideReachableIfRemoved / reducesTopologicalBlockerDistance / onMinimumBlockerPath / separatesCurrentComponentFromTarget / minimumPathBlockers`。`buildDependencyAccessAttribution` 对当前 boundary 与 nearbyBoundaries 逐一分析，并输出最相关的 `targetRelevantBoundary`。checker 增加 relevant/irrelevant access-cut synthetic controls。
- Reason: 云端 review 指出 firstUnresolvedAccessBoundary 按 Manhattan 最近选择，只能证明 evilHero 是“附近 observed boundary”，不能证明它位于 current component → I1016 的 access path 上。
- Evidence: 本机 `check:strategic-dependency-attribution` PASS；synthetic corridor 中 enemy on path 得到 minAccessBlockers=1 / onMinimumBlockerPath=true / separates=true，irrelevant enemy 全 false。`qualification:strategic-dependency-attribution` PASS；8-attempt targetRelevanceSummary：8/8 onMinimumBlockerPath、8/8 reducesTopologicalBlockerDistance、0/8 separates。minAccessBlockers 为 3/3/3/3/2/3/2/2。
- Outcome: 关键修正：3 个 frontier-exhausted attempt 中，原来的 nearest boundary `evilHero@9,10` 不在 minimum blocker path 上；target-relevant boundary 实际是 `evilHero@2,8` 与 `redKing@4,7`（2 blockers）。5 个 budget-incomplete attempt 的 nearby `evilHero@9,10` 在一条 3-blocker path 上（`evilHero@9,10` + `skeletonKnight@10,8` + `demonPriest@8,7`），但没有单一 boundary 形成 cut（separates=0）。这比 5.19a 初版更接近真实 access chain，但仍不宣称 causal proven。
- Next decision: 推远端 qualification；若复现，则据此讨论 5.19b 的 narrow access-prerequisite 设计，而不是直接实现。

## 最终实现

- `shared-solver/lib/strategic-dependency-attribution.js`：attribution schema、distance metrics、bestApproachStates observer、boundary classifier、per-attempt report。
- `shared-solver/lib/strategic-dependency.js`：`runDependencyConnector` 只读 observer 回调，observer 异常不得影响搜索。
- `shared-solver/lib/strategic-d2-search.js`：dependency-derived 模式挂接 observer，输出 `dependencyAccessAttributions`；`enableDependencyAccessAttribution=false` 可关闭。
- `shared-solver/check-strategic-dependency-attribution.js`：synthetic metrics/boundary controls、on/off no-semantic-change control、`--qualification-1000` 8-attempt contract。
- `shared-solver/package.json` 与 CI workflow 注册新检查。

## 验证与证据

```text
command: npm run check:strategic-dependency-attribution
result: PASS (exit 0)

synthetic distance: sameFloor / manhattan / targetAdjacent / targetActionAvailable 全对
boundary classifier:
  budget-exhausted        -> budget-incomplete / hypothesis
  lethal battle           -> battle-unsurvivable / observed
  yellowDoor + no key     -> missing-key / observed
  scripted event tile     -> event-condition / observed
  cross-floor target      -> topology/changeFloor / observed
no-semantic-change control:
  attribution on/off 的 calls/total/stats/best/stoppedReason 完全一致
```

```text
command: npm run qualification:strategic-dependency-attribution
result: PASS (exit 0)

frozen 1000-work dependency A/B:
  calls 8 / attempts 8 / distinct source fingerprints 8 / semantic dependencies 1
  boundaryKinds:
    budget-incomplete    ×5（proofStrength hypothesis）
    battle-unsurvivable  ×3（proofStrength observed，boundary evilHero@MT5:9,10）
```

## 结果与决定

当前状态：`IMPLEMENTED`，Repair 1 本地 qualification 通过；远端待绑定。

Repair 1 已经回答了本轮核心问题：

- `evilHero@MT5:9,10` 不是所有 attempt 的 target-relevant boundary。
- 3 个 frontier-exhausted attempt 的 minimum blocker path 实际是 `evilHero@MT5:2,8` + `redKing@MT5:4,7`。
- 5 个 budget-incomplete attempt 的 nearby `evilHero@MT5:9,10` 位于一条 3-blocker path：`evilHero@9,10` + `skeletonKnight@10,8` + `demonPriest@8,7`。
- 所有 8 个 attempt 的 target-relevant boundary 都在 minimum blocker path 上，但没有单一 boundary 把 current component 与 T 完全切开（separates=0）。

因此当前证据支持：T 的 access 依赖不是单点 cut，而是短 blocker path；5.19b 若授权，应编译“沿 minimum blocker path 的下一组 access prerequisite”，而不是只针对 nearest evilHero。该结论仍是 topological observation，不是动态 proven root cause。

## 已知限制与不宣称

- 本轮不宣称已证明 T 不可达；budget-exhausted attempt 仍只是 search-incomplete。
- `evilHero@MT5:9,10` 只在 5 个 budget-incomplete attempt 的 nearby boundary 中位于 minimum blocker path；在 3 个 frontier-exhausted attempt 中不是 target-relevant boundary。
- target relevance 是 static-grid counterfactual，不是动态 causal proof；separates=0 说明需要多 blocker path 解释。
- bestApproachStates 是诊断采样，不是 connector search 的最优性证明。
- attribution 完全不改变搜索，因此也不改变 5.18e 的负结论。

## 下一授权 gate

- 只有本轮的 per-attempt attribution 产出可复核 access boundary 后，才允许设计 5.19b Access-Prerequisite Compiler。
- 5.19b 必须继续使用离散 `T → blocker B → prerequisite P → completionPredicate(P)`，不得退回 scalar objective。
- 通过前不进入 dominance 或 D3。
