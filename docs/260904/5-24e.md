# PR-5.24e — Counterfactual Resource-Investment Repair Generation

```ini
MILESTONE = PR-5.24e
STATUS = IN_PROGRESS / REAL_RUN_2_COMPLETED_CF_HISTORIES_NO_USEFUL_PROGRESS
DATE_STARTED = 2026-09-04
BASE_COMMIT = 693d172df2c00b5e7d2628965ec684844930835c
SCOPE = 当正常省血型 repair histories 全部无法推进下游时，自动生成投资型 counterfactual 子目标，允许短期付出资源代价换取能力提升，并由规范 DP 执行产生真实新历史
```

## 问题与已有证据

PR-5.24d 终审（Real Run 2）确立了重要科学结论：
1. **多样性数量与调度机制不是瓶颈**：单次真实 anchor 重新扩展即保留了 16 条不同历史，且调度器成功实施了完全独立的下游探测试算（每条 200 步扩展）；
2. **瓶颈在于历史生成的同一性**：这 16 条历史在通用下游目标度量下全部表现为 `NO_MEASURABLE_PROGRESS`（0/16 推进）。原因在于现行 DP 目标与前沿保留机制默认均为“省血型”（血量优先、规避消耗），生成的候选历史全都在同一类保守解空间中打转，未生成愿意付出短期代价（扣血、花钱、耗钥匙、绕路）换取关键长期资源（ATK/DEF/MDEF/EXP/升级/装备/关键门开放）的反常识前史。

PR-5.24e 目标：当正常省血路线全部失效时，自动识别并生成具象的投资型 Counterfactual 子目标，通过规范 DP 搜索物化为全新的可行前史。

## 范围与非目标

本轮范围：
- 新增独立模块：`shared-solver/lib/counterfactual-repair.js`；
- 核心接口：`buildCounterfactualRepairIntents({ simulator, startCandidates, triggerFailure, failedSegment, candidateLimit })`；
- 投资机会定义（Investment Trade-off）：必须存在持久收益（ATK/DEF/MDEF/HP上限机会/EXP/等级/装备/物品/不可逆阻挡消除），并允许代价（扣当前HP/花钱/消耗钥匙）；
- Pareto 支配与类型覆盖：对收益维度（更大越好）与代价维度（更小越好）实施严格无权重 Pareto 支配筛选，并在非受控维度上按类别覆盖（stat, exp/level, equipment, item/resource, path/unlock）；
- 严禁直接伪造历史：scanner/evaluator 的 `record.after` 仅作为 candidate-generation evidence，必须将 intent 转化为具象的子目标（Concrete Subgoal），由规范 DP / 仿真器再次真实执行产生规范状态与路线；
- 触发条件严格受控：仅当当前深度至少 1 条真实历史完成 probe、且 `positiveProgressHistories === 0`、全局 stop 为 null 且属于信任的失败类时触发；
- 单轮限制（One Round Only）：第一版仅允许 1 轮 counterfactual generation，不进行递归展开；
- 新门禁套件：`shared-solver/check-counterfactual-repair.js`（G24-A 至 G24-F）。

非目标：
- 严禁重新开启旧 `failure-intent ranking`（不按 intentScore 重新排序已有候选）；
- 严禁使用加权评分（scalar intent score）或 bandit/UCB；
- 不修改 PR-5.24d 的假说调度、独立探测试算与状态身份契约；
- 不增加全局预算（30s / 50k / 2048MB 安全熔断保持不变）；
- 严禁人工 route hint 或 OnlyUp 专属特殊硬编码；
- 严禁扩大 candidateLimit / backtrackCandidateLimit。

## 初始设计

1. **机会扫描与 Trade-off 提取**：
   在回溯锚点处，从起始候选出发枚举前瞻动作，评估 `before` 到 `after` 的数值变化。过滤纯损失状态，仅提取产生明确能力增益（Gain）的投资机会。
2. **Pareto 剪枝与类别分散**：
   在 $(gain, cost)$ 空间计算 Pareto 支配集。消除被完全支配的劣质机会，并在保留集中按增益类型选取最多 `backtrackCandidateLimit` 个代表。
3. **具象 Subgoal 合成**：
   为选中的机会构造具体的目标断言（例如 `floorId`、`minHero`、`equipmentIncludes` 或 `removedTiles`），附带允许该动作链的 `actionPolicy`。严禁生成空目标 `{ type: "adaptiveResourceIntent" }`。
4. **规范 DP 实测物化（Realization）**：
   在父 wave 预算内，以 200 exp / 2000 ms 作为 realization probe，通过 `runSegmentAgainstFrontier()` 真实执行该子目标段，将产出的新状态作为新的 repaired frontier 输入后续回放与调度器。

## 验收与拒绝门 (G24)

| Gate | Evidence | Pass condition |
| --- | --- | --- |
| **G24-A** 扣血换经验投资 | 合成 fixture | 省血路径 HP 100 无法通关；投资路径扣血打怪（HP 100 $\to$ 70, EXP 0 $\to$ 10），生成投资 intent 并经规范 DP 物化为真实状态 |
| **G24-B** 投资解锁下游段 | 合成 fixture | 正常省血历史全无进展；由投资 intent 产出的前史在下游成功打通门禁，达成顶层 `FOUND === true` |
| **G24-C** 正常路径优先保持 | 合成 fixture | 当正常修复历史已具备正向进展（`WITHIN` 或 `ADVANCE`）时，counterfactual generation 严禁触发（`executions === 0`） |
| **G24-C2** 正常票证首轮全完备门控 | 合成 fixture | 必须等待首波所有 normal tickets 均完成至少 1 次 probe（`probeCount >= 1`）且全无进展后方可触发 CF；首轮未完备绝不抢跑 |
| **G24-C3** 空前沿占位符阻断门控 | 合成 fixture | 当 normal anchor 重扩产生空前沿（`merged=[]`）并退化为 placeholder ticket（`anchorOutputStateKey=null`）时，严禁触发 CF（`triggered=false, intentsGenerated=0`） |
| **G24-D** Pareto 无权重支配 | 单元断言 | 机会 A（cost 20, atk +2）支配 B（cost 40, atk +2），C（cost 30, atk +4）不被支配；断言仅保留 A 与 C，严禁标量加权排序 |
| **G24-E** 非纯属性通用类型 | 合成 fixture | 验证装备获取（equip）或开门/钥匙/路径解锁（path unlock）场景，证明机制非“专职刷怪” |
| **G24-F** 有界性与不递归 | 架构断言 | intent 数量 $\le$ backtrackCandidateLimit，candidateLimit 未膨胀，单深度仅执行 1 轮 counterfactual 展开 |
| **G24-G** 失败完备性凭证验证 | 合成 fixture | Subcase G1：失败段因预算耗尽未完全搜索（`candidateSliceSearchComplete=false`），CF 拒绝触发；Subcase G2：失败段具备权威规范完备证明（`candidateSliceSearchComplete=true`），CF 正常触发 |
| **G24-H** 通用物品与钥匙 Pareto 支配 | 单元断言 | 验证多属性及通用 inventoryCost / inventoryGain 分量支配；验证纯钥匙拾取机会（0 cost, yellowKey +1）作为资源投资机会被正确识别 |
| **G24-I** 跨楼层具象目标规范物化 | 合成 fixture | 验证跨楼层动作生成具象目标时 `allowedFloors` 包含所有相关楼层且去重，并通过真实规范 DP 物化到达目标楼层 |
| **G24-J** 跨起点 Pareto 隔离 | 单元断言 | 候选 A（HP 1000, cost 20, atk +2）与候选 B（HP 30, cost 10, atk +2）基线不同；严禁跨起点相互支配删除，A 与 B 投资机会均必须保留 |
| **G24-J2** 同起点 Pareto 支配有效 | 单元断言 | 同一候选 A 上 A1（cost 20, atk +2）与 A2（cost 40, atk +2），A1 正常支配 A2，Pareto 筛选在同起点内严格生效 |
| **G24-J3** 二维轮询覆盖度 | 单元断言 | 起点 A 有 5 个机会，起点 B 有 2 个，起点 C 有 2 个，`candidateLimit=3` 时通过 2D round-robin 确保选出 3 个且 A, B, C 各有代表，不被单一起点垄断 |
| **G24-K1** Primary 分批准入顺序 | 合成 fixture | 8 条保留正常历史，`candidateLimit=4` 下仅前 4 条（N0-N3）优先准入探测试算；在 CF 触发时，次级 4 条（N4-N7）仍处于 deferred 状态，未生成 active ticket |
| **G24-K2** Primary 进展优先保省血 | 合成 fixture | 当 Primary 正常历史产生正向进展（`WITHIN_SEGMENT_PROGRESS`）时，CF 修复严格不触发（`triggerReason = "normal-primary-progress"`），次级正常历史随后正常准入 |
| **G24-K3** CF 无效自动恢复次级正常 | 合成 fixture | Primary 正常历史全部无进展，CF 触发但产出 0 有效进展时，工作守恒机制自动恢复次级正常分支（N4-N7 获得探测试算） |
| **G24-K4** 有效 CF 原生延续通关 | 合成 fixture | Primary 全部无进展，CF 首轮试探达成 `WITHIN_SEGMENT_PROGRESS`，次级正常历史保持 deferred，原生 continuation 调度器直接发放第二轮延续并达成 `FOUND === true` |
| **G24-K5** 空间无增宽保持 | 架构断言 | `candidateLimit`、`backtrackCandidateLimit`、retained descriptor count、前沿大小均严格未变，证明仅重排准入顺序而非增宽搜索空间 |
| **G24-K6** Primary 未完备保持 Fail-Closed | 合成 fixture | Primary 探测试算受预算截断（如 N3 预算不足 probeCount=0）时，CF 严格不触发，次级正常分支严格不准入 |
| **G24-K7** CF 延续失败归还延期工作与完备性权能 | 合成 fixture | S1：CF 延续试探虽确定性完成但未 FOUND 时，次级延期正常分支（N4-N7）全额恢复准入并完成首轮探测；S2：当次级正常存在未探测工作时，全局预算截断绝不误判为 `exhausted`（`depthExhausted === false`）；S3：恢复准入的次级正常分支若具正向进展，无差别获得延续试探并可达成 `FOUND === true` |

## 迭代日志

### Iteration 1 — 2026-09-04

- 任务启动：继承 PR-5.24d 科学结论（多样性已就绪、瓶颈在历史生成），创建 PR-5.24e 里程碑文档；
- 模块构建：实现 `shared-solver/lib/counterfactual-repair.js`（Pareto trade-off 支配过滤、类别覆盖、具象子目标生成）；
- 求解器接入：在 `shared-solver/lib/segment-dp.js` 中接入受控触发门控（正常路径无进展、trusted 失败类、未中断）与规范 DP 物化路径（通过 `runSegmentAgainstFrontier` 真实搜出 `intentFrontier`，再由 anchor 扩展产出新候选并交由 PR-5.24d 调度器试探）；
- 门禁开发：编写 `shared-solver/check-counterfactual-repair.js`，实现 G24-A 至 G24-F 六项独立门禁；
- 门禁套件实测：
  - G24-A & B：成功演示扣血打怪（HP 100 $\to$ 70, EXP 10, LV 2, ATK 10）经规范 DP 实测物化，成功打通原本被卡死的下游 seg2，交付顶层 `FOUND === true`；
  - G24-C：正常省血路径已具备进展时，counterfactual generation 触发数为 0，零预算浪费；
  - G24-D：无权重 Pareto 支配准确剔除高代价同收益劣解（B），保留 A 和 C；
  - G24-E：非属性通用类型（装备与开门路径解锁）成功提取并生成具象约束目标；
  - G24-F：intent 数量受控 $\le$ candidateLimit，严格单轮展开不递归；
- 验证套件实测：
  - G24 Fast 模式 10 次重复测试（`check-counterfactual-repair.js ×10`）：10/10 全部通过（每次 <1s）；
  - PR-5.24c/d 快速门禁套件（`check-budgeted-repair-scheduling.js --fast`）：全绿通过；
  - 全量静态与核心回归（12/12 包含 manifest、no-tower-solver-js、solver-job、launcher、route-free-state、tower-ir-shadow、candidate-key-smoke、adaptive:contract、checkpoint-repair、core、resource-intent:contract、counterfactual-repair）：全部通过；
- 交付与下一步：第一批交付（模块+求解器接入+G24套件+回归）完成，提交并推送，停止等待云端审查；云端审查通过后授权执行单次真实 MT1→MT4 运行。

### Iteration 1 Repair 1 — 2026-09-04

- **四大生产级契约修复**：
  1. **Normal-first 严格完备触发门禁**：不仅要求下游 probe 完成数 $\ge 1$，更强制要求当前深度首波所有的 normal tickets 均完成至少 1 次探测试算（`normalDepthTickets.every(t => t.probeCount >= 1)`），且全无正向推进（`positiveInitial === 0`）时才允许触发 CF 修复，杜绝未探测试算的正常分支被抢跑旁路；
  2. **失败完备性权威凭证要求**：失败断言不得依赖粗粒度字符串匹配，必须具备权威规范完备性证明 `isReplayDeterminatelyComplete(failedExecution, ...)`。未完全搜索（`candidateSliceSearchComplete === false`）或中间中断的失败严禁触发 CF 探索；扩展信任失败类型覆盖 `floor-scope-mismatch`；
  3. **通用分量式 Pareto 资源记账**：实现泛化 `diffInventory(before, after)`，支持任意物品/钥匙（yellowKey, blueKey, redKey 等）的分量支配比较，严禁各品类硬编码；将 `moneyGain` 纳为正向收益维度，`moneyCost` 纳为代价维度；支持纯资源拾取（如拾取钥匙、钱币）作为正向投资机会被提取；
  4. **跨楼层具象子目标与状态合法性修复**：跨楼层转移（如 `changeFloor` / `floorFly`）生成的具象目标不再非法尝试移除目标楼梯 tile；自动收集并去重涉及到的前后楼层列表写入 `actionPolicy.allowedFloors`；规范格式化 `allowChangeFloors` 为 `[floorId:x,y]`，保障跨楼层 DP 物化规范执行。
- **G24 门禁套件扩展**：
  - 新增 G24-C2（首波 normal tickets 未完备则阻断 CF，全部探测后无进展则放行）；
  - 新增 G24-G（G1: 不完备搜索阻断 CF，G2: 具备完备证明放行 CF）；
  - 新增 G24-H（分量式 inventoryCost 支配验证，纯钥匙拾取投资提取）；
  - 新增 G24-I（跨楼层具象目标规范 DP 物化成功，到达目标楼层）。
- **实测验证结果**：
  - G24 fast 模式 10 次重复测试：10/10 全部通过；
  - PR-5.24c/d 快速门禁套件：全绿通过；
  - 全量 12 项静态与核心回归测试：全部通过。
- **当前状态**：Repair 1 完整实现并自测通过，推送至 `origin/dev`，等待云端审查通过后授权执行单次真实 MT1→MT4 诊断运行。

### Iteration 1 Repair 1a — 2026-09-04

- **两大 Candidate-Generation 关键 P1 修复**：
  1. **恢复真实 Normal History 触发守卫（P1-1）**：
     - 在 `shared-solver/lib/segment-dp.js` 中保留 `normalFirstRoundComplete`（首波所有 normal tickets 均完成至少 1 次 probe）的同时，重新强化真实历史断言：
       `const realNormalProbedTickets = normalDepthTickets.filter(t => t.probeCount >= 1 && t.anchorOutputStateKey != null);`
     - 严格要求 `realNormalProbedTickets.length >= 1`；当且仅当存在至少一条真正完成了下游探测试算且具备有效状态键的真实正常历史、且正向推进均为 0 时，才允许启动 Counterfactual 修复；
     - 当正常重扩产生空前沿退化为 placeholder 票证（`anchorOutputStateKey == null`）时，严禁触发 CF 探索，封死未经验证的 zero-real-history 旁路；
  2. **严格隔离不同 Start State 的 Pareto 支配（P1-2）**：
     - 在 `shared-solver/lib/counterfactual-repair.js` 中彻底消除全局跨起点的相对 delta 混洗与虚假支配；
     - 按 `startCandidate` 分组，在单一起点内分别执行无权重 `filterParetoOpportunities(oppsForCandidate)`；
     - 避免不同 baseline 的“便宜相对代价”（例如在 HP 30 下扣 10 血）错误淘汰另一有效 baseline 上的投资机会（例如在 HP 1000 下扣 20 血）；
     - 采用二维确定性轮询（2D Round-Robin across origins & kinds）：
       - Round 0：Origin A（第 1 类别首选）、Origin B（第 1 类别首选）、Origin C...
       - Round 1：Origin A（第 2 类别/下个机会）、Origin B...
       - 在各起点内部轮询 5 类类别（stat, exp/level, equipment, item/resource, path/unlock），在全局起点间公平轮询，严格约束 `selected.length <= candidateLimit`，不增宽且防止单起点垄断候选配额。
- **G24 门禁套件扩充与实测**：
  - 新增 **G24-C3**（空前沿 placeholder 具备 `probeCount=1` 但 `anchorOutputStateKey=null`，CF 严格阻断 `triggered=false, intentsGenerated=0`）；
  - 新增 **G24-J**（跨起点 Pareto 隔离：Origin A 与 Origin B 的相对投资机会互不消除，两者全数保留）；
  - 新增 **G24-J2**（同起点内 Pareto 支配正常工作：A1 支配 A2，次优解被精准剪除）；
  - 新增 **G24-J3**（多起点 2D 轮询覆盖：A(5), B(2), C(2) 在 limit=3 下各得一个代表位，不被 A 独占）。
- **实测验证**：
  - G24 fast 模式 10 次重复测试：10/10 全部通过；
  - PR-5.24c/d 快速门禁套件（`check-budgeted-repair-scheduling.js --fast`）：全绿通过；
  - 全量 12 项静态与核心回归测试：全部通过。
- **当前状态**：Repair 1a 生产级修复完毕，推送到 `origin/dev`，等待云端审查通过后直接授权单次真实 MT1→MT4 诊断运行。

### Real MT1→MT4 Diagnostic Run 1 — 2026-09-04

- **运行环境**：
  - HEAD: `1f3aa1f3383f49148366d471cb0ed266c31963d1`
  - Working Tree: Clean
  - Node: `v24.18.0`
  - OS: `win32 x64 10.0.26200` (Windows 11)
- **冻结配置**：
  - `searchIntent: "adaptive-feasible"`
  - `enableFailureBacktracking: true`, `adaptiveBacktrackDepth: 3`
  - `enableBudgetedRepairScheduling: true`, `enableBudgetedRepairContinuation: true`
  - `milestoneFrontierResourceDiversity: true`
  - `enableFailureIntentRanking: false`, anchor/replay ranking unset
  - `budgetScope: "global-run"`, `segmentExecutionMode: "isolated-process"`
  - `maxRuntimeMs: 30000`, `maxExpansions: 50000`, `maxRssMb: 2048`（安全熔断器）
  - 探测试算预算保持默认：first probe 200 exp / 2000 ms；continuation 400 exp / 4000 ms；candidateLimit 8
- **实测执行过程与数据**：
  1. **主线搜索**：
     - MT1 $\to$ MT2 成功
     - MT2 $\to$ MT3 成功
     - MT3 $\to$ MT4 失败：`failureClass = "floor-progress-blocked"`，`candidateSliceSearchComplete = true`（规范完备失败证明通过）
  2. **深度 1 回溯（锚点 `mt2-to-mt3`）**：
     - 锚点重扩产生 16 条真实保留历史（`h-d1w0h0` 至 `h-d1w0h15`），`anchorOutputStateKeyPresent = true`；
     - 调度器依次执行首轮探测试算：
       - 前 13 条假说（`h0` 至 `h12`）各自获得了下游 probe（大部分消费 200 exp / ~2000-3800 ms wall），在通用目标投影下均判定为 `NO_MEASURABLE_PROGRESS`（`stopReason: "probe-limited"` / `"incomplete"`）；
       - 累计搜索耗时达到 ~29,000 ms，触碰全局 30s 墙钟限制（`stoppedReason: "time-limit"`）；
       - 后 3 条假说（`h13`, `h14`, `h15`）因全局预算耗尽进入 `stopReason: "global-budget-exhausted"`，`probeCount = 0`；
     - **门禁检验**：
       - `normalFirstRoundComplete = false`（13/16 探测，3 条因全局超时 probeCount=0）；
       - `globalStopBeforeCf = "time-limit"`（非 null）；
       - 判定守卫生效：Counterfactual generation 严格阻断（`triggered = false, intentsGenerated = 0`），杜绝在首波 normal 未完全探测试算或已超时的状态下抢跑。
  3. **深度 2 回溯（锚点 `mt1-to-mt2`）**：
     - 全局已处于 `time-limit` 停止状态，`anchorExpandedCandidates = 0`，退化占位符 `h-d2w0`（`probeCount = 0, stopReason: "time-limit"`）；
     - CF 保持未触发。
- **核心对照指标**：
  - `normalPositiveProgressHistories`: 0
  - `counterfactualPositiveProgressHistories`: 0
  - `normalBestProgressClass`: `"NO_MEASURABLE_PROGRESS"`
  - `counterfactualBestProgressClass`: `"none"`（未触发）
  - `found`: false
  - 全局消耗：3,405 expansions，29,555 ms wall，峰值 RSS 336.9 MB（安全熔断器内）
- **P2 观测项**：
  - `CONCRETE_GOAL_POST_SELECTION_SLOT_WASTE = NOT_OBSERVED`（CF 触发前已被全局超时截断）。
- **Single Primary Verdict**：
  `PRIMARY_VERDICT = GLOBAL_RESOURCE_LIMITED_BEFORE_COUNTERFACTUAL_EVALUATION`
  （全局 30s 墙钟在深度 1 首波 16 条 normal histories 探测中途（第 13 条）耗尽，正常首轮未完备且 globalStop 为 time-limit，按生产契约严格阻断 CF 探索；不得对 counterfactual capability 下结论）。

### Iteration 2 — 2026-09-04

- **核心架构动机（解决 Admission Starvation）**：
  - Real Run 1 证实当前 `candidateLimit = 8` 在回溯阶段默认生成 `backtrackCandidateLimit = 16` 条保留历史；
  - 深度 1 首波 16 条 normal histories 的独立下游试探在第 13 条时即耗尽 30s 墙钟，导致次级正常的 3 条历史未 probe，进而将 Counterfactual Fallback 完全饿死；
  - 核心原则：**默认先充分尝试省血路线（Primary Tranche），但不应要求把回溯阶段扩展出来的所有 secondary conserve variants 全部试烂之后才允许尝试投资策略**；通过改变准入顺序（Admission Order）而非延长全局超时来解决饿死问题。
- **生产实现方案** (`shared-solver/lib/segment-dp.js`)：
  1. **Primary Normal Tranche 定义与物化分批**：
     - `primaryNormalLimit = Math.max(number((config && config.candidateLimit) || ..., 4), 4)`；
     - 锚点重扩后，`allHistoryDescriptors` 严格划分为 `primaryNormalDescriptors`（前 `primaryNormalLimit` 条，如 8 条）与 `secondaryNormalDescriptors`（剩余条数，如 8 条）；
     - 次级描述符暂存在 `deferredNormalDescriptors` 中，**严禁**立即生成 `probeCount=0, PROBE_PENDING` 的 active ticket，彻底避免卡死首轮全完备屏障；
  2. **Primary 首轮完备与分支准入决策**：
     - Primary 正常分支先行获得独立 200 exp / 2000 ms 下游探测试算；
     - 若 Primary 产生任何正向进展（`primaryPositiveProgressHistories > 0`）：CF 修复严格不启动（`triggerReason = "normal-primary-progress"`），次级正常分支随后准入继续省血探索；
     - 若 Primary 全部无进展（`primaryPositiveProgressHistories === 0`）且满足完备失败契约：立即触发 **Counterfactual Fallback Admission**，无需等待次级正常分支；
  3. **工作守恒与有用性分流（Work-Conserving Resumption）**：
     - 若 CF 未产生有效进展（`totalCfPositiveCount === 0 && !depthGoalReached`）：若全局预算允许，自动恢复 `deferredNormalDescriptors` 并执行试探，确保次级正常搜索空间绝不遗失；
     - 若 CF 产生有效进展（`totalCfPositiveCount > 0` 或 `depthGoalReached`）：次级正常分支保持 deferred，此时活跃票据仅包含 Primary + CF tickets（全部 `probeCount >= 1`），原生 PR-5.24d 延续调度器自然评估并向有效 CF ticket 发放第二轮延续（400 exp / 4000 ms），达成 `FOUND === true`；
  4. **紧凑遥测输出**：
     - `normalAdmission`: `{ retainedCount, primaryLimit, primaryAdmitted, primaryProbed, primaryPositive, secondaryDeferred, secondaryAdmitted, secondaryDeferredBecauseCounterfactualUseful }`；
     - `counterfactualAdmission`: `{ admittedAfterPrimary, admissionReason }`。
- **G24 门禁扩展（G24-K1 至 G24-K6）**：
  - **G24-K1**：验证 Primary 4 优先试探，CF 触发发生于次级 4 准入前；
  - **G24-K2**：验证 Primary 出现正向进展时阻断 CF，次级正常分支随后准入；
  - **G24-K3**：验证 CF 无进展时工作守恒恢复次级正常分支试探；
  - **G24-K4**：验证有效 CF ticket 自然通过原生延续调度器获得第二轮试探并达成 `FOUND === true`，次级正常分支全程保持 deferred 且无未试探票据；
  - **G24-K5**：验证各项候选上限与保留数量严格未变，无搜索空间增宽；
  - **G24-K6**：验证 Primary 受预算截断时严格保持 fail-closed。
- **实测验证结果**：
  - G24 fast 模式 10 次重复测试（`check-counterfactual-repair.js ×10`）：10/10 全部通过（全 19 项门禁全绿）；
  - PR-5.24c/d 快速门禁套件（`check-budgeted-repair-scheduling.js --fast`）：全绿通过；
  - 全量 12 项静态与核心回归测试：全部通过。
- **当前状态**：Iteration 2 实施完毕并通过全部回归，推送到 `origin/dev`，停止等待云端审查；云端审查通过后直接执行单次同配置真实 MT1→MT4 诊断运行。

### Iteration 2 Repair 1 — 2026-09-04

- **云端审查裁决与缺陷定位**：
  - 判定：`REQUEST_CHANGES`（P1 缺陷：Deferred secondary normal work 遗漏在 canonical completion authority 之外）。
  - 根因：当 CF 首轮试探有进展时，次级正常历史（N4-N7）暂存延期；随后 CF 获得延续试探但最终未找到目标（NOT FOUND）时，生产路径未将延期的次级正常历史归还调度器；且由于活跃票据池中已无 `PROBE_PENDING` 票据，导致该深度在次级正常从未被探测的情况下被错误判定为 `depthOutcome = "exhausted"`，违反了“未实际搜索的规范工作绝不能被 EXHAUSTED 覆盖”的铁律。
- **生产修复方案** (`shared-solver/lib/segment-dp.js`)：
  1. **完备性权能不变式（Completion Authority Invariant）**：
     - 在两处判定 `hasPendingSchedulerWork / depthOutcome` 的生产位置，引入 `hasOutstandingDeferredWork`：
       `hasOutstandingDeferredWork = Boolean((deferredNormalDescriptors && deferredNormalDescriptors.length > 0) || (normalAdmission && normalAdmission.secondaryDeferred > normalAdmission.secondaryAdmitted));`
       `hasPendingSchedulerWork = budgetedSchedulingEnabled && (repairScheduling.hypotheses.some(t => t.depth === depth && t.status === "PROBE_PENDING") || hasOutstandingDeferredWork);`
     - 严格约束：存在任何延期未探测次级工作时，`depthOutcome` 绝不能为 `exhausted`，`depthExhausted` 严格为 `false`；
  2. **CF 延续失败后的工作守恒恢复（Post-CF Resumption）**：
     - 明确生命周期语义：“CF useful”仅暂时延期次级正常分支，绝非永久删除；
     - 当 CF 延续试探结束且未达成 `FOUND` 时，若全局预算干净，立即将 `deferredNormalDescriptors` 全部恢复准入并执行独立首轮 probe；
     - 每次恢复准入次级工作时排空 `deferredNormalDescriptors`，消除重复试探隐患；
  3. **可重入延续阶段（Re-entrant Continuation Phase）**：
     - 将延续调度抽离为模块化可重入阶段 `executeContinuationRound()`；
     - 当恢复准入的次级正常分支（如 N5）获得正向进展时，可重新进入延续调度阶段，获得第二轮试探资格（400 exp / 4000 ms）并打通 `FOUND === true`，绝不沦为“二等公民”。
- **门禁扩展（G24-K7）**：
  - 新增 **G24-K7**（Useful-CF Failure Returns Deferred Work），在统一 fixture 下覆盖三大子场景：
    - S1：CF 延续试探确定性完成但未 FOUND 时，N4-N7 全部恢复准入并完成首轮试探（`secondaryAdmitted === 4`，全部 `probeCount >= 1`）；
    - S2：全局预算在次级恢复前/中截断时，`depthOutcome` 判定为 `incomplete/limited`，绝非 `exhausted`（`depthExhausted === false`）；
    - S3：恢复准入的次级正常分支若具正向进展，无差别获得延续试探并可达成 `FOUND === true`。
- **实测验证结果**：
  - G24 fast 模式 10 次重复测试（`check-counterfactual-repair.js ×10`）：10/10 全部通过（全 20 项门禁全绿）；
  - PR-5.24c/d 快速门禁套件（`check-budgeted-repair-scheduling.js --fast`）：全绿通过；
  - 全量 12 项静态与核心回归测试：全部通过。
- **当前状态**：Iteration 2 Repair 1 完整修复并通过全量回归，推送到 `origin/dev`。

### Real MT1→MT4 Diagnostic Run 2 — 2026-09-04

- **运行环境**：
  - HEAD: `675997112781f8bbcd1eea0294993c00d491334f`
  - Working Tree: Clean
  - Node: `v24.18.0`
  - OS: `win32 x64 10.0.26200` (Windows 11)
- **冻结配置**：
  - `searchIntent: "adaptive-feasible"`
  - `enableFailureBacktracking: true`, `adaptiveBacktrackDepth: 3`
  - `enableBudgetedRepairScheduling: true`, `enableBudgetedRepairContinuation: true`
  - `milestoneFrontierResourceDiversity: true`
  - `failure-intent ranking = OFF`
  - `budgetScope: "global-run"`, `segmentExecutionMode: "isolated-process"`
  - `maxRuntimeMs: 30000`, `maxExpansions: 50000`, `maxRssMb: 2048`（安全熔断器）
  - `candidateLimit: 8`
  - 探测试算预算保持默认：first probe 200 exp / 2000 ms；continuation 400 exp / 4000 ms
- **实测关键事实与顺序证据**：
  1. **Primary Tranche 准入与优先试探**：
     - 深度 1 锚点 `mt2-to-mt3` 产出 16 条正常保留历史（`retainedCount = 16`）；
     - 分批机制生效：前 8 条（`h-d1w0h0` 至 `h-d1w0h7`）作为 Primary Tranche 优先准入并执行独立下游试探（各自消费 200 exp / ~3300 ms wall）；
     - 8 条 Primary 探测试算在通用目标度量下全部判定为 `NO_MEASURABLE_PROGRESS`（`primaryPositive = 0`）；
     - 次级正常分支（N8 至 N15 共 8 条）**严格保持 deferred**（`secondaryDeferred = 8, secondaryAdmitted = 0`），未生成任何 active ticket。
  2. **Counterfactual Fallback 准入与规范物化**：
     - 门禁判定放行：`primaryNormalFirstRoundComplete = true`, `primaryRealHistories = 8`, `primaryPositive = 0`, `globalStop = null`, `failureExecutionDeterminateComplete = true`；
     - `counterfactualAdmission.admittedAfterPrimary = true`，`admissionReason = "primary-normal-exhausted-no-progress"`；
     - CF 生成 3 个意图，成功通过规范 DP 真实物化 2 个意图（零伪造状态）：
       - **Intent 0** (`path/unlock`): `cf-d0-path_unlock`，`pathOpportunity = 1`，产出 1 条修复历史；
       - **Intent 1** (`exp/level`): `cf-d1-exp_level`，主动扣血 295 HP 换取 +120 MDEF 与 +1 EXP，成功通过规范 DP 扩展锚点段产生 16 条全新修复历史（全部具规范 `anchorOutputStateKey`）；
  3. **CF 下游探测试算**：
     - Intent 0 历史 `h-d1w1` 试探后判定为 `NO_MEASURABLE_PROGRESS`（`outcome: "exhausted"`）；
     - Intent 1 修复历史（`h-d1w2h0` 至 `h-d1w2h5` 共 6 条真实历史）先后获得全额下游试探（消费 200, 200, 200, 135, 200, 67 exp）；
     - 6/6 条真实 CF 历史在下游 `mt3-to-mt4` 目标度量下全部判定为 `NO_MEASURABLE_PROGRESS`（`stopReason`: 5 条 `probe-limited`, 1 条 `incomplete`）；
     - 随后预算接近上限，后 10 条 CF 假说记录 `insufficient-probe-headroom`。
  4. **工作守恒与次级正常分支恢复（Work-Conserving Resumption）**：
     - 因所有 CF 历史均无正向推进（`positiveProgressHistoryCount = 0`），工作守恒机制立即恢复延期的次级正常分支（N8 至 N15 共 8 条，`secondaryAdmitted = 8`）；
     - 调度器准备试探 N8..N15 时触碰全局 30s 限制（29,528 ms），记录 `global-budget-exhausted`；
     - 完备性权能生效：`depthOutcome = "incomplete"`, `depthExhausted = false`，严格杜绝虚假 EXHAUSTED。
- **核心对照指标**：
  - `primaryPositiveProgressHistories`: 0
  - `counterfactualPositiveProgressHistories`: 0
  - `primaryBestProgressClass`: `"NO_MEASURABLE_PROGRESS"`
  - `counterfactualBestProgressClass`: `"NO_MEASURABLE_PROGRESS"`
  - `found`: false
  - 全局消耗：3,857 expansions，29,528 ms wall，峰值 RSS 343.2 MB（安全熔断器内）
- **科学结论与机制证明**：
  - `REAL_COUNTERFACTUAL_FALLBACK_ADMISSION = PROVEN`（准入架构成功打破饥饿，CF 在 8 条 Primary 后优先入场）；
  - `CANONICAL_COUNTERFACTUAL_REALIZATION = PROVEN_ON_REAL_ONLYUP`（真塔上真实物化扣血换抗性/经验意图，产生 16 条真实非保守前史）；
  - `COUNTERFACTUAL_HISTORIES_EVALUATED = PROVEN`（6 条真实 CF 历史实质执行下游探测试算）；
  - 核心瓶颈锁定：**当前发现的一阶反常识投资（扣 295 HP 换 120 MDEF / 1 EXP）虽然真实存在且规范物化，但在当前下游通用目标投影下尚不足以直接突破 MT4 门禁**。
- **Single Primary Verdict**：
  `PRIMARY_VERDICT = COUNTERFACTUAL_HISTORIES_NO_USEFUL_PROGRESS`
