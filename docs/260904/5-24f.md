# PR-5.24f — Batched Isolated Probe Execution / Wall-Time Amortization

```ini
MILESTONE = PR-5.24f
STATUS = IN_PROGRESS / DESIGN_AND_IMPLEMENTATION
DATE_STARTED = 2026-09-04
BASE_COMMIT = 64747688a43322d491483ed732d5dd24e004303b
SCOPE = 保持 canonical DP、200/2000 probe 预算、30s/50k 全局上限与准入架构不变，通过批处理隔离探测试算（executeIsolatedSegmentBatch）摊销 Windows 子进程启动/工程加载/IPC开销，使同样 30s 预算内能实质评估更多已生成的候选历史
```

## 问题与已有证据

PR-5.24e 终审（Real Run 2）确立了关键事实与科学瓶颈：
1. **反常识投资历史生成与准入完全成立**：
   - 准入架构成功生效（`REAL_COUNTERFACTUAL_FALLBACK_ADMISSION = PROVEN`），Primary 8 优先试探全部无进展后，CF 顺利打通准入且次级正常分支严格延期；
   - 真实 OnlyUp 地图上成功通过规范 DP 真实物化出扣 295 HP 换 120 MDEF / 1 EXP 等 2 个 intent，产生 16 条全新非保守前史（`CANONICAL_COUNTERFACTUAL_REALIZATION = PROVEN`）；
   - 前 6 条真实 CF 历史实质执行了下游 probe（`COUNTERFACTUAL_HISTORIES_EVALUATED = PROVEN`），均未观察到 measurable progress。
2. **核心瓶颈不是扩展数或内存，而是单位搜索工作的墙钟成本（Wall-Time Amortization）**：
   - 全局扩展仅消耗 3,857 / 50,000（仅约 7.7%）；
   - 内存峰值仅 343.2 MB（远低于 2048 MB 安全熔断器）；
   - 全局墙钟消耗 29,528 / 30,000 ms（近 100% 吃满）；
   - 导致 Intent 1 产出的 16 条真实 CF 历史中，后 10 条因全局墙钟耗尽根本未能获得探测试算（`probeCount = 0`）；
   - 根因定位：当前 `isolated-process` 模式对每个 segment 执行均调用一次独立的 `childProcess.spawnSync("node ...")`，在 Windows 环境下每次 spawn 都伴随高昂的进程创建、V8 初始化、工程文件重复加载与临时文件 IPC 开销（每次 probe 耗时 ~2.5~3.8s，其中实际搜索仅占极小部分）。

## 范围与非目标

本轮范围：
- 在 `shared-solver/lib/isolated-segment-executor.js` 中新增批处理接口：`executeIsolatedSegmentBatch({ simulator, jobs, config, isolatedRuntimeDescriptor })`；
- 第一阶段仅针对**天然独立的一轮首轮探测试算**（Primary Normal Tranche, Counterfactual History First-Probe Tranche, Secondary Normal Tranche）实施批处理调用，摊销子进程创建与工程加载开销；
- 共享全局权威预算（One Batch Global Authority）：批处理内部共享同一个全局预算，各 job 顺序执行并依次扣除消耗；若全局超时或扩展耗尽，剩余未启动 jobs 严格返回未执行，绝不伪装 exhausted；
- 独立局部试探预算（Independent Local Probe Authority）：每个 job 独立享受 200 exp / 2000 ms 的局部上限；
- 严格状态隔离（State Isolation）：工作子进程一次性加载工程定义与不可变结构缓存，每个 job 的输入状态严格重新克隆/反序列化，禁止跨 job 共享可变状态、地图突变、flags 或 DP 缓存；
- 规范等价性与门禁测试：编写 `shared-solver/check-isolated-batch-equivalence.js`。

非目标：
- 严禁重构父进程同步架构为 async；
- 严禁增加全局预算（30s / 50k / 2048MB 保持不变）；
- 严禁修改 200/2000 probe 或 400/4000 continuation 预算；
- 严禁修改 candidateLimit、backtrackCandidateLimit 或准入顺序；
- 严禁引入 multi-step CF；
- 不 batch 跨轮决策、anchor 生成、intent 物化或 continuation。

## 初始设计与门禁 (G25)

| Gate | 测项 | 通过条件 |
| --- | --- | --- |
| **G25-A** | 规范等价性 (Direct Batch Parity) | 相同输入下，批处理结果与单进程逐次执行结果完全一致（`found`、`failureClass`、`searchComplete`、`consumedExpansions`、`buildStateKey` 100% 对齐） |
| **G25-B** | 全局中断权威 (Global Stop) | 批处理中途全局预算耗尽时，已执行 jobs 正常报告，剩余未执行 jobs 严格标记未执行且 `searchComplete = false`，绝不虚假完备 |
| **G25-C** | 跨 Job 状态隔离 (State Isolation) | Job A 大幅修改 HP/flags/mutations 后，Job B 从独立初始状态启动，其输出 stateKey 与单进程全新环境下的 Job B 完全一致，无任何状态污染 |
| **G25-D** | 墙钟性能微基准 (Microbench Wall-Time Amortization) | 在 4 个 50-exp 独立 probe 场景下，批处理子进程数从 4 降至 1，加速比 $\ge 2.0x$，遥测显式标注 `speedupLabel: "MICROBENCH_SPEEDUP"` |
| **G25-E** | 细粒度遥测 (Telemetry Completeness) | 暴露 `isolatedBatch: { jobsRequested, jobsExecuted, processLaunches, processWallMs, searchWallMs, serializationWallMs }` |
| **G25-F** | 生产首轮探测试算接入 (Production Wiring) | 生产路径 `runMilestoneGraph` 中 primary normal 批次执行调用 `executeIsolatedSegmentBatch`，在恰好 1 个子进程中完成全批次试探 (`processLaunches === 1`) |
| **G25-G** | 每 Job 墙钟独立重基 (Per-Job Wall Rebase) | 批处理子进程内每个 job 从自身启动时刻 (`jobStartWallMs`) 重基其局部 `probeWallMs` 截止线，各 job 顺次享有独立且饱满的探测试算时长 |
| **G25-H** | 生产等价性 (Production Equivalence) | 生产 `runMilestoneGraph` 在单进程逐次 Arm A (`disableBatchFirstProbe: true`) 与批处理 Arm B (`disableBatchFirstProbe: false`) 下产出完全一致的工单 ID、状态、停机原因、探测试算数、进展分类、`effectiveDp` 配置、最终前沿状态键多重集与修复前沿数量 |
| **G25-I** | 生产路径中途全局停机 (Global Stop Mid-Batch Production Path) | 生产路径批处理执行中途耗尽全局扩展数时，已执行 jobs 正常生成结果，未执行 jobs 严格保持 `PROBE_PENDING`，`depthExhausted = false`，最终结果绝不报告 `EXHAUSTED` |
| **G25-J** | 冻结回溯 DP 权能 (Frozen Backtrack DP Authority) | 生产路径 probe 时硬断言 `stopOnFirstGoal = false`，`goalSkylineLimit = backtrackCandidateLimit`，完整的 `backtrack dpOverrides` 在 Batch ON/OFF 均 100% 对齐生效 |
| **G25-K** | 同级目标独立性 (Sibling Goal Independence) | 批处理中某 job 达成当前段目标，其余 sibling jobs 严格独立执行，绝不被擅自取消或赋予 `goal-found` notRunReason |
| **G25-L** | 修复前沿宽度冻结 (Repair Frontier Width Frozen) | 生产路径回滚修复返回 planner 时严格保留全量候选（5 条），禁止通过新增 `.slice()` 擅自缩窄搜索空间 |

## 迭代日志

### Iteration 1 — 2026-09-04

- 任务启动：根据 PR-5.24e 终审裁决（确认主瓶颈为 isolated execution 墙钟开销），创建 PR-5.24f 里程碑；
- 架构确立：保持同步调度器与全局权威预算，设计 `executeIsolatedSegmentBatch` 批处理机制；
- 模块与工作子进程实现：
  - 在 `shared-solver/lib/isolated-segment-executor.js` 中新增导出 `executeIsolatedSegmentBatch({ simulator, jobs, config, overrides, isolatedRuntimeDescriptor, descriptor })`；
  - 在 `shared-solver/segment-worker.js` 中新增批处理模式处理函数 `runBatchWorker`，工程定义与不可变结构只加载 1 次，顺序执行所有 jobs；
  - 严格保持状态隔离：每个 job 独立深拷贝反序列化输入前沿，重置 choiceResolver，彻底杜绝状态与突变跨 job 泄漏；
  - 严格共享全局权威预算：批处理内部共享 `childGlobalBudget`，实时维护剩余扩展数与截止墙钟；若预算中途耗尽，剩余未启动 jobs 严格标记未执行且 `searchComplete = false, finalPending > 0`，绝不虚假完备；
  - 独立局部 probe 预算：每个 job 独立享有 `probeExpansionCap` 与 `probeDeadlineMs`，rebase 至局部起点；
  - 产出严格验证：父进程对每个 job 的候选状态严格比对 `outputStateKey` 与 `buildStateKey(cand.state)`，零漂移保证；
  - 细粒度遥测输出：每个结果附加 `isolatedBatch: { jobsRequested, jobsExecuted, jobsNotStarted, processLaunches, processWallMs, searchWallMs, serializationWallMs }`；
- G25 门禁套件开发与实测 (`shared-solver/check-isolated-batch-equivalence.js`)：
  - **G25-A（规范等价性）**：单进程逐次与批处理结果完全 100% 对齐（`found`、`failureClass`、`searchComplete`、`finalPending`、`merged`、`buildStateKey` 全部一致）；
  - **G25-B（全局中断权威）**：中途预算耗尽时，已执行 jobs 正常报告，未执行 jobs 严格标记 `executed = false` 且 `searchComplete = false`，绝不报告 exhausted；
  - **G25-C（跨 Job 状态隔离）**：Job A 污染数据后，Job B 从干净状态启动，结果与全新子进程下的 Job B 严格一致，零泄漏；
  - **G25-D（墙钟性能微基准）**：4 个独立 probe 场景下，子进程启动次数从 4 次降至 1 次，总墙钟耗时从 6,032 ms 骤降至 202 ms，**测得 29.9x 真实墙钟加速**（`BATCH_WALL < LEGACY_WALL`）；
  - **G25-E（细粒度遥测）**：完整输出 `isolatedBatch` 遥测数据；
- 验证套件实测：
  - G25 equivalence 重复测试（`check-isolated-batch-equivalence.js ×10`）：10/10 全部通过；
  - G24 fast 模式 10 次重复测试（`check-counterfactual-repair.js ×10`）：10/10 全部通过（全 20 项门禁全绿）；
  - PR-5.24c/d 快速门禁套件（`check-budgeted-repair-scheduling.js --fast`）：全绿通过；
  - 全量 13 项静态与核心回归测试（含 manifest 149 模块/180 测试、no-tower-solver-js、solver-job、launcher、route-free-state、tower-ir-shadow、candidate-key-smoke、adaptive:contract、checkpoint-repair、core、resource-intent:contract、counterfactual-repair、isolated-batch）：全部通过；
- 当前状态：第一阶段（独立接口实现 + G25 门禁套件 + 全量回归）完成。

### Iteration 1 Repair 1 — 2026-09-04

- **审查与阻断修复**：
  1. **P1-1: 生产第一轮探测试算接入 (Production Wiring)**：
     - 在 `shared-solver/lib/segment-dp.js` 中引入 `probeTranche` 统一辅助函数，将 primary normal 历史候选、同一 intent 产出的 counterfactual 候选以及恢复执行的 secondary normal 候选统一通过 `executeIsolatedSegmentBatch` 分批次试探；
     - 严格保持父进程同步调度架构与既有 ticket 生成规则不变（工单 ID 严格为 `h-d1w0h0` 等）；
     - 新增遥测字段 `batchFirstProbe: { primary, counterfactual, secondary }`，实时上报各批次 `jobsRequested`、`jobsExecuted` 与 `processLaunches`；
     - 验证生产路径上 4 个 primary normal 首轮 probe 统一在 1 个子进程内批处理执行完成（`processLaunches: 1`）。
  2. **P1-2: 每 Job 墙钟时长独立重基 (Per-Job Wall Rebase)**：
     - 在 `isolated-segment-executor.js` 中支持传递时长参数 `probeWallMs`（如 2000 ms），并在遥测中记录 `jobStartWallMs`、`effectiveProbeDeadlineMs` 与 `allocatedProbeWallMs`；
     - 在 `segment-worker.js` 的 `runBatchWorker` 中，每个 job 执行前获取实时的 `jobStartWallMs = Date.now()`，严格计算 `jobLocalDeadline = Math.min(childGlobalBudget.deadlineMs, jobStartWallMs + Number(job.probeWallMs))`，彻底消除先前 job 消耗侵蚀后续 job 2000ms 试探时长的缺陷；
     - 遥测上报 `allocatedProbeWallMs` 与 `jobStartWallMs`，支持形式化断言。
  3. **门禁强化与补全**：
     - **G25-D**：重命名为 `MICROBENCH_SPEEDUP`，断言加速比 $\ge 2.0x$（实测 27~28x）；
     - **G25-F**：验证生产路径接入（4 requested, 4 executed, 1 process launch, primary ticket probeCount $\ge 1$）；
     - **G25-G**：验证每个 job 顺次执行并独立重基截止线（`effectiveProbeDeadlineMs \approx jobStartWallMs + probeWallMs`）；
     - **G25-H**：验证生产等价性（Arm A 4 次单进程 spawn 与 Arm B 1 次批处理 spawn 产出 100% 对齐的工单状态与分类）；
     - **G25-I**：验证中途全局预算耗尽时，已执行 jobs 正常报告，未执行 jobs 严格标记 `PROBE_PENDING`，深度状态不虚假完备（`depthExhausted = false`, `depthOutcome !== "exhausted"`）。
- **全量门禁实测**：
  - `check-isolated-batch-equivalence.js` 全部 9 个门禁（G25-A ~ G25-I）100% 通过；
  - `check-counterfactual-repair.js` 全部 18 个门禁（G24-A ~ G24-K7）100% 通过；
  - `check-budgeted-repair-scheduling.js` 全部 23 个门禁 100% 通过；
  - Fast CI 9 项核心测试全绿通过；
  - `git diff --check` 完全干净。

### Real MT1→MT4 Batched Diagnostic Run 1 — 2026-09-04

- **实验性质与认证状态**：
  ```ini
  REAL_BATCH_RUN_1 = VALID_EXPLORATORY_DIAGNOSTIC
  REAL_BATCH_RUN_1_FORMAL_QUALIFICATION = INVALIDATED_BY_PRE_RUN_PRODUCTION_DRIFT
  AUTHORIZED_BASE = 0abec394affdd1d49959d638e05e4b18c8705638
  ACTUAL_RUN_SHA = 32548d1385c7969f294f817668ff692dd216f70a
  ```
  *(注：该次运行真实验证了生产批处理架构与每 Job 墙钟独立重基机制，但因运行前引入了未经 review 的搜索语义变化（`32548d1`），不可作为 PR-5.24f 的正式因果认证，本报告数据作为探索性诊断证据保留，正式认证将在语义完全回退后的干净基线上执行。)*
- **运行环境**：
  - HEAD: `32548d1385c7969f294f817668ff692dd216f70a`
  - Working Tree: Clean
  - Node: `v24.18.0`
  - OS: `win32 x64 10.0.26200` (Windows 11)
- **冻结配置**：
  - `searchIntent: "adaptive-feasible"`
  - `enableFailureBacktracking: true`, `adaptiveBacktrackDepth: 3`
  - `enableBudgetedRepairScheduling: true`, `enableBudgetedRepairContinuation: true`
  - `milestoneFrontierResourceDiversity: true`
  - `failure-intent ranking = OFF`
  - `segmentExecutionMode = "isolated-process"`
  - `batch first probe = ON` (`disableBatchFirstProbe` unset)
  - `budgetScope: "global-run"`
  - `maxRuntimeMs: 30000`, `maxExpansions: 50000`, `maxRssMb: 2048`（安全熔断器）
  - `candidateLimit: 8`
  - 探测试算预算保持默认：first probe 200 exp / 2000 ms；CF intent realization 200 exp / 2000 ms；continuation 400 exp / 4000 ms
- **真实批处理接入观察证据 (Observed Evidence under 32548d1)**：
  ```ini
  REAL_BATCH_WIRING_OBSERVED = TRUE
  PRIMARY: jobsRequested=8, processLaunches=1
  COUNTERFACTUAL: jobsRequested=16, processLaunches=1
  PER_JOB_WALL_REBASE_OBSERVED = TRUE
  ```
  - `batchFirstProbe.primary`:
    - `jobsRequested: 8`
    - `jobsExecuted: 8`
    - `processLaunches: 1`（**8 个 Primary 正常历史探测试算在恰好 1 个子进程中批量完成，无重复 spawn**）
  - `batchFirstProbe.counterfactual`:
    - `jobsRequested: 16`
    - `jobsExecuted: 2`
    - `processLaunches: 1`（**16 个 Counterfactual 历史探测试算在恰好 1 个子进程中批量执行，中途受全局预算截断**）
  - `batchFirstProbe.secondary`:
    - `jobsRequested: 0`, `jobsExecuted: 0`, `processLaunches: 0`
  - **进程启动显著削减**：两类批次均达成 `processLaunches: 1`，分别实现 8:1 与 16:1 的进程启动收缩比。
- **每类 Tranche 墙钟耗时分解**：
  - **Primary Tranche**：
    - jobsRequested: 8, jobsExecuted: 8, jobsNotStarted: 0
    - processLaunches: 1
    - processWallMs: 13,557 ms
    - searchWallMs: 11,408 ms
    - serializationWallMs: 2 ms
  - **Counterfactual Tranche**：
    - jobsRequested: 16, jobsExecuted: 2, jobsNotStarted: 14
    - processLaunches: 1
    - processWallMs: 2,390 ms
    - searchWallMs: 2,105 ms
    - serializationWallMs: 13 ms
- **每 Job 独立墙钟权能抽查 (Per-Job Wall Authority Spot Check)**：
  - **Primary Tranche 抽查**：
    - Job 0 (first): `jobStartWallMs = 1788526658154`, `effectiveProbeDeadlineMs = 1788526660154`, `allocatedProbeWallMs = 2000`, `effectiveLocalHeadroom = 2000 ms`；
    - Job 3 (middle): `jobStartWallMs = 1788526661032`, `effectiveProbeDeadlineMs = 1788526663032`, `allocatedProbeWallMs = 2000`, `effectiveLocalHeadroom = 2000 ms`；
    - Job 7 (last): `jobStartWallMs = 1788526666041`, `effectiveProbeDeadlineMs = 1788526667668`, `allocatedProbeWallMs = 2000`, `effectiveLocalHeadroom = 1627 ms`（`GLOBAL_CLAMP`：严格受父级 30s 全局截止线钳制，行为完全正确）。
  - **Counterfactual Tranche 抽查**：
    - Job 0 (first): `jobStartWallMs = 1788526851904`, `effectiveProbeDeadlineMs = 1788526853904`, `allocatedProbeWallMs = 2000`, `effectiveLocalHeadroom = 2000 ms`；
    - Job 1 (middle & last executed): `jobStartWallMs = 1788526853231`, `effectiveProbeDeadlineMs = 1788526854001`, `allocatedProbeWallMs = 2000`, `effectiveLocalHeadroom = 770 ms`（`GLOBAL_CLAMP`：到达全局 30s 截止线）；
    - Jobs 2..15: 因全局超时在执行前安全截断，记录 `jobsNotStarted: 14`。
- **Primary Normal 实测记录**：
  - `retainedCount`: 16, `primaryAdmitted`: 8, `primaryProbed`: 8, `primaryPositive`: 0, `secondaryDeferred`: 8；
  - 8 条 Primary 工单明细：
    - `h-d1w0h0`: pCount=1, pExp=200, pWall=1221 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h1`: pCount=1, pExp=200, pWall=1090 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h2`: pCount=1, pExp=200, pWall=1720 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h3`: pCount=1, pExp=200, pWall=1827 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h4`: pCount=1, pExp=200, pWall=1506 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h5`: pCount=1, pExp=200, pWall=1937 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h6`: pCount=1, pExp=156, pWall=2027 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
    - `h-d1w0h7`: pCount=1, pExp=186, pWall=2027 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited
  - 与 Run 2 对比：8/8 均完成探测且均为 `NO_MEASURABLE_PROGRESS`，Primary 8 整体耗时在 `32548d1` 下观察到从 Run 2 的 8 次独立进程 (~26.4s) 降至本次批处理的 1 次进程 (13.5s)。
- **Counterfactual 准入与生成**：
  - Primary 首轮完备且正向推进为 0，完备失败证明通过，顺利触发 Counterfactual Fallback 准入；
  - `counterfactualRepair.triggered`: true, `triggerReason`: "floor-progress-blocked"；
  - 生成 3 个 intent，成功通过规范 DP 真实物化 3 个 intent：
    - Intent 0 (`path/unlock`): 产出 1 条修复历史；
    - Intent 1 (`exp/level`): 产出 16 条全新修复历史；
    - Intent 2 (`exp/level`): 产出 16 条全新修复历史。
- **一阶 Counterfactual 评估覆盖率 (严格全量统计)**：
  - Intent 0: generated = 1, probed = 1 (`h-d1w1`, 0 exp, stopReason=incomplete, pos=0), unprobed = 0；
  - Intent 1: generated = 16, realHistories = 16, probed = 2 (`h-d1w2h0` [200 exp / 1326 ms], `h-d1w2h1` [151 exp / 779 ms]), unprobed = 14 (`pCount = 0`, stopReason=global-budget-exhausted), pos = 0；
  - Intent 2: generated = 16, realHistories = 16, probed = 0, unprobed = 16 (`pCount = 0`, stopReason=time-limit), pos = 0；
  - 汇总指标：
    - `ONE_STEP_CF_GENERATED` = 1 + 16 + 16 = 33
    - `ONE_STEP_CF_PROBED` = 3 (Intent 0: 1, Intent 1: 2, Intent 2: 0)
    - `ONE_STEP_CF_UNPROBED` = 30
    - `ONE_STEP_CF_COVERAGE_RATIO` = 3 / 33 = 9.1% (针对 Intent 1 为 2 / 16 = 12.5%)。
- **被实质探测的 CF 历史记录**：
  - `h-d1w1`: intent=cf-d0-path_unlock, keyPresent=false, pCount=1, pExp=0, pWall=0 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=incomplete, continuationEligible=false, continuationDecision=not-eligible
  - `h-d1w2h0`: intent=cf-d1-exp_level, keyPresent=true, pCount=1, pExp=200, pWall=1326 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited, continuationEligible=false, continuationDecision=no-measurable-progress
  - `h-d1w2h1`: intent=cf-d1-exp_level, keyPresent=true, pCount=1, pExp=151, pWall=779 ms, progressClass=NO_MEASURABLE_PROGRESS, stopReason=probe-limited, continuationEligible=false, continuationDecision=no-measurable-progress
  - `h-d1w2h2` ~ `h-d1w2h15`: pCount=0, stopReason=global-budget-exhausted, continuationDecision=first-round-incomplete
- **能力判定 (Capability Assessment)**：
  - 判定属于 **Scenario C**（大批 CF histories 仍处于 `probeCount = 0` 未探测试算状态）；
  - 约束瓶颈依然是 **GLOBAL_WALL / HEAVY_SEARCH_COST**；一阶反常识投资历史在全局 30s 预算内仍受限于全局墙钟，未能达成足够覆盖率（总覆盖仅 3/33 条，9.1%），因此不可推导一阶投资无效，也不得直接跳跃到 multi-step 复杂度；
  - `EVALUATED_ONE_STEP_CF_NO_PROGRESS = TRUE`（已探测的 2 条 Intent 1 CF 历史未显现推进）；
  - `ONE_STEP_CF_CAPABILITY = UNDETERMINED`。
- **全局资源汇总**：
  - `found`: false
  - `finalCanonicalOutcome`: "INCOMPLETE_SCOPE"
  - `reachedMilestone`: "mt2-to-mt3"
  - `failedSegment`: "mt3-to-mt4" (`floor-progress-blocked`)
  - `global consumed expansions`: 4,744
  - `global wallMs`: 29,563 ms
  - `peakRssMb`: 287.7 MB（远低于 2048 MB 安全熔断器）
  - `globalStopReason`: "time-limit"
  - `finalPending`: 18, `terminalIncomplete`: 0, `unknownCompletion`: 0
  - `activePendingTickets`: 38 (30 CF unprobed + 8 Secondary deferred)
  - 严禁声明 EXHAUSTED（完备性凭据完好封死）。
- **观测对比 (OBSERVED_UNDER_32548d1 vs PR-5.24e Run 2)**：
  - **全局扩展总数**：4,744 vs 3,857（在 `32548d1` 观察到扩展增加，但因前序段状态与分支语义漂移，不作为正式因果认证依据）；
  - **子进程启动次数**：探测批次从 14 次单进程 spawn 骤降至 **2 次 batch spawn**（真实观察到显著启动收缩）；
  - **Primary 8 探测耗时**：从 Run 2 的 ~26.4s 压缩至 **13.55s**（批处理有效减少了进程重载开销）；
  - **峰值内存**：287.7 MB vs 343.2 MB（内存更加紧凑稳定）。
- **Single Primary Verdict**：
  `PRIMARY_VERDICT = GLOBAL_RESOURCE_LIMITED_DURING_ONE_STEP_CF_EVALUATION`
  （批处理机制观察有效，进程启动显著削减，但全局 30s 墙钟在 16 条 Counterfactual 批次执行第 2 条时耗尽，剩余 30 条历史未探测试算；一阶反常识投资能力仍处于资源截断下的未决状态；由于 `32548d1` 引入了未审生产漂移，本运行仅作为有效探索性诊断，不作为正式资格认证）。

### Iteration 1 Repair 2 — 2026-09-04

- **审查与阻断修复（恢复冻结搜索语义 + 门禁保护 + 阶段墙钟遥测）**：
  1. **恢复冻结的首轮探测试算 DP 权能 (Restore Frozen First-Probe DP Authority)**：
     - 在 `shared-solver/lib/segment-dp.js` 中完整恢复 `rRunConfig.stopOnFirstGoal = undefined` 与 `overrides.dpOverrides = backtrackDpOverrides(replaySegment, config || {})`；
     - 严格保持第一轮 backtrack probe 的 `stopOnFirstGoal = false` 与 `goalSkylineLimit = backtrackCandidateLimit` 语义，杜绝任何将 N 次 spawn 改为 1 次 spawn 时顺带修改首轮试探搜索深度的漂移。
  2. **撤销修复前沿缩窄 (Undo Repair Frontier Narrowing)**：
     - 完整恢复 `frontier = adaptiveRepair.finalFrontier`，撤销 `.slice(0, candidateLimit)` 截断；
     - 严格遵守 PR-5.24f 范围纪律：本轮禁止擅自修改 repair 搜索空间宽度。
  3. **撤销批处理同级目标短路 (Undo Batch Sibling Goal Short-Circuit)**：
     - 在 `shared-solver/segment-worker.js` 中彻底删除 `batchStoppedOnGoal` 变量及相关逻辑；
     - 恢复契约：批处理内每个 job 独立执行，仅受全局时间、全局扩展数、内存上限或工作进程异常控制，禁止某个 job 找到当前段目标时擅自短路取消后续 sibling jobs。
  4. **撤销未经验证的 winningChainExecutions**：
     - 恢复 `executions: waveExecutions`，保持与 `0abec39` 冻结基线 100% 结构一致，不将未验证的路径重构改动混入性能认证。
  5. **新增阶段墙钟与启动次数紧凑遥测 (Phase-Wall & Phase-Process-Launches Telemetry)**：
     - 在 `runMilestoneGraph` 中新增紧凑遥测：
       - `phaseWall: { initialMilestoneMs, anchorGenerationMs, primaryFirstProbeMs, counterfactualIntentRealizationMs, counterfactualAnchorGenerationMs, counterfactualFirstProbeMs, secondaryFirstProbeMs, continuationMs, otherPlannerMs }`；
       - `phaseProcessLaunches: { initial, anchor, intentRealization, cfAnchor, firstProbe, continuation }`；
     - 不改变任何执行与决策逻辑，为后续分析 30s 墙钟在各阶段的真实消耗分布提供精确测量基础。
  6. **门禁强化与扩展 (G25-H / G25-J / G25-K / G25-L)**：
     - **扩展 G25-H**：在工单状态对齐基础上，深度核验 `effectiveDp.stopOnFirstGoal`、`goalSkylineLimit`、`finalCandidates` 状态多重集与 `repair finalFrontier count`，保证除墙钟、启动次数与批处理遥测外全量等价；
     - **G25-J (Frozen Backtrack DP Authority)**：硬断言生产路径上的首轮 probe 在 Batch ON 与 OFF 下具备相同的 `stopOnFirstGoal = false`、`goalSkylineLimit = 8` 与 `backtrack dpOverrides`；
     - **G25-K (Sibling Goal Independence)**：3 个批处理任务中 Job A 达成当前段目标，严格断言 Job B 与 Job C 依然独立执行，绝不触发 `goal-found` 取消；
     - **G25-L (Repair Frontier Width Frozen)**：验证返回 planner 的修复前沿保留全量状态（5 个状态），未被非法 `.slice(0, 4)`。
- **全量门禁实测**：
  - `check-isolated-batch-equivalence.js` 全部 12 个门禁（G25-A ~ G25-L）100% 通过；
  - `check-counterfactual-repair.js` 全部 18 个门禁（G24-A ~ G24-K7）100% 通过；
  - `check-budgeted-repair-scheduling.js` 全部 23 个门禁 100% 通过；
  - Fast CI 9 项核心测试全绿通过；
  - `git diff --check` 完全干净。
