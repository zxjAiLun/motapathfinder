# PR-5.24g — Profile-Guided Canonical DP Hot-Path Optimization

```ini
MILESTONE = PR-5.24g
STATUS = APPROVED_AND_CLOSED
DATE_STARTED = 2026-09-04
BASE_COMMIT = 179aa2142ae3ed0cbb8848808e88c1a9419970e5
PRODUCTION_BASELINE = 5a417d1e8ad1a883161c325fc21647631df2934a
ITERATION_1_QUALIFICATION_RECORD = 82b09fa6ac7f87dc1c007488bccfa0d7fd5ea6f9
SCOPE = 不改变任何搜索空间、剪枝规则、状态等价、候选顺序、预算或调度语义，只降低每次 canonical DP expansion 的 CPU / wall cost
```

## 目标

在同一 Iteration 中完成:
1. 用现有 `perf.js` profiler 定位 top hot path
2. 实现至少一个真实优化
3. canonical parity (G26-A/B)
4. deterministic performance A/B (G26-D)

## Baseline Profile (Iteration 1)

Fixture: 真实 OnlyUp canonical simulator, mt2-to-mt3 segment from real MT1 state, frontier-exhausted deterministic completion (~520 expansions).

### Top-Level Self-Time (Baseline, 4 warm runs median)

| Bucket | ms | Share |
|--------|-----|-------|
| **stabilization** | **10,668** | **71.8%** |
| primitiveEnumeration | 1,698 | 11.4% |
| walkReachability | 1,503 | 10.1% |
| stateKeyAndDominance | 526 | 3.5% |
| applyAction | 466 | 3.1% |
| otherExpansionOverhead | 138 | 0.9% |
| frontierQueue | 36 | 0.2% |
| actionEvaluation | 5 | 0.0% |

### Stabilization Subphases (Baseline)

| Subphase | ms |
|----------|-----|
| battleScan | 6,015 |
| hazardBuild | 2,481 |
| pickupScan | 1,643 |
| applyStep | 251 |
| battleApply | 86 |
| pickupApply | 157 |
| autoEvent | 7 |

### Baseline Semantic Counters (Key)

- `hazardBlockIndexCalls`: 16,799
- `hazardCellsScanned`: 2,839,031
- `hazardBlocksMaterialized`: 1,959,028
- `battleResolverEvaluateCalls`: 54,811
- `stabilizationPasses`: 10,236

## Iteration 1 — hazardBlockIndex Reference-Identity Memoization

### 优化目标

`buildBlockIndex()` 在 `stabilization` 内消耗 1,966ms (fast path)。每次调用遍历整个楼层 (13×15 = ~195 cells)，总调用 16,799 次 = ~3.2M cell iterations。

### 优化实现

**文件**: `shared-solver/lib/movement-hazards.js`

**机制**: 以 `floorState` 对象引用身份作为缓存键，将 `buildBlockIndex` 结果以不可枚举属性 (`__blockIndexCache`) 存储在 `floorState` 对象上。当且仅当 `floorState` 对象引用相同时直接返回缓存结果。

**等价性保证**:
- `buildBlockIndex` 的输出仅取决于 `(project, floorId, floorState.removed, floorState.replaced)`
- 在 canonical search 中，`floorState` 对象通过 `JSON.parse(JSON.stringify(state))` 克隆，相同引用意味着相同的 removed/replaced 集合
- 缓存属性为 `enumerable: false`，不会影响 `JSON.stringify`、`Object.keys`、state key 构建或序列化
- 当 `floorState` 被替换（新对象引用），缓存自动失效（新引用无缓存属性）

### G26 门禁实测

#### G26-A: Exact Search Equivalence ✅

```
expansions: 520 (match)
found: true (match)
frontierSize: 0 (match)
stoppedReason: null (match)
acceptedStates: 839 (match)
goalSkylineCount: 8 (match)
goalSkylineKeys: sorted multiset (match)
route: action trace (match)
searchOutcome: (match)
```

#### G26-B: Expansion-Order Equivalence ✅

```
expansionsCompared: 100 (first 100 expansion DP keys identical)
```

#### G26-C: State-Key / Mutation Safety ✅

```
sameObjectConsistent: true
differentObjectSameState: true
allMutationsInvalidate: true (HP/flags/inventory/floorStates/equipment/visitedFloors)
```

#### G26-D: Performance A/B

**Baseline (legacy path, no cache)**:
- Median wallMs: 16,392
- hazardBlockIndex: 3,206 ms
- hazardCellsScanned: 2,839,031

**Optimized (fast path + reference-identity cache)**:
- Median wallMs: 14,239
- hazardBlockIndex: 889 ms
- hazardBlockIndexCacheHits: 10,088
- hazardCellsScanned: 1,134,159

**改进**:
- **Overall wall improvement**: 16,392 → 14,239 = **13.1%** ✅ (≥ 10% threshold)
- **Target bucket reduction**: hazardBlockIndex 3,206 → 889 = **72.3%** ✅ (≥ 20% threshold)
- Cache hit rate: 10,088 / 16,799 = 60.1%
- Cell scan reduction: 2,839,031 → 1,134,159 = 60.1%

#### G26-E: Existing Search Contracts

- G25 A-L (isolated-batch): ✅ 全部通过
- G24 (counterfactual-repair): ✅ 全部通过
- Core regressions: ✅ 通过
- MT1 real route gate: ✅ 通过
- Route-free-state: ✅ 通过
- manifest: ✅ 通过 (149 modules, 181 tests)

**已知 Pre-existing Failures (与本次优化无关)**:
- `check-candidate-key-smoke`: timing-sensitive flaky test (fails intermittently with/without cache)
- `check-onlyup-segment-dp`: MT1→MT7 heavy test (fails without cache too)

## 迭代日志

### Iteration 1 — 2026-09-04

- 创建 `shared-solver/check-dp-hot-path.js` hot-path fixture:
  - 真实 OnlyUp canonical simulator + mt2-to-mt3 segment from real MT1 state
  - Frontier-exhausted deterministic completion (~520 expansions)
  - 集成 `perf.js` profiler with `profileExpansionCost: true`
  - G26-A/B/C/D 门禁全部通过
- Baseline profiling 确定 top bucket 为 `stabilization` (71.8%)，其中 `hazardBlockIndex` 占 1,966ms (fast path) / 3,206ms (legacy)
- 实现 `buildBlockIndex` reference-identity memoization:
  - `shared-solver/lib/movement-hazards.js`: 以 `floorState` 对象引用作为缓存键
  - `shared-solver/lib/perf.js`: 新增 `hazardBlockIndexCacheHits` 计数器
  - 不可枚举属性，零序列化/排序影响
- Deterministic A/B 性能对比确认 **13.1% overall wall improvement** 和 **72.3% target bucket reduction**
- 全量回归通过（G25 A-L, G24, core, route gate, route-free-state, manifest）

### Iteration 1 Repair 1 — 2026-09-05

#### 变更原因 (Cloud Review REQUEST_CHANGES 修复)
1. **P1-1: REFERENCE_IDENTITY_DOES_NOT_IMPLY_TILE_MUTATION_IDENTITY**: 发现 JS 中存在同一 `state` 或 `floorState` 对象的就地修改 (`removeTileAt`, `replaceTileAt`, `protectPresentTiles`/`restorePresentTiles`, `applySnapshotFloorMutations`)。纯引用比较会导致同对象 tile 修改后读到陈旧 block index。
2. **P1-2: G26-A/B 不是真比较**: 原 G26-A/B 是运行两次 cache ON 测确定性，未做真正的 cache OFF (`enableHazardBlockIndexMemoization: false`) vs ON (`true`) 的真对偶校验。
3. **P1-3: G26-D 未交替运行**: 原 A/B 测试是跑完 4 次 baseline 再跑 4 次 cache，易受环境升温与 JIT 顺序偏差影响。

#### 修复实现
1. **Mutation Epoch 权威跟踪**:
   - `shared-solver/lib/state.js`: 引入并导出 `getFloorMutationEpoch(floorState)` 与 `markFloorStateMutated(floorState)`。
   - `markFloorStateMutated` 递增 `floorState.__tileMutationEpoch`，并防御性删除已存在的 `__blockIndexCache`。
   - 审计并更新了全库所有 `floorState` 修改点：
     - `state.js`: `removeTileAt`, `replaceTileAt`
     - `route-store.js`: `applySnapshotFloorMutations`
     - `reach-and-battle-oracle.js`: `protectPresentTiles`, `restorePresentTiles`
     - `segment-dp.js`: `protectPresentTiles`, `restorePresentTiles`
2. **Cache 记录绑定 (floorState, epoch)**:
   - `shared-solver/lib/movement-hazards.js`: `buildBlockIndex` 缓存记录存为 `{ epoch: currentEpoch, blocks }`。
   - 读取缓存时严格校验 `cached.epoch === getFloorMutationEpoch(floorState)`。任何就地修改后，epoch 不匹配，缓存自动失效重建。
3. **生产级配置开关 `enableHazardBlockIndexMemoization`**:
   - 默认开启 (`true`)。
   - 贯穿 `StaticSimulator`、`AutoActionResolver`、`step-simulator.js`、`isolated-segment-executor.js` 以及 `segment-worker.js`，支持随时关闭。
4. **重构 G26 门禁套件 (`shared-solver/check-dp-hot-path.js`)**:
   - **G26-A (True Exact Search Equivalence)**: Arm A (OFF) vs Arm B (ON) 严格比对 `found`, `expansions`, `frontierSize`, `stoppedReason`, `acceptedStates`, `generated`, `registered`, `goalSkylineCount`, `goalSkylineKeys`, `action trace summaries`, `full action route objects`, `searchOutcome`，100% 逐项一致。
   - **G26-B (True Expansion-Order Equivalence)**: Arm A (OFF) vs Arm B (ON) 比较前 100 次扩展的 DP state key，100% 逐项一致。
   - **G26-C1 (State-Key Mutation Sensitivity)**: 8 维状态变更（同一对象重复读、不同对象同状态、HP、flags、inventory、floorStates、equipment、visitedFloors）失效灵敏度测试全部通过。
   - **G26-C2 (Cached Block Index Mutation Invalidation)**: 在同一 `s0` / 同一 `floorState` 上触发 `buildMovementHazards` → `removeTileAt` → `buildMovementHazards`，断言 epoch 递增、旧缓存删除、陈旧 epoch 零缓存命中、重构 blocks 不再包含被移除 tile；再触发 `replaceTileAt`，断言新 tile 生效。
   - **G26-C3 (Real Stabilization Mutation Path)**: 真实 `stabilizeState` 自动拾取/自动战斗就地修改，对比开启缓存的 post-mutation hazards 与全量深拷贝 fresh uncached hazards，`damage`, `repulse`, `ambush`, `betweenAttackLocs` 100% 严格一致。
   - **G26-D (Committed Reproducible Performance A/B)**: Interleaved 交替执行 (Warmup OFF → Warmup ON → 4×[OFF, ON])。

#### G26-D 修复后测试数据 (Interleaved 4 Pairs, 8 Runs Total)

- **Arm A (Cache OFF)**:
  - Median wall: 8,716 ms
  - Median CPU: 8,515.5 ms
  - Median Expansions/sec: 60
  - hazardBlockIndex: 1,170 ms
  - Cells scanned: 2,839,031
  - Individual wallMs: [8757, 8674, 8436, 8862]
- **Arm B (Cache ON, Mutation-Safe)**:
  - Median wall: 8,178 ms
  - Median CPU: 8,149 ms
  - Median Expansions/sec: 64
  - hazardBlockIndex: 835 ms
  - Cells scanned: 2,004,678
  - Cache hits: 4,937
  - Individual wallMs: [7916, 8293, 8521, 8063]
- **改进指标**:
  - **Overall wall improvement**: **6.2%**
  - **Target bucket improvement**: **28.6%** (满足 `target bucket self-time improvement >= 20% AND overall wall improvement statistically clear` fallback contract 门禁)
  - `faster: true` (3/4 pair 变快，中位数整体优于 OFF)

### Formal Real MT1→MT4 Hot-Path Qualification Run 1 — 2026-09-05

- **运行环境**：
  - HEAD: `5a417d1e8ad1a883161c325fc21647631df2934a`（QUALIFICATION_SHA）
  - Working Tree: Clean（已严格验证）
  - Node: `v24.18.0`
  - OS: `win32 x64 10.0.26200` (Windows 11)
  - Memoization 默认状态: `enableHazardBlockIndexMemoization = true`（生产开启）
- **运行前回归补齐**：
  - `check-counterfactual-repair.js`: PASSED (G24 A-K7, 20/20 全部通过)
  - `check-budgeted-repair-scheduling.js --fast`: PASSED
  - `check:route-free-state`: PASSED
  - `check:onlyup-mt1-real-route-gate`: PASSED (10/10 decisions, strict replay matched)
- **冻结配置**：
  - `searchIntent: "adaptive-feasible"`
  - `enableFailureBacktracking: true`, `adaptiveBacktrackDepth: 3`
  - `enableBudgetedRepairScheduling: true`, `enableBudgetedRepairContinuation: true`
  - `milestoneFrontierResourceDiversity: true`
  - `failure-intent ranking = OFF`
  - `segmentExecutionMode = "isolated-process"`
  - `batch first probe = ON`
  - `budgetScope: "global-run"`
  - `maxRuntimeMs: 30000`, `maxExpansions: 50000`, `maxRssMb: 2048`
  - `candidateLimit: 8`
  - 探测试算预算：first probe 200 exp / 2000 ms；CF intent realization 200 exp / 2000 ms；continuation 400 exp / 4000 ms
- **阶段墙钟分解 (Phase Wall)**：
  ```text
  phaseWall:
    initialMilestoneMs:                10,829 ms (36.7%)  [-1,867 ms vs 5.24f Formal Run 2 (12,696 ms), -14.7%]
    anchorGenerationMs:                 3,928 ms (13.3%)  [+795 ms vs 5.24f Formal Run 2 (3,133 ms)]
    primaryFirstProbeMs:               14,768 ms (50.0%)  [+1,083 ms vs 5.24f Formal Run 2 (13,685 ms)]
    counterfactualIntentRealizationMs:      0 ms ( 0.0%)  [CF not triggered]
    counterfactualAnchorGenerationMs:       0 ms ( 0.0%)  [CF not triggered]
    counterfactualFirstProbeMs:             0 ms ( 0.0%)  [CF not triggered]
    secondaryFirstProbeMs:                  0 ms ( 0.0%)  [no deferred]
    continuationMs:                         1 ms ( 0.0%)
    otherPlannerMs:                         8 ms ( 0.0%)
  totalPhaseWall: 29,534 ms
  global wallMs:  29,535 ms
  ```
  **TOP 3 WALL CONSUMERS**:
  1. `primaryFirstProbeMs`: 14,768 ms (50.0%) — 8-job batch DP 搜索本体
  2. `initialMilestoneMs`: 10,829 ms (36.7%) — 前向主线搜索 (较 5.24f 提速 14.7%)
  3. `anchorGenerationMs`: 3,928 ms (13.3%) — 锚点生成阶段
- **阶段进程启动次数 (Phase Process Launches)**：
  ```text
  phaseProcessLaunches:
    initial:            3  (forward path, NOT batched)
    anchor:             4  (per-depth isolated, NOT batched)
    intentRealization:  0  (CF not triggered)
    cfAnchor:           0  (CF not triggered)
    firstProbe:         1  (BATCHED: 8 jobs in 1 process)
    continuation:       0  (not triggered)
  TOTAL_PROCESS_LAUNCHES = 8
  ```
- **Initial + Primary 吞吐与阶段拆解**：
  - `initialMilestoneMs`: 10,829 ms（MT1, MT2 成功，初始 MT2→MT3 失败，前向初始主线结束），较 5.24f (12,696 ms) 明显提速 **14.7% (-1,867 ms)**；
  - `anchorGenerationMs`: 3,928 ms（共 4 个锚点生成调用，含 MT1→MT2 回滚重扩 115 步生成 8 个代表，以及 MT3→MT4 失败后在深度 1 和 2 的重扩，执行 238 步）；
  - `primaryFirstProbeMs`: 14,768 ms（批处理 8-job 全部执行，消耗 1,093 扩展，耗时 14,358 ms，成功修复 MT2→MT3 打通至 MT3，HP 10,820 / ATK 97 / DEF 70 / MDEF 490）；
  - `mt3-to-mt4` 阶段：消耗 1,365 扩展，耗时 7,824 ms，峰值 RSS 252.7 MB，结果为 `floor-search-incomplete`；
  - 第二轮回退阶段：在深度 1 执行 151 扩展耗时 1,034 ms，深度 2 执行 87 扩展耗时 505 ms 后 30s 墙钟耗尽。
- **Batch 契约保持**：
  - `primary batch`: `jobsRequested: 8, jobsExecuted: 8, jobsNotStarted: 0, processLaunches: 1, processWallMs: 14740ms, searchWallMs: 14358ms`；
  - 批处理机制完全正常，单进程承载 8 个独立 job，每 job 独立 2000ms 墙钟重基生效。
- **Counterfactual Admission**：
  - `counterfactualRepair.triggered: false`；
  - 未触发原因：30s 墙钟在深度 2-3 的正常回溯锚点生成阶段耗尽（`globalStopReason: time-limit`），正常首轮未完备；
  - CF 候选历史生成为 0，探测试算为 0。
- **全局权能与未决状态**：
  - `found: false`；
  - `reachedMilestone: mt2-to-mt3`；
  - `failedSegment: mt3-to-mt4` (`floor-search-incomplete`)；
  - `activePendingTickets: 2` (h-d2w0, h-d3w0 均处于 `PROBE_PENDING`)；
  - `finalPending: 12`, `terminalIncomplete: 0`, `unknownCompletion: 0`；
  - `depthOutcome`: depth 1 `exhausted`，depth 2 `incomplete`，depth 3 `incomplete`；
  - **严禁声称 EXHAUSTED**（存在 2 个活跃未决工单及 12 个 pending 候选）。
- **与 PR-5.24f Formal Run 2 的严格对比**：
  | 指标 | PR-5.24f Formal Run 2 (`862c684`) | PR-5.24g Qualification Run 1 (`5a417d1`) | Delta |
  |---|---|---|---|
  | 全局扩展 (Tree Expansions) | 3,375 | 3,025 | -350 |
  | 全局墙钟 (Global Wall) | 29,526 ms | 29,535 ms | +9 ms |
  | 峰值 RSS (Worker Peak RSS) | 346.2 MB | 252.7 MB | -93.5 MB |
  | Initial Milestone 耗时 | 12,696 ms | 10,829 ms | **-1,867 ms (-14.7%)** |
  | Primary First-Probe 耗时 | 13,685 ms | 14,768 ms | +1,083 ms |
  | Anchor Generation 耗时 | 3,133 ms | 3,928 ms | +795 ms |
  | 进程启动总数 | 8 | 8 | 0 (完全一致) |
  | 批处理子进程数 | 1 (8 jobs) | 1 (8 jobs) | 0 (完全一致) |
  | CF Reached | false | false | 一致 |
  | 终止原因 | time-limit | time-limit | 一致 |
- **分析与归因**：
  - **前向主线加速确认有效**: `initialMilestoneMs` 从 12,696 ms 降至 10,829 ms（**缩短 1.87 秒，提升 14.7%**），证实 `hazardBlockIndex` memoization 确实在真实前向搜索中带来了可测的端到端提速；
  - **端到端总吞吐瓶颈依然受限**: 尽管前向主线释放了约 1.87 秒墙钟，但在 MT2→MT3 修复批处理（14.77s）和后续 MT3→MT4 前向尝试（7.82s）中，总用时依然吃满了 30 秒预算，剩余时间不足以让第二轮回退完成深度 2-3 的锚点生成并进入 CF 阶段；
  - **瓶颈转移与再定位**: 正如 Baseline profiler 所揭示的，`stabilization` 内部最大的耗时源并非 `hazardBlockIndex`（1,966 ms），而是 **`battleScan` (6,015 ms)**。单项优化 `hazardBlockIndex` 产生的 ~6% micro fixture / 14.7% initial milestone 提速不足以彻底突破全局 30s 预算壁垒；
- **Single Primary Verdict**：
  ```ini
  PRIMARY_VERDICT = GLOBAL_RESOURCE_LIMITED_BEFORE_COUNTERFACTUAL_EVALUATION
  END_TO_END_THROUGHPUT_EFFECT = INITIAL_PHASE_ACCELERATED_BUT_TOTAL_WALL_STILL_BINDING
  NEXT_BINDING_PROBLEM = BATTLE_SCAN_EVALUATION_AND_STABILIZATION_HOTSPOT
  ```
- **下一方向判定**：
  - 维持 `enableHazardBlockIndexMemoization = true` 生产默认（安全生效，零状态污染，6.2% microbenchmark 改善保留）；
  - 下一迭代（Iteration 2）根据 Baseline Profiler 针对 `stabilization` 内占比最高的 **`battleScan` / `battleResolverEvaluate`**（6,015 ms / 54,811 calls）进行 profile-guided hot-path 攻坚，不再盲目调优 block index，也不跳跃至 multi-step CF。

### Iteration 1 终审结论与正式关账 — 2026-09-05

```ini
PR_5_24g = IN_PROGRESS

ITERATION_1 = APPROVED_AND_CLOSED_WITH_POSITIVE_LOCAL_PERFORMANCE_AND_NEUTRAL_REAL_CAPABILITY_RESULT

PRODUCTION_COMMIT = 5a417d1e8ad1a883161c325fc21647631df2934a
QUALIFICATION_RECORD = 82b09fa6ac7f87dc1c007488bccfa0d7fd5ea6f9

MUTATION_SAFE_MEMOIZATION = PROVEN
LOCAL_CANONICAL_SPEEDUP = PROVEN
LOCAL_FIXTURE_MEDIAN_WALL_GAIN = 6.2%

REAL_INITIAL_PHASE_WALL_DELTA = -14.7% OBSERVED_ONLY
REAL_END_TO_END_THROUGHPUT_GAIN = NOT_PROVEN
REAL_CF_HEADROOM_GAIN = NOT_PROVEN
MT4_CAPABILITY_GAIN = NOT_PROVEN

P0 = NONE
P1 = NONE
P2_1 = G26_D_DOES_NOT_ENFORCE_PERFORMANCE_ACCEPTANCE_THRESHOLD
P2_2 = TARGET_BUCKET_IMPROVEMENT_USES_LAST_PAIR_NOT_MEDIAN

NEXT_ITERATION = PR_5_24g_ITERATION_2
NEXT_TARGET = BATTLE_SCAN
```

## Iteration 2 — Battle-Scan / Battle-Evaluation Hot-Path Optimization

### 目标与边界
在保持 auto-battle、battle damage、fast-reject、search order、DP/state 语义 100% 完全不变的前提下，降低 stabilization 中 battle scanning 的 CPU / wall cost。
- 绝不修改战斗伤害公式；
- 绝不修改 zero-damage acceptance 定义；
- 绝不修改 autoBattle 搜索排序与剪枝；
- 绝不跳跃至 multi-step CF。

### 1. 深度 Profiling 下钻与 AutoBattle Scan 漏斗分析

在确定性 520 expansions fixture (`mt2-to-mt3`) 下对 `evaluateBattle` 内部耗时与 AutoBattle 漏斗进行深度探测：

#### scanBattleEvaluation 内部阶段拆解 (Baseline)
- `battleScanMs`: ~2,223 ms
- `scanBattleEvaluationMs`: ~1,369 ms
  - `battleEstimateKeyBuildMs`: 363 ms (26.5%)
  - `battleEstimateCacheLookupMs`: 351 ms (25.6%)（注：此计时包含其内部调用的 `battleEstimateCacheHitReturnMs`）
  - `battleEstimateCacheHitReturnMs`: 256 ms (18.7%)（命中时深拷贝 `cloneJson` 开销）
  - `battleEstimateUncachedComputeMs`: 805 ms (58.8%)
  - `battleEstimateCacheStoreMs`: 61 ms (4.5%)
- **计时说明**:
  `battleEstimateCacheLookupMs` 包裹了 `cacheGet()` 的全过程，其内部已包含 `battleEstimateCacheHitReturnMs`，二者不可简单累加（P2 纠偏）。但 `KeyBuild` (363ms) 与命中深拷贝 `HitReturn` (256ms) 明确构成了 evaluation 管道中最大的非计算开销。

#### 缓存统计与 AutoBattle 漏斗
- `Cache Hits`: 56,763 / 64,049 (**Hit Rate = 88.6%**)
- `scanBattleCandidateChecks`: 69,924
- `scanFastRejectChecks`: 69,924
- `scanFastRejectSkipped`: 15,343 (**Fast Reject Rate = 21.9%**)
- `scanBattleResolverEvaluateCalls`: 54,581 (**Authoritative Eval Rate = 78.1%**)
- `scanBattleRejectedNonZeroDamage`: 51,630 (占权威评估的 94.6%)
- `scanBattleRejectedNoDamageInfo`: 1,545 (2.8%)
- `scanBattleAcceptedZeroDamage`: 1,406 (**Zero-Damage Accept Rate = 2.576%**)

#### 诊断归因与 Case 选型
数据完全符合 Cloud Review 定义的 **Case C (cache hit 很高，但 key-build 与 cloneJson 吞掉收益，只优化 lookup plumbing)**：
- 缓存命中率高达 88.6%，但在 5.6 万次命中中每次均执行复杂的对象容器构建与序列化，并在命中返回时反复执行深拷贝 `cloneJson`；
- 审计确认，全库所有调用者对 `evaluateBattle` 返回的 `battle` 对象均为只读访问（只读 `damageInfo.damage`、`supported`、`guards`），不存在任何就地修改。

### 2. 单项优化实现 (`enableFastBattleEstimateCache`) (Repair 1 封死正确性漏洞)

**文件**: `shared-solver/lib/battle-resolver.js`

1. **紧凑定长数组无碰撞键构建器 (`buildFastBattleEstimateKey`)**：
   - 彻底废除初始草案中存在分隔符碰撞风险的裸字符串拼接，改用原生紧凑数组定长序列化：
     `JSON.stringify([floorId, x, y, enemyId, hp, atk, def, mdef, lv, hpmax, mana, manamax, eq, invList, flagsList, remList, repList])`；
   - 消除中间嵌套对象/数组分配，直接借助 V8 C++ 级底层 `JSON.stringify` 保证各类型、分隔符、转义与复杂对象边界完全无歧义；
   - 导出 `buildFastBattleEstimateKey` 与 `buildLegacyBattleEstimateKey`，测试硬保证两者等价类完全双射（`legacyKey(A) === legacyKey(B) <=> fastKey(A) === fastKey(B)`）。
2. **深度冻结不可变快照与零拷贝直返 (Deep-Frozen Immutable Snapshot & Zero-Clone Direct Return)**：
   - 彻底废除仅顶层浅冻结的 `Object.freeze`；
   - 实现递归 `deepFreeze` 辅助函数（递归冻结 `damageInfo`、`enemyInfo`、`guards` 及嵌套结构）；
   - **存储时深拷贝一次并递归冻结**：`cacheSet` 执行 `storedValue = deepFreeze(cloneJson(value))`；
   - **命中时零拷贝直返**：`cacheGet` 命中直接返回不可变快照，既不重新深拷贝，也绝不污染原始 `this.project` 或运行时怪兽定义；
   - 任何调用者若尝试就地修改返回快照（如 `res.damageInfo.damage = 999`），在严格模式下直接抛错，普通模式下静默拦截，下一次缓存读取 100% 保持原始权威值。
3. **全链路开关透传**:
   - `enableFastBattleEstimateCache` 默认为 `true`；
   - 贯穿 `FunctionBackedBattleResolver`、`StaticSimulator`、`isolated-segment-executor.js` 与 `segment-worker.js`，可随时独立关闭回退。

### 3. G27 门禁实测结果 (`check-dp-hot-path.js`)

在确定性 520 expansions fixture (`mt2-to-mt3`) 下执行：

```json
{
  "schema": "motapathfinder.dp-hot-path.v3",
  "contractStatus": "passed",
  "iteration": "Iteration 2 Repair 1 (Battle-Scan Optimization)",
  "iteration1_regression_gates": {
    "G26-A": { "exactSearchEquivalence": true, "expansions": 520 },
    "G26-B": { "expansionOrderEquivalence": true, "expansionsCompared": 100 },
    "G26-C1": { "stateKeyMutationSensitivityVerified": true, "allMutationsDiffer": true },
    "G26-C2": { "cachedBlockIndexMutationInvalidationVerified": true },
    "G26-C3": { "realStabilizationMutationPathVerified": true }
  },
  "iteration2_gates": {
    "G27-A": {
      "battleResultExactParityVerified": true,
      "enemiesChecked": 10,
      "extendedConditionsChecked": 5,
      "allParityMatched": true
    },
    "G27-B": {
      "autoBattleScanParityVerified": true,
      "zeroDamageTargetsCount": 22,
      "targetsExactMatch": true
    },
    "G27-C": {
      "stabilizationExactParityVerified": true,
      "allStateFieldsMatch": true
    },
    "G27-D": {
      "searchParityVerified": true,
      "expansions": 520,
      "acceptedStates": 839,
      "goalSkylineCount": 8,
      "first100ExpansionsMatched": true,
      "routeExactMatch": true,
      "cacheOffAndOnIdentical": true
    },
    "G27-E": {
      "performanceMeasured": true,
      "pairsCount": 4,
      "acceptanceThresholdMet": true,
      "armA_off": {
        "medianWallMs": 6102,
        "medianCpuMs": 6453,
        "medianExpansionsPerSec": 85,
        "battleScanMs": 2419,
        "scanBattleEvaluationMs": 1473,
        "battleEstimateKeyBuildMs": 402,
        "battleEstimateCacheLookupMs": 377,
        "battleEstimateCacheHitReturnMs": 272,
        "battleEstimateUncachedComputeMs": 859,
        "individualWallMs": [6409, 6133, 5976, 6070]
      },
      "armB_on": {
        "medianWallMs": 5749,
        "medianCpuMs": 6102,
        "medianExpansionsPerSec": 90,
        "battleScanMs": 2255,
        "scanBattleEvaluationMs": 1282,
        "battleEstimateKeyBuildMs": 430,
        "battleEstimateCacheLookupMs": 80,
        "battleEstimateCacheHitReturnMs": 0,
        "battleEstimateUncachedComputeMs": 875,
        "battleEstimateCacheHits": 56861,
        "battleEstimateCacheMisses": 7188,
        "battleEstimateHitRatePercent": 88.8,
        "fastRejectRatePercent": 21.9,
        "individualWallMs": [5718, 5720, 5778, 5917]
      },
      "pairImprovements": [
        { "pair": 0, "wallGain": 10.8, "targetGain": 20.0 },
        { "pair": 1, "wallGain": 6.7, "targetGain": 17.6 },
        { "pair": 2, "wallGain": 3.3, "targetGain": 12.7 },
        { "pair": 3, "wallGain": 2.5, "targetGain": 13.0 }
      ],
      "overallWallImprovementPercent": 5.8,
      "targetBucketMedianImprovementPercent": 15.3,
      "targetBucketLastPairImprovementPercent": 13.0,
      "faster": true
    },
    "G27-F": {
      "battleCacheMutationSafetyVerified": true,
      "all8DimensionsDiverged": true
    },
    "G27-G": {
      "fastKeyEquivalenceVerified": true,
      "targetedCollisionTestsPassed": 7,
      "syntheticStatesTested": 500,
      "uniqueEquivalenceClasses": 447,
      "exactBijectionProven": true
    },
    "G27-H": {
      "cachedResultAliasSafetyVerified": true,
      "damageMutationBlocked": true,
      "enemyInfoMutationBlocked": true,
      "guardsMutationBlocked": true,
      "cacheHitPristineMatched": true,
      "projectEnemyNotFrozen": true
    }
  }
}
```

#### 门禁核心亮点
1. **G27-A (Battle Parity)**: 10 种跨层敌怪 + 5 种扩展变异条件（cache hit, floor mutation, flag mutation, inventory mutation, equipment mutation），`supported`, `damage`, `turn`, `mon_hp`, `mon_atk`, `mon_def`, `guards`, `enemyInfo` 逐项完全一致。
2. **G27-B (AutoBattle Scan Parity)**: 高属性状态下 22 个零伤候选目标（enemyId, coords, distance, continuePast）完全一致。
3. **G27-C (Stabilization Parity)**: 真实 `stabilizeState` 产出的 stateKey 与所有状态字段 100% 严格一致。
4. **G27-D (Search Parity)**: 520 expansions 确定性完成，前 100 次扩展 DP key、route trace、goal skyline 与 searchOutcome 100% 逐项完全一致。
5. **G27-E (Performance A/B)**:
   - **4/4 Pair 全部加速**（Pair 0: +10.8%, Pair 1: +6.7%, Pair 2: +3.3%, Pair 3: +2.5%）；
   - **Overall Wall 中位数提升**: **5.8%**（6,102 ms → 5,749 ms，满足 `>= 5%` 门禁标准，代码显式 `assert.ok(meetsThreshold)`，P2-1 彻底解决）；
   - **Target Bucket (`scanBattleEvaluation`) 中位数提升**: **15.3%**（1,473 ms → 1,282 ms，成对中位数计算，P2-2 彻底解决）；
   - `battleEstimateCacheHitReturnMs`: 从 **272 ms 彻底归零 (0 ms)**；
   - `battleEstimateCacheLookupMs`: 从 **377 ms 降至 80 ms**。
6. **G27-F (Mutation Safety)**: 8 维变异（HP, ATK, DEF, MDEF, equipment, inventory, flags, floor mutations）均产生完全不同的 cache key，严格杜绝 stale cache。
7. **G27-G (Fast-Key Equivalence & Collision Resistance)**:
   - 7 组定向碰撞测试全部通过（包含 flags 中分隔符 `b,c:d` vs 独立 key、装备中含逗号 `["a,b"]` vs `["a","b"]`、flags 包含嵌套对象、字符串含 `|` 管道符、包含 unicode 与空串、布尔值与字符串 `"true"`、flags 包含数组）；
   - 500 个随机对抗性合成状态属性测试，Legacy 产生 447 个等价类，Fast 产生 447 个等价类，**在规范求解器状态域上达成 100% 严格双射，无碰撞**。
8. **G27-H (Cached Result Alias Safety)**:
   - 缓存写入对象深层不可变性测试通过：尝试就地篡改 `resA.damageInfo.damage`、`resA.enemyInfo.money`、`resA.guards.push(...)` 均被成功拦截；
   - 第二次 evaluate 读出的 `resB` 与原始结果 100% 相同；
   - `this.project.enemysById` 确认未被冻结，零运行时污染。

### Formal Real MT1→MT4 Combined Hot-Path Qualification Run 2 — 2026-09-05

- **运行环境**：
  - HEAD: `db2e6c120f3855a97e62694a4a2946772e56bb8a`（QUALIFICATION_SHA）
  - Working Tree: Clean（已严格验证）
  - Node: `v24.18.0`
  - OS: `win32 x64 10.0.26200` (Windows 11)
  - 生产双重优化激活: `enableHazardBlockIndexMemoization = true` 且 `enableFastBattleEstimateCache = true`
- **冻结配置**：
  - `searchIntent: "adaptive-feasible"`
  - `enableFailureBacktracking: true`, `adaptiveBacktrackDepth: 3`
  - `enableBudgetedRepairScheduling: true`, `enableBudgetedRepairContinuation: true`
  - `milestoneFrontierResourceDiversity: true`
  - `failure-intent ranking = OFF`
  - `segmentExecutionMode = "isolated-process"`
  - `batch first probe = ON`
  - `budgetScope: "global-run"`
  - `maxRuntimeMs: 30000`, `maxExpansions: 50000`, `maxRssMb: 2048`
  - `candidateLimit: 8`
  - 探测试算预算：first probe 200 exp / 2000 ms；CF intent realization 200 exp / 2000 ms；continuation 400 exp / 4000 ms
  - 仅执行一次真实运行，无任何运行时补丁。
- **阶段墙钟分解 (Phase Wall)**：
  ```text
  phaseWall:
    initialMilestoneMs:                16,615 ms (56.3%)  [含 MT1 (888ms), MT2 (1188ms), MT3→MT4 (13741ms/2764exp)]
    anchorGenerationMs:                 3,438 ms (11.6%)  [MT1 回滚 1232ms + 第二轮回退 1309ms + 125ms]
    primaryFirstProbeMs:                9,478 ms (32.1%)  [-5,290 ms (-35.8%) vs Run 1 (14,768 ms)]
    counterfactualIntentRealizationMs:      0 ms ( 0.0%)
    counterfactualAnchorGenerationMs:       0 ms ( 0.0%)
    counterfactualFirstProbeMs:             0 ms ( 0.0%)
    secondaryFirstProbeMs:                  0 ms ( 0.0%)
    continuationMs:                         0 ms ( 0.0%)
    otherPlannerMs:                         7 ms ( 0.0%)
  totalPhaseWall: 29,538 ms
  global wallMs:  29,539 ms
  ```
- **阶段进程启动次数 (Phase Process Launches)**：
  ```text
  phaseProcessLaunches:
    initial:            3  (forward path, NOT batched)
    anchor:             4  (per-depth isolated, NOT batched)
    intentRealization:  0
    cfAnchor:           0
    firstProbe:         1  (BATCHED: 8 jobs in 1 process)
    continuation:       0
  TOTAL_PROCESS_LAUNCHES = 8
  ```
- **Primary Repair Tranche 与批处理吞吐翻倍实证**：
  - `jobsRequested: 8, jobsExecuted: 8, jobsNotStarted: 0, processLaunches: 1`；
  - `processWallMs: 9,449 ms, searchWallMs: 9,211 ms, serializationWallMs: 2 ms`；
  - **首探批处理耗时从 14.74s 大幅缩短至 9.45s（节省 5.29 秒，缩短 35.8%）**；
  - **首探吞吐翻倍**：完成 1,389 扩展 / 9.21s = **150.8 exp/s**（上一轮 Run 1 为 1,093 扩展 / 14.36s = 76.1 exp/s，**吞吐提升 +98.2%**）；
  - 成功以 HP 9,891 / ATK 97 / DEF 70 / MDEF 490 打通 MT2→MT3 修复至 MT3。
- **下游主线 MT3→MT4 探索深度加倍**：
  - 由于首探批处理省下约 5.3 秒墙钟，下游 MT3→MT4 前向主线获得了 **13,741 ms** 的充裕探索时间（上一轮仅有 7,824 ms）；
  - **MT3→MT4 探索扩展数翻倍**：从 Run 1 的 1,365 扩展跃升至 **2,764 扩展 (+102.5%)**；
  - 下游吞吐亦从 174.4 exp/s 提升至 **201.1 exp/s (+15.3%)**。
- **Counterfactual Admission**：
  - `counterfactualRepair.triggered: false`；
  - 未触发原因：MT3→MT4 初始失败后触发第二轮回溯，30s 墙钟在深度 2-3 的正常锚点生成阶段耗尽（深度 1 执行 151 扩展耗时 1309ms，深度 2 执行 9 扩展耗时 125ms 后超时），正常首轮尚未完备；
  - `ONE_STEP_CF_GENERATED = 0`，`ONE_STEP_CF_PROBED = 0`。
- **全局权能与未决状态**：
  - `found: false`；
  - `reachedMilestone: mt2-to-mt3`；
  - `failedSegment: mt3-to-mt4` (`floor-search-incomplete`)；
  - `activePendingTickets: 2` (h-d2w0, h-d3w0 均处于 `PROBE_PENDING`)；
  - `outstandingDeferredNormals: 0`；
  - `finalPending: 18`, `terminalIncomplete: 0`, `unknownCompletion: 0`；
  - `depthOutcome`: depth 1 `exhausted`，depth 2 `incomplete`，depth 3 `incomplete`；
  - **严禁声称 EXHAUSTED**（存在 2 个活跃未决工单及 18 个 pending 候选）。
- **与 PR-5.24g Iter1 Run 1 及 PR-5.24f Formal Run 2 的严格对比**：
  | 关键指标 | PR-5.24f Formal Run 2 (`862c684`) | PR-5.24g Iter1 Run 1 (`5a417d1`) | PR-5.24g Iter2 Run 2 (`db2e6c1`) | Run 2 vs Run 1 Delta | 评价 |
  |---|---|---|---|---|---|
  | **全局扩展数 (Tree Expansions)** | 3,375 | 3,025 | **4,642** | **+1,617 (+53.5%)** | **真实端到端搜索量暴增** |
  | **全局墙钟 (Global Wall)** | 29,526 ms | 29,535 ms | 29,539 ms | +4 ms | 严格受控于 30s 预算 |
  | **Primary Batch Probe 耗时** | 13,685 ms | 14,768 ms | **9,478 ms** | **-5,290 ms (-35.8%)** | **首探批处理用时大幅收缩** |
  | **Primary Batch Probe 吞吐** | ~80 exp/s | 76.1 exp/s | **150.8 exp/s** | **+74.7 exp/s (+98.2%)** | **首探吞吐近乎翻倍** |
  | **MT3→MT4 Attempt 耗时** | ~10.9s | 7,824 ms | **13,741 ms** | **+5,917 ms (+75.6%)** | 释放的墙钟被全额注入下游主线探索 |
  | **MT3→MT4 Attempt 扩展数** | ~1,800 | 1,365 | **2,764** | **+1,399 (+102.5%)** | **下游搜索深度翻倍** |
  | **Anchor Generation 耗时** | 3,133 ms | 3,928 ms | 3,438 ms | -490 ms (-12.5%) | 锚点重扩略有节约 |
  | **Worker 峰值 RSS** | 346.2 MB | 252.7 MB | 281.5 MB | +28.8 MB | 远低于 2048 MB 安全熔断器 |
  | **进程启动总数** | 8 | 8 | 8 | 0 | 完全一致 |
  | **批处理子进程数** | 1 (8 jobs) | 1 (8 jobs) | 1 (8 jobs) | 0 | 完全一致 |
  | **CF Reached** | false | false | false | 0 | 墙钟在第二轮回退中耗尽 |
- **分析与归因**：
  - **真实端到端吞吐提升观测到强正向信号**: 在完全相同的 30s 生产配置下，观察到全局真实 canonical expansion 从 3,025 次提升至 **4,642 次 (+53.5% OBSERVED)**；严格 causal speedup 由 fixed-work G26/G27 OFF/ON fixtures 建立，不由跨 Real Run 百分比建立；
  - **首探批处理耗时显著收缩**: Iteration 1（图块索引记忆化）+ Iteration 2（战斗缓存查找管道零拷贝与定型序列化）累积优化将 Primary Batch Probe 耗时从 **14.77 秒压缩至 9.48 秒（节约 5.29 秒，-35.8% OBSERVED）**，探测试算吞吐由 76.1 exp/s 提升至 **150.8 exp/s (+98.2% OBSERVED)**；
  - **下游 MT3→MT4 主线探索获得更多剩余墙钟**: 节省下来的 5.3 秒墙钟使得求解器在 MT3→MT4 这一极高复杂度段上完成了 **2,764 次扩展（翻倍于上一轮的 1,365 次）**；
  - **瓶颈转移**: 尽管端到端搜索量显著增加，但 MT3→MT4 本身单位 canonical work 依然昂贵（13.74s 仅推进 2.7k 扩展即耗尽可用时间），导致第二轮回溯无法在 30s 内完备，CF 仍未获得准入。当前瓶颈为 **MT3→MT4 下游规范搜索本体成本**（而非回溯预算调度策略）。
- **Single Primary Verdict**：
  ```ini
  PRIMARY_VERDICT = COMBINED_HOT_PATH_OPTIMIZATION_INCREASED_CANONICAL_THROUGHPUT

  END_TO_END_EFFECT = REAL_EXPANSIONS_INCREASED_BY_53_PERCENT_AND_BATCH_PROBE_TIME_REDUCED_BY_36_PERCENT
  NEXT_BINDING_PROBLEM = MT3_TO_MT4_DOWNSTREAM_CANONICAL_SEARCH_COST
  ```
- **下一方向判定**：
  - 双重 Hot-Path 优化确认具备极其显著的端到端真实吞吐提升（30s 内从 3k 扩展提升到 4.6k 扩展，主线探索翻倍），永久保留为生产默认；
  - PR-5.24g 到此正式闭环关账，不再开 Iteration 3，转入 **PR-5.24h (MT3→MT4 Downstream Canonical Search Cost Reduction)**。

### PR-5.24g 里程碑终审与正式关账 — 2026-09-05

```ini
PR_5_24g = APPROVED_AND_CLOSED_WITH_LOCAL_CAUSAL_SPEEDUPS_AND_REAL_THROUGHPUT_GAIN_OBSERVED

FINAL_PRODUCTION_COMMIT = db2e6c120f3855a97e62694a4a2946772e56bb8a
FINAL_QUALIFICATION_RECORD = 3c4ae80a8c704b996aa4b2f202aa0962a339dcfd

ITERATION_1_HAZARD_BLOCK_INDEX_MEMOIZATION = APPROVED
ITERATION_2_FAST_BATTLE_ESTIMATE_CACHE = APPROVED

MUTATION_SAFE_BLOCK_INDEX_CACHE = PROVEN
FAST_BATTLE_CACHE_CANONICAL_PARITY = PROVEN
FAST_BATTLE_CACHE_ALIAS_SAFETY = PROVEN
FAST_KEY_CANONICAL_DOMAIN_EQUIVALENCE = PROVEN

LOCAL_CAUSAL_PERFORMANCE_GAIN = PROVEN

REAL_GLOBAL_EXPANSIONS = 4642
REAL_EXPANSION_DELTA_VS_ITER1 = +53.5% OBSERVED
REAL_EXPANSION_DELTA_VS_PRE_HOT_PATH = +37.5% OBSERVED

REAL_END_TO_END_POSITIVE_THROUGHPUT_SIGNAL = PROVEN
EXACT_REAL_CAUSAL_SPEEDUP_PERCENT = NOT_ESTABLISHED

MT4_CAPABILITY = NOT_PROVEN
CF_HEADROOM = NOT_RESTORED
ONE_STEP_CF_CAPABILITY = STILL_UNDETERMINED

PRIMARY_VERDICT = COMBINED_HOT_PATH_OPTIMIZATION_INCREASED_CANONICAL_THROUGHPUT

P0 = NONE
P1 = NONE
P2_1 = REAL_RUN_PERCENT_DELTAS_ARE_OBSERVATIONAL_NOT_STRICT_CAUSAL_AB
P2_2 = REAL_RUN_DID_NOT_REPORT_AGGREGATED_BATTLE_AND_HAZARD_CACHE_TELEMETRY

NEXT_MILESTONE = PR_5_24h
NEXT_BINDING_PROBLEM = MT3_TO_MT4_DOWNSTREAM_CANONICAL_SEARCH_COST
```
