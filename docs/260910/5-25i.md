# PR-5.25i — On-Policy Floor Transition Dynamics Probe

```ini
MILESTONE = PR-5.25i
STATUS = COMPLETE / RESULT_RECORDED
DATE = 2026-09-10
PREVIOUS_MILESTONE = PR-5.25h (CLOSED_AFTER_REPAIR_1, dd5e597 = APPROVED_WITH_P2_TEST_NOTE)
SCOPE = 零训练 telemetry probe：这 2355 次 changeFloor 到底在干什么？
```

## 0. 动机

5.25h Repair 1 的 telemetry：`changeFloor` 被选了 **2355 / 3072** 步，却仍只有 1/32 到 MT3、0 MT4/MT5。
所以本轮不调 H / T，只回答：这些换层动作**实际在做什么**。

## 1. 冻结协议（零训练）

```ini
RETRAIN = FALSE          MODEL_CHANGE = FALSE        FEATURE_CHANGE = FALSE
CORPUS_CHANGE = FALSE    T = 1.0                     H = 96
ROLLOUTS = 32 PAIRED SEEDS（与 dd5e597 相同）
CONTROL = PROPER_KIND_PRIOR_ONLY
TREATMENT = PROPER_KIND_PRIOR + WITHIN_KIND_RESIDUAL
TERMINAL = MT5 && floorStates.MT5.removed["6,7"]
```

只新增 telemetry；`changeFloor` 的 transition 由 **apply 之后的 state.floorId** 读出，因此是精确的。

## 2. 结果

```bash
npm run check:learned-prior-on-policy-floor-dynamics --prefix shared-solver
# -> shared-solver/routes/generated/learned-prior-on-policy-floor-dynamics.result.json
```

| 指标 | CONTROL（kind-prior only） | TREATMENT（hierarchical） |
| --- | --- | --- |
| floor transitions | `MT1->MT2: 871`, `MT2->MT1: 861` | `MT1->MT2: 1159`, `MT2->MT1: 1143`, **`MT2->MT3: 27`**, `MT3->MT2: 26` |
| floor changes / forward / backward | 1732 / 871 / 861 | 2355 / 1186 / 1169 |
| immediate reversals | 1709（**98.7%**） | 2322（**98.6%**） |
| MT2 steps（forward stair 可用） | 1033（**0，0.0%**） | 1484（**30，2.0%**） |
| forward available steps / chose forward | 907 / 871（96.0%） | 1238 / 1186（95.8%） |
| treatment forward prob mass（available 时） | — | **0.9624** |
| rolling states / unique exact / revisits | 3104 / 672 / 2432（78.4%） | 3104 / 956 / 2148（69.2%） |
| 首次 MT2-forward-stair 可用 step | 从未 | min 40，mean 40.0 |
| reach | MT2 23/32, MT3 0/32, MT4 0, MT5 0, blueKing 0 | MT2 32/32, MT3 1/32, MT4 0, MT5 0, blueKing 0 |

> 注意口径：`forward available steps`（907 / 1238）大部分发生在 **MT1**（MT1→MT2 相对当前层就是 forward）。
> 决定性的数字是 **MT2 上 forward（MT2→MT3）楼梯的可用比例：control 0.0%、treatment 2.0%**。

## 3. 分类（显式准则 + 原始数字）

```ini
B_PREREQUISITE_SEQUENCING_PROBLEM : mt2ForwardFraction < 0.5
C_HORIZON_PRESSURE_OBSERVED       : forwardAppearsLate AND forwardChoiceRate >= 0.5
A_ON_POLICY_WITHIN_KIND_TRANSFER  : forwardChoiceRate < 0.5 OR reversalRate >= 0.3
D_CYCLIC_PROPOSAL_BEHAVIOR        : revisitRate >= 0.5
```

```ini
VERDICT = B_PREREQUISITE_SEQUENCING_PROBLEM
  primary   : B   (mt2ForwardFraction = 0.020 treatment / 0.000 control)
  co-observed: A (仅由 reversalRate 子句触发), D (revisitRate 0.69/0.78)
  refuted   : A 的**主信号**（forward 可用时选错）—— 可用时 95.8% 选 forward，prob mass 0.9624
  not supported: C（forward 不是“接近 step 96 才出现”，而是**极少出现**；首次出现在 step 40）
```

### 3.1 逐条解读

- **B 成立（主因）**：在 MT2 上，MT2→MT3 前进楼梯在 **1484 步里只有 30 步可用（2.0%）**；
  control 更是 **0/1033（0.0%）**。也就是说，绝大多数 MT2 状态**根本不存在**通向 MT3 的换层动作。
- **A 被否证（就其本义）**：一旦 forward 楼梯可选，模型几乎总是选它（95.8%），
  treatment 对其分配的 probability mass 为 **0.9624**。所以“楼梯 kind 采不到”或“采到却选错”
  都**不**是当前解释。（A 之所以被列为 co-observed，只因为其 `reversalRate >= 0.3` 子句——
  而那是 B 的**机制**，不是 within-kind transfer 失败。）
- **D 作为共机制成立**：98.6–98.7% 的换层是**立即反向**（MT1→MT2 后马上 MT2→MT1），
  exact-state revisit 69–78%。即 policy 在 MT1↔MT2 之间**乒乓**。
- **C 不成立**：不是“前进楼梯快结束时才出现”，而是基本不出现。

## 4. 解释（这是本轮最有价值的结论）

> **换层动作本身没有被选错；失败发生在“走到让前进楼梯解锁的那个状态”这件事上。**

policy 会正确地选择可用的 forward 楼梯（95.8% / mass 0.96），但它在 **98.6% 的情况下立刻把楼梯走回去**，
从而永远无法完成解锁 MT2→MT3 所需的（战斗 / 道具 / 事件 / 钥匙类）前置序列。
因此：

```ini
OFFLINE_RANKING_IS_NOT_THE_BOTTLENECK = OBSERVED
STAIR_SELECTION_IS_NOT_THE_BOTTLENECK = OBSERVED
PREREQUISITE_SEQUENCING = PRIMARY_BOTTLENECK
PING_PONG = CO_MECHANISM (98.6% immediate reversals)
A_ON_POLICY_WITHIN_KIND_TRANSFER = REFUTED_BY_ITS_OWN_PRIMARY_SIGNAL
```

这解释了 5.25h/5.25g/5.25e 的一连串反差：offline 上 policy 能把 chosen action 排在
同 kind 的第一名（changeFloor 0.1250），但 offline rank 只评价**“在已有 legal action 集里选哪个”**，
它天然**不测**“如何把需要的 legal action 变成存在”这一多步 prerequisite credit。

## 5. 对下一步的含义（需评审授权，本轮未实施）

reviewer 预置判断条件为：“若 B 成立，则把 hierarchical policy 放回 graph search / MCGS proposer
才有清晰理由；否则可能只是把一个 OOD policy 装进更大的搜索器。”

本轮的 B 结论**满足了该条件**，且额外说明：

```text
需要一个负责 prerequisite sequencing 的组件（graph search / MCGS backbone），
让现有 policy 只承担它被证明擅长的部分：在给定 legal action 集内做 within-kind 选择。
明确**不**建议的做法: 单纯把 H 从 96 加大（DQ 意义上的续杯，因为 98.6% 是反向乒乓）。
```

`ROLLOUT` / `MCGS` 的新授权仍需评审给出；本轮未开 5.25j，未接 MCGS，未扩 corpus，未调 T/H。

## 6. 附带：P2 test note 已处理

评审指出 `samplerMatchesSoftmax` 名不副实（它再次调用 `kindMass(...)`，没有真正经过
`sampleKindThenAction()`）。本轮已改为：

```ini
analyticTwoStageMatchesSoftmaxPerAction = TRUE   # 逐 action 比较 shared analytic helper
samplerImplementationFollowsAnalyticDistribution = TRUE  # 20 万次抽样频率 vs analytic（treatment，含 residual）
controlSamplerFollowsAnalyticDistribution = TRUE          # 同上，control（kind 内均匀）
```

即新增共享 helper `analyticTwoStageProbabilities()`，并**真正对 sampler 实现做频率检验**，
而不是只验证解析组合。断言名同步收紧。
