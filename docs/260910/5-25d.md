# PR-5.25d — Non-Overlapping Trajectory Generalization Probe

```ini
MILESTONE = PR-5.25d
STATUS = STEP_0_COMPLETE / DESIGN_AWAITING_REVIEW
DATE = 2026-09-10
PREVIOUS_MILESTONE = PR-5.25c (CLOSED_AS_BOUNDED_NEGATIVE, APPROVED_WITH_P2_NOTES c0754fc)
SCOPE = 在冻结模型的前提下，先把训练语料换成真正不重叠的 train/held-out family，
        再判断 learned pairwise prior 是否具备可泛化的 action ranking。
```

## 0. 方向来源

PR-5.25c 的结论把瓶颈定位得很清楚：

```text
MODEL_CAPACITY               不是目前首先需要验证的变量
DATA_DIVERSITY / NON-OVERLAPPING_BEHAVIOR_COVERAGE   才是
```

5.25c 的 aggregate sanity 之所以退化，根因不是模型，而是训练语料只有两条高度重叠的 route：
`mt1-mt2-hp3834`(23 decisions) 是 `mt1-mt3-i893-hp8425`(33 decisions) 的完整前缀，
真正未见 decision 只有 10 条。

因此本轮**先不换模型**，只换数据契约。

## 1. 冻结设计

```ini
NEXT_CANDIDATE = PR_5.25d
TITLE = NON_OVERLAPPING_TRAJECTORY_GENERALIZATION_PROBE

PRIMARY_VARIABLE = TRAINING_CORPUS_DIVERSITY

KEEP_FIXED = FEATURE_SCHEMA_V1
             16_HIDDEN_MLP
             PAIRWISE_RANKING (softplus(-(chosen-neg)))
             TRAINING_RECIPE
             SOFTMAX_T_1_0
             HORIZON_96

NEW_OFFLINE_GATE = NON_OVERLAP_CHOSEN_ACTION_RANK < uniform expectation
                   # 只统计训练集里不存在的 (buildStateKey(state), chosen fingerprint)
                   # 不再使用 route-name holdout aggregate
```

不要在同一轮同时改模型与数据，否则结果不可归因。明确**不**在下一轮做：
更大 MLP、cross-entropy、Transformer、value head、PPO、MCGS。

## 2. Step 0：non-overlapping corpus inventory（本轮已完成）

### 2.1 方法

只做只读 inventory，不训练：

1. 扫描 `shared-solver/routes/fixtures` + `routes/latest` 全部 `*.route.json`。
2. 对每条 route 从 chaos MT1 初始状态做严格 replay；decision identity =
   `(buildStateKey(reconstructed state), chosenActionFingerprint)`。
3. 按 final floor 分成 TRAIN family（`final <= MT3`，排除 MT4/MT5 qualification family）
   与 HELD-OUT family（`final >= MT4`）。
4. 统计 held-out decisions 中 signature 不出现在 TRAIN union 的数量。

历史 key 三态（route 由多代 solver 写出，逐 route 自动探测）：

```text
legacy     hero.hp = null（保留 hpmax/manamax）
dominance  hero.hp/hpmax/manamax = null（当前 buildDominanceKey 形状）
canonical  不置空（当前 buildStateKey 形状）
# 三者都额外为 snapshot.floors 中每个 floor 补空 mutation（历史 serializer 行为）
```

复现命令与产物：

```bash
npm run check:learned-prior-corpus-inventory --prefix shared-solver
# -> shared-solver/routes/generated/learned-prior-corpus-inventory.result.json (gitignored)
```

### 2.2 结果（STEP 0）

```text
scanned / replayed        = 54 / 28 routes  (1416 decisions replayed)
key modes                 = legacy 17 / dominance 11
distinct routes           = 24 (4 exact-duplicate sequences dropped)
distinct signatures       = 247
TRAIN  (final <= MT3)     = 15 routes / 474 decisions / 93 distinct signatures
HELD-OUT(final >= MT4)    =  9 routes / 720 decisions / 154 distinct unseen signatures
unseen decision sum       = 423 (max on one held-out route = 78)
```

held-out 逐 route 未见占比：

| held-out route | unseen / n | % | final |
| --- | --- | --- | --- |
| `latest/adaptive-mt7-entry-from-blueking.route.json` | 78/111 | 70% | MT7 |
| `latest/mt5-blueking-kill.route.json` | 59/92 | 64% | MT5 |
| `latest/segmented-mt5-blueking-from-third-gate.route.json` | 59/92 | 64% | MT5 |
| `latest/mt5-corrected-after64-gems.route.json` | 50/83 | 60% | MT5 |
| `latest/mt5-problem-before-9-10.route.json` | 47/80 | 59% | MT5 |
| `latest/mt4-to-mt5-blueking-blocker.route.json` | 44/77 | 57% | MT5 |
| `latest/mt5-blueking-local.route.json` | 43/76 | 57% | MT5 |
| `fixtures/mt1-mt4-hp6428-best.route.json` | 22/55 | 40% | MT4 |
| `fixtures/mt1-mt4-hp4459-atk421-def318-mdef5012.route.json` | 21/54 | 39% | MT4 |

advisory verdict（显式准则，非隐藏阈值）：

```ini
CRITERION = distinct_unseen_signatures >= 50
            AND max_unseen_on_one_heldout_route >= 20

VERDICT = SUFFICIENT_BEHAVIOR_CORPUS
```

### 2.3 未 replay 的文件（诚实记录）

```text
non-mt1-start          3   (起点不是 chaos MT1，需要按其自身 start.snapshot 重放；本轮未做)
pre-mismatch@1        12   (route 记录带 start offset：decisions[0].preStateKey != start.stateKey)
ambiguous(0/1)@N       8   (枚举出的 primitive action 指纹唯一但应用后 post-state 不匹配；需单独归因)
no-fingerprint-match   1
start-key-mismatch     2
```

这些 route 一律**排除**而非猜测式纳入。若后续需要扩大 TRAIN，应单独归因
`pre-mismatch@1`（start-offset）与 `ambiguous(0/1)`（travel variant / 不同 post-state key 语义）两类。

## 3. 与 5.25c 的关系

5.25c 的退化（aggregate 0.1119，unseen 0.5533）在 Step 0 得到解释：

```text
5.25c TRAIN = MT2/MT3 only  ->  HELD-OUT = MT2/MT3 only  ->  max unseen = 10
5.25d TRAIN = final <= MT3  ->  HELD-OUT = final >= MT4  ->  max unseen = 78, distinct unseen = 154
```

结论：**问题从来不是没有数据，而是 5.25c 的数据契约把 held-out 限制在了与 TRAIN 高度重叠的子集里。**
因此 `NEXT_BLOCKER = INSUFFICIENT_BEHAVIOR_CORPUS` **不成立**。

## 4. 下一步（需要明确授权）

Step 0 只回答“有没有不重叠数据”，本轮**不训练、不 rollout**。若获授权进入 Step 1：

1. 冻结 split：TRAIN = `final <= MT3` 的 15 条 distinct route；HELD-OUT = `final >= MT4` 的 9 条
   （若评审要求，可把 MT4 两条移出 held-out 或移入 TRAIN，需在 Step 1 前冻结）。
2. 用**与 5.25c 完全相同的模型代码**（`lib/learned-action-prior.js`，不改 feature/超参）训练。
3. offline gate 改为只在不重叠 decision 上计算：
   `NON_OVERLAP_CHOSEN_ACTION_RANK < uniform expectation (0.5)`。
4. 只有该 gate 通过，才考虑再次烧 real rollout（horizon 96，T=1.0）。

> 待评审确认的一项设计选择：held-out family 是否包含 MT4 两条
> （`fixtures/mt1-mt4-hp6428-best`、`fixtures/mt1-mt4-hp4459-...`）。它们在 Step 0 中
> 只有 21–22 条未见 decision，且与 TRAIN 共享较多前缀；MT5/MT7 才是真正不重叠的主体。
