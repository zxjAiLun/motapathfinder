# PR-5.24d — Post-Anchor Repair-History Hypothesis Diversification

```ini
MILESTONE = PR-5.24d
STATUS = IN_PROGRESS / REPAIR_1_FAIL_CLOSED_IDENTITY_IMPLEMENTED_PENDING_CLOUD_REVIEW
DATE_STARTED = 2026-08-31
BASE_COMMIT = 2574ce3670643882efc445cefd00e525c9b5afd7
SCOPE = 将 adaptive repair hypothesis 边界从 input wave 下沉至 repaired output history，共享单次 anchor re-expand，为每条保留的历史赋予独立的 bounded first downstream probe
```

## 问题与已有证据

PR-5.24c 终审（F1/F2 Diagnostic）确认：在基于 input-wave 划分 hypothesis 的结构下，真实 MT1→MT2 回滚因 `inputFrontier.length = 1` 仅生成单个 ticket `h-d1w0`，且该 ticket 下 anchor re-expand 后的整个 merged 前沿被打包成单一 replay frontier 试探，既无法区分各保留资源历史的下游推进效果，也使得单一候选消耗 probe 阻断了其他候选的下游试探。

然而，求解器早已具备完备的通用资源多样性（Resource Diversity）保留机制（Pareto protection、resource novelty、skyline roles）。即：
> **Canonical DP 已经努力保存了不同资源历史，但 scheduler 的 hypothesis 边界还停留在“从哪个输入状态开始 rollback”。**

PR-5.24d 消除这一结构失调：将“不同输入波次间的公平调度”下沉推进为“不同修复输出历史间的公平调度”。

## 范围与非目标

本轮范围（`segment-dp.js` 为主）：
- 共享单次 anchor 重新扩展（Shared Anchor Expansion）：每个 input wave 仅执行一次 anchor 扩展，产生 `expandedAnchor.merged = [A, B, C, ...]`。
- 假说粒度下沉：新增 `buildRepairedHistoryHypotheses` 纯 helper，将 `expandedAnchor.merged` 中的每个保留候选拆分为独立的 repaired-history hypothesis（`replayFrontier = [candidate]`）。
- 假说标识与遥测：`hypothesisId`（包含 parentWave 与 candidate identity/rank）、`parentWaveId`、`anchorOutputCandidateId`、`anchorOutputRank`。
- 首轮公平性屏障（First-probe fairness barrier）：同一 depth 下所有 repaired-history hypotheses 完成首轮 probe（`probeIndex === 1`）之前，绝不发放任何第二轮延续授权（`second grant`）。
- 独立进度度量：
  - `historicalAnchorProgress`：原历史 `anchor.merged` 的全局最优投影；
  - `repairedAnchorProgress`：该假说对应的**单一** repaired candidate 投影（不再使用全前沿 best）；
  - `replayBestProgress`：该假说下游 replay 产生的最优投影；
  - `goalProgressAfter = bestOfProgressProjections(repairedAnchorProgress, replayBestProgress)`。
- 门禁体系：G23 A–F（覆盖单输入拆分、首轮公平性、独立进度区分、延续胜出、无枚举膨胀、单候选等价性）。

非目标：
- 不扩大 DP 枚举或搜索宽度（`candidateLimit`、`goalSkylineLimit`、`backtrackCandidateLimit` 严格保持不变）。
- 不修改进度度量比较器、不新增进度字段、不加权重。
- 不提前用 progress 过滤删除 output histories（repaired == historical 依然获得 first probe）。
- 不调整 30s/50k 算法预算、不做 Linux qualification、不做 failure-intent 调优。

## 初始设计

1. **共享 Anchor 扩展**：
   在 `tryAdaptiveCheckpointRepair` 中，当某个 anchor wave 开始执行时，在父 wave 预算下执行一次 `runSegmentAgainstFrontier(...)`。
   若 anchor 扩展产生 $K$ 个保留候选（$K \le \text{backtrackCandidateLimit}$），则将其映射为 $K$ 个 child hypotheses。
2. **Replay 试探解耦**：
   每个 child hypothesis 拥有独立的 `replayFrontier = [candidate]`，并在其绑定的局部 probe 预算下独立执行下游 replay。
3. **进度与授权分离**：
   每个 child hypothesis 独立计算 `progressClass` 与 `continuationEligible`。只有首轮所有 child hypotheses 均完成 first probe 后，才依据离散进度决策是否给予 second grant。

## 验收与拒绝门

| Gate | Evidence | Pass condition |
| --- | --- | --- |
| G23-A 单输入多输出拆分 | 合成 fixture | 1 个 input candidate 扩展出 3 个 repaired outputs → 产生 3 个 history hypotheses（非 1 个） |
| G23-B 首轮公平性屏障 | 合成 fixture | A1、B1、C1 全部 probe 完成后，才允许出现任何 probeIndex=2 事件 |
| G23-C 独立进度区分 | 合成 fixture | A/C 无进展（NO_MEASURABLE），B 有进展（WITHIN）→ 仅 B continuationEligible，A/C 保持 false |
| G23-D 第二轮胜出 | 合成 fixture | A1/B1/C1 后仅 B 获得 second grant 并在 B2 达成 FOUND → 顶层 FOUND 且 winner=B |
| G23-E 无枚举膨胀 | 架构断言 | anchorExpandedCandidateCount 前后一致，childCount $\le$ retained merged count $\le$ backtrackCandidateLimit |
| G23-F 单输出等价性 | 合成 fixture | 当 `expandedAnchor.merged.length === 1` 时，行为与 PR-5.24c 结构完全等价 |
| G23-G 历史身份防逃逸与漂移 (Repair 1) | 合成 fixture | Subcase 1（漂移丢失）：胜出历史未重现时 fail-closed 标记 `history-not-reproduced`，不重放任何 sibling，found=false，exhausted=false；<br>Subcase 2（顺序漂移）：前沿重排后依规范身份精确命中胜出历史重放并达成 FOUND |

## 迭代日志

### Iteration 1 — 2026-08-31

- 任务启动：建立 PR-5.24d 里程碑文档，继承 PR-5.24c 科学结论。
- 架构实现：
  - 在 `segment-dp.js` 中抽象 pure helper `buildRepairedHistoryHypotheses({ depth, waveIndex, anchor, expandedAnchor, candidateLimit })`；
  - 实施共享 anchor re-expansion（单 wave 执行一次，消费 `anchorGenerationExpansions`），输出拆分为独立 `repaired-history hypotheses`；
  - 每个 repaired-history hypothesis 独立分配试探预算（`historyProbeExpansions`）、独立绑定单状态 `replayFrontier = [candidate]`；
  - 进度投影度量完全对齐 PR-5.24c（`repairedAnchorProgress` 采用单候选状态投影，`historicalAnchorProgress` 保持原历史 anchor 输出最优投影）；
  - 首轮公平性屏障（first-round barrier）严格保持，多输出时 continuation 重放精确隔离胜出候选历史。
- 门禁体系（G23 A–F）：
  - 在 `check-budgeted-repair-scheduling.js` 中新增合成多元模拟器 `buildDiversifiedSyntheticSimulator(gateAtk)` 与门禁 `gatePostAnchorHypothesisDiversification()`；
  - G23-A（单输入多输出拆分）：1 个 input candidate 扩展出 3 个 repaired outputs，准确生成 3 个 tickets（`h-d1w0h0`, `h-d1w0h1`, `h-d1w0h2`）；
  - G23-B（首轮公平性屏障）：断言 A1, B1, C1 均在任何 probeIndex=2 前完成；
  - G23-C（独立进度区分）：A/C 判定 `NO_MEASURABLE_PROGRESS` 且 ineligible，B 判定 `WITHIN_SEGMENT_PROGRESS` 且 eligible，证明无聚合污染；
  - G23-D（第二轮胜出）：B 获得 second grant 并在 B2 达成 `goal-reached`，顶层交付 `FOUND === true` 且 `secondGrantWinner = "h-d1w0h2"`；
  - G23-E（无枚举膨胀）：断言 `childHypothesisCount === 3 <= 8 <= backtrackCandidateLimit`；
  - G23-F（单输出等价性）：单候选输出时保持生成单个 ticket `h-d1w0`，完全对齐 PR-5.24c。
- 验证套件实测：
  - `--fast ×10`：10/10 全部通过（每次 ~1s）；
  - Heavy 门禁 ×1（`check-budgeted-repair-scheduling.js`）：全部通过，`contractStatus: "passed"`，G23 耗时 0.0s；
  - 静态与核心回归 ×1：`check:manifest`、`check:no-tower-solver-js`、`check:solver-job`、`check:launcher`、`check:route-free-state`、`check:tower-ir-shadow`、`check:candidate-key-smoke`、`check:adaptive:contract`、`check:checkpoint-repair`、`check:core`、`check:resource-intent:contract` 全部通过。
- 交付与下一步：第一批交付（架构+门控）完成，提交并停止，等待云端审查；云端审查通过后授权执行真实 MT1→MT4 运行。

### Iteration 1 Repair 1 — 2026-08-31

- 云端审查结论：`REQUEST_CHANGES`（P1 缺陷：second-grant continuation restart 重新扩展 anchor 后，若胜出候选 ID 不在结果前沿中，存在 `else if (contRepairFrontier[rank])` 和 `else if (contRepairFrontier[0])` 两层 fail-open 回退，导致二轮授权可能被篡改执行在其他历史 candidate 上）。
- 修复变更：
  - 规范身份强化：在 `buildRepairedHistoryHypotheses` 中增加规范 `anchorOutputStateKey`（经 `buildDpStateKey` 计算），票据遥测保留 `anchorOutputCandidateId`、`anchorOutputStateKey`，`anchorOutputRank` 明确降级为仅供遥测展示，绝不参与回退决策；
  - 严格精确匹配：二轮 restart anchor 产出后，仅当且仅当存在精确匹配的 `anchorOutputCandidateId` 或 `anchorOutputStateKey` 时才孤立提取该候选进入 downstream replay；
  - 彻底 Fail-Closed：若未重现胜出历史，严禁 fallback 到旧 rank、[0] 或全前沿，直接将 ticket 的 `continuationDecision` 与 `grantHistory[1].outcome` 标记为 `history-not-reproduced`，stopReason 为 `incomplete`，`status = "PROBE_PENDING"`，不重放任何 sibling 候选，`found = false`，`exhausted = false`，全局预算停止保持 null；
  - P2 遥测语义记录：ticket 的 `consumedExpansions` 包含同一次共享的 anchor generation 成本，各 ticket 累加不代表实际全局工作量；全局权威工作量严格以 `globalBudget.consumedExpansions` 为准。
- 门禁扩展（G23-G 纳入 `--fast` 与 heavy 门套件）：
  - Subcase 1（漂移丢失）：模拟 restart 时省略分支 B，断言 B 票据 probeCount=2、outcome/decision 均为 `history-not-reproduced`、未触碰 sibling、FOUND=false、未 exhausted；
  - Subcase 2（顺序漂移）：模拟 restart 时颠倒输出顺序使 B 排名变化，断言依赖规范身份仍精确匹配 B 并达成 FOUND。
- 验证套件实测：
  - `--fast ×10`：10/10 全部通过（每次 ~1s）；
  - Heavy 门禁 ×1（`check-budgeted-repair-scheduling.js`）：全部通过，`contractStatus: "passed"`，G23 耗时 0.0s；
  - 静态与核心回归 ×1：`check:manifest`、`check:no-tower-solver-js`、`check:solver-job`、`check:launcher`、`check:route-free-state`、`check:tower-ir-shadow`、`check:candidate-key-smoke`、`check:adaptive:contract`、`check:checkpoint-repair`、`check:core`、`check:resource-intent:contract` 全部通过。
- 交付与下一步：Repair 1 修复完成，提交并停止，等待云端审查授权真实 MT1→MT4 运行。
