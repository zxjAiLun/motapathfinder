# Benchmarks

Benchmark inputs live under:

- `benchmarks/public/**`
- `benchmarks/hidden/**`

Benchmark outputs live under:

- `benchmarks/results/**`

Do not write solver implementation code here. Benchmark runners should call `shared-solver/public.js` or shared-solver CLI commands.
