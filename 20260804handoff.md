# Motapathfinder 项目交接文档

更新时间：2026-08-18

当前分支：`dev`

当前已核验 solver 基线：`9967abb`（PR-5.19u `APPROVED / CLOSED`；PR-5.19v 已授权）。

最近功能轮：PR-5.19l Witness-Backed Discrete Survival Opportunity Prerequisite（Repair 1，已关闭）

最近完成轮：PR-5.19m Post-Recovery Continuation Breakpoint Attribution（`APPROVED / CLOSED`，Case A：next prerequisite compiled but blocked by `call-cap-exhausted`；见 [`docs/260817/5-19m.md`](docs/260817/5-19m.md)；上一功能轮 PR-5.19l 见 [`docs/260815/5-19l.md`](docs/260815/5-19l.md)）

最近完成轮：PR-5.19n Residual Paid-Witness Graph Reuse Attribution（`APPROVED / CLOSED`，R1：paid residual suffix available；见 [`docs/260817/5-19n.md`](docs/260817/5-19n.md)）

最近完成轮：PR-5.19o Prefix-Compatible Residual Witness-Suffix Recovery（`APPROVED / CLOSED`，`MECHANISM POSITIVE / NOT PROMOTED`；见 [`docs/260817/5-19o.md`](docs/260817/5-19o.md)）

最近完成轮：PR-5.19p Post-O3 Residual Prefix Attribution（`APPROVED / CLOSED`，P1：second paid prefix residual available；见 [`docs/260817/5-19p.md`](docs/260817/5-19p.md)）

最近完成轮：PR-5.19q Second Prefix-Compatible Residual Witness-Suffix Recovery（`APPROVED / CLOSED`，`MECHANISM POSITIVE / NOT PROMOTED`，remote run `32116821998` full suite success；见 [`docs/260817/5-19q.md`](docs/260817/5-19q.md)）

最近完成轮：PR-5.19r O4 Continuation Boundary Attribution（`VERIFIED`，`REVIEW = APPROVED (observation-only)`，`RESEARCH_VERDICT = MECHANISM PATHWAY HEALTHY / BLOCKED BY CALL CAP`；见 [`docs/260818/5-19r.md`](docs/260818/5-19r.md)）

最近完成轮：PR-5.19s Hierarchy Call Allocation Attribution（`APPROVED / CLOSED`，`RESEARCH_VERDICT = DERIVED-PROGRESS-AWARE CALL ALLOCATION ATTRIBUTED`；productive: level1=level2+=1.0 > root=0.2；见 [`docs/260818/5-19s.md`](docs/260818/5-19s.md)）

最近完成轮：PR-5.19t Pre-Hierarchy Root Call Chronology Attribution（`APPROVED / CLOSED`，`RESEARCH_VERDICT = PRE-HIERARCHY ROOT CALL CHRONOLOGY ATTRIBUTED`；first hierarchy activation 时 root spent=5/remaining=3，root #5 无 signal 但 productive；见 [`docs/260818/5-19t.md`](docs/260818/5-19t.md)）

当前阻塞边界：

```text
hierarchical prerequisite depth > available connector call budget
Evidence: 5.19m Case A（O2/O3 recovery → next prerequisite → call-cap-exhausted）
         5.19r O4 Case A（O4 recovery → next prerequisite → call-cap-exhausted）
```

注意：这不等价于“任何 residual reuse 都无效”；call allocation、bounded hierarchy scheduling、dynamic call reservation 均未测试且未授权。

最近完成轮：PR-5.19u Pre-Charge Root Attempt Separability Attribution（`APPROVED / CLOSED`，FINAL_COMMIT=`9967abb`；U1 + `NO-EARLIER-POSITIVE-ATTACK-MARGIN-OR-EXACT-ATTEMPT-EVIDENCE`；见 [`docs/260818/5-19u.md`](docs/260818/5-19u.md)）

当前活动轮：PR-5.19v Pre-Hierarchy Root Retry Novelty Attribution（`AUTHORIZED`，observation-only；见 [`docs/260820/5-19v.md`](docs/260820/5-19v.md)）

下一授权 gate：归因 #1-#4 是否含 charge-time 可识别的 retry（同 prerequisite+boundary、无指标改善/被支配），仅作未来 shadow counterfactual 输入；禁止 skip/suppress/reorder、fixed-8/8→9 A/B、O5、reroot、第 9 次 search、reservation/agenda/allocation 改动、D3。

## 文档维护合同

- 当前真相与 milestone 索引：本文件（tracked）。
- 活动轮次记录：`docs/YYMMDD/<milestone-id-with-hyphens>.md`，规则见 [`docs/project-documentation-system.md`](docs/project-documentation-system.md)。
- 同一轮跨日继续更新原 milestone 文件，不为每日进度复制文档。
- 状态词严格区分 `AUTHORIZED / IMPLEMENTED / VERIFIED / ACCEPTED`；未找到路线、性能改善和 strict replay closure 也必须分开。
- `20260804checkpoint.md` 与 `shared-solver/RESEARCH_PROGRESS.md` 是累计历史，不与本文件竞争当前状态。
- generated runs/probes 默认不提交；提升为 tracked fixture 必须在 milestone 文档写明理由和输入。
- milestone 状态、基线、正式 artifact、限制或下一步变化时，本文件必须同步更新。

## 一句话状态

PR-5.7a 的 Stop B 已完成归因：action scope 无 trimming，third-gate 目标与 tracked route 一致，真正缺口是两处资源时序没有进入粗 checkpoint——`MT4:11,11` 必须保留 `MT4:2,5` 的 HP-positive guard chain，`MT4:8,3`/I621 必须延迟到 MT5 bottom pair 之后。PR-5.7b 仅细化 milestone/presentTiles，不改 DP key、dominance、skyline、candidateLimit 或 timeout。相同 MT4 tracked fixture、每段 500 expansions、无 wall timeout：旧 coarse graph **158.9s / 4050 expansions / third-gate FAIL**；最终细化版 **19.6s / 607 expansions / third-gate REACHED**（约 8.1x wall、85% expansions 降幅；wall 只作本机方向性证据）。严格因果 replay 锁定延迟 I621 的 **+13,846 HP**。

PR-5.7c 已确认 5.7b 不只是跨过 third-gate，而是直接闭合整条 MT5 boss 链：相同 fixture/预算跑到 `mt5-blueking-kill` 为 **19.320s / 645 expansions / SUCCESS**，third-gate 后 `sustain → I894 → final stats → HP → blueKing` 合计仅 **38 expansions**。最终 hero `hp4464/atk2167/def2135/mdef13010/lv8/exp1101`、装备 I894；35 decisions 严格 replay 通过（route fingerprint `a2b1b4f6…`）。相对旧 coarse graph 为 **4050 FAIL → 645 SUCCESS**（expansions -84.1%，wall 约 8.22x，仅方向性）。5.7c 只增加 closure/CI 合同，没有继续改 production 建模。

PR-5.8a 已完成 post-MT5 现图基线：相同 tracked MT4 fixture，先以 **645 expansions** 闭合 `mt5-blueking-kill`，再以每段 500 expansions、`maxRuntimeMs=0` 跑 `MT6 → MT7 → MT8 entry`。post-MT5 在 **362 expansions / 48.960s** 后到达 `mt7-bottom-double-fairy`，首个失败段为 `mt7-special80-ready`；全链累计 **1007 expansions**。两个失败 attempt 均只展开 17 次，frontier 归零，`actionTrimmed=0`、无 expansion exhaustion、无 wall timeout；到达前缀 23 decisions strict replay 通过（fingerprint `14ca77de…`）。失败候选 hero 为 `hp499741/atk5767/def5535/mdef30010/exp813`。5.8a 只证明 coarse checkpoint 的 `MT6:6,8 silverSlime → MT6:2,1 evilFairy/+500 DEF` 与 tracked 已知路线反序，结论是 **strong resource-timing hypothesis**，不是已闭环 causal verdict。墙钟仅是本机方向性证据。

PR-5.8b 已用最小 counterfactual 消除上述 P1：只把 `MT6:2,1` 防御 checkpoint 前移并用 `presentTiles` 保证它发生在 `MT6:6,8` 前；其余 milestones byte-equivalent（除三条必要 predecessor/goal edge）、affected segment 的 DP/action policy 不变，运行参数仍为 candidateLimit 8、500 expansions/segment、`maxRuntimeMs=0`、goal-directed，且 production key/dominance/selection 未改。结果从 5.8a 的 **special80 FAIL / 362 post-MT5 expansions** 变为 **special80 REACHED / 508 expansions / 17.045s**，最终 hero `hp2672845/atk5767/def5535/mdef30010/exp881`；24 decisions strict replay 通过（route `e0ea77ee…`，exact state `0564b870200d0113`）。因此 `MT6:2,1 → MT6:6,8` 防御时序现可冻结为 **CAUSAL_ROOT_CAUSE**。继续到 MT8 的同预算探测在下一段 `mt7-left-sword` 命中 500-expansion budget（0 trimming、无 wall timeout），只记为下一轮 `INCOMPLETE_WITHIN_BUDGET`，本轮不扩预算也不追加 checkpoint。

PR-5.8c 已纠正上述 probe 的表面归因：从 strict special80 exact state `0564b870200d0113` 单独运行 `mt7-left-sword`，固定 candidate/goal skyline 8、500 expansions、`maxRuntimeMs=0`、`stopOnFirstGoal=false`，结果不是 `found=false`，而是 **expansion 1 已出现完整目标、500 expansions 后 found=true 且 skyline 未穷尽**。确定性诊断为 generated 2275 / accepted 799 / dominance rejected 1477 / live frontier 201，158 个 goal nodes（84 active，最终保留 8），`actionTrimmed=0`、`stoppedReason=null`、所有 goal deficits=0。赢家 hero `hp4436803/atk8767/def6535/mdef30010/exp2237`；新增的 8-decision strict replay 通过（route `a36088b…`，state `22e75aa45404c026`）。因此旧 probe 只是被“任何 expansion exhaustion 都断言失败”的摘要器提前截断，**左剑不存在未找到路线的 budget blocker，也不应加预算或 checkpoint**。显式 long-horizon 诊断还能到达 MT8 entry，但整段 strict replay 在 decision 13 报 post-state mismatch；该结果只作下一轮 replay/lineage attribution 的新 P1，不作为 MT8 closure。

PR-5.8d 已把 decision 13 mismatch 归因到 recorded-action resolver，而非搜索或路线 lineage。相同 strict special80 起点、candidateLimit 8、每段 500 expansions、`maxRuntimeMs=0` 再次到达 MT8 entry；37-step full route 切出 24-step prefix + 13-step suffix，route-record 第一遍 reconstruction 与 winner exact state 一致，13 个 decision 的 pre/post exact chain 连续，前 12 步 strict replay 全匹配。第 13 步 `changeFloor@MT7:6,0` 的 choice fingerprint `changeFloor|MT7|6,0|:next|,` 实际对应 **3 个不同 travel variant**（path 7/9/17，travelState 与 post-state 各不相同）；resolver 在 post-state 消歧前按 choice fingerprint 去重，只留下 path-7，而被丢弃的 path-17 variant 可精确产生 recorded post-state `cb06ec29bfa758c4`。因此 verdict 冻结为 **`RECORDED_ACTION_FINGERPRINT_ALIAS_DEDUP`**：prefix/lineage 正确、resolver 无输入副作用、simulator 可复现 winner edge；5.8d 只增加只读 observer、归因合同与 qualification，不改预算、checkpoint、DP key/dominance/selection 或 replay 语义。

PR-5.8e 已按 choice/travel 分层语义完成最小 repair：choice fingerprint 保持不变；recorded-action enumeration 只折叠跨 API 返回的**同一 travel variant**，同 choice 的不同 path/travelState 一直保留到 resolver 真正 apply 后用 exact post-state 评分。synthetic contracts 锁定三类边界：唯一 exact-post 必选中；0 exact-post 的并列在正反枚举顺序下都 fail-closed；多个 exact-post 时优先 recorded path，无 path evidence 时用稳定 variant identity tie-break 并报告 `exactPostAliasCount`。真实 A/B 中 legacy choice-dedup 仍精确复现 decision 13 失败；repair B 在 aliases=3 中选中唯一 exact-post 的 path-17，special80→MT8 suffix **13/13 strict replay**，24-prefix+13-suffix **37/37 full-lineage strict replay**，route reconstruction 与 final exact state `5734ff36fe4d25c4` 一致（suffix route `9714c59a…`，full route `ecc13226…`）。verdict **`MT8_STRICT_REPLAY_CLOSED`**；未改搜索预算、checkpoint、milestone graph、DP key、dominance 或 selection。

PR-5.8f 已把 solver outcome 永久拆成四个正交字段：`goalFound`、`frontierExhausted`、`budgetExhausted`、`searchComplete`。共享 taxonomy 同时进入 primitive DP、segment attempt、Solver Doctor 与 solver-job result；旧 `expansionBudgetExhausted` 字段继续兼容。Doctor 对 `goalFound=true && searchComplete=false` 使用 **`feasible-incomplete`**，明确报告“已有可行路线、剩余 frontier 未穷尽”，不会进入 failure classifier。synthetic truth table 覆盖 found/not-found × complete/incomplete、first-goal early stop、action trimming、time/RSS budget；真实 5.8c witness 仍精确是 **500 expansions / frontier 201 / expansion 1 first goal / 8-decision strict replay**，新四元组固定为 **`true / false / true / false`**。本轮只改诊断、report 与 qualification contract；搜索图、预算、checkpoint、key、dominance、selection、replay 全未改。

PR-5.8g 已完成 recorded replay candidate-apply fast path。resolver 的候选 identity、variant retention、exact-post 主评分、dominance/fingerprint/path fallback 与 ambiguity/tie-break 全不变；只把原本最终必定 hard reject 的 target/stance/direction 检查前移到 apply 之前，并在同一次 resolution 内复用赢家已计算的 post-state（缓存不跨 state/simulator/route）。synthetic unique/no/multiple exact-post、正反枚举、recorded-path 与稳定 tie-break 全保持。真实 37-step full-lineage A/B：legacy **32,846ms / 8,633 candidate applies / 8,593 after-apply hard rejects / 37 winner reapplies**；optimized **2,458ms / 40 candidate applies / 8,593 before-apply hard rejects / 37/37 selected post-state reuse**，replay wall 约 **-92.5%**、candidate apply 约 **-99.5%**（wall 为同机方向性；apply 计数为确定性硬证据）。suffix 13/13、full lineage 37/37、final exact `5734ff36fe4d25c4` 与 route fingerprints 全保持。相同默认完整 checker 外层 wall **346,788ms → 249,835ms（约 -28.0%，含搜索噪声，仅方向性）**。未改搜索、预算、checkpoint、milestone、DP key、dominance 或 selection。

PR-5.9a 已完成 candidate-default MT1 exp9 的 reachability reuse 归因，且仍保持 production exact-state LRU 行为不变、诊断默认关闭。固定 workload 继续得到 **116 expansions / winner exact `a2ff379819ac9003` / route `c0adb2d9…` / objective 1346 / strict replay true**。149 次 reachability 请求中 exact cache 为 **26 hits / 123 misses**；123 个 miss 全部已通过逐 exact-state 的 safe-fast safety classification。按 `floor + start + current-floor mutations` 只读投影分组后为 73 个唯一 topology closure，22 个重复组可解释 **50 次理论可复用 miss（40.65%）**，normalized path skeleton mismatch=0；这些重复组的 exact-state 差异全部来自 hero HP。两次本机方向性观测为 wall **7,591–8,024ms**、reachability phase **221–224ms**。verdict **`SAFE_FAST_SKELETON_REUSE_CANDIDATE`**：证据授权 5.9b 做 skeleton cache，但不允许缓存/跳过每个 exact state 的 safety classification，也不能直接复用携带旧 state 的 reachability nodes。

PR-5.9b 已将 safe-fast walk 拆成无 state payload 的 topology/path skeleton 与逐请求 current-state rebase，并保留原 exact-state LRU。skeleton key 为 `floor + start + current-floor mutations`；每个 exact miss 仍重新执行 poison/directional/live-auto-event/hazard/stability safety classification，unsafe 状态只走 legacy exact。真实 MT1 exp9 独立进程 A/B/B/A：exact LRU 保持 **26 hits / 123 misses / 123 stores**；skeleton builds **123→73**、hits **0→50**，nodes expanded **6526→3497**、transition attempts **26104→13988**，而 state clones 6649 与 dominance-key builds 6526 有意保持。116 个 expanded states / 434 条 action+successor exact corpus 指纹 `2ac91e5d1ce0aed2` 完全一致；winner/route/objective/116 expansions/strict replay 全 parity。方向性中位数：search **4130→3999ms（-3.2%）**、reachability phase **600.7→522.7ms（-13.0%）**、end-to-end **17364→14465ms（-16.7%，噪声较大）**。verdict **`SAFE_FAST_SKELETON_CACHE_PROMOTED`**；下一性能归因应针对 rebase 的 clone/key build，而不是继续扩大 topology cache key。

PR-5.9c 已用默认关闭的 node accessor/escape recorder 完成 rebase cost 归因，未引入 lazy state、未改 visited representation 或 action semantics。相同 MT1 exp9 仍为 winner `a2ff379819ac9003` / route `c0adb2d9…` / objective 1346 / 116 expansions / strict replay true。123 次 rebase eager 物化 **6526 nodes**，对应 6526 dominance-key builds；另有 123 个 stability probe，合计 6649 clones。消费者累计读取 `node.state` **333,011 次**并覆盖全部 6526 nodes，但 `node.key` property **0 reads**。真正进入 emitted `action.travelState` 的只有 **760 action references / 566 unique nodes（8.67%）**；**5960 nodes（91.33%）**没有 travel-state escape。完整 consumer matrix 无 unscoped 桶：battle 289、event 116、changeFloor 29、floorFly 326 actions；door/pickup/interactPickup/regionSignature 分别大量扫描却输出 0 travel action。verdict **`EAGER_TRAVEL_STATE_MATERIALIZATION_OVERBUILD`**。这授权下一轮设计 topology-first adjacency + on-demand current-state materialization，但不允许简单把 `node.state` 换成 getter——当前所有节点确实会被消费者读取，必须先拆 consumer 的 topology lookup 与 stateful candidate validation。

PR-5.9d 已把 safe-fast reachability 改成 topology-first nodes：adjacency/tile lookup 使用原 exact base state + skeleton coordinate/path，只有通过 endpoint predicate、确实需要 stateful validation 或生成 action 的 stance 才 memoized clone 当前 travel state；`node.state/node.key` 兼容接口仍存在，外部读取时按需物化，legacy-exact/unsafe 路径完全不变。MT1 exp9 A/B/B/A 中 topology nodes 保持 6526，实际 materialized nodes **6526→722（-88.9%）**，总 state clones **6649→845（-87.3%）**，walk-node dominance-key builds **6526→0**；5804 nodes 完全不物化。116 states / 434 action+successor corpus `2ac91e5d1ce0aed2`、winner/route/objective/116 expansions/strict replay 全 exact parity。方向性中位数：search **3896.5→3077ms（-21.0%）**、reachability **577.7→227.4ms（-60.6%）**、enumerateActions **689.1→379.5ms（-44.9%）**、end-to-end **13835.8→13466.1ms（-2.7%，受 corpus/replay 噪声影响）**。verdict **`TOPOLOGY_FIRST_MATERIALIZATION_PROMOTED`**。本轮同时刷新确定性 PR-4.8b1 route-output diagnostic：WhiteIsland decision-2 candidate count 24→25、OnlyUp milestone 按当前规范顺序归一化；两次独立 rebuild 字节一致，两个项目 final exact state 均不变。

本地 `check:validation` 的 static layer **49/49 passed**；其 live-progress wrapper 从 repo root 启动会把 repo root 误判为 tower root。显式传入 OnlyUp root 后，topology-first on/off 都在同一个首步出现既有 runtime drift（solver 路径预期自动拾取 `MT1:2,7`，h5 runtime 未拾取，HP 相差 100），故该 live mismatch 与 5.9d 无因果关系，作为独立 carry 保留，不得用它否定本轮 exact corpus / strict replay gate，也不得在未归因前顺手改 runtime semantics。

PR-5.9e 已用默认关闭的 observation-only candidate-outcome recorder 把 5.9d 剩余 materialization 缺口完全归因。相同 MT1 exp9 workload 中 722 materialized nodes、566 unique travelState nodes，剩余 **156/156 全部只属于 battle consumer**：119 个 stance 仅邻接 lethal battle，31 个仅邻接 `damageInfo=null`，另 6 个同时邻接两类；按 node set union 计数，不能把 125 lethal 与 37 no-damage-info 简单相加。642 enemy adjacency events / 568 unique stance 中，289 viable events 全部成为 289 emitted battle actions；没有 unsupported、dedup-rejected 或 dedup-replaced，故 **dedup contribution=0**。observation on/off 的 winner `a2ff379819ac9003`、route `c0adb2d9…`、objective 1346、116 expansions、156 accepted、strict replay 与 reachability clone/key cost 全 parity。verdict **`REMAINING_MATERIALIZATION_ATTRIBUTED`**，机制 **`BATTLE_PRE_ACTION_REJECTION_MATERIALIZATION`**；本轮只做归因，不改变 battle/replay/search semantics。

PR-5.9f 已把 battle viability evaluation 与最终 mutable travelState materialization 分开：safe-fast topology node 先在短生命周期、冻结的 base exact state + stance projection 上评估 battle；projection 精确保留 travel `loc/direction/steps`，现有 battle evaluator 仍按原边界 clone hero/inventory/flags，guard/location damage 与 battle cache identity 均不变。只有 viable action 才物化完整 travelState。独立进程 A/B/B/A 中 materialized nodes **722→566**、总 reachability state clones **845→689**，5.9e 的 **156 residual→0**；battle materialized nodes 568→289，而 emitted battle travelState nodes 保持 289。116-state / 434-action+successor exact corpus `2ac91e5d1ce0aed2`、winner `a2ff379819ac9003`、route `c0adb2d9…`、objective 1346、116 expansions、156 accepted 与 strict replay 全 parity。verdict **`BATTLE_EVALUATION_PROJECTION_PROMOTED`**；legacy-exact/unsafe 路径和 action/dedup/apply semantics 未改。

PR-5.9g 已用独立进程 A/B/B/A 对 5.9b/d/f 累计优化做 closure requalification。A 保留 safe-fast 但关闭 skeleton cache、topology-first materialization、battle projection；B 为当前 production default。语料覆盖 6 个 MT1 goals/objectives、tracked MT2→MT3 I893 与 MT4→MT5 entry，共 8/8 workload 全部 found、strict replay，winner exact、route、expansions/accepted/attempt scale 全 parity。确定性总结构成本：skeleton builds **897→458（-48.9%）**、nodes expanded **47,296→21,338（-54.9%）**、transition attempts **189,184→85,352（-54.9%）**、reachability state clones **48,299→7,403（-84.7%）**、dominance-key builds **47,402→318（-99.3%）**，且每个 workload 均非增。wall 只作方向性：MT1 外层含 replay/process 噪声，未宣称统一加速；真实 MT4→MT5-entry 搜索样本 **4475→3230ms**。verdict **`PR_5_9_REACHABILITY_OPTIMIZATION_CLOSED`**；PR-5.9 主线正式 CLOSED，下一轮必须先重新 profile 全求解路径，不能继续围绕已降至次要比例的 reachability 猜测优化。

PR-5.10a 已给 perf tracker 增加向后兼容的 **exclusive/self-time** 视图（旧 inclusive `phaseMs` 保留），解决嵌套 `applyAction/enumerate/reachability/clone` 重复计时；新增 `sortActions` 与未归类 residual，四个 workload 均独立进程、锁 exact winner/route、确定性 scale 与 strict replay。结论不是单一全局热点：representative MT1、tracked MT4→MT5 entry、完整 MT5 blueKing 的 self-time 第一均为 **applyAction**；但 special80→MT8（5634 expansions，goal found + incomplete skyline）第一变为 **reachability**，最终本机方向性样本为 97.1s / 42.7%，其次 applyAction 54.5s / 23.9%、clone 44.2s / 19.4%（比例不作硬阈值）；5374 reachability misses 中 **1722 legacy-exact**、reachability clones **302,510**。verdict **`HOTSPOT_SPLIT_BY_WORKLOAD_SCALE`**。当时计划的 MT8 fallback/clone 与短中链 applyAction 归因已被 2026-08-12 discovery 主线取代，现仅作为按需 carry；不得从本轮直接授权 CompactState/Rust/action representation/key/dominance/selection/budget 改动。

PR-5.10b 已完成 Discovery Capability Audit，并把旧的性能归因顺序降级为 blind workload 触发的 carry。初始状态→MT5 blueKing 的人类计划输入精确为 **28 milestones / 27 intermediate goals / 27 implicit + 27 explicit order edges / 109 minHero fields / 46 removed tiles / 105 hard-present tiles / 45 allowed-floor entries / 38 allowed-transition entries / 68 per-segment DP fields**；另有 1 exact-damage、6 equipment、7 soft-present、3 action-survivable target hints。A0→A5 消融合同证明：删除 `startFrom` 不会删除数组顺序，当前架构从 A0 起就没有 unordered-event planning；A5 生成单一 terminal-only D3 spec，只保留 `bossDefeated MT5:6,7 blueKing`，`actionPolicy={}`、`dp={}`。verdict **`ASSISTED_EXECUTION_NOT_AUTONOMOUS_DISCOVERY`**。本轮不运行 blind solve，不改 DP/key/dominance/selection/budget；`check:static` **22/22 passed**。下一轮 PR-5.10c 负责运行首次权威 blind baseline。

PR-5.10c 已建立首个真正的 terminal-only D3 baseline。独立 `blind-goal.v1` 输入只含 project/rank 与最终 `bossDefeated MT5:6,7 blueKing`；schema 遇到 route/fixture/milestone/startFrom/minHero/present/removed/allowedFloors/allowChangeFloors/region/priorityMode 等字段一律 fail-closed。canonical initial state、单 segment、`actionPolicy={}`、`dp={}`，固定 1000 expansions、`maxRuntimeMs=0`、2GB soft heap、candidate/goal skyline 8、first-goal + goal-directed（搜索控制，不是路线提示）。权威结果：**33.046s / 1000 expansions / 3123 generated / 1960 accepted / 1164 dominance rejected / frontier 496 / 1321 active keys / deepest MT3 / max depth 39 / actionTrimmed 0**；floor expansions MT1=115、MT2=408、MT3=477，action candidates battle=2052/changeFloor=1059/equip=12。`goalFound=false / frontierExhausted=false / budgetExhausted=true / searchComplete=false`，verdict **`BLIND_GOAL_NOT_FOUND_WITHIN_BUDGET`**；这不是无路线证明。8-expansion clean micro contract 同样固定为 frontier 15、未完成，并锁住预算 wiring（曾发现 options 未进入 `dpOverrides`，已修复）。本轮不改 production DP key/dominance/selection。

PR-5.11 已把 blind run 变成人可读的 search trace。observer 在每个 decision depth 聚合 expanded/action-set/generated/rejected/max-frontier、floor、action kind 与 rejection reason；解释器生成固定 before/after 卡，若没有 after 则明确 `awaiting-after-run`，若 expansions/runtime/heap/candidate/skyline/priority 等控制不同则 `control-mismatch`，禁止因果结论。深度表显式标注口径：expanded/generated 是 parent decision depth，而部分 rejection 描述 successor depth，因此不伪造 per-depth rejection rate。正式 1000-expansion traced run 的确定性结构仍为 **3123 generated / 1960 accepted / 1164 dominance rejected / frontier 496 / 1321 active keys / deepest MT3 / max depth 39**；人类审查桶为 dominance reject **1164（37.3% generated）**、changeFloor ordering candidates **1059（33.9%）**、live frontier **496（15.9%）**、action trimming **0**。每个桶都带 hard-count/inspection-candidate 区分和 `optimizationClaim=false`；known route 未参与。trace-on wall **56.084s** 含 observer 成本，不能与 5.10c 轻量 33.046s 直接比较，只有相同 trace controls 的后续 A/B 才可比较 wall。focused checks 与当前 manifest static **56/56** 通过。

PR-5.12 已建立 route-free Automatic Macro Graph。输入严格只有塔工程、canonical initial state 和 terminal Boss；先从真实 `changeFloor` 静态图自动求 MT1→MT5 最短 floor corridor，再用 TowerIR 提取 walk components、Boss/普通战斗候选、楼梯、门、一次性物品/装备、脚本事件及 map/flag/item mutation。当前真实图 fingerprint `4ce95f879dc8c04e`：**409 nodes / 722 edges / 140 walk components / 116 enemies / 132 items / 9 floor transitions / 2 doors / 6 mutation hooks**；目标唯一绑定 `MT5:enemy:6,7:blueKing`，8 条 corridor floor-transition edge。重要归因：两扇 MT5 `specialDoor` 静态要求 `specialKey`，但 corridor 内供应为 0；真实依赖由 `blueKing afterBattle` 写 `flag:door_MT5_4_1` → autoEvent 条件读取 → `openDoor`，图中以 4 条 mutation dependency candidate edge + scripted mutation target 表示，未伪造 key supplier。115 个普通 enemy 仍只是 `combat-gate-candidate`，整个图明确标为 `candidate-graph-not-proof`，尚未承担搜索/剪枝 correctness。

PR-5.13 已把自动图接成最小分层 planner：机器自动生成 MT2/MT3/MT4/MT5 floor-entry stages + terminal blueKing，不含手写血量/攻防/物品/事件顺序；局部 transition 仍由 canonical simulator + segment DP 完成。1000-expansion prototype：MT2 **62 expansions**、MT3 **206**，随后 MT4 stage 用完余下 **732**；总 wall 约 14.4s，最终 deepest MT3、route decisions 34。相对 5.10c 单段 before（33.046s、1000 expansions、deepest MT3）只减少 wall/显式隔离阶段，**没有提高 goal/deepest-floor correctness outcome**。本轮还跑了多个被否证的实验：平均分配 per-stage budget、入口预收集 Pareto、default agenda、current-stage 0/4/8 alternatives，均在同预算更早失败或把预算耗在 MT2/MT3；因此未提交这些策略。冻结 verdict：`HIERARCHICAL_STAGE_EXECUTION_WORKS_BUT_TOP1_ENTRY_LIMITS_DISCOVERY`。下一步必须做 failure-triggered save/load 式回溯：下游失败后才回开上一个入口 alternatives，并用未来可行性反馈选入口；不能预先穷举每层 skyline。

PR-5.13b 已实现 failure-triggered checkpoint/backtracking：后续 stage 失败才启用 `landmarkArchive`，优先复用本次失败搜索已经付费产生的 mobility/equipment/resource/survival checkpoint，再按需重开上一 floor-entry alternatives；所有状态以 16-hex 可读指纹 + hero/equipment/inventory 摘要报告。1000-expansion 对照仍未到 MT4，但不再只有“超时”：primary 在 MT4 probe 的 128 expansions 内已找到 `I893` equipment checkpoint（例如 MT3 hp4837/atk112/def100），后续 checkpoint/entry alternatives 最深推进到 decision depth **48**（原 blind/5.13 最深 **39**），仍在 MT3。冻结结论：`FAILURE_TRIGGERED_BACKTRACKING_EXPOSES_USEFUL_CHECKPOINTS_BUT_D3_REMAINS_OPEN`；更深 depth 不是路线成功，且不得继续把 MT3 楼梯口 archive 当作 MT3 内部资源分支。

PR-5.14 已建立 D0-D3 统一资格矩阵，严格分离执行能力、局部发现与自主发现。最终留档同机结果：D0 已知 MT1→MT4 路线 **55/55 strict replay**；D1 从去除历史 route/depth/counters 的 MT2 checkpoint 只给局部目标 MT3，**12 expansions / 6/6 strict replay / 0.568s**；D2 从去除历史 route/depth/counters 的真实首次 MT5 entry 状态只给 `blueKing` Boss 目标，**1000 expansions / frontier 202 / 22.537s / not found**；D3 从 canonical initial 只给最终 Boss，failure-triggered planner **1000 expansions / 最深 MT3 / 17.212s / not found**。D2/D3 都是 `goalFound=false/frontierExhausted=false/budgetExhausted=true/searchComplete=false`，不能解释为无路线。相对 5.10c D3 before（33.046s / 1000 / deepest MT3），5.14 planner 的 wall 方向性降到 17.212s（**-47.9%**），但 deepest/goal outcome 未改善，不能称为搜索成功。正式 verdict：`EXECUTION_AND_LOCAL_DISCOVERY_VERIFIED_AUTONOMOUS_DISCOVERY_OPEN`；当前只能声明 D0/D1，不能声明 D2/D3 自主发现。

PR-5.15a 已把 D2 的第一失败面从“1000 expansions 没找到”收紧为可复现的 planning-envelope 缺口。搜索输入仍只有 detached MT5 entry state + terminal Boss；fixture 只在测试外壳构造合法存档，route/depth/counters 清零后不进入搜索。权威诊断 A 组仍为 **1000 expansions / MT5=641 / MT4=359 / frontier 202 / actionTrimmed 0**，生成 1673 battle + 1359 changeFloor candidates，但 **I894 candidate=0、Boss candidate=0**；四元组仍 false/false/true/false。自动图从 entry floor 到 terminal floor 的 shortest corridor 只含 `MT5`，虽看见 28 resource candidates（含 I894）和 20 combat candidates，却没有任何 resource→Boss prerequisite edge。B 组仅将 allowed floor 限为该图包络 `MT5`，**5 expansions / frontier 0 / searchComplete=true / Boss not found**，证明当前状态必须离开该包络继续准备；而 A 组实际把 35.9% expansions 用在图外 MT4。正式 verdict：`D2_FIRST_FAILURE_ATTRIBUTED_TO_INCOMPLETE_AUTOMATIC_PLANNING_ENVELOPE`。本轮只新增 observer/checker/报告，不改 production key、dominance、selection；下一轮必须从 state-visible revisit 能力自动扩图，再编译 simulator-validated Boss feasibility subgoals，禁止写死 I894/坐标/事件顺序。

PR-5.15b 已完成自动 planning envelope 与 feasibility subgoal 编译，但尚未执行/闭合 D2。`buildAutomaticMacroGraph(..., envelopeMode=state-visible-revisitable)` 将 shortest corridor 与当前 state 的 visited floors 取静态可达且能回到 terminal 的闭包；detached MT5 entry 自动得到 **MT1..MT5**，不再漏掉 MT4 回访。编译器只检查当前地图仍存在的装备，逐项用真实 EquipmentResolver 做 counterfactual，再按 Boss attack/defense deficit reduction 排序；该状态自动选出 **I894@MT5:11,5**，effective atk/def/mdef 971/931/6137→1243/1192/7856，attack deficit **1030→758**、defense deficit **3932→1952**。若该 tile 已移除则候选为 0；Boss enemy 缺失 fail-closed；重复编译结果一致。输入合同明确 known route/milestone/order/threshold 均为 false。单独把自动生成的 `equipmentIncludes:I894` 交给现有 DP 的实验仍是 **1000 expansions / frontier 220 / not found**，所以 5.15b 只冻结 `AUTOMATIC_FEASIBILITY_SUBGOALS_COMPILED`，不得宣称 D2 改善；5.15c 必须从 graph topology 自动生成并执行到该资源的 predecessor stages。

PR-5.5e 已用仓库现有 tracked OnlyUp 长路线补齐 visitedFloors/changeFloor production-faithful surface，不造 synthetic tower、不引入 untracked whiteisland，也不把 boundary arrival 冒充 changeFloor。`routes/fixtures/mt1-mt2-hp3834.route.json` 在当前 simulator 下从真实初始状态重放前 11 decisions，第 12 步精确匹配 recorded travel variant `changeFloor@MT1:6,0`（fingerprint `changeFloor|MT1|6,0|:next|,`）并 live apply：parent `MT1/[MT1]`、exact `0346b4ba143966bc` → child `MT2/[MT1,MT2]`、exact `ca2cb576183965e6`。hole detector 现在要求 parent provenance、真实换层、旧 visited 集保留和目标层新增；三个负控 + 一个正控通过。19-workload matrix 仍为 **44/44/535 / 0 merge / 0 unsafe**，inventory closure 仍 10 distinct，visitedFloors 正式 CLOSED。该真实边的 production identity 与 candidate identity 都改变，partition `equal`、candidate collision=false，因此 **5.5f evidence gate 未触发，不允许无证据 refinement**。本轮只扩 observation-only recorder provenance 与 qualification；不改 production key/pruning/dominance/ranking/search behavior。

项目已经从“能搜索几步的脚本”发展为具备明确 SolverModel、区域/分段 DP、路线生成、严格 live replay、h5save 断点恢复、GUI 诊断、终局目标合同和本地 Solver Launcher 的求解平台。**PR-5.3 系列已正式关闭为稳定基线**；**PR-5.4a — Multi-Region Solver Baseline 已完成全部 5 个提交 + Repair 1/3a**（solve-task.v2 有序 region 合同、顺序执行 + 真实状态传递、multi-region-route.v1 赢家链复合结构 + runtime 边界连续性、Launcher 有序 region builder、最终验收控制 + CI）。**PR-5.4b — 表示层性能基线与图模型守门轮**：Commit 1（OnlyUp 性能基线）完成；**Commit 2（canonical segment-DP route-free state）完成**——新增 meta.rawRouteLength + getRawRouteLength 统一读取（rawRouteLength > route.length > decisionDepth），canonical 搜索状态强制 route-free（root/child/frontier/skyline/goal-archive 均不携带 materialized route，诊断 routeFree.stateViolations=0/materializedRouteStateCount=0/maxRawRouteLength），route 物化改为 detached clone（不污染 nodes Map 的 canonical state），route-length 相关 tie-break/dominance/objective/rank 全部改走 getRawRouteLength；新增 check-route-free-state-contract.js（标量计数/legacy 迁移/tie-break/route-free 守门）。**Commit 2 Repair（742547b）**：ensureMeta 首次迁移 rawRouteLength（route.length→decisionDepth，显式 0 权威）、searchDP root 从 getRawRouteLength(initialState) 继承累计值（max 与 prefix.length）、detached 物化不再用 materialized.route.length 覆盖 canonical rawRouteLength（route.length=materialized entries，rawRouteLength=decision+auto 累计）、诊断改名 nonEmptyRouteStateCount、route.length objective 仍读 materialized 数组、Commit-1 指纹 parity 移入 check:perf-baseline、check:route-free-state 加入 CI。**Commit 2 Repair 2（ae10d42）**：legacy 首次迁移改 max(route.length, decisionDepth)（旧 canonical 空 route+正 depth 不再塌成 0，重复读确定性）；新增 getMaterializedRouteLength（materialized entries）与 rawRouteLength（decision+auto）双概念；goal archive 排序改用 detached materialized clone（stateForNode），route.length objective 读取真实物化长度而非 canonical 空数组；新增 searchDP-root 跨段继承单测 + 真实 2-region 组合（region A auto 累计保留 ≥29）；route-free 合同 control 修正为 commit1ParityOwnedByPerfBaseline，parity 由 check:perf-baseline 输出。性能对比：expanded/generated/registered/depth 与 routeFingerprint（c0adb2d9…）完全不变，peakRss 454.6→365.1MB、peakHeap 267.4→219.6MB、wallMs 21344→18877（部分为运行噪声；canonical DP 原本即 storeRoute:false，收益有限是预期）。——perf tracker 接入 canonical segment-DP 路径（reachability / enumerateActions / applyAction / buildDpStateKey 计时 + expanded/generated/registered/dominated 计数 + 决策深度 diagnostics.depth），新增确定性 benchmark `bench-perf-baseline.js`（motapathfinder.perf-baseline.v1，固定 smoke region）与合同检查 `check-perf-baseline-contract.js`（结果与普通执行一致、schema 稳定、两次运行结构一致）；参考快照 `shared-solver/profiles/perf-baseline-commit1.json` 为本地生成文件（profiles/ 目录被 gitignore，不属于仓库），可用 `npm run bench:baseline -- --profile representative-baseline --output profiles/perf-baseline-commit1.json` 重新生成。基线热点（representative-baseline，exp9/MT1，116 expansions，depth max 12）：buildDpStateKey ≈ reachability ≈ 18.7s（主导）> cloneState 0.8s > enumerateActions；dominanceRejected=112 与 DP 自身诊断对齐；peakRss=454.6MB（进程 maxRSS 高水位）、peakHeap=267.4MB、内存采样 3 次（searchDP 循环内 32 expansions/次）。**Commit 3（single-Region TowerIR shadow，bc5f2f3）**：新增 lib/tower-ir.js（确定性可序列化 TowerIR v1 编译器：静态组件/POI/边/索引 + sourceFingerprint/irFingerprint）与 lib/tower-ir-shadow.js（动态投影 + evaluateTowerIRReachability + legacy 结构参考 + shadow 对照诊断）。审计结论：现有 topology 模式是休眠的实验（无生产接线、仍是 per-state 格 BFS，无法消除 buildDpStateKey/reachability 热点）；walk 模式是 HP/hazard 感知的完整语义。shadow 对照结构语义（静态组件 + removed/replaced 覆盖，含开放 POI 链式桥接与 replaced-transit 格），walk 的 HP/hazard 语义单独文档化。representative 80 个真实捕获状态 mismatch=0；Commit-2 route/winner/objective/search-size parity 全保留；searchDP 新增 captureExpandedStates 捕获钩子 + towerIrShadowCheckState 纯观察钩子。check:tower-ir-shadow 加入 CI。IR：MT1 1 层/25 组件/52 POI（enemy 23/item 26/changeFloor 1/event 2）/16 边，JSON 25.5KB，编译 1.2ms，IR 评估 0.38ms/state（比 legacy 结构参考 0.24ms 慢——如实报告，shadow 原型）。**Commit 3 Repair（72516d3）**：reachable cells 改完整集合比较（cellSet mismatch 分类，witness 输出 legacyOnly/towerIrOnly cells，反例合同：数量同成员异必须 fail）；TowerIR 端点改从 ir.pois 独立生成（kind/tileId/transition 不再复用 legacy project 扫描，POI 编译错误可见——enemy→item/删除 POI/changeFloor target 篡改三条负面合同均产生 mismatch 且不影响生产）；sourceFingerprint 覆盖完整事件结构（events/autoEvent/firstArrive/eachArrive/parallelDo/beforeBattle/afterBattle/afterGetItem/afterOpenDoor 全量 hash，改事件 payload 或 autoEvent 必变）；synthetic door differential（左组件-door-右组件：关门不连通/door endpoint 可见，开门连通/endpoint 消失，legacy 与 IR 一致）；IR 递归 deepFreeze（修改 POI 抛 TypeError）；重复 scope floor fail-closed（fingerprint 敏感测试改用 MT1+MT2 合法不同 scope）；cache 统计拆分为 statesChecked/uniqueStatesEvaluated/cachedChecks/matchedChecks/mismatchedChecks。corpus：80 checked/46 unique/34 cached/0 mismatched。**PR-5.4c（StructuralKey / ResourceLabel Shadow）**：Commit 1 Key Dependency Corpus（d48f0a1+178bb60，纯观测）——lib/key-dependency-corpus.js 对每个真实捕获 DP state 记录：current exact key（region 模式）、structural 投影（legacy region signature + TowerIR 可达性）、resource 投影（hero/inventory/flags）、event 投影（triggered auto events/auto counters/depth）、action 集 + 每 action 的 successor exact fingerprint。分析：完整分解（structural+resource+event）下 merge hazard=0（同分解状态行为完全一致）；exact-key 合并 14 组产生 52 个行为差异（1 个 actionSet + 51 个 successor——后者是 HP/dominance 语义，DP key 故意忽略 HP 由 skyline 保留最优）；heroFieldVariance：hp 41/atk 11/def 8/mdef 8/lv 3/exp 9。production parity 与 Commit 2 完全一致。check:key-dependency 加入 CI。**Commit 1 Repair（8899623）**：候选 key 重构——structuralCandidate 改用 TowerIR（startComponentId + reachable components/POIs/endpoints + mutationFingerprint，不含精确 loc 与 legacy regionKey）；resourceIdentity 不含 HP（dominance label 单独）；eventHazardLabel 记录 triggered events；候选 key 三元组（candidateStructuralResourceKey / candidateFullBehaviorKey / legacyDecompositionKey）；零碰撞 → evidenceStatus=insufficient-collisions（绝不宣称 safe）；dominance-aware 成对分类（dominance-safe：HP action 超集 + 共享 action successor 行为投影相同 + successor HP 单调；metadata-only：仅 decisionDepth/route/counters 不同；unsafe：共享 action 行为投影不同 / HP 不单调 / 低 HP 有高 HP 没有的 action）；action identity 规范化（summary + 稳定 full-action fingerprint + travelState fingerprint）；successor 多层 fingerprint（exact + dp key + 候选 key + 行为投影 + terminal）；witness 含可读 action summaries。代表性结果：116 状态 / 66 唯一候选 key / 22 碰撞组 / 72 状态在碰撞组 / **99 对 unsafe / 0 dominance-safe / 0 metadata-only**——关键发现：component 级 structural candidate 太粗，同组件不同位置的相邻交互不同（如 event@MT1:6,5）导致 action set 不同，这正是 Commit 2/CEGAR 要细化的字段。production parity 完全不变。**PR-5.4c Commit 3（Candidate-Key Minimality + MT1 Promotion Gate，03235c3）**：Gate A shadow-only——partition audit（exact→candidate Set 双向映射：splitExactKeyCount=32/splitExtraCandidateKeyCount=37/mergedCandidateKeyCount=0/maxCandidateKeysPerExactKey=3 → **partitionRelation=strict-refinement**，候选是 exact 分区的严格细分、无 merge）；split witness field-level diff 定位到 **structuralCandidate.startComponentId**（event label 与 zero/absent normalization 均非 split 原因——without-event-label 与 normalized-resource profile 都是 99 状态）；根因：production exact key 的 walk 可达性含 battle-closure（HP 感知），TowerIR structural 不含，walk 等价但结构不同组件的状态被候选 key 拆分。candidate profile matrix（current-full/normalized-resource/without-event-label 全部 safe 0 unsafe 但全部 99 状态 delta -37）→ CEGAR minimality 无 profile 能缩小 registry。**Gate A FAILED（delta -37 < 0）→ Gate B 未运行 → 最终决策 NO_PROMOTION**。负控：missing-atk 合成 pair unsafe=1（代表性 0，因 atk 与 exp/level 联动）、BROKEN unsafe=59。searchDP 新增 dpStateKeyBuilder 实验钩子（默认 null → 生产 buildDpStateKey，byte-for-byte 不变）。check:candidate-key-promotion 加入 CI。**PR-5.4c 正式结论：TowerIR candidate key 语义有前景但当前形态不值得生产推广（严格细分 +37 状态）**。**Commit 3 Repair（5e5f6c1）——决策翻转 PROMOTION_CANDIDATE**：新增 without-start-component profile（单变量：current-full 仅删 startComponentId），split field distribution 聚合全 32 个 split exact key：**{structuralCandidate.startComponentId: 32}，splitExactKeysOnlyStartComponent=32，other=0**——startComponentId 是唯一 split 原因（event label 与 zero/absent normalization 排除）。without-start-component：62 states / **delta 0 / partitionRelation=equal / 0 unsafe / 0 split / 0 merge**（TowerIR reachable closure 本身够用，startComponentId 是冗余 anchor 而非行为 identity——确认评审假设，无需 battle-closure-aware 算法）。Gate A PASS（minimal-safe=without-start-component）。Gate B 真实 strict replay（verification.strictReplay=true，headless 浏览器双跑）：correctnessExact=true + strictReplayVerifiedBoth=true；key-phase A 15.7s/268 calls vs B 0.16s/268（~96x，标注 shadow estimate）；wall A 25.3s vs B 31.6s（walk reachability 仍留在 enumerateActions，仅报告不硬 gate）。**最终决策 PROMOTION_CANDIDATE**：MT1 guarded experimental candidate（dpStateKeyBuilder 实验钩子，非默认 production）。unknown candidate profile fail-closed（throw 不 silent fallback）。**PR-5.4d（Guarded MT1 Candidate Key + Paired Performance Gate，d7362a1）**：lib/guarded-candidate-key.js 把 without-start-component 提升为正式 experimental profile（dpKeyProfile：production-region 默认 / experimental-mt1-tower-ir-v1），scope fail-closed（floor 非 MT1 或未知 profile 抛错，禁 silent fallback），绑定 regionId/floors/projectFingerprint/regionFingerprint/TowerIR source+ir fingerprint/candidateProfileVersion。修 3 个诊断 P2：split field distribution 改 canonical vs 全部 representatives；correctness 用 routeRecord.decisions summaries + execution.objectiveValue；search scale 用 actionsGeneratedByKind/actionsKeptByKind 求和 + registry。searchDP 增加 enumerateActions/applyAction phase 计时（只读）。check-candidate-key-paired-benchmark：真实 strict replay 双验证（A/B correctness byte-exact，objective fingerprint b54217a8… 一致）+ 4 paired rounds（A/B/B/A/A/B/B/A）。**Paired 结果（median）：A key 17.3s vs B key 0.09s（~191x）；A wall 19.7s vs B wall 10.7s（wallRegFactor 0.54，B 整体快 ~2x）；reachability cost-shift 确认（A key-walk 17.3s → B enumerate-walk 7.8s）**。**最终 verdict：GUARDED_PROFILE_APPROVED**（仍默认 production-region，未默认晋级）。check:candidate-key-paired-benchmark 加入 CI。**PR-5.4d Repair（d27217a）——自包含 guarded profile + pinned baseline**：dpKeyProfile 成为真正选择接口——executeSolveJob 执行边界调用 resolveDpKeyProfile（production-region 默认无 builder / experimental-mt1-tower-ir-v1 单独启用 guarded builder / 未知 profile 在 DP 前 fail-closed）。pinned manifest APPROVED_MT1_BASELINE 绑定权威指纹：projectFingerprint d50cdaaf…（buildProjectFingerprint().fingerprintSha256，非空串 hash）、projectStructural 954efc84…、regionSpecFingerprint 510312b1…（归一化 spec 结构化，排除 goal）、towerIrSource 96a0bb0f…、towerIr 3c4b7c9b…；运行时与实际编译指纹比对，漂移即 throw（enemy stats tamper / RegionSpec tamper / wrong floor 均被拒）。修 3 个 P2：registered 改 dp.acceptedStates（156 canonical）；paired round 每轮记录 winner/route/objective 并锁基线；reachability audit 拆 reachabilityTimingShiftObserved + reachabilityCallAttributionComplete（cache mode none-exposed）。**Paired 结果（median，本地）：A key 38.1s vs B key 0.2s（~189x）；A wall 42.2s vs B wall 25.2s（wallReg 0.6）；registered 156/156；16 项 control 全真**。**verdict：GUARDED_PROFILE_APPROVED**（仍默认 production-region，未默认晋级）。**Repair 收尾（6626049）**：check:candidate-key-promotion 的 Gate B 改用 pinned resolveDpKeyProfile（profile-alone，不再函数注入）——首次 CI 因旧 API 引用失败（createGuardedKeyResolver 已移除）即被发现并修复；最终 marker HEAD CI 双 job 全绿（含 check:candidate-key-promotion + check:candidate-key-paired-benchmark）。**Commit 1 Repair 2（db1e9a6）**：action choice 与 travel variant 分离——actionChoiceFingerprint 只含 kind/floorId/目标 x,y/targetId(enemy/item/door/tool)/event choicePath/changeFloor target，不含 travelState/HP/path/stance；travelVariantFingerprint 单独记录 path/stance/direction + travel 状态投影。choice-set 等价用 choice fingerprint；同 choice 多 travel variant 保留为列表。successor 失败 fail-visible：枚举/applyAction/投影错误 → analysis-error（绝不静默判 safe）。terminal 投影真实化：alive/dead/goalReached(真实 goal predicate exp>=9)/terminalClass，参与分类（低 HP goal 高 HP 未达 → unsafe；共享 choice terminalClass 差异 → unsafe）。witness 用 lowHpState/highHpState + actionOnlyLow/actionOnlyHigh 方向。metadata label 补全 materializedRouteLength/auto counters。代表性重算：**116 状态/66 唯一候选 key/22 碰撞组/72 状态，99 对全部 dominance-safe（0 unsafe/0 metadata-only/0 analysis-error/0 unclassified），travelVariantMismatch=99**——证实上一轮 99 unsafe 全部是 action identity 把 travelState/HP 混入 choice 制造的假差异；候选 key（TowerIR component+resource+event）在 MT1 代表性语料上行为等价且 HP 单调。production parity 不变。**PR-5.4c Commit 2（Dual-Key Registry Shadow，94aa8c1）**：lib/dual-key-shadow.js 在 canonical searchDP 中通过纯观察 recorder 钩子（candidateKeyShadowRecorder，默认关）记录每个 enqueue 决策（state + exact key + production keep/reject/replace），post-pass 模拟候选 key（TowerIR StructuralKey+ResourceIdentity+EventHazardLabel，HP 仅 dominance）的注册行为。候选 key 构建抽为 buildCandidateDpKey API（不接 buildDpStateKey production 分支）。shadow bucket 用 Commit 1 classifier（complete variant-set dominance coverage）分类；unsafe/analysis-error/unclassified fail-visible（failOnUnsafeForContract 供合同）。决策分歧矩阵 + productionKeptShadowRejected witness。负控：BROKEN key → unsafeMerge>0 且 production 不变。顺手修两个 P2：duplicate-choice 按 stateFingerprint 唯一计数（16 非 64）、equal-HP 双向 choice-set/variant 对称覆盖。代表性四数字：**1) 在线碰撞 206（62 唯一候选 key）；2) 全部 dominance-safe（0 unsafe/0 error/0 unclassified）；3) hypotheticalReduction = 0（MT1 上候选 key 不减少状态数）；4) 候选 key 构建 0.27ms vs production key 46.9ms（~170x 更省，因避免 walk reachability BFS）**。production parity 完全不变。check:dual-key-shadow 加入 CI。**Commit 2 Repair（a367fd8）**：修正度量口径——① 移除 exactDpKey→candidateKey 语义 cache（gate 模式 off，每条 recorder state 独立 buildCandidateDpKey，candidateKeyBuildCalls=statesRecorded=268，postPassExactKeyReuseHits=0）；② 审计 exactKeyToCandidateKeys：**37 个 exact key 被候选 key 拆成多个（candidate 更细——含 triggeredAutoEvents 而 exact key 忽略）**；③ equivalent → shadowRejectEquivalent（不入 bucket，matrix 与 occupancy 一致）；④ 拆分事件决策与最终 occupancy（productionAcceptedEvents/candidateAcceptedEvents/candidateRejectedDominated/Equivalent/Replace/Unsafe + finalActiveStates/finalUniqueKeys + candidateFinalStatesFromProductionAccepted/Rejected）；⑤ hypotheticalStateDelta 允许负数（不 clamp）。修正后代表性结果：**candidateFinalActiveStates=99 vs productionFinalActiveStates=62 → hypotheticalStateDelta=-37（候选会多保留 37 个状态，是扩张非缩减）；uniqueKeyDelta=-37；shadowRejectEquivalent=7/RejectDominated=38/ReplaceDominated=124；0 unsafe/0 error/0 unclassified**。searchDP 新增 registry diagnostics（finalUniqueKeys/finalActiveStates，纯观察）。key phase 估计（perf tracker 真实 buildDpStateKey 30.7s/268 calls vs 候选投影 145.8ms）→ estimatedKeyPhaseRatio≈210x，标注 "shadow estimate, not production speedup"。production parity 完全不变。**Commit 1 Repair 3（79a867b）**：variant-set dominance coverage——弃用 findChoiceRecord 第一条语义，改为 findChoiceRecords 消费该 choice 的全部 production travel variants；每个 low variant 需存在 dominance-covering high variant（successor structuralCandidate/resourceIdentity/eventHazardLabel 相同 + terminal 兼容 + high successor HP >= low successor HP），high 侧额外 variant 允许；任意 variant（含非首条）的 applyAction/投影错误 → analysis-error；travel variant 不做精确字符串匹配（只用于诊断）。分类补 equivalent（HP 相同 + 行为相同 + 无 metadata 差异，区别于 metadata-only）。goal predicate 精确为 floorId==="MT1" && exp>=9。代表性重算：**99 对全部 dominance-safe（0 unsafe/0 equivalent/0 metadata-only/0 analysis-error/0 unclassified），variantDiagnostics：64 状态含重复 choice（production 确有多 variant，max 2/choice），variantPairsChecked 378，lowVariantsCovered 352，unmatchedLowVariantCount 0，highOnlyVariantCount 0，variantCoverageUnsafeCount 0，variantAnalysisErrorCount 0**——complete variant-set 下候选 key（TowerIR component+resource+event）在 MT1 代表性语料上全量 dominance-safe。production parity 不变。**PR-5.4e（MT1 Workload Matrix + Default-Promotion Decision，进行中）**：新增 check-mt1-workload-matrix-contract.js——6 个真实 MT1 workload（heroAtLeast exp9/exp6/exp8 + maximize hero.atk 第二 objective + tileRemoved (4,1)/(2,1) 两个真实 tile 终点），每个跑 A=production-region vs B=experimental-mt1-tower-ir-v1，锁：A/B correctness byte-exact（found/winner/route/decisions/objective）、**双语料 partition audit**（A 与 B 各自的 enqueue corpus 都要求 without-start-component split=0/merge=0/equal/0 unsafe/0 error/0 unclassified）、完整结构计数（expanded/generated/registered=acceptedStates/dominanceRejected/finalActiveStates/finalUniqueKeys，不预设相同）、真实 reachability 归因。**重要发现：smoke RegionSpec 自带 dpBudget.maxRuntimeMs=10000（10s 时间预算），导致 production 搜索时间受限非确定**——tile4_1 的 A 搜索两次跑出不同 winner（54/31 expansions，val 1199/1019）；合同必须 maxRuntimeMs=0（跑到底）才能做确定性 A/B 对比（已确认非候选 key 问题，是生产求解器真实属性）。reachability 真实归因（perf tracker reachability phase + simulator cache stats）：exp9 基线 **A 266 次 BFS/33.95s vs B 123 次/13.20s**——候选 key 消除 key-path 冗余 walk，总 reachability 工作量下降而非转移。已本地验证：exp9 基线 A/B 完全一致（winner a2ff3798/1346 匹配 PR-5.4b，scale 116/267/156/112/62/62，structureExact=true）；tile4_1 禁时间预算后 A/B 也完全一致（winner ec78c20a/1539）。全量 6-workload matrix 本地运行中，CI 待跑。另修两个 carried P2：candidateProfileVersion 改为独立 pinned assertion（expectedCandidateProfileVersion 字面量 "without-start-component-v1"，若候选 identity profile 变更则派生 actual 不匹配 → 拒绝直到重新认证）；reachability audit 改用真实 instrumentation（reachability phase counts + getReachabilityCacheStats，不再用 enumerateActions calls 代替）。simulator 新增 getReachabilityCacheStats 访问器（reachability cache hits/misses/stores）。未提交。

**后续主线已全部收口（详见"已完成主线"表）**：PR-5.4e（workload matrix + Repair 收 3 P1 + 2 P2 → `MT1_DEFAULT_PROMOTION_ELIGIBLE`）→ PR-5.4f（MT1 Default Promotion + Explicit Rollback → `MT1_DEFAULT_PROMOTION_ACCEPTED`，未指定 profile 在 approved MT1 scope 走候选）→ PR-5.4g（post-promotion 全路径回归，`computeRegionSpecFingerprint` 剥离 projectRoot 重基线 → `MT1_DEFAULT_PROMOTION_REGIME_STABLE`）→ **PR-5.5 系列（研究/证据轮，零 production 行为改动）**：5.5a（multi-Region dual-key shadow + boundary corpus + Repair 收 3 P1）→ 5.5b（11→18 workload corpus + Repair 1/2 收 scope 隔离、production-legal semantics、witness 路径）→ 5.5c（semantic-diversity hunt + Continuation + Repair 1/2/3 收 union 聚合、transition evidence、mutation 表示）→ **5.5d（Production-Faithful Fixture Qualification：只用 tracked OnlyUp Start floor 真实 sealed key room 补齐 inventory hole——`start-door-key-entryA` workload，inventory CLOSED（acquire=I600/interactPickup、consume=greenKey/openDoor、canonical 同 edge mutation、filled=true），visitedFloors PARTIAL（maxVisitedFloorCount=2 via 真实跨 floor boundary + arrival，changeFloorExecuted=false 如实））**。当前研究冻结：**19 workload / 44 pre / 44 boundary / 535 post / 0 real merge / 0 unsafe / `NO_COLLISION_OBSERVED`（≠ SAFE/CERTIFIED/PROMOTION_CANDIDATE）**。

**PR-5.6 已完成 5.6a→e**：compound route lineage 完整；goal dependency graph 与显式 `first-feasible`/`adaptive-feasible` 搜索意图落地；`admissible-v1` 只用完整证据做必要条件剪枝；bounded multi-level checkpoint repair 有 synthetic depth-2 成功控制；goal projection 做增量 requirement cache；两条 tracked route portfolio 可用独立进程并发且 strict replay/exact state 完全一致。当前长链仍未闭合：MT3→MT5 blueKing 的 probes 停在 `mt5-third-gate`，这只是预算内未完成，不是无路线证明。

**PR-5.7a Candidate Quality Shadow 已完成并触发 Stop B**：capture-all raw goal archive 保留可继续搜索的物化路线，milestone frontier 用未修改的选择器审计 capacity 1/2/4/8，并给每个候选相同的 third-gate expansion budget。tracked MT3 fixture 得到 8 次 first-sweep attempt、140 个唯一 raw goal candidate、18 个 milestone candidate；后者先按现有 DP identity 去重为 3 个，capacity 4/8 均无额外容量淘汰。140/140 downstream probes 都在 frontier 清空后失败（最多 29 expansions，0 budget exhaustion，0 success），因此当前 corpus 没有 retained-fail / evicted-success witness，**不得进入 Minimal Skyline Role Refinement**；下一步转查 action scope / milestone target / resource timing / segment abstraction。

## PR-5.16 / PR-5.17 重新校准后的主线

PR-5.16a 已把原 5.11 的“发生了什么” trace 提升为人可参与判断的 Search Observatory。权威 D2 1000-expansion 基线在同一份报告中并列 terminal objective、自动 feasibility hypothesis、candidate funnel、预算流向、goal-rank plateau、human-review candidates 与下一项可证伪实验：Boss candidate=0；自动选择的 I894 预计减少 attack/defense deficit **272/1980**，但 candidate=0/checkpoint=0；MT5/MT4 expansions=**641/359**，battle/changeFloor incoming=599/400，单一 `changeFloor@MT4:6,12` 入口产生 228 次展开；3032 generated、1490 accepted successors（1491 registered states 含 root）、1542 dominance rejected、frontier202；最大相同 goal-rank tuple 覆盖 343 expansions。所有低收益项仅标 `observed/inspection-candidate` 且 `wasteProven=false`，必须经 same-control A/B 才能升级成优化结论。自动 next experiment 是 topology-derived predecessor compilation；明确固定预算并保持 production key/dominance/selection 不变。冻结 `SEARCH_INTENT_AND_COST_BASELINE_VISIBLE`，本轮不改变搜索结果。

当前不再把单个 DP priority 当作完整路线发现器。职责冻结为：全局 planner 从最终 goal 反推可行性缺口和 AND/OR 依赖；局部 canonical DP 只执行一个机器生成的局部目标并返回多角色 checkpoint；下游失败把缺口反馈给高层，再按需回溯。known route 只用于 simulator/replay correctness 与 blind 结果事后对照，不得生成 milestone、事件顺序、资源阈值或 floor hint。

| 轮次 | 目标 | 必须回答的问题 | Promotion gate |
|---|---|---|---|
| PR-5.16a | Search Observatory | 机器在尝试什么、预算花在哪里、哪些只是嫌疑、下一项 A/B 是什么 | D2 真实报告 + 同控制比较合同；不改搜索语义 |
| PR-5.16b | Global AND/OR Dependency Planner | Boss 为什么不可行、有哪些可替代资源、目标前有哪些门/怪/事件/换层依赖 | 仅由 tower/state/terminal goal 编译；不得读取 route/milestone/order |
| PR-5.16c | Local DP Checkpoint Executor | 对一个依赖节点能否返回 survival/resource/equipment/mobility 等多个战略出口 | checkpoint 角色真实不同、strict replay、不能只保留 top-1 |
| PR-5.16d | Failure Feedback + On-demand Backtracking | 下游失败能否具体改变上游选择，而非盲目加预算 | 报告 failed assumption、replacement branch、retry cost 与因果 A/B |
| PR-5.17a | D2 Promotion | 只给 detached 楼层入口 + Boss，能否自主找到 | found + 完整 strict replay；faster not-found 不算通过 |
| PR-5.17b | D3 Promotion | 只给塔 + canonical initial + final Boss，能否自主找到 | found + 完整 strict replay；与 5.10c/5.14 同口径对比 |

每个后续功能轮必须同时输出：goalFound/frontierExhausted/budgetExhausted/searchComplete、wall/expansions/generated/accepted/rejected/frontier、目标/子目标候选是否进入 funnel、预算最大三类流向、Top hypotheses、review candidates、same-control before/after。任何“无用搜索”判断都必须标明是 observed hypothesis 还是 causal A/B proven；仅 wall 下降但 found/deepest-stage 不变不得称为路线发现改善。

PR-5.16b 已建立第一版 route-free AND/OR dependency planner。自动图补齐相邻非 transit POI 之间的双向 `poi-contact` 静态边（此前 I894 与相邻 goldHornSlime 都只连各自组件，图被错误截断）；OnlyUp 图仍为 409 nodes，但 edge **722→906**。从 detached MT5 entry + terminal Boss + 自动 feasibility subgoal 出发，planner 为 I894 编译出 **2 个 OR alternatives**，方案内分别有 **5 / 7 个 AND blockers**；两条方案的公共前置是 `MT5:10,5 goldHornSlime`。当前状态 simulator probes 将候选分为 5 个 `viable-at-current-state` 与 6 个 blocked，其中包含 `unbeatable-at-current-stats` 和伤害 211957 的 `lethal-at-current-hp`；probe 明确是 current-resources-at-target counterfactual，不冒充路径执行。移除 `poi-contact` 后目标必须报告 disconnected；无 selected feasibility subgoal 也 fail-visible。Search Observatory 已接入 OR/AND board，并把下一实验推进为局部 DP 执行首个 viable prerequisite + multi-role checkpoints。冻结 `AUTOMATIC_AND_OR_DEPENDENCY_PLAN_COMPILED`；本轮没有改变 production DP key/dominance/selection/budget，也没有宣称 D2 已改善。

PR-5.16c 已把 canonical segment-DP 收窄成 automatic prerequisite 的局部 checkpoint executor。输入只含 tower + route-free current state + 5.16b dependency plan；执行器自动选择 alternative-1 的首个当前可行 blocker `MT5:8,11 skeletonKing`，goal 直接使用合法 `tileRemoved` schema，不经人工翻译。固定 64 expansions 中 expansion **1** 首次完成目标；继续搜索用于出口多样化而不是掩盖发现成本：210 generated = 181 accepted + 29 rejected，45 raw goal nodes 保留 8 checkpoints，frontier62。保留结果覆盖 highest-hp / best-combat / highest-atk / highest-def / highest-mdef / highest-exp / shortest 共 7 种角色，且为 **8 exact states / 8 semantic summaries / 8 route fingerprints**；8/8 独立 route record strict replay，全量重复运行指纹确定。若没有 viable prerequisite，明确返回 `NO_CURRENTLY_VIABLE_DEPENDENCY_PREREQUISITE`。冻结 `LOCAL_DEPENDENCY_MULTI_ROLE_CHECKPOINTS_VERIFIED`；本轮仍未做下游反馈，也不宣称 D2 closure。

PR-5.16d 已把下游失败接回 checkpoint/OR 选择。依赖执行严格限定为每条 AND alternative 的首个未完成项，禁止越过被卡住的前置去执行后项；每个 checkpoint 都重建 route-free graph、feasibility 与 dependency plan。真实 D2 因果 A/B：只保留原 top-1 `best-combat/highest-atk` 时，alternative-1 的 `MT5:9,10 evilHero` 与 alternative-2 的 `MT5:2,8 evilHero` 均为 lethal，结果 `DEPENDENCY_FEEDBACK_REQUIRES_NEW_SUBGOAL`；保留 8 个出口后，反馈改选 `highest-mdef` checkpoint-5，并从 alternative-1 切到 alternative-2，首项变为可行的 `MT5:3,10 skeletonPresbyter`（damage26644 / survival margin227479）。下一局部执行 expansion **1** 找到目标；32 expansions 共 88 generated=73 accepted+15 rejected、21 raw goals→8 checkpoints、frontier16，8/8 strict replay。组合回放按父 checkpoint 记录增量 segment decisions，累计 route lineage 仍保留。本轮冻结 `DEPENDENCY_FEEDBACK_ADVANCED_WITH_STRICT_REPLAY`：证明额外 63 expansions 的 checkpoint 多样性有因果价值，但还没有连续执行到 I894/Boss，D2 仍 OPEN。

PR-5.17a1 在第二个自动 prerequisite 后第一次进入“所有 OR 首项 blocked”的递归修复面。8 个 checkpoint 的最优即时形态仍不能承受 evilHero；compiler 对当前地图仍存在的物品逐项 pickup/equip counterfactual，再用同一 automatic graph 编译每个资源的可达依赖。共 348 个 repair candidates；收益最高的 `I1009@MT5:7,3` 虽能给 checkpoint-2 +819200 HP、把预期余量推到771048，但 4 条资源路径都以原 lethal evilHero 开头，因此明确拒绝 circular high-benefit suggestion。按收益次序验证到第59个可启动候选时，选择 `I1014@MT5:12,11`：+100 DEF 令 evilHero damage 407701→168451、预期余量46775；其资源路径只含一个当前可打的 `MT5:11,11 devilWarrior`。局部执行 expansion1 找到该前置，32 expansions 中 70 generated=41 accepted+29 rejected、frontier4、4 retained checkpoints 全 strict replay。冻结 `AUTOMATIC_BLOCKER_REPAIR_IDENTIFIED`；I1014 尚未实际拾取，D2 仍 OPEN。

PR-5.17a2 已把上述组件接成同一 route-free hierarchical loop，而不是逐轮手工调用 checker。它从 detached MT5 entry + terminal blueKing 自动连续完成五个 prerequisite：`MT5:8,11 skeletonKing → MT5:3,10 skeletonPresbyter → MT5:11,11 devilWarrior → MT5:1,11 skeletonKnight → MT4:10,5 devilWarrior`；中间分别由 I1014、I1013、I642 counterfactual 自动触发三次 blocker repair。五段全部 goalFound + strict replay，总成本 156 expansions / 423 generated / 325 accepted / 98 rejected。最终两个 retained checkpoints 均为 hp40578/atk1097/def915/mdef6310/lv7/exp315/I893；evilHero damage60453，实际 margin -19875。此时 120 个有益单物品候选、40 个唯一 checkpoint/resource access 全部不可启动；终态距离 lv8 仍差 285 EXP，直接可见动作仅 2 个 changeFloor，0 battle、0 fightToLevelUp。故 5.17a2 的停止不是 timeout 或预算耗尽，而是当前 repair vocabulary 不含“跨层寻找 EXP/复合资源链”；下一轮 5.17a3 必须新增这一一般化能力并回到同一 gate 复验，D2 仍 OPEN。

PR-5.17a3 重新审计后收回了“当前首缺口一定是 EXP”的过早判断。根因先落在上层控制语义：segment DP 虽记录 expansion-1 first goal，但 goal skyline 未保证保留它；后续 checkpoint feedback 又倾向即时高 HP，可能在没有必要性证明时提前消费一次性怪物/回血/事件。现在 local executor 显式保留 `first-goal`，正常反馈与历史回溯优先 least-commitment checkpoint；root 与历史 portfolio 均可作为机器 save/load 点。repair 也不再把静态 pickup counterfactual 当成已实现收益：它必须沿自动 dependency 连续执行到实际目标；若真实 local DP 完整穷尽仍未取得目标，就产生 `blocker-repair-rejected`、恢复原 portfolio，并按 exact-state/resource experiment key 去重。固定 6-round checker 中 I621 的高收益建议真实执行为 **9 expansions / 15 generated / 8 accepted / 7 rejected / frontier0 / searchComplete=true / goalFound=false**，旧语义会在此丢失主线，新语义继续从未污染 checkpoint 选择并完成 I1014。全轮 **220 expansions / 694 generated / 488 accepted / 206 rejected**，对比 5.17a2 的 156 expansions 是 +64 的试错成本，不是性能胜利；得到的是可观察、可回滚且不会重复同一失败实验的控制闭环。D2 仍 OPEN，下一轮优先把候选分数改为实际获取成本后的 net margin，并分离/缓存重复宏图编译时间。

PR-5.17a4 将 repair 的“看起来有益”与“真实解除 blocker”彻底拆开。候选仍可先用 counterfactual 做便宜预筛，但一旦真实执行到资源，控制器会在 actual checkpoint 上重新调用 battle resolver 计算原 prerequisite。真实证据：I1014 free-pickup 预测 evilHero damage93931 / margin **+3583**；获取路径支付 devilWarrior **65810 HP** 后，actual damage93931 / margin **-62227**，prediction error **-65810**。它相对 repair 前仍带来 actual margin gain 72190，所以分类为 `improved-but-still-blocked`，不得写成 `blocker-unblocked`。报告同时包含 acquisition HP/ATK/DEF/MDEF/EXP delta 与增量 decision 数。编译成本也第一次结构化计数：相同 8-checkpoint/9-access 输入下，旧式每个 access 重建 graph 为 **17 builds / 0 reuse**，复用 checkpoint graph 后为 **8 builds / 9 reuse**，selected experiment 与 370 candidates 均不变；wall 只作辅助。下一轮让 actual closure class 参与后续组合修复选择，避免把一次正增益误当终点。

PR-5.17a5 把 actual verification 从只读报告接回控制器事务边界：`blocker-unblocked` 和 `improved-but-still-blocked` 才提交真实 checkpoint；`no-net-improvement`、`repair-target-not-realized`、无 goal/checkpoint 都回滚到输入 portfolio，并对 exact-state/resource experiment 去重。真实 14-round 对照中，I642 的 free-pickup 预测 margin **+22687**，实际获取后 margin **-27998** 且被分类 `no-net-improvement`；5.17a4 之前会接受该状态再等历史回溯，当前 round13 直接 `blocker-repair-rejected`，round14 从历史 checkpoint 继续。新增 `MOTAPATHFIND_D2_STREAM=1` 的逐轮 NDJSON 事件，长运行不再结束后才可见。首三局部段的 phase timing 显示：64/32-expansion DP search 约 **1.2–1.7s**，checkpoint route-record + 8路 strict replay 约 **2.8–5.4s**，dependency evaluation 约 **0.9–1.0s**；因此下一性能主线是减少探索中间 checkpoint 的重复证据构造，同时最终入选链仍必须完整 strict replay，不能靠取消验证制造假加速。

PR-5.17a6 针对上述 timing 做了第一项不牺牲证据强度的优化。旧 local executor 为 DP 建一个 simulator，又为 8 个 checkpoint 的 route record/replay 各建两个，共 **17 instances**；`StaticSimulator` 的 replay 只操作由 snapshot 恢复出的 state，因此现在同一 local execution 复用一个 simulator。明确控制组 `reuseCheckpointSimulator=false` 仍可复现旧行为。固定真实输入中 before/after 均为 64 expansions、8 retained checkpoints、8/8 strict replay，所有 exact-state/route fingerprints 与 role selection 相同；instances **17→1**，顺序 wall **8.866s→5.191s（方向性 -41.4%）**。正确性合同优先于 wall；本轮未减少候选、未跳过 replay，也未改 production DP 语义。

PR-5.17a7 将 repair 去重从 exact checkpoint identity 收紧为与实验结果有关的语义 identity：资源/战斗状态、真实 flags、visited floors 与地图 mutation 均保留，hero 坐标/方向不直接决定 repair 身份；access dependency fingerprint 与 blocker evidence fingerprint 分开加入，`__leaveLoc__` 这种返回楼层导航缓存明确排除。真实 30-round 同控中，旧版两组 I642 分别在 round18/19（predicted **+22644** / actual **-28041**）和 round29/30（predicted **+22394** / actual **-28291**）逐字重复；新版对应 repair trials **22→20**，两组相邻重复均消失，第二组首试提前到 round28。总 expansions **775→821**，因为省出的轮次立即用于历史分支/下一候选，并非纯 wall/节点加速；`blockerUnblockedCount` 仍为 0，D2 仍 OPEN。下一轮必须缓存/增量化每轮重复 macro graph 与 access 编译，并继续寻找第一个 actual unblock。

PR-5.17a8 为同一 hierarchical run 增加 exact-state scoped repair compilation cache，只缓存 macro graph + feasibility/dependency analysis 与 state/resource access summary；不缓存 simulator state、候选选择或 DP 结果。真实 6-round A/B 的 repair 顺序、四组 predicted/actual margin 与搜索总量 **220/694/488/206** 完全一致。cache off→on：graph builds **25→17**、graph-backed access compilations **87→68**，命中 **8 checkpoint analyses + 19 access summaries**；repair compilation 累计 wall **23.380s→18.718s（方向性 -19.9%）**。round5 I621 失败回滚后，round6 的同一 5-checkpoint portfolio 从 **5 builds→0 builds / 5 cache hits**。D2 仍未 actual unblock；下一轮继续审计 candidate counterfactual 重算与历史 portfolio 调度，不扩大 DP key/dominance。

PR-5.17a9 将自动 repair resource 从 item 扩展到 simulator-confirmed viable combat reward。combat counterfactual 必须真实支付伤害、结算奖励/升级/afterBattle，再区分即时 `blockerImprovement` 与仅 `levelProgress`；前者仍优先，后者只有实际取得 EXP/等级才以 `progressed-toward-level-threshold` 提交。真实 A/B 在 round13 同样否决 I642 no-net 后：旧控制 round14 直接 historical backtrack；新控制自动选择 `MT4:10,5 devilWarrior`，9 expansions 完成，actual delta **HP -42685 / ATK +20 / DEF +20 / MDEF +200 / EXP +26**，margin 仍 -27998，明确不叫 unblock。下一轮新状态仍有 **180 item + 36 combat** candidates、**72 unique access probes**，但全部 non-startable（首项被 lethal evilHero gate 阻断），随后才回溯。首失败面因此推进为“需要复合/跨 gate EXP chain”，不是 timeout 或单资源候选缺失；D2 仍 OPEN。

PR-5.17a10 将上述 failure signal 接回候选调度：默认仍是 blocker-first；当一个真实 combat repair 已取得 EXP、随后在该 portfolio 无单节点 repair 可启动时，历史回溯切为 `level-progress-first`，直到实际升级或 blocker-unblocked 才恢复。真实 fixture 合同在同一 checkpoints 下锁定默认选择 `I1014`、进展模式选择 `MT4:8,11 poisonBat`（counterfactual +16 EXP）。20-round 运行在 round15 切换后，round16–20 连续选择 combat reward；相对 5.17a9 的 50-round 控制仅有 5 次尾部 combat progress，新版 50-round 有 **27 次 combat repair**，但 **1215→1229 expansions**、actual unblock 仍 0。最终 frontier 只有 lv7/exp263 或289、route length10/11，证明这些试验分散在历史分支，并未沿单一存档累积至 lv8；repair compilation 累计 **144.410s / 179 graph builds / 700 unique access probes**，是当前可见浪费。故本轮只关闭“失败后优先尝试 EXP”的选择缺口，D2 仍 OPEN；5.17a11 必须让 cumulative progress 决定 portfolio/history 延续，而不是加轮数。

PR-5.17a11 将进展模式中的 history 与 repair candidate 都改为绝对累计进展：history 按 `level → EXP → route length → recency`，combat candidate 按 simulator 结算后的 `levelAfter/expAfter`，默认 blocker-first 顺序不变。20-round 的候选线路由最多 11 decisions 延长到 15，但绝对 EXP 仍停 289；这排除了“只差一种排序 tie-break”。随后控制器从 combat-progress dead end 自动生成当前状态的下一等级目标 `minHero.lv=8`，不提供怪物顺序。三个不同 exact checkpoint 均为 **lv7/exp315**，每个局部 DP 都在 **9 expansions / frontier0 / searchComplete=true / goalFound=false** 完整穷尽；目标不是 budget/time failure，而是到达该 blocked state 后已不存在可执行的升级链。相同 20-round 总量为 **489 expansions / 1359 generated / 981 accepted / 378 rejected**；D2 仍 OPEN。下一轮必须从这些失败 witness 向更早 history 编译前置 EXP/resource chain，在进入 lethal gate 前保留升级可能，不能继续给死状态加轮数。

PR-5.17a12 把 `lv+1` failure 变成有界 backward save/load：最多 4 个历史 probes，先按当前可执行 `fightToLevelUp / visible battle EXP`，再按绝对进展排序，避免占满正常 backtracking。关键修复是未达到 lv8 时不再丢弃局部 DP 的 best-progress：只有 level/EXP 严格上升才构造 route record、strict replay 并作为 `levelProgressPartial` 提交；事务未完成前 terminal dependency 不得抢占。固定真实 contract 从 MT5 entry **exp77→173**，32 expansions / frontier57 / `goalFound=false`，但保留的 6-decision checkpoint strict replay 通过。集成链中旧版从 exp205 找到 exp221 后直接丢弃；新版同一 32-expansion witness 保存 exp221，随后 combat repair 在该 lineage 上实际 **+94 EXP→315**。这证明 partial progress 能真实累积，同时也证明 lv8 假设仍不足：exp315 后再次 9 expansions 完整穷尽、0 visible battle EXP，D2 未 closure。下一轮回到一般 blocker resource/event dependency；level probe 保留为一种 failure-driven 工具，不能成为硬编码主路线。

PR-5.17a13 用成功路线做 **post-hoc oracle only**，没有把 fixture/order/threshold 输入自主 planner，首次把 D2 的坏→好差异压到一个单变量。共同状态仍保留 `I621@MT4:7,3`；坏序在高损链前触发 `battle:skeletonKing@MT4:8,3` 并自动吃掉 I621，好序先保留它、完成 MT5 bottom chain 后再触发。两边最终 `atk1077/def895/mdef6110/lv7/exp289` 完全一致，只有好序多 **13,846 HP**。继续支付同一个 MT4 devilWarrior 后，evilHero damage 均为 60,453：坏序 `hp49,345 / margin -11,108 / lethal`，好序 `hp63,191 / margin +2,738 / viable`。route-free hierarchical planner 自己能在 round5 发现 I621，但现有 vocabulary 只把它编译为 `tileRemoved`（consume-now），没有 `presentTiles`/defer intent；到该轮累计已花 **201 expansions / 657 generated / 469 accepted / 188 rejected**，随后 I621 acquisition 又以 **9 expansions / frontier0 / searchComplete=true / goalFound=false** 完整失败。冻结 verdict `HIERARCHICAL_REPAIR_MISSING_DEFERRED_RESOURCE_INTENT`：问题不是 DP key/budget，也不是资源不可见，而是 blocker feedback 没有把“保留资源的 option value”接到 hierarchical local goal。下一轮 5.17a14 应生成一般化 `preserve-now → cost-chain → consume-later` 事务，并用同一 A/B 证明不依赖 I621 坐标特调。

PR-5.18a 已停止上述 5.17a14 repair 方向，建立独立最小 D2 战略纵切。物品/怪物存在状态使用 **immutable base 2D floor grid + sparse `removed/replaced` mutations** 物化为共享 option map；节点只持共享 snapshot/fingerprint，不复制整塔。真实 MT4+MT5 初始 map 为 62 options（33 item/29 enemy），移除 I621 后 grid cell 与 fingerprint 均准确变化。搜索输入仍只有 tower + detached state + terminal Boss；不读 route/milestone/order/threshold，使用 raw primitive POI actions、六个公平 agenda（goal/survival/power/resource-options/novel/low-commitment），第一版只做 exact-state merge。64-expansion contract 为 **272 generated / 204 accepted / 68 exact merge / frontier141 / 54 option maps / max strategic depth15**，六队列均实际展开，并直接观测 `battle:...MT4:8,3` 自动消费 `I621@MT4:7,3`。首次 8-node 含 floorFly 控制产生 1,105/1,134 floorFly variants、frontier904，因此最小 D2 默认只含完整 primitive actions + interactPickup，floorFly 作为显式 deferred supplemental capability，不冒充完整 solver。相同 1000-expansion当前基线仍 **goalFound=false / frontier825 / 32.310s / terminal action 0**，3508 generated、1824 accepted、1684 exact merge、143 shared option maps、max depth17；旧 blind DP 为 **goalFound=false / frontier202 / 22.537s**。因此本轮是可观察架构纵切，不是性能/正确性胜利；新证据把下一卡点锁到战略评价：goal/power witness 仍落在 `lv7/exp315/hp6713` 死状态，novel witness 甚至退回 MT2，资源队列虽保留全部33件 item却只推进到 depth2。下一轮必须改善一般化 future-option/blocker reachability 评价，同时保持所有队列公平和 heuristic-only 不剪枝；不得回到 I621 专项 repair。

PR-5.18b 把 primitive variants 聚合为 choice-level `StrategicTransition`，携带 reachable-POI 前后差、active/implicit opportunity consumption、irreversible cost 与 terminal blocker progress；六队列重排为 blocker/survival/power/future-options/low-cost/semantic-novelty。reachable index 只按 exact state 缓存，底层 topology reuse 仍由 simulator 的安全分类决定；door/event/portal 的存在性直接查 after-state，不再误用仅含 item/enemy 的 option map。semantic novelty 使用 lineage-level `newlyDiscoveredPOIs`，回到已访问楼层不会重复计新。不可计算 Boss damage 时先比较 ATK 缺口：初始 `-1223`，1000-expansion最好 `-923`，共有1025个 transition 改善 blocker progress；但 `terminalActionGenerated=0`。同预算正式结果为 **false/false/true/false、deferredWorkRemaining=true、48.562s、frontier1119、3342 choices/3749 variants、2118 accepted、1224 exact merge、407 deferred post states**。deferred exact posts 尚未展开，因此 frontier exhaustion 也不得称 complete；5.18c 直接接 local DP connector 与 lazy travel/deferred-post/floorFly resolution。现有 `probe:strategic-d2-search` 是正式参数化入口，一次性 `runs/generated/probe-518b-summary.js` 不保留。

PR-5.18c 已把 deferred exact post、lazy floorFly 与 direct-unavailable 终局 choice 接成可核查的惰性工作，并加入有界局部 primitive connector。connector 逐边绑定 exact pre/post，验证时从当前状态重新枚举 recorded-action variant；真实 MT5→MT4 两步链已在主搜索中物化并 2/2 strict replay。frontier cap 单列 `frontier-trimmed`，lazy-work 的 schema/id/status 不可被 payload 覆盖；floorFly 真正按 target-floor choice 聚合，24-expansion 控制解析 95 variants/79 exact posts，并把 63 个非 canonical posts 送回 deferred queue。正式 1000 **strategic**-expansion run 仍为 **false/false/true/false、42.719s、frontier1058、3382 choices、2057 accepted、1374 exact merge、438 deferred、49 lazy materialized**；connector 另耗 **1979 expansions**（总搜索 work 2979），16 calls / 0 resolved（15 budget / 1 true frontier exhaustion），terminal action 仍 0。故本轮冻结为 **`VERIFIED / NOT_PROMOTED`**：机制正确性已验证，但 terminal-directed connector 只增加成本、未推进终局，D2 仍 OPEN。

PR-5.18d 把 connector 从“接终局 action”改成“接由 blocker 推导的中间目标”：`strategic-blocker.js` 读终局 battle 为 `attack-blocked/lethal/viable`，推导 `combat-power/survival` 中间度量，并用有界 local optimizer 最大化该度量、在最佳中间状态上重新评价 terminal blocker。real control 中 attackMargin `-1223 → -1073`（2-step chain strict replay 一致）；synthetic control bestScore 3 / chain 3。same-total-work A/B（1000 total）：strategic-only baseline 最佳 attackMargin `-903`；blocker-derived candidate 也 `-903`、`blockerConnectorImproved=8/8`、wall ~1.9x。因此机制正确但未使全局 terminal prerequisite 前移，冻结为 **`VERIFIED / NOT_PROMOTED`**，D2 仍 OPEN。Repair 1（`5db3ff3` + `7acba36`）已把 `maxTotalSearchExpansions` 收口为 strict hard cap、outcome 增加 strategic/total budget-exhausted 与 `stoppedReason`、checker 增加 shared-budget edge controls、CI 增加 `qualification-strategic-blocker-connector` job；远端 run `31821010021` 实际执行并通过。下一轮需给 connector 一个战略 frontier 本身不做的中间目标，而不是再次最大化 `progressScore`。

PR-5.18e 已把 connector 目标改成由真实 terminal-blocker evidence 编译出的离散 prerequisite：vocabulary 仅 `equipment-acquisition` 与 `resource/power-opportunity-acquisition`，completionPredicate 是唯一成功条件。Repair 1 分离 semantic dependency 与 source-exact-state attempt identity；Repair 2 改为 outstanding ≤ 1 的 feedback-aware scheduling，并记录 8 条 attempt-level telemetry。frozen 1000-work A/B 中 candidate 701 strategic + 299 dependency、calls 8、同一 semantic target 从 8 个不同 source exact state 依次重试、5 budget-exhausted + 3 frontier-exhausted、satisfied/stateCreated/globalBlockerAdvanced 全 0、best `-903`、terminal action 0。本地与远端 qualification 双 PASS，正式冻结为 **`VERIFIED / NOT_PROMOTED` + `APPROVED / CLOSED`**；下一候选方向是 prerequisite discovery / access dependency chain，但尚未 AUTHORIZED，不再给现有 connector 加预算。

## 当前仓库状态

- 已关闭轮：`PR-5.19l`（`APPROVED / CLOSED`，`MECHANISM POSITIVE / NOT PROMOTED`，文档在 `docs/260815/5-19l.md`），P1 full witness-edge-chain replay 本地 + 远端 PASS；远端 Repair 1 run `31966008291` / job `95211191570` success。
- `PR-5.19k`（`APPROVED / CLOSED`，文档在 `docs/260815/5-19k.md`），research verdict EDGE-LEVEL-NAMED-SURVIVAL-OPPORTUNITIES-IDENTIFIED。
- `PR-5.19j`（`APPROVED / CLOSED`，文档在 `docs/260815/5-19j.md`），research verdict CLASS-B-SURVIVAL-PROGRESS-BUT-NOT-VIABLE。
- `PR-5.19i`（`MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260815/5-19i.md`）已提交关闭。远端 run `31955825970` success，`qualification-strategic-continuation-anchor-expansion-scheduling` job `95186226407` success。
- `PR-5.19h`（`APPROVED / CLOSED`，文档在 `docs/260815/5-19h.md`），observation CASE1：stage anchor 840 never expanded；breakpoint identified。
- `PR-5.19g`（`DEFERRED`，文档在 `docs/260815/5-19g.md`），observation NEGATIVE：tree-descendant hypothesis 被否证；retroactive mechanism 未实现。
- `PR-5.19f`（`MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260815/5-19f.md`）已提交关闭。远端 run `31950565107` success，`qualification-strategic-battle-stage-prerequisite` job `95173284026` success。
- `PR-5.19e`（`MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260815/5-19e.md`）已提交关闭。Repair 1 lifecycle-safe priority 本地+远端双 PASS；Repair 1 远端 run `31945583749` / job `95160936893` success；首版远端 run `31942473357` / job `95153538807` success。
- `PR-5.19d`（`MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260815/5-19d.md`）已提交关闭。Repair 1 lineage-safe resume 本地+远端双 PASS；Repair 1 远端 run `31938540040` / job `95144195151` success；首版远端 run `31935712152` / job `95137189989` success。
- `PR-5.19c`（`APPROVED / CLOSED`，文档在 `docs/260815/5-19c.md`）已关闭。本地+远端 qualification 双 PASS；远端 run `31932641911` success，`qualification-strategic-battle-viability` job `95129701291` success。
- `PR-5.19b`（`MECHANISM POSITIVE / NOT PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260815/5-19b.md`）已提交关闭。Repair 1 frozen A/B：satisfied 2 / stateCreated 1 / globalBlockerAdvanced 0 / best `-903`；远端 run `31891120236` job `95027432171` success。
- `PR-5.19a`（`APPROVED / CLOSED`，文档在 `docs/260815/5-19a.md`）已提交关闭。最终 review：P0=0 / P1=0 / P2=0 blocking；Repair 2 修正 0-1 BFS deque 与 full structural path attribution；远端 full run `31884457398` success（job `95011626889`）。
- `PR-5.18e`（`VERIFIED / NOT_PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260815/5-18e.md`）已提交关闭。最终 review：P0=0 / P1=0 / P2=1（仅 evidence count 6/2→5/3 修正）。
- `PR-5.18e` 提交链：implementation `29cc965`；Repair 1 `3bbad8f`；Repair 2 `c44a4b7`；Repair 2 qualification marker `49ecf20`；远端 full run `31871841424` success（job `94981620062`）。Repair 2 1000-work A/B：candidate 701+299 / calls 8 / 同一 semantic target 从 8 个不同 source exact state 依次重试 / 5 budget-exhausted + 3 frontier-exhausted / `dependencySatisfied=0` / `dependencyStateCreated=0` / `dependencyGlobalBlockerAdvanced=0` / best `-903` / terminal action 0。
- `PR-5.18d`（`VERIFIED / NOT_PROMOTED` + `APPROVED / CLOSED`，文档在 `docs/260814/5-18d.md`）已提交关闭：实现提交 `22cd01e` + close marker `e02d78a`；Repair 1 提交 `5db3ff3` + close marker `7acba36`（新增 `qualification-strategic-blocker-connector` CI job，远端 run `31821010021` success）；docs/handoff binding `3d87cbd`。独立复核记录在 `docs/260815/5-18d-repair1-review.md`。
- `dev` 的当前已提交 solver 基线为 `29cc965`（PR-5.18e implementation）；5.18e final closure 位于本文件所在提交。此前 `docs/agent-benchmarking.md` 与 `docs/solver-architecture.md` 的未提交删除已按远端恢复，本轮没有携带该 carry。
- 共享求解器位于 `shared-solver/`，是唯一的 canonical solver 实现。
- `Only upV2.1/Only upV2.1/` 和 `whiteisland（9）/` 中的 tower-local solver/runtime 文件保持冻结；除非明确做迁移或归档，不要在那里增加 solver 逻辑。

## 已完成主线

| 主线 | 状态 | 核心结果 |
| --- | --- | --- |
| PR-5.1a/a1/a1a | 已关闭 | start-offset、runtime identity、checkpoint 后逐楼层 `leaveLoc` merge、跨楼层 continuation |
| PR-5.1b/b1 | 已关闭 | h5save 导出、loader-owned restore、fresh runtime continuation、fingerprint/payload binding |
| PR-5.1c/c1 | 已关闭 | legacy route fingerprint 三态、GUI/API projection、真实 Chromium smoke 和失败负控 |
| PR-5.2a/b/c | 已关闭 | 内存上传、verified-only resume、boundary/next gate、suffix step/play/pause、异步 API 202/409、失败清理和诊断保留 |
| PR-5.3a/a1 | 已关闭 | 手动 SolverModel、compact solver state、model fingerprint、runtime raw snapshot 与 solver projection 分层 |
| PR-5.3b | 已关闭 | ObjectiveSpec 合同、终局候选排序、proof claim、objective metadata 和 strict replay 重算 |
| PR-5.3b1 | 已关闭 | 终局 goal archive 专用 comparator、Objective-Search 兼容性预检、proof claim 记录候选截断、composed route objective metadata 重算 |
| PR-5.3b2 | 已关闭 | score descriptor direction 参与兼容性校验、route 附加后重定 bestGoalState、validateObjectiveSpec 走完整 compile 路径 |
| PR-5.3c | 已关闭 | SolveTask 合同与稳定 fingerprint、外部 compiled 信任边界、SolverJob 状态机、cooperative cancel、Progress/Result 合同、失败分类、worker 执行、file job store、CLI |
| PR-5.3c1 | 已关闭 | 有效 SolveTask identity（dpBudget 合并/rank/project fp/maxExpansions>=1）、并发槽位、cancel-token 真实中止、Job 内真实 strict replay、诚实实时 progress、route/depth objective 一致性、统一失败 result envelope |
| PR-5.3c2 | 已关闭 | 预留槽位取消后队列推进、executor 同步异常 INTERNAL_ERROR、decisionDepth 与 routeLength 拆分、compose 完整 routeLength、objective-safe 实时 bestKnown 投影、strictReplay:false 不发 verified-route、executable 预检 |
| PR-5.3c3 | 已关闭 | strict replay 对 objective 可选（legacy 无 objective 也 completed）、实时 routeLength 不填 decisionDepth、exactness 标记、executable 预检真实加载项目、terminal progress 持久化 |
| PR-5.3d | 已完成 | 本地 Solver Launcher GUI MVP：tower registry、task preflight、job API、SSE progress、手动 SolveTask 构建器、job dashboard、重启恢复、真实 Chromium UI smoke |
| PR-5.3d1 | 已关闭 | 手动 Search 成为执行权威（含 maxRuntimeMs=0）、effectiveSegments preflight、progress budget 携带实际值、failureClass 中文映射/未完成语义、legacy 不显示 objective、终态无 Cancel、Retry×2、UI 重排 |
| PR-5.3d2 | 已完成 | 任务级预算覆盖所有 repair/backtrack DP、progress budget 明确 per-attempt scope + current/total、validate 不再静默吞 effectiveSegments 构建错误、修复 roadmap 路径文本损坏、maxRuntimeMs=0 隐藏运行时间×2 |
| PR-5.3d3 | 已完成 | effectiveSegments 明确 per-attempt scope（每次 attempt 上限 + 起始候选数）、Retry 按 failureClass 精确生成（runtime/expansion/action 各自 ×2，非预算失败无预算按钮）、恢复 budget.source 与兼容别名、runtime ratio clamp≤1、空 current 显示 —、public-api 格式清理 |
| PR-5.3d4 | 已完成 | 共享 start-candidate resolver（执行与 preflight 一致，execution 真正消费 segment.dp.startCandidateLimit）、attemptCaps 按阶段报告（initial/configuredRepair/backtrackRetry）、ACTION_TRIMMED Chromium 控制 + actions2 缩放验证、打开配置载入原 job task、failureClass 精确建议文案 |
| PR-5.3d5 | 已完成 | frontier-dependent 的 attemptCaps.initial 公开为 null（不低报为 candidateLimit）、configured-repair 扩宽 frontier 控制、打开配置完整恢复 Tower/Region/Objective/Verification/Search |
| PR-5.3d6 | 已完成 | 打开配置恢复用户提交的 task.model（不再回落到 region.spec.model）、Tower 切换时 await loadRegions 同步 state.tower/fingerprint/region options、null cap 显示“随 frontier”、修正 handoff 内旧矛盾表述、Chromium 断言 model/tower/projectRoot/region 与 job 一致 |
| PR-5.3 系列 | 正式关闭 | 独立验收 APPROVED：预算权威、Progress 合同、failure/retry 语义、attempt truthfulness、Launcher 精确任务恢复；剩余仅非阻塞跨 Tower fixture 与 CI 覆盖增强 |
| PR-5.4e Repair | 已收口 | MT1 Workload Matrix 收口 3 个 P1 + 2 个 P2：per-workload goal predicate（`buildSegmentGoalPredicate`）、6 个 workload 真实 pinned baseline（A==pin && B==pin，未 pin fail-closed）、reachability cache 从真实 solve simulator 读取（`executeSolveJob` 返回内部 simulator）、partition audit 改 cheap-equality-first（merge=0 时不跑 behavior CEGAR）。verdict `MT1_DEFAULT_PROMOTION_ELIGIBLE`。 |
| PR-5.4f | 已收口 | MT1 Default Promotion + Explicit Rollback：`resolveDpKeyProfile` 三态语义（显式 `production-region`=rollback 永不自动改回 / 未指定=approved-MT1 scope-aware fallback 到候选 `approved-mt1-default`、其他 scope 保持 production `scope-unapproved-fallback` / 显式 experimental 保留 fail-closed）；返回 `requestedProfile/effectiveProfile/selectionReason` 诊断（`executeSolveJob` 透出 `execution.profileSelection`）。6-workload matrix 改 A=default/B=explicit-candidate/R=rollback parity；新增 `check-mt1-default-promotion-contract`（Gates A–H）。verdict `MT1_DEFAULT_PROMOTION_ACCEPTED`。 |
| PR-5.4g | 已收口 | Post-Promotion Full-Tower Regression：新增 `check-mt1-post-promotion-regression-contract`（S1–S4）——单 Region 默认→candidate+pinned、显式 rollback→production 结构、unapproved scope implicit fallback（无 candidate builder）、campaign/multi-Region v2 per-region isolation（approved MT1→candidate、unapproved→production、无泄漏）。`executeSolveJobV2` 每 region 在 execution boundary 解析 profile（fresh executeConfig，候选 builder 不跨 region 泄漏），regionSummary 携带 `profileSelection`。修复 promotion 一致性 bug：`computeRegionSpecFingerprint` 剥离 `projectRoot`（v1 保留 v2 删除 → 同 region 双路径指纹不同），approved baseline `expectedRegionSpecFingerprint` 重基线 `510312b10d5ccec1→36b7477cad2d6a96`（其余 pin 不变）。verdict `MT1_DEFAULT_PROMOTION_REGIME_STABLE`。 |
| PR-5.5a | 已收口（研究轮） | Multi-Region Candidate-Key Shadow & Boundary Corpus。新增 `lib/multi-region-key-shadow.js`（纯观察 shadow）+ `check-multi-region-key-shadow-contract`。三层 corpus（pre-boundary / boundary-transfer / post-boundary）+ state/boundary 双 partition + 有序 CEGAR（boundary-transfer 第一）。fixture：R0=approved MT1（多终端候选）+ R1=unapproved（production fallback）。负控制 fail-visible。结果 `NO_COLLISION_OBSERVED`（不是 safe）。**零 production 改动**。 |
| PR-5.5a Repair | 已收口 | 按 review 修正 3 P1 + 2 P2：P1-1 per-region IR/context（pre-boundary→R0 IR，boundary/post/CEGAR→R1 IR，不再共享 R0 IR）；P1-2 R1 真实 `start.type="floor"` entry（hero 位移 / exact identity 变化 / `__leaveLoc__` fail-fast 断言）；P1-3 boundary gate 改比 post-boundary semantic identity（production DP key），HP/dominance 级 exact 差异只做诊断并交给 classifyPair（dominance-safe control 证明不误判）；P2 删 checkpointFingerprint（ancestry 恒 `{}`）；P2 verdict pin `NO_COLLISION_OBSERVED`。结果仍 `NO_COLLISION_OBSERVED`（3 层 equal partition）。 |
| PR-5.5b | 已收口（研究轮） | Multi-Region Boundary Corpus Expansion：`check-multi-region-boundary-matrix-contract`（11 workload：5 R0 goal × entryA、5 R1 boundary 变体 × exp6、3-Region chain）。R0 goal 显式构造（P2-3 关闭）；R1 goal 静态校准保证真实搜索；coverage gate（pre/boundary/post/actionKinds ≥2）；4 个检测器 control（all-colliding / HP-only dominance-safe / boundary-semantic-drift / post-boundary-action-drift）；第一次真实 merge → witness artifact（字段级 projection diff）到 runs/generated/ + needs-review。结果 `NO_COLLISION_OBSERVED`（24 pre / 24 boundary / 106 post，0 merge）。**零 production 改动**。 |
| PR-5.5b Repair | 已收口 | 按 review 修正 3 P1 + 2 P2：P1-1 collision scope 统一成真实生产竞争范围（state partition 按 region-execution-context + candidate key、boundary partition/CEGAR 按 boundaryIndex + candidate key、witness 同 scope；新增 cross-boundary key reuse neutral control + same-boundary 正控制）；P1-2 coverage/signature/CEGAR 全改 production-legal action semantics（`buildSegmentActionProvider` + `buildStateBehavior` semantics-preserving `actionProvider` 选项；parity 断言 raw-only=policy 禁止 kind、legal-only=provider 附加 kind；raw-vs-legal control 实测 dominance-safe）；P1-3 witness 增 production identity decomposition diff（regionKey/reachableEndpointsKey/mutationSummary/hero/equipment/inventory/flags/visitedFloors/eventHazardLabel/loc/floorId）；P2 注释 10→11、显式 boundaryTransferCount gate。结果仍 `NO_COLLISION_OBSERVED`（0 merge / 0 unsafe）。 |
| PR-5.5b Repair 2 | 已收口 | 补 2 个 P1 闭环：P1-2 legal provider 改用真实 production milestone segment（`buildRegionMilestoneSpec` → actual segment，segment.goal/presentTiles/resource-timing 全参与）+ segment parity 诊断（actual vs actionPolicy-only，11 workload 0 divergence）；P1-3 witness 路径真实执行验证（构造 collision → `captureMergeWitness` → 解析 artifact → 断言 productionIdentityDiff 含非 exactDpKey 解释字段，实测 regionKey/reachableEndpointsKey/mutationSummary/loc → cleanup）。P2 workload-specific carry 证据（inventoryUse inventory 值传入、flagCarry flag 值保持）。结果仍 `NO_COLLISION_OBSERVED`。 |
| PR-5.5c | 已收口（研究轮） | Multi-Region Corpus Expansion（找第一次真实 collision）：matrix 扩到 18 workload——cross-products（r0-exp2/exp9/tile4_1 × entryB/entryC/inventoryUse/flagCarry）、deep R1 搜索（candidateLimit 4 / goalSkylineLimit 16，post 26 样本）、4-Region chain（R0→R1→R2→R3，3 boundary）。changeFloor/MT2 topology 变体实测不可达/平凡后弃用。结果 `NO_COLLISION_OBSERVED`（42 pre / 42 boundary / 189 post，0 merge）。**零 production 改动**。 |
| PR-5.5c Continuation | 已收口（研究轮） | High-Semantic-Diversity Collision Hunt：**P2-3 修正**（deep fixture 实测 10→10 无增量、26 样本实为 r0-exp2-entryB → 删除 deep，换 mutation-divergence workload r0-tile2_1-entryA）；新增 **semantic-diversity report**（per workload + 全局：regionKey/reachableEndpoints/mutationSummary/inventory/flags/visitedFloors/legalActionSet/loc distinct 计数）+ **semantic gate**（每 workload ≥1 维度 ≥2；全局 mutation/reachability/flags/legalActions ≥2）。关键发现：inventory=1 / visitedFloors=1 恒定（auto-pickup 捡光 MT1 物品 + 单 floor corpus，如实报告）。结果 `NO_COLLISION_OBSERVED`（18 workload，42/42/181，0 merge）。 |
| PR-5.5c Continuation Repair | 已收口 | 修复 globalSemanticDiversity 聚合 bug（P1）：max(per-workload) → 真实 union（合并全部 records 的 value sets）；新增 synthetic aggregation regression control（W1/W2 inventory 各 1 distinct → 全局 2；{M1,M2}∪{M2,M3} → 3 而非 max(2,2)）。修复后真实全局：regionKey 30 / reachableEndpoints 30 / mutationSummary 29 / flags 36 / legalActionSet 49 / loc 17；**inventory=1、visitedFloors=1 在真实 union 下仍成立 → narrowness finding 被证据证明**。gate 结论不变、实现修干净；header/assertion 文案同步。结果仍 `NO_COLLISION_OBSERVED`。 |
| PR-5.5c Cross-Topology / Inventory Collision Hunt | 已收口（研究轮） | 攻击真实 union 证明的两个空洞：OnlyUp MT1 无 door/key/drop/changeFloor（inventory 恒定）；sample floor 可破坏墙迷宫搜索爆炸；改用 **whiteisland（9）真实第二塔**——`wi-a1-door-key-entryA`（R0 OnlyUp MT1 exp2 → R1 whiteisland A1，开门 goal）：真实 key acquire→consume（inventory distinct 11）、door 产生 284 floor mutations、跨塔 boundary 状态跨 2 floor。contract 支持 per-region project（regionContext 用自己的 tower）；legal kinds 改从 action `.kind` 提取；raw-vs-legal summary parity 移除（provider 即 production，rawVsLegalControl 覆盖）。结果 `NO_COLLISION_OBSERVED`（19 workload，44/44/**1342** post，0 merge）——inventory **11** / visitedFloors **2** / regionKey 245 / legalActionSet 478，**两个空洞被真实证据补齐**。 |
| PR-5.5c Cross-Topology Repair | 已收口 | 修正 3 个 P1：P1-1 whiteisland untracked（gitignored）→ marker CI ENOENT，跨塔 workload 删除、corpus 回 18 workload production-faithful；P1-2 新增 same-project fail-fast gate（chain 内全部 region 同一 project，与 executeSolveJobV2 一致），禁止 harness-only 跨 project 冒充 production corpus；P1-3 新增 fail-closed hole-closure gates（inventory：distinct≥2 + 真实 acquire/consume transition 证据；visitedFloors：maxVisitedFloorCount≥2，不再用 distinct sets）。如实结论：**两空洞仍 OPEN**（MT1 无门/key/drop；sample0 墙挡、sample1 缺 specialKey、sample2 爆炸、Start 钥匙被封）；whiteisland A1 探索（inventory 11）降级为 exploratory 依据。结果 `NO_COLLISION_OBSERVED`（18 workload，42/42/181，0 merge）。 |
| PR-5.5c Cross-Topology Repair 2 | 已收口 | 修复 P1-3 假阳性：inventory hole detector 从任意 state-pair Cartesian product 改为**真实 parent→action→child edge**（dp-search recorder payload 补 observation-only transition provenance：parentStateKey/parentInventory/parentMutations/actionKind/actionSummary，不进 key/pruning/solver behavior）；acquire=child key 增 + pickup/interactPickup；consume=child key 减 + openDoor/useTool 且同 transition 上 mutations 变化；新增正负 synthetic controls（无 parent edge 的 state 多样性 → 不误判；S0--pickup-->S1--openDoor-->S2 → filled）。visitedFloors closure 补 changeFloorExecuted/arrivalExecuted/postArrivalSearchObserved，filled 要求真实 changeFloor。结果 `NO_COLLISION_OBSERVED`（18 workload，42/42/181，0 merge），两空洞如实 OPEN。 |
| PR-5.5c Cross-Topology Repair 3 | 已收口 | 修复 P1-4 mutation representation mismatch：parent 存 canonical `listFloorMutationSummary` array、child 之前用 raw single-floor object（无 mutation 时 `[]` vs `{"removed":{}...}` 恒不等 → 假阳性）。新增 `mutationSummaryFingerprintOf` 共享 helper，child 与 recorder 同一 canonicalization；新增 negative-2 control（真实 openDoor + key 1→0 但 canonical mutation 无变化 → 不判 consume）；正控改用 canonical 格式。结果 `NO_COLLISION_OBSERVED`（18 workload，42/42/181，0 merge），两空洞如实 OPEN。 |
| PR-5.5d | 已收口（研究轮） | Production-Faithful Classic Tower Fixture Qualification：**只用 tracked OnlyUp**（不引入 whiteisland、不膨胀仓库）。发现 OnlyUp Start floor 是真实 sealed key room（entry (6,4) 进密室、goal 开 green door (5,5)）；新增 `start-door-key-entryA` workload：354 post、大量真实 openDoor edge（probe 观测 344 条，非 matrix 冻结字段）、inventory distinct 10。**inventory hole CLOSED**（acquire=I600 via interactPickup、consume=greenKey via openDoor，canonical 同 edge mutation 证据，filled=true）。**visitedFloors hole PARTIAL**（maxVisitedFloorCount=2 via 真实跨 floor boundary + arrival；但 changeFloorExecuted=false——Start stairs 被 steel/special door 挡、MT1 stairs 不可达、MT2 怪物过强，如实报告）。结果 `NO_COLLISION_OBSERVED`（19 workload，44/44/535，0 merge）。 |
| PR-5.5e | 已收口（研究/qualification 轮） | Maintained tracked fixture `mt1-mt2-hp3834.route.json` live replay 到 decision 12 的真实 `MT1 -> MT2` changeFloor edge；parent/child exact `0346b4ba143966bc -> ca2cb576183965e6`，visited `[MT1] -> [MT1,MT2]`，四类 fail-closed control 通过，visitedFloors hole CLOSED。matrix 仍 19 workload / 44/44/535 / 0 merge / 0 unsafe；该边 production/candidate identity 均改变，candidate collision=false，故 5.5f 未获授权。 |
| PR-5.6a–e | 已收口 | Route lineage integrity → goal dependency graph → admissible bounds → bounded adaptive checkpoint repair → incremental requirement cache + independent-process parallel route portfolio。默认 skyline / production key / dominance 不变；长链 third-gate 仍 `INCOMPLETE_WITHIN_BUDGET`。 |
| PR-5.7a | 已收口（shadow 研究轮） | First-Sweep Candidate Quality Shadow：capture-all raw goal states + materialized routes；原选择器 capacity 1/2/4/8；固定 expansion downstream probes；真实 corpus 140 raw / 18 milestone / 3 unique DP keys，140 fail / 0 budget / 0 success → Stop B `EVICTED_CANDIDATES_ALSO_FAIL`。零 production selection/key/dominance 改动。 |
| PR-5.7b | 已收口 | Third-Gate Failure Attribution + Segment Refinement：定位 MT4 guard chain 与 I621 delayed-heal 时序，细化 checkpoint；158.9s / 4050 FAIL → 19.6s / 607 third-gate REACHED，因果 replay +13,846 HP。 |
| PR-5.7c | 已收口（closure） | Long-Chain Requalification：不再改建模；18 段完整闭合至 MT5 blueKing，645 expansions，third-gate 后 38，35 decisions strict replay，最终 I894 + hp4464。PR-5.7 CLOSED。 |
| PR-5.8a | 已收口（failure baseline） | Post-MT5 固定 expansion / 无 wall timeout 基线：362 expansions 到 `mt7-bottom-double-fairy`，首失败 `mt7-special80-ready`；全链 1007 expansions；0 trimming / 0 exhaustion / strict replay true。发现 MT6 +500 DEF 与第二中路警卫反序的 strong hypothesis；未在 5.8a 提前宣称 causal。 |
| PR-5.8b | 已收口（causal repair） | 单变量前移 `MT6:2,1` 防御 checkpoint；相同预算下 special80 `FAIL → REACHED`，508 post-MT5 expansions，24 decisions strict replay，verdict `CAUSAL_ROOT_CAUSE`。当时只观测到左剑 500-exhaustion；5.8c 已证明它不是 not-found blocker。 |
| PR-5.8c | 已收口（budget attribution baseline） | exact special80 单状态下左剑 expansion 1 即满足完整目标；500 expansions 时 `found=true`、158 goal nodes、frontier 201，只是 skyline 未穷尽。8 decisions strict replay 通过，冻结 verdict `FEASIBLE_GOAL_FOUND_WITH_INCOMPLETE_SKYLINE`；不得用 exhaustion 冒充 not-found。long-horizon MT8 搜索成功但 strict replay decision 13 mismatch，留给 5.8d。 |
| PR-5.8d | 已收口（strict replay attribution） | 13-decision suffix lineage 连续、前 12 步 exact replay；decision 13 的同一 choice fingerprint 下有 3 个 path/travel/post variant，resolver 过早 fingerprint-dedup 丢掉唯一 exact-post path-17 variant。冻结 verdict `RECORDED_ACTION_FINGERPRINT_ALIAS_DEDUP`，本轮不修 replay 语义。 |
| PR-5.8e | 已收口（travel-variant replay repair） | choice identity 不变，variant-aware enumeration 保留同 choice 的不同 path/travelState；unique/no/multiple exact-post 三类 synthetic contract 全通过。真实 legacy A 复现 decision 13，repair B 选 path-17；suffix 13/13、full lineage 37/37 strict replay，final exact `5734ff36fe4d25c4`，verdict `MT8_STRICT_REPLAY_CLOSED`。 |
| PR-5.8f | 已收口（outcome taxonomy / doctor hygiene） | DP、segment、doctor、job result 统一输出 `goalFound/frontierExhausted/budgetExhausted/searchComplete`；found+incomplete → `feasible-incomplete`，不再作为 failure。synthetic truth table + 真实 left-sword 500/201 witness 锁定四元组 `true/false/true/false`；零搜索行为改动。 |
| PR-5.8g | 已收口（recorded replay fast path） | 保持 choice/travel/exact-post 语义，把硬结构拒绝前移并复用同次 resolution 的 winner post-state。真实 full lineage：32,846ms/8,633 applies → 2,458ms/40 applies；8,593 hard rejects 不再 apply，37/37 winner post-state reuse，exact final parity。 |
| PR-5.9a | 已收口（reachability reuse attribution） | exact cache 行为不变且诊断默认关闭。MT1 exp9：26 hits/123 misses；123 safe-fast builds 可按 73 个 topology skeleton 分组，22 个重复组、50 个理论可复用 miss（40.65%）、closure mismatch=0；winner/route/objective/116 expansions/strict replay 全部 pin 住。授权 5.9b skeleton cache，不提前改生产语义。 |
| PR-5.9b | 已收口（safe-fast skeleton cache） | 每个 exact state 仍独立 safety classification；只缓存 state-free topology/path skeleton 并 rebase 当前 state。真实 A/B/B/A：123→73 builds、50 hits、topology work -40.65%；116 states/434 action+successor exact corpus、winner/route/objective/scale/strict replay parity。search wall 中位数约 -3.2%，reachability phase 约 -13.0%。 |
| PR-5.9c | 已收口（rebase cost attribution） | 默认关闭、纯观察。6526 eager nodes 全部被现 API 读取 333,011 次，但 node.key 0 reads；只有 566 nodes（8.67%）进入 760 个 action.travelState，5960（91.33%）未 escape。consumer matrix 完整且无 unscoped；结果/strict replay parity。冻结 `EAGER_TRAVEL_STATE_MATERIALIZATION_OVERBUILD`。 |
| PR-5.9d | 已收口（topology-first materialization） | safe-fast 先用 base state + topology coordinate/path 筛 endpoint，再按需 clone stance；legacy-exact 不变。6526 topology nodes 仅物化 722，clones 6649→845，dominance keys 6526→0；116-state/434-action corpus 与 strict replay exact parity。 |
| PR-5.9e | 已收口（remaining materialization attribution） | 722 materialized 中未 escape 的 156/156 全为 battle pre-action rejection：119 lethal-only、31 no-damage-info-only、6 overlap；所有 viable stance 均 emitted，dedup/unsupported 贡献 0。纯观察 on/off 结果与成本 parity。 |
| PR-5.9f | 已收口（battle evaluation projection repair） | 冻结 base-state + stance projection 先做只读战斗可行性判断，只为 viable action clone 完整 travelState。materialized 722→566、clones 845→689、residual 156→0；exact corpus、winner/route/objective/scale/strict replay parity。 |
| PR-5.9g | 已收口（reachability optimization requalification） | 6 个 MT1 + 2 条 tracked real route 全部 exact/scale/strict-replay parity；累计 skeleton builds -48.9%、topology work -54.9%、clones -84.7%、dominance-key builds -99.3%。冻结 verdict `PR_5_9_REACHABILITY_OPTIMIZATION_CLOSED`。 |
| PR-5.10a | 已收口（full solve hotspot reprofiling） | exclusive/self-time 去除嵌套重复计时；短中链 top=applyAction，MT8 top=reachability（本机方向性 42.7%）并伴随 1722 legacy-exact + 302,510 clones。冻结 verdict `HOTSPOT_SPLIT_BY_WORKLOAD_SCALE`。 |
| PR-5.10b | 已收口（discovery capability audit） | 初始→MT5 输入 28 milestones、109 resource fields、46 removed/105 present tiles、45 floor/38 transition entries、68 DP fields；A0 即缺 unordered-event planning，A5 terminal-only D3 spec 零中间提示。冻结 verdict `ASSISTED_EXECUTION_NOT_AUTONOMOUS_DISCOVERY`。 |
| PR-5.10c | 已收口（terminal-only D3 baseline） | canonical initial→MT5 blueKing，零 route/milestone/order/threshold/floor hints；1000 expansions/33.046s 到 MT3，frontier 496、1321 active keys、0 trim，四元组 false/false/true/false。冻结 `BLIND_GOAL_NOT_FOUND_WITHIN_BUDGET`，不是无解。 |
| PR-5.11 | 已收口（search trace explainability） | decision-depth/floor/action/rejection trace + fixed before/after card；不同 controls 拒绝因果比较。首个 review buckets：1164 dominance reject、1059 changeFloor candidates、496 live frontier、0 trim，均不自动宣称浪费。 |
| PR-5.12 | 已收口（automatic macro graph） | 只用塔 + canonical initial + terminal Boss 自动生成 MT1→MT5 floor corridor 与 409-node/722-edge macro candidate graph；识别 Boss、楼梯、门、物品/装备、脚本 mutation，并正确表达 blueKing flag→autoEvent→door，零 milestone/route 输入。图是 planner 候选层，不是 proof。 |
| PR-5.13 | 已收口（hierarchical planner v1） | 自动 floor stages 已真实执行：62 到 MT2、206 到 MT3、余下 732 未到 MT4；与 blind before 同为 deepest MT3，未伪称改善。确认 top-1 入口是当前限制；预收集 Pareto/均分预算/current-stage 0/4/8 均被真实矩阵否证，下一步转 failure-triggered 回溯。 |
| PR-5.13b | 已收口（failure-triggered checkpoint/backtracking） | 失败后复用本轮 landmark checkpoints，再回开上一入口；真实捕获 I893/MT3 checkpoint，最深 decision 39→48，但仍未进入 MT4。机制与人可读成本报告成立，D3 correctness outcome 未改善。 |
| PR-5.14 | 已收口（D0-D3 blind qualification） | D0 55/55 replay、D1 12 expansions + 6/6 replay 通过；D2 MT5 entry→Boss 1000/frontier202 未找到，D3 initial→Boss 1000/deepest MT3 未找到。冻结为局部发现已验证、自主发现仍 open。 |
| PR-5.15a | 已收口（D2 first-failure attribution） | A=1000 expansions，MT5 641/MT4 359、frontier202，I894/Boss candidate 均 0；自动图包络只有 MT5 且无 resource→Boss edge。B=MT5-only 5 expansions 完整穷尽仍未找到，锁定 incomplete automatic planning envelope；未改生产搜索语义。 |
| PR-5.15b | 已收口（automatic feasibility subgoals） | state-visible revisit envelope 自动扩为 MT1..MT5；当前地图资源 counterfactual 自动选择 I894，使 Boss attack/defense deficit 1030/3932→758/1952。直接执行该 goal 1000/frontier220 仍未找到，执行闭包留给 5.15c。 |
| PR-5.16a | 已收口（Search Observatory baseline） | D2 的目标假设、candidate funnel、预算账本、rank plateau、人工审阅项和下一 A/B 已统一成可读报告；1000 expansions 中 I894/Boss candidate 均 0、MT4 revisit 359、最大 rank plateau 343，所有 waste claim 保持未证明。 |
| PR-5.16b | 已收口（Automatic AND/OR Dependency Planner） | 补齐连续 POI contact 后，自动为 I894 生成 2 个 OR 方案及各自 5/7 个 AND blockers；公共 goldHornSlime 与 viable/lethal/unbeatable simulator evidence 可见，断图和无子目标均 fail-visible。 |
| PR-5.16c | 已收口（Local Multi-Role Dependency Executor） | 自动执行首个 viable blocker，first goal expansion=1；64 expansions 收集 45 raw goals→8 个不同 state/route checkpoints、7 种角色，8/8 strict replay 且重复确定。 |
| PR-5.16d | 已收口（Failure Feedback + On-demand Backtracking） | top-1 checkpoint 的两条 OR 首项均 blocked；8-checkpoint portfolio 自动改选 highest-mdef、切换 alternative-2，下一前置 expansion 1 找到且 8/8 增量 strict replay。 |
| PR-5.17a1 | 已收口（Startable Blocker Repair） | 348 个资源反事实中拒绝收益更高但依赖循环的 I1009；第59个可启动候选 I1014 能修复 evilHero 可行性，其唯一前置 expansion1 找到并 strict replay。 |
| PR-5.17a2 | 已收口（Hierarchical Discovery Loop） | route-free 循环连续完成 5 个 prerequisite、三次自动资源修复；156 expansions 全段 strict replay，首个未支持类别锁定为跨层 EXP/复合资源链，D2 仍 OPEN。 |
| PR-5.17a3 | 已收口（Least-Commitment + Reversible Trials） | first-goal checkpoint 被正式保留；高收益但真实不可达的 I621 以 9 expansions 完整否决，原 portfolio 自动恢复并继续 I1014。220 expansions 不是加速，而是机器 save/load 与失败实验去重证据。 |
| PR-5.17a4 | 已收口（Actual Net Margin + Graph Reuse） | I1014 predicted +3583 与 actual -62227 并列，正确标为 improved-but-still-blocked；同输入 graph builds 17→8、9 次复用且选择不变。 |
| PR-5.17a5 | 已收口（Actual Closure + Streaming Trace） | I642 predicted +22687 / actual -27998 被当场回滚；逐轮 NDJSON 与 phase timing 显示 checkpoint record/replay 比 DP search 更贵。 |
| PR-5.17a6 | 已收口（Shared Simulator Replay Pipeline） | simulator instances 17→1；同样 64 expansions/8 checkpoints/8 strict replay/fingerprints，wall 8.866s→5.191s（方向性）。 |
| PR-5.17a7 | 已收口（Semantic Repair Experiment Identity） | 排除 `__leaveLoc__` 导航缓存但保留资源/flags/access/blocker 语义；30-round 重复 I642 trials 2→0、repair trials 22→20，expansions 775→821（省出预算继续探索，非纯加速），D2 仍 OPEN。 |
| PR-5.17a8 | 已收口（Exact-State Repair Compilation Cache） | 6-round parity 保持 220/694/488/206 与 repair/margin 全同；graph builds 25→17、access compiles 87→68、compilation wall 23.380s→18.718s（方向性）。 |
| PR-5.17a9 | 已收口（Combat-Reward Progress Repair） | old round14 backtrack→new 9-exp combat reward；实际 +26 EXP 但 margin 仍 -27998，下一轮 216 candidates/72 access 全不可启动，D2 仍 OPEN。 |
| PR-5.17a10 | 已收口（Failure-Driven Progress Priority） | 进展死路后切到 level-progress-first；50-round combat repairs 增至27次，但最终仅 lv7/exp289、0 unblock，锁定下一缺口为历史分支没有单调保留累计 EXP。 |
| PR-5.17a11 | 已收口（Cumulative Progress + Level Threshold） | history/candidate 改按绝对 level/EXP；自动 lv8 目标在三个 exp315 exact states 均9 expansions完整穷尽失败，锁定升级链必须在更早状态规划。 |
| PR-5.17a12 | 已收口（Replay-Verified Partial Progress） | backward probes 按可执行 EXP 潜力且上限4；best-progress 仅在绝对 EXP 上升并 strict replay 后提交，真实 exp205→221→315，但 lv8 仍不可达。 |
| PR-5.18a | 已收口（D2 战略纵切） | 停止 5.17a14 repair 方向；immutable base grid + sparse mutations 物化共享 option map、raw primitive actions、六个公平 agenda、exact-state merge；1000 expansions false/false/true/false、terminal action 0，卡点锁定到战略评价。 |
| PR-5.18b | 已收口（StrategicTransition contract） | choice-level transition 聚合 + blocker/survival/power/future-options/low-cost/semantic-novelty 六队列重排；best ATK gap `-1223 → -923`、1025 transitions 改善 blocker progress，但 terminalActionGenerated=0。 |
| PR-5.18c | 已收口（lazy resolution + local connector） | deferred exact post、lazy floorFly 与 direct-unavailable 接成可核查惰性工作 + 有界局部 primitive connector；1000 strategic + 1979 connector = 2979 total work、16 calls / 0 resolved、terminal action 0。`VERIFIED / NOT_PROMOTED`。 |
| PR-5.18d | 已收口（blocker-derived connector + Repair 1） | connector 改为接由 blocker 推导的中间目标；real control attackMargin `-1223 → -1073`、blockerConnectorImproved 8/8；same-total-work A/B 中 best attackMargin 仍 `-903`。Repair 1 关闭 shared-budget hard cap、outcome taxonomy、checker、CI 与 diagnostics 问题；远端 run `31821010021` success。`VERIFIED / NOT_PROMOTED`，D2 仍 OPEN。 |
| PR-5.18e | 已关闭（dependency-derived intermediate target；`VERIFIED / NOT_PROMOTED` + `APPROVED / CLOSED`） | Repair 1 分离 semantic/attempt identity 并实现 pickup→equip；Repair 2 改为 outstanding≤1 feedback-aware 调度并增加 attempt telemetry。本地+远端 run `31871841424` 双 PASS；Repair 2 A/B candidate 701+299 / calls 8 / 同一 semantic target 从 8 个 source exact states 依次重试 / 5 budget-exhausted + 3 frontier-exhausted / satisfied 0 / stateCreated 0 / globalBlockerAdvanced 0 / best `-903` / terminal action 0。D2 仍 OPEN。 |

## 当前核心合同

### SolverModel

`shared-solver/lib/solver-model.js` 是求解器维护字段的声明入口。模型优先级为手动模型、preset、自动建议、legacy fallback；当前公开 v1 只接受已实现的字段语义。

Only Up 的显式模型关闭 `hpmax`、`mana`、`manamax`、`money`、`followers`，保留 HP dominance 以及战斗、成长、装备相关 key 字段。disabled 字段不能被补零后伪装成已维护字段，也不能作为 objective 引用。

### ObjectiveSpec

实现位于 `shared-solver/lib/objective-spec.js`，schema 为 `motapathfinder.objective-spec.v1`。当前支持：

- `clear`：只要求满足 RegionSpec goal，可使用终局 tie-breaker。
- `max-final-hp`：先满足 goal，再最大化 `hero.hp`。
- `maximize`：在已满足 goal 的终局上最大化受支持的数值字段。
- `maximize-score`：终局专用加权数值项，不能参与中间状态淘汰。
- `lexicographic`：按声明顺序比较多个目标/比较器。

Objective 字段在 RegionSpec normalize/preflight 阶段绑定 SolverModel 验证。disabled、snapshot-only、未知字段、非数字字段、非法 weight 以及搜索不保留的字段必须在搜索前以结构化错误拒绝：

- `hero.<field>` 只有 `key` mode 字段可优化；`value` mode 字段（如 `hpmax`/`manamax`）拒绝优化（`OBJECTIVE_FIELD_NOT_SEARCH_PRESERVED`）。
- `hero.hp` 只能 max（或 score 中非负 weight）；min 方向与 same-key HP dominance 冲突（`OBJECTIVE_CONFLICTS_WITH_DOMINANCE`）。
- `decisionDepth`/`route.length` 只能 min；max 方向被拒绝（`OBJECTIVE_INVALID_DIRECTION`）。
- `inventory.*` 进入 DP identity，两个方向都安全。
- 负 `hero.hp` weight、正 depth/route weight 被拒绝（`OBJECTIVE_NON_MONOTONE_WEIGHT`）。
- score descriptor 的 `direction` 会翻转有效权重符号：校验使用 `effectiveWeight = direction === "min" ? -weight : weight`。`min score + 正 HP weight`、`min score + 负 route/depth weight` 被拒绝；`min score + 负 HP weight`、`min score + 正 route/depth weight` 是安全等价形式并被接受。
- descriptor `direction` 严格限定为 `max`/`min`。

ObjectiveSpec 只决定合法终局候选“哪一个更好”，不决定搜索过程中“删掉谁”。以下语义保持不变：

- DP key；
- 相同 key 内的 HP dominance 和代表状态选择；
- action enumeration / legality；
- state projection；
- milestone planning、adaptive repair、容量和默认策略。

`searchDP` 提供纯终局钩子 `goalStateComparator`，只用于 goal archive 排序、`bestGoalNode`、goal archive audit 和 final goal result；不进入 `bestByKey`、`isBetterForSameDpKey()`、agenda、action ranking 或中间剪枝。`searchSegmentDP` 只在 objective 命中的最后一个 segment 传入 `(left, right) => -objective.compareCandidates(left, right)`，非终局 segment 保持旧 comparator。

由于 production simulator 在搜索时用 `storeRoute:false`（`state.route` 保持为空、只有 `decisionDepth` 增长），`bestGoalNode` 入队时不能按 route-length 判定。搜索结束后会先为 active goal nodes 重建 route，再用 objective comparator 排序，并从归档首位重新确定 `bestGoalNode`，保证 `bestGoalState`、`goalState`、`result.route` 一致指向目标最优状态。

`validateObjectiveSpec()` 走完整 compile 路径，因此直接调用公开 validator 的 SolveTask / Launcher / 第三方调用者也能捕获 Objective-Search 兼容性错误（field mode、dominance 冲突、score direction、direction 合法性），与 compile 路径保持一致。

### SolveTask / SolverJob

- `shared-solver/lib/solve-task.js`：`motapathfinder.solve-task.v1`，把 `RegionSpec + SolverModel + ObjectiveSpec + search` 固化为稳定、可序列化、可校验的 task，生成 `taskFingerprint`（绑定 tower identity、normalized RegionSpec、model/objective fingerprint、search budget、action policy、schema version；排除 jobId、createdAt、绝对路径、UI label、输出路径）。外部输入中的 `compiled:true` 一律剥离重新编译。
- `shared-solver/lib/solver-job.js` + `solver-job-manager.js`：`queued/running/completed/failed/cancelled` 状态机（非法转换抛 `JOB_INVALID_STATE_TRANSITION`），`JOB_PAUSE_UNSUPPORTED` 结构化拒绝 pause，`submit/getJob/cancel/subscribe` API，`maxConcurrentJobs` 排队。
- `shared-solver/lib/solver-progress.js`：`motapathfinder.solver-progress.v1`，sequence 单调、无虚假 percent（只提供 budget 消耗比例），DP observer 事件经 accumulator 节流聚合，bestKnown 区分 `progress-state` / `goal-candidate` / `verified-route`。
- `shared-solver/lib/solver-job-result.js`：`motapathfinder.solver-job-result.v1`，结果绑定 task/model/objective/tower/route fingerprint；失败分类中预算耗尽、action trimming、policy filtered、milestone 过严均为可重试的不完整搜索诊断，不等于无解。
- `shared-solver/lib/solver-worker-runner.js` + `solver-job-worker.js`：child process 执行，不阻塞主线程；cooperative `shouldStop` 取消，取消后 `stoppedReason = "cancel-requested"`、`failureClass = "cancelled"`。
- `shared-solver/lib/file-job-store.js`：`runs/jobs/<jobId>/` 原子落盘（tmp + fsync + rename）。
- `shared-solver/run-solve-task.js`：CLI，`--progress-format=ndjson`。
- `searchDP` / `runMilestoneGraph` 新增 `shouldStop` 钩子，不改变搜索语义。

### PR-5.3c1 语义硬化

- SolveTask：生成唯一 `effectiveSearch`（预算优先级 `task.search` > `regionSpec.search` 顶层 > `regionSpec.search.dpBudget` > `regionSpec.dpBudget` > defaults）；`maxExpansions >= 1`（拒绝 0）；有效 rank 进入 fingerprint；project fingerprint 从真实项目内容计算/校验（提供的不匹配则 `INVALID_TASK`）；fingerprint 与 executeConfig 来自同一 effective 对象。
- Manager：`_pump` 同步预留 starting slot（`runningCount + startingCount <= maxConcurrentJobs`）；默认使用 worker executor（`allowInProcess` 显式 opt-in）。
- 取消：worker 用 cancel-token 文件，`shouldStop()` 同步 `fs.existsSync` 检测，即使 CPU 密集搜索阻塞事件循环也能中止；超 grace period 强制 `child.kill()`。
- Strict replay：Job 内部在 `strictReplay:true` 时真实执行 runtime replay（`replayRouteFile`），失败映射 `STRICT_REPLAY_FAILED`（不是 INTERNAL_ERROR）；`strictReplay:false` 输出 `verificationStatus: "not-requested"`。
- Objective 值：job result 采用 route artifact metadata 的权威值（真实模拟器重放结果），保证 route/depth objective 正确；result value、metadata value、strict replay 重算值三者一致才能 completed。
- Progress：新增 `segmentStarted` / `attemptStarted` / `segmentCompleted` / `goalCandidateImproved` 生命周期事件；`actionSetGenerated.trimmedCount` 计入 trimming；`progress-state.goalReached=false`；搜索期间实时发布 goal-candidate bestKnown；completed/failed/cancelled terminal progress 由 manager 发布。
- Result：失败/取消任务也生成统一的 `solver-job-result.v1` envelope（status=failed/cancelled，identity 完整）。

### PR-5.3c2 语义硬化

- Manager：`_startReserved` 在 reserved job 已被取消时重新 `_pump()`，队列不会卡死；`_settleError` 统一处理 executor 创建/启动的同步异常（`INTERNAL_ERROR` envelope）；`submit` 使用 `compileExecutableSolveTask` 执行预检（projectRoot 必须存在、项目可加载、fingerprint 非空）。
- 路线指标：`verifyRouteObjective` / objective 评估拆分为 `{decisionDepth, routeLength}`；`replayRouteFile` 返回 `{finalRuntimeSnapshot, decisionDepth, runtimeRouteLength, objectiveVerification}`；buildRouteRecord 的 route-length metadata 使用完整候选 route 长度（含 auto-step），compose 使用 rawRoute 全长；`stats.depth`（决策数）与 `stats.routeLength`（完整路线长）分开展示。
- Progress：`goalCandidateImproved` 事件由搜索器用 Objective comparator 确认改进，携带 `objectiveFingerprint/value/trace/objectiveValueExact`，accumulator 不再用 `>=` 自行比较（min/lexicographic/非标量正确）；inventory 目标投影真实值；route.length 入队阶段标记 inexact（value=null），route 重建后发布精确值；`strictReplay:false` 发布 `route-artifact`（verified=false），不再发布 `verified-route`；bestKnown routeLength 用完整路线长。


### PR-5.3d Launcher（GUI MVP）

- `shared-solver/launcher/server.js` + `router.js` + `job-api.js` + `tower-registry.js` + `run-solver-launcher.js`：Node 内置 HTTP，默认只监听 `127.0.0.1`。Tower Registry 服务端校验 projectRoot/计算 fingerprint/列出 RegionSpec，拒绝路径穿越，不开放任意文件读取。
- API：`GET /api/health|towers|towers/:id|towers/:id/regions|towers/:id/regions/:rid`、`POST /api/tasks/validate|/api/jobs`、`GET /api/jobs|/api/jobs/:id|/api/jobs/:id/result|/api/jobs/:id/route`、`POST /api/jobs/:id/cancel`、`GET /api/jobs/:id/events`（SSE：snapshot replay + Last-Event-ID + heartbeat + terminal close）。
- HTTP 映射：`INVALID_TASK 400`、`JOB_NOT_FOUND 404`、`JOB_INVALID_STATE_TRANSITION / JOB_PAUSE_UNSUPPORTED 409`、accepted 202；job 列表不内嵌 route artifact。
- UI（`launcher/ui/index.html|style.css|app.js`）：Tower/Region 选择、SolverModel 字段矩阵（默认 hp dominance、mana/hpmax 关闭，RegionSpec model 覆盖 UI 默认）、Objective builder（clear/max-final-hp/maximize/score/lexicographic）、search budget、strictReplay 开关、normalized task + fingerprint preview、job dashboard（phase/segment/预算消耗/bestKnown exactness/failure，无虚假完成百分比，decisionDepth 与 routeLength 分离展示）。
- 重启语义：terminal jobs 从 FileJobStore 恢复；stale queued/running 记录显示为 `interrupted`（view-model，不修改 SolverJob 状态枚举）。
- 新增 `check-launcher-api.js`（unit-plus-micro，静态套件）与 `check-launcher-ui-live.js`（integration-local，真实 Chromium）。

### PR-5.3d1 Launcher UX & Effective Budget Integrity

- **手动 Search 是执行权威**：`runMilestoneGraph` 主 segment 循环无条件应用任务级 `dpOverrides`（`maxExpansions/maxRuntimeMs/maxActionsPerState/goalSkylineLimit/dpSkylineMax/stopOnFirstGoal`），覆盖 RegionSpec/segment（含 decomposer generated segment）的局部预算；`maxRuntimeMs=0` 表示不限时，绝不会产生 `RUNTIME_BUDGET_EXHAUSTED`。新增 `manualSearchOverrides()` 与 `effectiveSegmentBudgets()`。
- **Preflight 返回 effectiveSegments**：validate API 为每个 segment 返回 `{segmentId, maxExpansions, maxRuntimeMs}`（手动覆盖后），UI 展示的是实际将执行的预算。
- **Progress budget 携带实际值**：budget 增加 `source`（task-search）、`expansions`、`elapsedMs`；`maxRuntimeMs=0` 时 `runtimeBudgetUsedRatio` 为 null（不再显示不一致）。
- **UI**：`SolverJob.toJSON` 增加 `failure`；failureClass 中文映射（`RUNTIME_BUDGET_EXHAUSTED`→「未完成 · 时间预算耗尽」等，黄色 `incomplete` 徽标）；legacy 无 objective 的 bestKnown 显示 `Legacy · HP`，不叫 objective；终态 job 只显示 Retry/详情、不显示 Cancel；Retry ×2 读取原 job task 再加倍预算重提；Builder 重排为顶部 Tower/Region+Run、三栏（Job History / 可折叠 Task Configuration / sticky Preview）+ 底部 Current Job dashboard；顶栏显示「服务正常」。
### PR-5.3c3 收尾

- strict replay 的 objective 对账只在显式 ObjectiveSpec 存在时执行；legacy 无 objective 任务只验证 runtime route replay，结果 `objective: null`、`verificationStatus: "verified"`，不会 null dereference。
- 实时 bestKnown 拆分 `decisionDepth`（入队时已知）与 `routeLength`（route 重建前为 null + `routeLengthExact:false`）；重建/回放后发布 `routeLength` 真实值 + `routeLengthExact:true`。禁止把 decisionDepth 填入 routeLength。
- `compileExecutableSolveTask` 真实 `loadProject(projectRoot)` 并计算项目 fingerprint，不吞错误、不允许外部 fingerprint 替代实际值。
- terminal progress 同时发给 subscriber 并写入 `progress.ndjson`（最后一条为 completed/failed/cancelled）。

### PR-5.3d Solver Launcher GUI MVP

- 新增 `shared-solver/launcher/`：`server.js`、`router.js`、`job-api.js`、`tower-registry.js`，使用 Node 内置 http，默认监听 `127.0.0.1`。
- Tower registry：校验 projectRoot、服务端计算 project fingerprint、列出 RegionSpec，防路径穿越（regionId 严格校验 + 根目录边界检查）。
- 稳定 API：`GET /api/health|towers|towers/:id|towers/:id/regions|towers/:id/regions/:rid`、`POST /api/tasks/validate`、`POST /api/jobs`、`GET /api/jobs|jobs/:id|jobs/:id/result|jobs/:id/route`、`POST /api/jobs/:id/cancel`、`GET /api/jobs/:id/events`（SSE）。HTTP 映射 400/404/409/202；job 列表不含完整 route artifact。
- SSE：snapshot 重放 + `Last-Event-ID` + 20s heartbeat + terminal 事件后关闭。
- UI（`launcher/ui/`）：手动 SolveTask 构建器（Tower/Region、SolverModel 字段矩阵默认 mana/hpmax/add-point disabled、Objective 五模式、Search budget、Verification、Normalized Preview）+ Job dashboard（state/phase/segment/budget 消耗比例/bestKnown exactness/failure；禁止虚假完成百分比；decisionDepth 与 routeLength 分开展示）。
- 重启语义：terminal jobs 从 FileJobStore 恢复；stale queued/running 显示为 `Interrupted — previous worker is no longer attached`（仅 view-model，不改 SolverJob 状态枚举）。
- `run-solver-launcher.js` CLI：`npm run launcher --prefix shared-solver`。

### Proof claim

- `clear` 找到路线时可报告 `goal-found`。
- 含最大化项的目标只有在 action scope、预算、搜索耗尽条件完整，并且 goal archive / milestone frontier 没有截断时才能报告 `bounded-optimal`。
- goal archive 被 `goalSkylineLimit` 截断、milestone frontier 被 `candidateLimit` 截断、archive 未按 objective comparator 排序、或目标引用了 DP 不保留的字段时，即使 action set 完整也只能报告 `bounded-optimal-within-retained-frontier`。
- first-goal 强停、预算耗尽、action trimming、unsafe stop 或不完整 action scope 时只能报告 `candidate-only`。
- objective claim 现在包含 `goalArchiveObjectiveAware`、`goalArchiveTrimmed`、`milestoneFrontierTrimmed`、`searchPreserving` 字段。
- 这意味着“找到一条路线”不等于“证明目标最优”。

### Route / replay identity

Objective route metadata 包含：

```text
check:launcher:live           passed   (打开配置恢复 task.model/tower/projectRoot/region 后 buildTask 与原 job 一致)
check:solver-job              passed   (frontier-dependent initial=null、repair 扩宽控制、显式 cap 原样)
check:launcher:live           passed   (打开配置恢复 objective/strictReplay/maxActionsPerState 后 buildTask 一致)
check:solver-job              passed   (attemptCapResolverConsistency：执行/preflight 共享 resolver、attemptCaps 按阶段)
check:launcher:live           passed   (ACTION_TRIMMED→actions2 且缩放后 maxActionsPerState×2)
check:solver-job              passed   (effectiveSegments per-attempt scope、budget.source/别名、runtime ratio clamp≤1)
check:launcher:live           passed   (Retry 按 failureClass 精确生成：runtime→time2、expansion→exp2、unlimited→无 time2)
check:solver-job              passed   (repair/backtrack 预算权威、per-attempt progress scope/current/total、configured repair 不限时)
check:launcher                passed   (planning preflight 失败返回 400 PLANNING_PREFLIGHT_FAILED)
check:launcher                passed   (effectiveSegments 反映手动预算、maxRuntimeMs=0 不产生 RUNTIME_BUDGET_EXHAUSTED)
check:launcher:live           passed   (真实 Chromium：manual Search 权威、terminal 无 Cancel、legacy 显示 Legacy · HP、
                                         未完成语义 + Retry×2、Builder 三栏布局)
check:solver-job              passed   (effectiveSegmentBudgets 反映手动覆盖、maxRuntimeMs=0 覆盖 segment 局部预算、失败 envelope 带 failure)
objectiveSpec
objectiveFingerprint
finalObjectiveValue
objectiveComparisonTrace
```

strict live replay 会从最终 runtime snapshot 重新计算 objective value，并校验 fingerprint 与 metadata。resume route fingerprint 也绑定 objective fingerprint，防止相同最终状态但不同目标的 artifact 误复用。没有 objective metadata 的 legacy route 继续按旧语义回放。

## 主要代码入口

- `shared-solver/lib/solver-model.js`：SolverModel normalize、验证和 fingerprint。
- `shared-solver/lib/objective-spec.js`：ObjectiveSpec schema、compiler、字段验证、Objective-Search 兼容性校验、比较和 proof policy。
- `shared-solver/lib/dp-search.js`：`searchDP` 的终局 `goalStateComparator` 钩子、goal archive 截断诊断。
- `shared-solver/lib/region-spec.js`：RegionSpec objective preflight 与 proof claim（含候选截断范围）。
- `shared-solver/lib/segment-dp.js`：区域/分段搜索、goal skyline、milestone frontier 截断和最终候选排序接缝。
- `shared-solver/lib/route-store.js`：route metadata、objective value/trace、组合路线 objective metadata 重算和 fingerprint。
- `shared-solver/lib/live-replay.js`：strict runtime replay 与 final objective 重算。
- `shared-solver/lib/replay-resume-artifact.js`：h5save/resume route identity binding。
- `shared-solver/run-region-dp.js`：区域 DP CLI、summary 和 route metadata 组装。
- `shared-solver/lib/solve-task.js`：SolveTask 合同、task fingerprint 和外部 compiled 信任边界。
- `shared-solver/lib/solver-job.js` + `solver-job-manager.js`：SolverJob 状态机、核心执行、manager 队列/cancel/subscribe。
- `shared-solver/lib/solver-progress.js`：Progress 合同与 DP observer 聚合器。
- `shared-solver/lib/solver-job-result.js`：Job Result 合同与失败分类。
- `shared-solver/lib/solver-worker-runner.js` + `solver-job-worker.js`：child process job 执行。
- `shared-solver/lib/file-job-store.js`：job 文件持久化。
- `shared-solver/run-solve-task.js`：SolveTask CLI（ndjson progress）。
- `shared-solver/check-objective-spec-contract.js`：ObjectiveSpec 合同 checker。
- `shared-solver/check-objective-spec-live.js`：真实 Chromium strict objective replay checker。
- `shared-solver/check-objective-safe-archive.js`：目标安全归档与 proof scope checker。
- `shared-solver/check-solve-task-contract.js` / `check-solver-job-contract.js` / `check-solver-job-live.js`：PR-5.3c 系列合同与真实 job smoke checker。
- `shared-solver/launcher/server.js` / `router.js` / `job-api.js` / `tower-registry.js`：Launcher 服务器、路由、API、tower registry。
- `shared-solver/launcher/ui/`：Launcher 静态 UI（index.html / style.css / app.js）。
- `shared-solver/run-solver-launcher.js`：Launcher CLI。
- `shared-solver/check-launcher-api.js` / `check-launcher-ui-live.js`：Launcher API 与真实 Chromium UI smoke checker。


### PR-5.3d2 Repair Budget Authority & Per-attempt Progress

- **repair/backtrack 也服从任务级预算**：`withManualBudgetAuthority(config, overrides)` 在 repair/backtrack 各自 override 合并后最后应用 `manualSearchOverrides`（`tryRepairFromConfiguredMilestone`、`tryRepairFromPreviousMilestone`、expanded-previous-segment、backtrack retry 全部覆盖）。`maxRuntimeMs=0` 在 repair 上同样不限时，不会产生 `RUNTIME_BUDGET_EXHAUSTED`。
- **Progress budget 明确 scope**：`budget.scope = "per-attempt"`；`budget.current` 携带活动 attempt 的 `segmentId/attempt/expansions/elapsedMs/maxExpansions/maxRuntimeMs` 与 ratios（≤1），`budget.total` 独立累计 `expansions/elapsedMs`；累计 total 不再除以 per-attempt cap。
- **validate 不再静默吞错**：projectRoot 存在但 milestone 构建失败时返回结构化 400（`PLANNING_PREFLIGHT_FAILED`），不再返回 `valid:true + effectiveSegments:[]`；成功响应带 `effectiveSegmentsStatus`。
- **UI/文档**：`maxRuntimeMs=0` 的任务失败时不显示“运行时间 ×2”（无操作）；修复 `docs/solver-roadmap.md` 中被 `
` 拆坏的 launcher 路径文本。


### PR-5.3d3 Final Public Contract Closure

- **effectiveSegments 明确 per-attempt scope**：`effectiveSegmentBudgets` 返回 `{segmentId, budgetScope: "per-attempt", perAttempt: {maxExpansions, maxRuntimeMs}, maxStartAttempts}`；UI 文案改为「每次 attempt 上限（预算作用于每个候选 attempt，非整个 segment 总量）」。progress 与 preflight 两个公开视图不再相互矛盾。
- **Retry 按 failureClass 精确生成**：`RUNTIME_BUDGET_EXHAUSTED`（仅 maxRuntimeMs>0）→ 运行时间 ×2；`EXPANSION_BUDGET_EXHAUSTED` → 扩展预算 ×2；`ACTION_TRIMMED` → 动作候选 ×2 + 打开配置；`GOAL_NOT_REACHED` → 按原配置重试 + 打开配置；`STRICT_REPLAY_FAILED/INTERNAL_ERROR` → 无预算型按钮。`SolverJob.toJSON` 携带 task search budget，瞬时失败也能正确渲染 Retry 按钮。
- **Progress 合同兼容**：恢复 `budget.source`；`scope/current/total` 保留；补回 deprecated 扁平别名（expansions/elapsedMs/expansionBudgetUsedRatio/runtimeBudgetUsedRatio/expansionBudgetExhausted，均镜像 current）；runtime ratio clamp ≤ 1。
- **UI**：空 `budget.current` 显示 `—`（不再 0.0%）；修复 `docs/public-api.md` 损坏的重复段落与 `n- 字面量。

### PR-5.3d4 Attempt Cap Truthfulness Closure

- **共享 start-candidate resolver**：新增 `resolveStartCandidateLimit(segment, config, overrides, frontierLength)`，执行路径（`runSegmentAgainstFrontier`）与 preflight（`effectiveSegmentBudgets`）共用同一 resolver；执行路径现在真正消费 `segment.dp.startCandidateLimit`，preflight 在不提供 frontier 时报告确定性 cap 或 `null`（frontier 相关时绝不伪造数字）。
- **attemptCaps 按阶段报告**：`effectiveSegments` 的 `attemptCaps = { initial, configuredRepair, backtrackRetry }`——initial 用共享 resolver（初始 segment=1，显式 start cap 原样公开；无显式 cap 时为 null=随 frontier，不低报为 candidateLimit）、configuredRepair 用 `repairStartCandidateLimit`（未设则为 null=随 frontier）、backtrackRetry 用 `backtrackCandidateLimit`（≥8）。不再有单一 `maxStartAttempts` 低报。
- **P2 收尾**：ACTION_TRIMMED Chromium 控制（actions2 缩放后新任务 maxActionsPerState 翻倍）；「打开配置」读取失败 job 的原 task 载入 Builder 再滚动；failureHtml 建议按 failureClass 精确（runtime/expansion/action 各自措辞，GOAL_NOT_REACHED 无预算建议）。

### PR-5.3d5 Frontier-dependent Cap & Builder Restore Closure

- **attemptCaps.initial 诚实化**：后续 segment 无显式 start cap 时 `attemptCaps.initial` 公开为 `null`（frontier-dependent），不再替换成自身的 `candidateLimit`——因为 configured repair 可用 `repairCandidateLimit` 保留超过任务 `candidateLimit` 的前沿。新增 `checkFrontierDependentInitialCapIsNull`（null 语义 + repair 扩宽控制 + 显式 cap 原样报告）。
- **打开配置完整恢复**：`loadTaskIntoBuilder` 现在恢复 Tower id、Region spec（按 spec.id 匹配 select）、SolverModel、Objective（mode/field/terms/objectives、legacy→clear）、Verification.strictReplay、Search 全字段；Chromium 控制验证恢复后 `buildTask()` 与原 job 的 objective/strictReplay/maxActionsPerState 一致。

### PR-5.3d6 Exact Model & Tower Restore Closure

- **task.model 精确恢复**：`loadTaskIntoBuilder` 现在读取 normalized task 的独立 `task.model`（用户提交、真正参与求解并影响 fingerprint 的 SolverModel），不再只从 `region.spec.model` 恢复；Chromium 控制把 `hp: dominance→key`、`money: disabled→key` 提交后，恢复 + validate 得到的 `restoredTask.model.heroFields` 与原 job 一致。
- **Tower 切换真实同步**：`restoreTowerAndRegion` 在 Tower id 变化时 `await loadRegions()`，同步 `state.tower` / `state.towerFingerprint` / region options / tower-meta；`state.regionSpec` 保留 job 自身的精确 RegionSpec。
- **P2 收尾**：`attemptCaps.initial=null` 在 UI 显示“初始=随 frontier”（不再 `?`）；修正 handoff d4 段落中“cap ?? candidateLimit”的旧表述，避免文档内两套合同并存。
## 已验证证据

最近一轮验证结果（PR-5.3d6）：

```text
check:launcher                passed   (registry 路径穿越拒绝、task validate fingerprint、结构化 400、job 202、SSE 单调/terminal、
                                         cancel 409、result/route 区分、重启恢复、stale running 显示 interrupted)
check:launcher:live           passed   (真实 Chromium Launcher：tower/region、model 默认、preflight、worker job verified-route、
                                         refresh 恢复、strictReplay:false route-artifact、legacy completed、auto-step depth1/route5)
check:solve-task                passed   (effective search/dpBudget 继承、rank/project fp 绑定、maxExpansions>=1、外部 compiled 拒绝)
check:solver-job                passed   (状态机、并发 barrier、cancel-reserved 队列推进、executor 同步异常、
                                         worker cancel 延迟/中止、objective-safe bestKnown 投影、STRICT_REPLAY_FAILED 映射、
                                         composed 完整 routeLength、realtime routeLength exactness、
                                         malformed project 预检拒绝、terminal progress 持久化、统一 result envelope)
check:solver-job:live           passed   (Job 内真实 Chromium strict replay；auto-step routeLength=5 > decisions=1；
                                         route.length/decisionDepth + strictReplay:true 均 completed；
                                         legacy 无 objective + strictReplay:true 也 completed)
check:launcher                  passed   (registry 路径穿越拒绝、task validate fingerprints、非法 objective 400、
                                         job 202、SSE 单调+terminal、cancel terminal 409、重启恢复、stale running → interrupted)
check:launcher:live             passed   (真实 Chromium：builder/preflight/worker job/verified-route/无虚假百分比/
                                         刷新恢复/strictReplay:false route-artifact/legacy 无 objective/
                                         auto-step decisionDepth=1 routeLength=5)
check:objective-spec            passed
check:objective-spec:live       passed
check:objective-safe-archive    passed
check:static                    25/25
manifest                        97 modules / 80 tests
tower solver freeze             147 files
replay:start-offset:live        passed
replay:h5save-resume:live       passed
replay:h5save-gui-flow:live     passed
```

Objective live smoke 已确认：真实 Chromium replay 后 final objective value 与重新计算结果一致，objective fingerprint 为 `b54217a839b77018`。

PR-5.4e Repair 本地证据（2026-08-08）：

```text
check:mt1-workload-matrix        passed   6 workload 全 pin、allCorrectnessExact、allFingerprintLocked、
                                          allPartitionEqual、allPartitionSafe、behaviorAuditRan=false（merge=0 → 不跑行为 CEGAR）、
                                          reachabilityAttributionReal；verdict = MT1_DEFAULT_PROMOTION_ELIGIBLE
check:candidate-key-paired-benchmark --smoke  passed  GUARDED_PROFILE_APPROVED；cache hits 从 0/0 修正为 A=151/B=26（真实 solve simulator）
check:dual-key-shadow-contract  passed  PROMOTION_CANDIDATE；mergedCandidateKeyCount=0、shadowUnsafeMerge=0
check:manifest                  passed   103 modules / 91 graded tests
```

PR-5.4f 本地证据（2026-08-08）：

```text
check:mt1-workload-matrix        passed   6 workload 全 pin、allDefaultCandidateParity、allDefaultMatchesPin、allRollbackMatchesPin、
                                          defaultPromotes/candidateExplicit/rollbackEffective 全 true；verdict = MT1_DEFAULT_PROMOTION_ACCEPTED
check:mt1-default-promotion      passed   Gates A–H：scope promotion、rollback 恢复 production 结构 116/267/156/112/62/62、
                                          implicit fallback vs explicit fail-closed（wrong-floor/region-drift/project-drift）、
                                          代表 default+rollback strict replay verified、production invariants 未变；verdict = MT1_DEFAULT_PROMOTION_ACCEPTED
check:candidate-key-paired-benchmark --smoke  passed  GUARDED_PROFILE_APPROVED；reachabilityTotalReduced=true（P2-2 修复）
check:candidate-key-promotion   passed
check:manifest                  passed   103 modules / 92 graded tests
```

PR-5.4g 本地证据（2026-08-08）：

```text
check:mt1-post-promotion-regression  passed   S1 默认→candidate+pinned、S2 rollback→production 结构 116/267/156/112/62/62、
                                              S3 unapproved implicit fallback（无 candidate builder）、S4 campaign v2 per-region isolation
                                              （approved MT1→candidate、unapproved→production、无泄漏）；verdict = MT1_DEFAULT_PROMOTION_REGIME_STABLE
check:mt1-workload-matrix           passed   MT1_DEFAULT_PROMOTION_ACCEPTED（新指纹 36b7477cad2d6a96）
check:mt1-default-promotion         passed   MT1_DEFAULT_PROMOTION_ACCEPTED（Gate G 新指纹 true）
check:multi-region                  passed   v2 per-region resolution 无回归
check:candidate-key-paired-benchmark --smoke  passed   GUARDED_PROFILE_APPROVED
check:candidate-key-promotion       passed
check:manifest                      passed   103 modules / 93 graded tests
```

PR-5.5d 本地证据（2026-08-08）：

```text
check:multi-region-boundary-matrix  passed   19 workload（+ start-door-key-entryA：OnlyUp Start 真实密室开门/钥匙消耗）、
                                              inventoryHoleClosure: distinctInventories=10、acquireExecuted=true（I600/interactPickup）、
                                              consumeExecuted=true（greenKey/openDoor，canonical 同 edge mutation）、filled=true → inventory hole CLOSED；
                                              visitedFloorsHoleClosure: maxVisitedFloorCount=2、arrivalExecuted=true、
                                              postArrivalSearchObserved=true、changeFloorExecuted=false、filled=false → visitedFloors PARTIAL（如实）；
                                              inventoryTransitionControls: negative1=false、negative2=false、positive=true；
                                              coverageSummary: 44 pre / 44 boundary / 535 post、totalMerged=0、totalUnsafe=0；
                                              6 controls + witness path 全过；verdict = NO_COLLISION_OBSERVED（pin 死）
check:manifest                      passed   104 modules / 95 graded tests
```

PR-5.6 本地证据（2026-08-10）：

```text
check:onlyup:segments               passed   route lineage + legacy one-level repair + synthetic adaptive depth=2 success
check:goal-directed-search          passed   MT2 exact parity/strict replay；20→16 expansions；MT4 first goal 49→17
check:goal-feasibility-bounds       passed   5 类 admissible witness + incomplete evidence keep + tracked strict replay
check:route-search-portfolio        passed   serial 20.28s → parallel 8.59s（2.36x）；2 routes exact state/route parity
check:route-free-state              passed   canonical route-free / compound route patch 无回归
check:manifest                      passed   106 modules / 101 graded tests
check:static                        passed   44/44 clean manifest checks
long adaptive probe                 incomplete   depth1/2/3 upstream candidates 2/8/1；仍失败于 mt5-third-gate
```

PR-5.7a 本地 shadow 证据（2026-08-10，wall-clock 不冻结）：

```text
check:candidate-quality-shadow      passed   capture-all materialized routes + 1/2/4/8 + Stop A/B/C controls
MT5 source corpus                   passed   8 attempts / 140 raw goals / 18 milestone candidates / 3 unique DP keys
retention matrix                    observed capacity 1→1；2→2；4→3；8→3（每档 15 个同-key dedup；capacity 4/8 无 cap drop）
third-gate probes                   140 failed / 0 budget-exhausted / 0 reached；max 29 expansions；frontier empty
verdict                             EVICTED_CANDIDATES_ALSO_FAIL / STOP_B_RETENTION_NOT_PRIMARY
```

PR-5.7b/c MT5 closure 证据（2026-08-11，wall-clock 不冻结）：

```text
check:mt5-third-gate-resource-timing passed   +13,846 HP causal replay；607 expansions 到 third-gate
check:mt5-blueking-long-chain        passed   18 milestones / 645 expansions / 0 trim / 0 budget exhaustion
post-third-gate chain                passed   38 expansions 到 sustain / I894 / final stats / HP / blueKing
strict recorded-decision replay      passed   35/35；route a2b1b4f6…；final exact d63323d8…
historical coarse → current          4050 FAIL → 645 SUCCESS；-84.1%；wall 约 8.22x（方向性）
check:manifest                       passed   109 modules / 106 graded tests
check:static                         passed   45/45 clean manifest checks
```

常用验证命令：

```bash
npm run check:objective-spec --prefix shared-solver
npm run check:objective-spec:live --prefix shared-solver
npm run check:objective-safe-archive --prefix shared-solver
npm run check:solve-task --prefix shared-solver
npm run check:solver-job --prefix shared-solver
npm run check:solver-job:live --prefix shared-solver
npm run check:static --prefix shared-solver
npm run check:validation --prefix shared-solver
npm run check:mt1-workload-matrix --prefix shared-solver   # 全量；本地可先 MT1_WORKLOAD_SUBSET=<id> 单跑
npm run check:mt1-default-promotion --prefix shared-solver
npm run check:mt1-post-promotion-regression --prefix shared-solver
npm run check:multi-region-key-shadow --prefix shared-solver
npm run check:multi-region-boundary-matrix --prefix shared-solver   # 本地可 MR_WORKLOAD_SUBSET=<id> 单跑
npm run check:candidate-key-paired-benchmark --prefix shared-solver   # 或加 --smoke
npm run check:goal-directed-search --prefix shared-solver
npm run check:goal-feasibility-bounds --prefix shared-solver
npm run check:route-search-portfolio --prefix shared-solver
npm run check:candidate-quality-shadow --prefix shared-solver
npm run audit:candidate-quality:mt5 --prefix shared-solver
npm run check:mt5-third-gate-resource-timing --prefix shared-solver
npm run check:mt5-blueking-long-chain --prefix shared-solver
```

## 下一主线

**PR-5.5e、PR-5.6、PR-5.7、PR-5.8a/b/c/d/e/f/g、PR-5.9a/b/c/d/e/f/g 与 PR-5.10a 均已 CLOSED**。2026-08-12 重新审视后，项目目标重新锁定为：**用户只提供塔、初始状态与最终目标，机器承担试路、排除和事件顺序组合；已知路线只能在运行后作为隐藏 oracle，不得作为 discovery planner 输入。** 原 5.10b/5.10c 性能归因暂停，只有 blind workload 证明某热点阻塞自主发现时才恢复。

### 重新校准后的执行顺序

1. **PR-5.10b Discovery Capability Audit**：量化 milestone、区域/楼层、事件顺序、资源阈值、route fixture 和逐段搜索参数等人工信息；建立 A0→A5 消融梯度，明确当前能力从哪里开始依赖人工答案。只观察，不改 DP key、dominance、selection 或预算。
2. **PR-5.10c Blind Discovery Baseline（已完成）**：terminal-only D3 已在 1000 expansions 到达 MT3、frontier 仍 496，作为后续所有 planner/trace 优化的 before 基线；不得靠增加 timeout/candidateLimit 改写它。
3. **PR-5.11 Search Trace Explainability（已完成）**：每轮固定输出 before/after、decision-depth/floor/action/rejection 和 human review buckets；不同控制不允许因果比较。known route 仍只允许以后作为 post-hoc oracle，不得进入 blind planner。
4. **PR-5.12 Automatic Macro Graph**：只从塔和 simulator 语义自动提取 Boss、楼梯/传送、门、一次性物品/装备、商店/加点、stat gate、地图/flag mutation 等不可逆或高价值事件，并生成候选依赖；不得读取 hand-authored milestone 或 route fixture。
5. **PR-5.13b Failure-Triggered Macro Backtracking**：first-feasible 前进；只有下游 stage 失败才回到上一 floor-entry 收集额外候选，按失败面反馈保留 HP/攻防/魔防/钥匙/装备/flags/未消费资源角色。必须报告 retry expansions 与入口候选差异，不能用预先全 skyline 代替按需 save/load。
6. **PR-5.14 D0–D3 Blind Qualification**：D0=已知路线 strict replay；D1=只给局部起终点；D2=只给楼层入口与 Boss；D3=只给初始状态与最终 Boss。D0/D1 只证明执行/局部发现；只有 D2/D3 找到且 strict replay 才能声明自主路线发现。
7. **PR-5.15a D2 First-Failure Attribution（已完成）**：先扩 automatic planning envelope 到 runtime state 已访问且可回访的 floors，再从扩展图候选编译 Boss feasibility dependency。不得把 observed MT4/I894 直接写成路线提示；修复必须由通用 state-visible reachability + simulator probe 推出。
8. **PR-5.15b Automatic Feasibility Subgoals（已完成）**：envelope 与 counterfactual equipment ranking 已闭合；不能把自动选中的 I894 人工写回 milestone spec。
9. **PR-5.16a Search Observatory（已完成）**：今后先显示 goal/hypothesis/candidate funnel/budget/plateau/review/next experiment，再允许修改 planner。5.16b 从 selected resource 的图拓扑编译 AND/OR predecessor dependencies；不是把单一路径重新包装为自动 milestone。
10. **PR-5.16b AND/OR Dependency Planner（已完成）**：保留 2 个 topology alternatives 与各自 blocker conjunction，不把最短路径当答案。5.16c 由局部 DP 执行 planner 选出的首个当前可行 prerequisite，返回 survival/resource/equipment/mobility 等多角色 checkpoint，并要求 strict replay。
11. **PR-5.16c Local Multi-Role Dependency Executor（已完成）**：只从已完成 prerequisite 的 goal skyline 输出 checkpoint，不把中途 landmark 冒充完成状态。5.16d 必须逐个用下游 blocker 重新评估这 8 个出口，让失败原因真正改变 checkpoint 或 OR alternative 选择。
12. **PR-5.16d Failure Feedback + On-demand Backtracking（已完成）**：top-1 failure 已真实改变 checkpoint 与 OR branch，且下一 prerequisite 已执行闭合。5.17a 必须把这套循环扩展到 D2 terminal goal；不能把“一次反馈成功”写成整条路线发现成功。
13. **PR-5.17a1 Startable Blocker Repair（已完成）**：repair value 与 dependency startability 必须联合验证，禁止选择会回到原 blocked gate 的循环资源。下一轮从已击杀 devilWarrior 的 checkpoints 继续拾取 I1014，再返回 I894 主依赖。
14. **PR-5.17a2 Hierarchical Discovery Loop（已完成）**：I1014/I1013/I642 的前置与实际 pickup 已由连续循环闭合；当前实际状态证明单物品 repair 空间已穷尽且不是 budget/time blocker。下一轮进入跨层 EXP/复合资源修复，不得手写已知路线战斗顺序。
15. **PR-5.17a3 Least-Commitment + Reversible Trials（已完成）**：5.17a2 的“单物品空间已穷尽”不再作为冻结事实；first-goal 丢失与 free-pickup counterfactual 已被识别。失败 repair 必须真实执行、完整否证、恢复 portfolio 并去重。下一轮先算 acquisition-cost-adjusted actual margin，再决定是否需要 EXP/复合资源类别。
16. **PR-5.17a4 Actual Net-Margin Observatory（已完成）**：repair prediction、acquisition delta、actual blocker status/margin、prediction error、closure class 必须分别报告；`improved-but-still-blocked` 只能触发下一组合修复，不能冒充已解锁。checkpoint graph 可复用，但不能跨 exact state 复用。
17. **PR-5.17a5 Actual Closure Commit/Rollback（已完成）**：`no-net-improvement` 与未实现 repair 一律回滚；stream event 是长运行默认诊断接口。探索期可分级验证 checkpoint，但任何被提交到最终路线的 decision chain 仍需 full strict replay。
18. **PR-5.17a6 Shared Simulator Replay Pipeline（已完成）**：只允许复用无跨 state mutation 的 simulator 服务；保持 checkpoint 数、route records、strict replay 与 fingerprints 完全一致。下一轮回到 D2 组合 repair 语义，不继续做无证据微优化。
19. **PR-5.17a7 Semantic Repair Experiment Identity（已完成）**：repair identity 必须分开表示资源状态、acquisition access 与 blocker evidence；hero/`__leaveLoc__` 导航差异不得制造重复试验，真实 flags 与 access 差异仍必须保留。30-round 只证明重复 trial 归零，不证明 D2 closure；下一轮优化反复 graph/access compilation。
20. **PR-5.17a8 Exact-State Repair Compilation Cache（已完成）**：cache 仅在同一 run、同 exact state 与同 resource access 下复用 graph/analysis/access summary；不得跨 run、跨 state 或缓存可变 simulator state。下一轮优先审计候选 counterfactual 重算和历史 portfolio 调度，D2 仍 OPEN。
21. **PR-5.17a9 Combat-Reward Progress Repair（已完成）**：可打敌人的 EXP/升级收益可成为自动 repair，但“取得 EXP”与“解除 blocker”必须分开；progress closure 可提交到 reversible history，不得写成 unblock。下一轮必须生成能跨当前 lethal gate 的复合 EXP/资源 OR chain，而不是继续单节点候选。
22. **PR-5.17a10 Failure-Driven Progress Priority（已完成）**：progress repair 后的不可启动状态会让候选调度优先 combat reward；这只解决“试什么”，不保证“保留哪条存档”。50-round 最终 EXP 仍低于升级线，下一轮以累计等级/EXP 与路线 lineage 选择 continuation portfolio，禁止用更多 rounds 掩盖分支横跳。
23. **PR-5.17a11 Cumulative Progress + Level-Threshold Probe（已完成）**：progress 排序必须使用 absolute level/EXP，而非布尔 gain；从当前 state 自动导出的 `lv+1` goal 可作为复合链探针。exp315 的三组 exact states 都完整穷尽失败，下一轮应把 level failure 作为 backward dependency，从更早 history 搜索可保留的 EXP 链。
24. **PR-5.17a12 Replay-Verified Partial Progress（已完成）**：未达到复合目标不等于所有中间状态都应丢弃；只有 absolute level/EXP 严格提升的 best-progress 才能提交，且必须 route-record + strict replay。partial objective 完成前禁止 terminal planner 抢占。exp315 的再次穷尽否证“升级就是唯一 repair”，下一轮重新比较成功 oracle 与 autonomous state 的资源/事件差异。

### 5.10b 首轮已锁定的审计口径

- OnlyUp 初始状态→MT5 blueKing 的 authored plan 含 28 个阶段（27 个中间目标/顺序边）；当前 runner 即使删除 `startFrom` 仍按数组顺序执行，所以 unordered-event planning 在 A0 就不存在，A4 只是把该结构性上限暴露出来。
- 审计必须分别计数：route fixture、milestone 数、显式/隐式顺序边、`minHero` 字段、exact damage/equipment、removed/present/preferred tiles、allowed floors/changeFloor 坐标、region 字段与 per-segment DP tuning。
- A5 terminal-only spec 只能保留 `bossDefeated + floorId/x/y/enemyId`；必须生成一个且仅一个 segment，`actionPolicy={}`、`dp={}`，不得泄漏任何中间阈值/楼层白名单/路线证据。
- `ASSISTED_EXECUTION_NOT_AUTONOMOUS_DISCOVERY` 是能力审计结论，不是否定 simulator、strict replay 或局部 DP 的价值；现有 DP 定位调整为 hierarchical planner 的局部求解内核。

- **5.5f 保持未排期**：只有后续新 workload 出现真实 same-scope candidate merge 才恢复 collision CEGAR/minimal refinement；当前 changeFloor edge 的 production/candidate identity 都不同，不能据此改 key。
- **MT5 不再是 correctness blocker**：early-gem 133-expansion branch 只是性能候选；不得为了进一步压 wall 修改 production key/dominance/selection 或扩大 timeout/candidateLimit。
- 后续方向（尚未排期）：更大塔的完整求解承诺与自动 analyzer；自动 SolverModel 推断（必须保持 manual 权威）；pause/resume 与 frontier checkpoint；跨 project/tower campaign 作为独立 production feature（不得为了 collision corpus 顺手塞进研究线）；远程访问、认证、多机调度。
- 历史 P2 carry（保持隔离）：fast CI <3min（P2-1）；paired benchmark stale diagnostic（P2-2）。

Launcher 已提供：`npm run launcher --prefix shared-solver`。

## 交接时必须保持的边界

- 不要把 ObjectiveSpec 引入 DP dominance、state key、action pruning 或 capacity。
- 不要把 `goalStateComparator` 引入 `bestByKey`、`isBetterForSameDpKey()`、agenda、action ranking 或中间状态剪枝。
- 不要把 first-goal 结果标记为 optimize objective 的 optimal。
- 不要信任外部输入中的 `compiled:true`；SolveTask 外部 JSON 必须重新 normalize/compile。
- 不要把 `pause` 伪装成 `cancel`；`JOB_PAUSE_UNSUPPORTED` 必须结构化拒绝。
- 不要把预算耗尽 / action trimming / policy filtered / milestone 过严报告为“已证明无解”；这些是可重试的不完整搜索诊断。
- 5.8f 后 outcome 四字段必须保持正交：`goalFound=true` 不能被 budget/frontier 状态覆盖；`searchComplete=false` 只说明搜索未穷尽。Doctor/job result 对 found+incomplete 必须保持成功路线与 `feasible-incomplete` 诊断，不能回退成 failure/not-found。
- 5.8g 后不得为了 replay 性能恢复 choice-level dedup，也不得把 post-state cache 跨 state/simulator/route 共享；只有 target/stance/direction 这种原 resolver 已经 hard reject 的条件可以安全前移。exact-post、fallback 与 ambiguity/tie-break 顺序必须保持 5.8e parity。
- 5.9a 后 reachability reuse 只对**已由当前 exact state 独立判定为 safe-fast**的 walk 生效；cache key 至少包含 floor、start 与 current-floor mutations。缓存值只能是 topology/path skeleton，不能携带旧 hero/state；命中必须 rebase 当前 state。legacy-exact、poison、hazard、live auto event、directional item 或 stabilization-sensitive 状态一律不得进入该复用层。
- 5.9b 后 exact-state LRU 与 skeleton LRU 必须分层：skeleton hit 不得冒充 exact hit；stats 必须分别报告 builds/hits/stores/rebases/unsafeBypasses/safetyClassifications。任何 lazy travelState 后续方案都必须先锁 116-state action/successor exact corpus，不得把共享 state 对象交给 action/apply 路径。
- 5.9c 后不得把 5960 个 non-escape nodes 直接解释成“state 无人使用”：当前 consumer 确实读取全部 6526 nodes，只是大多用于可由 base state + coordinate 满足的 topology/tile/predicate 检查。repair 必须先拆 lookup/validation/materialization 三阶段；diagnostic proxy/accessor 继续默认关闭，不得进入正常运行路径。
- 5.9d 后 safe-fast `visited` 的主 identity 是 coordinate/path topology；`node.state/node.key` 只是兼容的 memoized materialization surface。内部 consumer 应优先使用 `getLookupState/materializeNodeState` 分层，不能为了方便重新扫描 `node.state`；`nodesRebased` 表示 topology nodes，实际 clone 必须看 `nodesMaterialized/stateClones`。
- 不要用自动 analyzer 覆盖用户手动 SolverModel。
- 不要修改 tower-local solver/runtime 来绕过 shared-solver 合同。
- 失败搜索不能直接解释为“无路线”；先检查 `failureClass`、`actionTrimmed`、`expansionBudgetExhausted`、`stoppedReason`、`frontierSize` 等诊断字段。
- `bounded-optimal` 之前先确认 `goalArchiveObjectiveAware`、`goalArchiveTrimmed`、`milestoneFrontierTrimmed`、`searchPreserving` 状态。
- 任何新的 GUI/live 功能都需要真实浏览器或 runtime smoke；projection/helper 单测不能替代真实路径。
- 不要把 PR-5.4f 的 MT1 默认 promotion 扩散到其他 floor/Region/project；未指定 profile 只在 approved MT1 scope 走候选，其余 scope 必须回到 `production-region`。
- 显式 `production-region`（rollback）必须始终选择旧 key path，即使当前是 approved MT1 也不得自动改回。
- 显式 `experimental-mt1-tower-ir-v1` 的 fail-closed guard（漂移 throw）不得因 promotion 被削弱。
- `computeRegionSpecFingerprint` 必须剥离 `projectRoot`（v1 保留 v2 删除 → 双路径指纹不一致）；approved baseline `expectedRegionSpecFingerprint` 已重基线到 `36b7477cad2d6a96`，不可回退到旧的 `510312b10d5ccec1`。
- multi-Region v2 的 profile 解析是 per-region 的（fresh executeConfig，候选 builder 不跨 region 泄漏）；不得把单个 region 的候选 builder 应用到后续未认证 region。
- 5.5a 的 multi-Region shadow 是纯观察（research）：不参与 frontier pruning / dominance / ranking / checkpoint / route；`NO_COLLISION_OBSERVED` ≠ safe ≠ multi-Region key 已认证。不要因"shadow 没发现问题"就顺手 default-on。
- Region boundary 必须被视为 successor semantics（boundary-transfer equivalence 是 CEGAR 第一步），不是 state serialization seam。
- 5.5a Repair 后：corpus 必须用 per-region IR/context（pre-boundary→R0 IR，boundary/post/CEGAR→R1 IR）；boundary gate 比较 post-boundary **semantic identity（production DP key）**，HP/dominance 级 exact 差异只做诊断并交给 classifyPair；`NO_COLLISION_OBSERVED` verdict 本轮 pin 死，升级必须显式 review。
- 5.5b 后：R0 goal 必须显式构造（P2-3，禁止隐式继承 smoke goal）；coverage gate 是"无 merge"声明的必要条件；第一次真实 merge 必须写 witness artifact（字段级 projection diff）并 needs-review，禁止当场 refinement。
- 5.5b Repair 后：collision 判定的 group key 必须统一成真实生产竞争 scope（state partition=region-execution-context + candidate key；boundary partition/CEGAR/witness=boundaryIndex + candidate key；跨 boundary key reuse 不是 merge）；shadow 的 coverage/signature/CEGAR 必须走 production-legal action semantics（`buildSegmentActionProvider`），raw-vs-legal 差异只允许是 policy 禁止 kind 或 provider 附加 kind（floorFly/interactPickup）；witness 必须含 production identity decomposition diff（不只是 hash）。
- 5.5c 后：chain 内全部 region 必须同一 tracked project（same-project fail-fast gate，与 executeSolveJobV2 一致）；hole-closure 的 acquire/consume 必须是**真实 parent→action→child edge**（recorder observation-only provenance：parentInventory/parentMutations/actionKind/actionSummary），parent/child mutation 必须同一 canonicalization（`mutationSummaryFingerprintOf`），任意 state pair 差值不是 transition evidence；`NO_COLLISION_OBSERVED` ≠ safe/certified/promotion candidate。
- 5.5e 后：inventory 与 visitedFloors 两个 surface hole 均已用 tracked OnlyUp 真实关闭。changeFloor closure 必须保留 parentStateKey/parentFloorId/parentVisitedFloors provenance，并证明 live action 真实换层、保留旧 visited 集且新增目标层；旧 fixture stateKey 因历史 empty-floor serialization drift 只作诊断，当前 exact fingerprints 才是权威。该边 candidate collision=false，禁止无证据进入 5.5f；仍不得引入 untracked tower 或 harness-only 跨 project campaign。
- 5.6 后：`first-feasible` / `adaptive-feasible` 都是显式搜索意图；默认仍为 `skyline`。admissible-v1 只能消费 complete declared evidence，未知证据必须 keep。adaptive repair 必须有 bounded depth/budget、保留失败 witness，不能把失败升级成无解证明。并发 portfolio 只并行独立进程路线用例，不共享 simulator/state。
- 5.7a 后：candidate-quality shadow 只能观察 raw goal archive / milestone frontier，不得改变 production selection、DP key 或 dominance；capacity 1/2/4/8 必须区分同-key dedup 与真正 cap drop。Stop B 已触发时不得添加 skyline role，下一步必须先做 failure attribution。

## 交接起点

```powershell
Set-Location E:\AUbuntuProject\project\motapathfind
git switch dev
git pull --ff-only origin dev
npm run check:static --prefix shared-solver
```

开始已授权的 PR-5.19b（Battle Access-Prerequisite Compiler）前，先阅读：

```text
AGENTS.md
docs/project-documentation-system.md
docs/260814/5-18d.md
docs/260815/5-18d-repair1-review.md
docs/260815/5-18e.md
shared-solver/lib/strategic-transition.js
shared-solver/lib/strategic-d2-search.js
shared-solver/lib/strategic-connector.js
shared-solver/lib/strategic-blocker.js
shared-solver/check-strategic-blocker-connector.js
```
